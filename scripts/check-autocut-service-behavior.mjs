import path from 'node:path';
import process from 'node:process';
import { pathToFileURL } from 'node:url';
import { existsSync, mkdirSync, readFileSync, rmSync, writeFileSync } from 'node:fs';
import { builtinModules, createRequire } from 'node:module';
import ts from 'typescript';

const rootDir = process.cwd();
const packageJson = JSON.parse(readFileSync(path.join(rootDir, 'package.json'), 'utf8'));
const failures = [];
const pass = [];
const nativeSetTimeout = globalThis.setTimeout;
const nativeClearTimeout = globalThis.clearTimeout;
const revokedObjectUrls = [];
const objectUrlSources = new Map();
const browserDownloadRequests = [];
const clipboardWrites = [];
const moduleLoaderOutRootDir = path.join(rootDir, 'artifacts', 'service-behavior-modules');
const moduleLoaderOutDir = path.join(
  moduleLoaderOutRootDir,
  `${process.pid}-${Date.now().toString(36)}`,
);
let moduleLoaderOutDirPrepared = false;
const desktopNativeHostSource = readFileSync(
  path.join(rootDir, 'packages/sdkwork-autocut-desktop/src/native-host.ts'),
  'utf8',
);
const taskDetailPageSource = readFileSync(
  path.join(rootDir, 'packages/sdkwork-autocut-tasks/src/pages/TaskDetailPage.tsx'),
  'utf8',
);
const tasksPageSource = readFileSync(
  path.join(rootDir, 'packages/sdkwork-autocut-tasks/src/pages/TasksPage.tsx'),
  'utf8',
);
const tasksServiceSource = readFileSync(
  path.join(rootDir, 'packages/sdkwork-autocut-services/src/service/tasks.service.ts'),
  'utf8',
);
const desktopMediaRuntimeSource = readFileSync(
  path.join(rootDir, 'packages/sdkwork-autocut-desktop/src-tauri/src/media_runtime.rs'),
  'utf8',
);

function assertRule(condition, message) {
  if (condition) {
    pass.push(message);
  } else {
    failures.push(message);
  }
}

function assertEqual(actual, expected, message) {
  assertRule(Object.is(actual, expected), `${message} (expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)})`);
}

function assertDeepEqual(actual, expected, message) {
  assertRule(
    JSON.stringify(actual) === JSON.stringify(expected),
    `${message} (expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)})`,
  );
}

function assertIncludes(collection, value, message) {
  assertRule(collection.includes(value), message);
}

function assertNotOwnProperty(object, propertyName, message) {
  assertRule(
    !Object.prototype.hasOwnProperty.call(object ?? {}, propertyName),
    `${message} (unexpected property ${JSON.stringify(propertyName)} on ${JSON.stringify(object)})`,
  );
}

function assertOwnProperty(object, propertyName, message) {
  assertRule(
    Object.prototype.hasOwnProperty.call(object ?? {}, propertyName),
    `${message} (missing property ${JSON.stringify(propertyName)} on ${JSON.stringify(object)})`,
  );
}

function assertNumberBetween(actual, min, max, message) {
  assertRule(
    typeof actual === 'number' && actual >= min && actual <= max,
    `${message} (expected ${min} <= value <= ${max}, got ${JSON.stringify(actual)})`,
  );
}

function assertSegmentsOrderedAndNonOverlapping(segments, message) {
  const invalidIndex = Array.isArray(segments)
    ? segments.findIndex((segment, index) =>
        index > 0 &&
        typeof segment?.startMs === 'number' &&
        typeof segments[index - 1]?.endMs === 'number' &&
        segment.startMs < segments[index - 1].endMs
      )
    : -1;
  assertRule(
    Array.isArray(segments) && invalidIndex === -1,
    `${message} (segments must be ordered and non-overlapping, got ${JSON.stringify(segments)})`,
  );
}

const smartSliceTranscriptBoundaryToleranceMs = 80;

function isAutoCutNgOrRetakeTranscriptText(text) {
  const normalizedText = normalizeSubtitleCueText(text).toLowerCase();
  return /(?:\u5570\u55e6|\u91cd\u65b0\u5f55|\u91cd\u5f55|\u7b97\u4e86|ng|retake|re-record|record again|show you the same thing)/iu.test(normalizedText);
}

function getSmartSliceEligibleTranscriptCoverageSegments(segments) {
  const normalizedSegments = (Array.isArray(segments) ? segments : [])
    .filter((segment) =>
      Number.isFinite(segment?.startMs) &&
        Number.isFinite(segment?.endMs) &&
        segment.endMs > segment.startMs &&
        normalizeSubtitleCueText(segment.text)
    )
    .map((segment) => ({
      ...segment,
      startMs: Math.round(segment.startMs),
      endMs: Math.round(segment.endMs),
      text: normalizeSubtitleCueText(segment.text),
    }))
    .sort((firstSegment, secondSegment) =>
      firstSegment.startMs - secondSegment.startMs ||
        firstSegment.endMs - secondSegment.endMs,
    );
  const timelineStartMs = normalizedSegments[0]?.startMs ?? 0;
  const timelineEndMs = normalizedSegments.at(-1)?.endMs ?? timelineStartMs;
  const timelineDurationMs = Math.max(1, timelineEndMs - timelineStartMs);
  const discardedTailStartIndex = normalizedSegments.findIndex((segment, index) =>
    index > 0 &&
      isAutoCutNgOrRetakeTranscriptText(segment.text) &&
      segment.startMs >= timelineStartMs + timelineDurationMs * 0.65
  );
  const publishableSegments = discardedTailStartIndex >= 0
    ? normalizedSegments.slice(0, discardedTailStartIndex)
    : normalizedSegments;
  return publishableSegments.filter((segment) =>
    normalizeSubtitleCueText(segment.text) && !isAutoCutNgOrRetakeTranscriptText(segment.text)
  );
}

function getSmartSliceClipCoverageRanges(clip) {
  if (Array.isArray(clip?.sourceSegments) && clip.sourceSegments.length > 0) {
    return clip.sourceSegments
      .map((segment) => ({
        startMs: Math.max(0, Math.round(segment?.startMs ?? 0)),
        endMs: Math.max(0, Math.round(segment?.endMs ?? 0)),
      }))
      .filter((segment) => segment.endMs > segment.startMs);
  }

  const startMs = typeof clip?.speechStartMs === 'number'
    ? Math.max(0, Math.round(clip.speechStartMs))
    : typeof clip?.sourceStartMs === 'number'
      ? Math.max(0, Math.round(clip.sourceStartMs))
      : typeof clip?.startMs === 'number'
        ? Math.max(0, Math.round(clip.startMs))
        : undefined;
  const endMs = typeof clip?.speechEndMs === 'number'
    ? Math.max(0, Math.round(clip.speechEndMs))
    : typeof clip?.sourceEndMs === 'number'
      ? Math.max(0, Math.round(clip.sourceEndMs))
      : typeof clip?.startMs === 'number' && typeof clip?.durationMs === 'number'
        ? Math.max(0, Math.round(clip.startMs + clip.durationMs))
        : undefined;
  return startMs !== undefined && endMs !== undefined && endMs > startMs
    ? [{ startMs, endMs }]
    : [];
}

function doesSmartSliceRequestCoverTranscriptSegment(clips, segment) {
  const segmentStartMs = Math.round(segment.startMs);
  const segmentEndMs = Math.round(segment.endMs);
  const ranges = (Array.isArray(clips) ? clips : [])
    .flatMap((clip) => getSmartSliceClipCoverageRanges(clip))
    .map((range) => ({
      startMs: Math.max(segmentStartMs, range.startMs),
      endMs: Math.min(segmentEndMs, range.endMs),
    }))
    .filter((range) => range.endMs > range.startMs)
    .sort((firstRange, secondRange) =>
      firstRange.startMs - secondRange.startMs || firstRange.endMs - secondRange.endMs
    );

  let coveredUntilMs = segmentStartMs;
  for (const range of ranges) {
    if (range.endMs <= coveredUntilMs) {
      continue;
    }
    if (range.startMs > coveredUntilMs + smartSliceTranscriptBoundaryToleranceMs) {
      return false;
    }
    coveredUntilMs = Math.max(coveredUntilMs, range.endMs);
    if (coveredUntilMs >= segmentEndMs - smartSliceTranscriptBoundaryToleranceMs) {
      return true;
    }
  }

  return coveredUntilMs >= segmentEndMs - smartSliceTranscriptBoundaryToleranceMs;
}

function getUncoveredSmartSliceTranscriptSegments(clips, transcriptSegments) {
  return getSmartSliceEligibleTranscriptCoverageSegments(transcriptSegments)
    .filter((segment) => !doesSmartSliceRequestCoverTranscriptSegment(clips, segment));
}

function assertSmartSliceNativeRenderRequestClipsReady(clips, sourceDurationMs, message) {
  assertRule(Array.isArray(clips) && clips.length > 0, `${message} includes native render clips`);

  let previousRenderedEndMs;
  (Array.isArray(clips) ? clips : []).forEach((clip, index) => {
    const clipNumber = index + 1;
    const startMs = Math.round(clip.startMs);
    const durationMs = Math.round(clip.durationMs);
    const renderedEndMs = startMs + durationMs;
    const sourceStartMs = Math.round(clip.sourceStartMs ?? startMs);
    const sourceEndMs = Math.round(clip.sourceEndMs ?? renderedEndMs);
    const speechStartMs = Math.round(clip.speechStartMs ?? sourceStartMs);
    const speechEndMs = Math.round(clip.speechEndMs ?? sourceEndMs);

    assertRule(
      Number.isFinite(startMs) && startMs >= 0,
      `${message} clip ${clipNumber} has a finite non-negative startMs`,
    );
    assertRule(
      Number.isFinite(durationMs) && durationMs > 0,
      `${message} clip ${clipNumber} has a finite positive durationMs`,
    );
    assertRule(
      previousRenderedEndMs === undefined || startMs >= previousRenderedEndMs,
      `${message} clip ${clipNumber} starts after the previous rendered clip ends (startMs=${startMs}, previousRenderedEndMs=${previousRenderedEndMs})`,
    );
    if (Number.isFinite(sourceDurationMs)) {
      assertRule(
        renderedEndMs <= sourceDurationMs,
        `${message} clip ${clipNumber} stays inside source duration (endMs=${renderedEndMs}, sourceDurationMs=${sourceDurationMs})`,
      );
    }
    assertRule(
      sourceEndMs > sourceStartMs && sourceStartMs >= startMs && sourceEndMs <= renderedEndMs,
      `${message} clip ${clipNumber} source range stays inside rendered timing (startMs=${startMs}, renderedEndMs=${renderedEndMs}, sourceStartMs=${sourceStartMs}, sourceEndMs=${sourceEndMs})`,
    );
    assertRule(
      speechEndMs > speechStartMs && speechStartMs >= sourceStartMs && speechEndMs <= sourceEndMs,
      `${message} clip ${clipNumber} speech range stays inside source range (sourceStartMs=${sourceStartMs}, sourceEndMs=${sourceEndMs}, speechStartMs=${speechStartMs}, speechEndMs=${speechEndMs})`,
    );

    previousRenderedEndMs = renderedEndMs;
  });
}

function normalizeSubtitleCueText(text) {
  return String(text ?? '').trim().replace(/\s+/gu, ' ');
}

function normalizeSubtitleCueCoverageText(text) {
  return normalizeSubtitleCueText(text)
    .replace(/\s+([,.;:!?])/gu, '$1')
    .replace(/([,.;:!?])(?=\S)/gu, '$1 ')
    .replace(/\s+/gu, ' ')
    .trim();
}

function countSubtitleCueDisplayUnits(text) {
  let units = 0;
  for (const character of normalizeSubtitleCueText(text)) {
    units += /\p{Script=Han}/u.test(character) ? 2 : 1;
  }
  return units;
}

function joinSubtitleCueCoverageText(segments) {
  return normalizeSubtitleCueCoverageText(
    (Array.isArray(segments) ? segments : [])
      .map((segment) => segment?.text)
      .filter((text) => typeof text === 'string' && text.trim())
      .join(' '),
  );
}

function assertArrayIncludes(actual, expectedItem, message) {
  assertRule(
    Array.isArray(actual) && actual.includes(expectedItem),
    `${message} (expected array to include ${JSON.stringify(expectedItem)}, got ${JSON.stringify(actual)})`,
  );
}

function assertArrayNotIncludes(actual, unexpectedItem, message) {
  assertRule(
    Array.isArray(actual) && !actual.includes(unexpectedItem),
    `${message} (expected array not to include ${JSON.stringify(unexpectedItem)}, got ${JSON.stringify(actual)})`,
  );
}

function captureConsoleDiagnostics(action) {
  const originalError = console.error;
  const originalWarn = console.warn;
  const originalInfo = console.info;
  const calls = [];
  console.error = (...args) => {
    calls.push({ level: 'error', args });
  };
  console.warn = (...args) => {
    calls.push({ level: 'warning', args });
  };
  console.info = (...args) => {
    calls.push({ level: 'info', args });
  };

  try {
    action();
  } finally {
    console.error = originalError;
    console.warn = originalWarn;
    console.info = originalInfo;
  }

  return calls;
}

async function captureConsoleDiagnosticsAsync(action) {
  const originalError = console.error;
  const originalWarn = console.warn;
  const originalInfo = console.info;
  const calls = [];
  console.error = (...args) => {
    calls.push({ level: 'error', args });
  };
  console.warn = (...args) => {
    calls.push({ level: 'warning', args });
  };
  console.info = (...args) => {
    calls.push({ level: 'info', args });
  };

  try {
    await action();
  } finally {
    console.error = originalError;
    console.warn = originalWarn;
    console.info = originalInfo;
  }

  return calls;
}

function resolveWorkflowSourceName(params) {
  if (params.file?.name) {
    return params.file.name;
  }

  if (typeof params.url === 'string' && params.url.trim()) {
    try {
      const parsed = new URL(params.url.trim());
      const fileName = parsed.pathname.split('/').filter(Boolean).at(-1);
      return fileName || parsed.hostname || params.url.trim();
    } catch {
      return params.url.trim();
    }
  }

  return undefined;
}

function assertTaskNameUsesSourceAndSecondTimestamp(task, sourceName, message) {
  const taskName = String(task?.name ?? '');
  assertRule(
    taskName.startsWith(`${sourceName} `),
    `${message} starts with the original source file name (expected prefix ${JSON.stringify(`${sourceName} `)}, got ${JSON.stringify(taskName)})`,
  );
  assertRule(
    / \d{8}-\d{6}$/u.test(taskName),
    `${message} appends a second-precision YYYYMMDD-HHmmss timestamp (got ${JSON.stringify(taskName)})`,
  );
}

function assertAutoCutTaskIdUsesUuidV7(taskId, expectedType, message) {
  assertRule(
    new RegExp(`^task-${expectedType}-[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`, 'u').test(taskId),
    `${message} uses a normalized task type prefix and RFC 9562 UUIDv7 suffix`,
  );
}

function createTrustedLocalMediaFile(commons, sourcePath, name = 'native-source.mp4', overrides = {}) {
  return commons.createAutoCutTrustedLocalFile({
    sourcePath,
    name,
    byteSize: overrides.byteSize ?? 321000,
    mimeType: overrides.mimeType ?? 'video/mp4',
    mediaType: overrides.mediaType ?? 'video',
    hasAudioStream: overrides.hasAudioStream ?? true,
    hasVideoStream: overrides.hasVideoStream ?? (overrides.mediaType !== 'audio'),
  });
}

async function assertRejects(action, expectedMessagePart, message) {
  let rejectedError = null;

  try {
    await action();
  } catch (error) {
    rejectedError = error;
  }

  const rejectedMessage = rejectedError instanceof Error ? rejectedError.message : '';
  assertRule(rejectedError instanceof Error, `${message} rejects`);
  assertRule(
    rejectedMessage.includes(expectedMessagePart),
    `${message} explains ${expectedMessagePart}`,
  );
}

async function assertDoesNotReject(action, message) {
  try {
    await action();
  } catch (error) {
    void error;
  }
  assertRule(true, `${message} completes without fatal rejection`);
}

function assertSourceIncludes(source, expectedText, message) {
  assertRule(
    source.includes(expectedText),
    `${message} (missing ${JSON.stringify(expectedText)})`,
  );
}

function readZipUint16(bytes, offset) {
  return bytes[offset] | (bytes[offset + 1] << 8);
}

function readZipUint32(bytes, offset) {
  return (
    (bytes[offset] |
      (bytes[offset + 1] << 8) |
      (bytes[offset + 2] << 16) |
      (bytes[offset + 3] << 24)) >>> 0
  );
}

async function readStoredZipEntries(blob) {
  const bytes = new Uint8Array(await blob.arrayBuffer());
  const decoder = new TextDecoder();
  const entries = new Map();
  let offset = 0;

  while (offset + 4 <= bytes.length) {
    const signature = readZipUint32(bytes, offset);
    if (signature === 0x02014b50 || signature === 0x06054b50) {
      break;
    }

    assertEqual(signature, 0x04034b50, 'task package ZIP uses local file headers before the central directory');
    const compressionMethod = readZipUint16(bytes, offset + 8);
    const compressedSize = readZipUint32(bytes, offset + 18);
    const uncompressedSize = readZipUint32(bytes, offset + 22);
    const fileNameLength = readZipUint16(bytes, offset + 26);
    const extraLength = readZipUint16(bytes, offset + 28);
    const fileNameStart = offset + 30;
    const fileNameEnd = fileNameStart + fileNameLength;
    const dataStart = fileNameEnd + extraLength;
    const dataEnd = dataStart + compressedSize;
    const fileName = decoder.decode(bytes.slice(fileNameStart, fileNameEnd));
    const data = bytes.slice(dataStart, dataEnd);

    assertEqual(compressionMethod, 0, `task package ZIP stores ${fileName} without an unsupported compression dependency`);
    assertEqual(data.length, uncompressedSize, `task package ZIP preserves the uncompressed size for ${fileName}`);
    entries.set(fileName, {
      bytes: data,
      text: decoder.decode(data),
    });
    offset = dataEnd;
  }

  assertRule(entries.size > 0, 'task package ZIP contains at least one file entry');
  return entries;
}

async function saveVerifiedLocalSpeechTranscriptionSettings(
  services,
  overrides = {},
) {
  const settings = await services.getAutoCutSettings();
  await services.saveAutoCutSpeechTranscriptionSettings({
    ...settings.speechTranscription,
    providerId: 'local-whisper-cli',
    executablePath: 'D:/tools/whisper-cli.exe',
    modelPath: 'D:/models/ggml-large-v3-turbo.bin',
    language: 'auto',
    ...overrides,
  });
  return services.markAutoCutSpeechTranscriptionProviderTested({
    ready: true,
    diagnostics: [],
  });
}

class MemoryLocalStorage {
  #items = new Map();

  get length() {
    return this.#items.size;
  }

  getItem(key) {
    return this.#items.has(key) ? this.#items.get(key) : null;
  }

  setItem(key, value) {
    this.#items.set(key, String(value));
  }

  removeItem(key) {
    this.#items.delete(key);
  }

  clear() {
    this.#items.clear();
  }

  key(index) {
    return [...this.#items.keys()][index] ?? null;
  }
}

class AutoCutCustomEvent extends Event {
  constructor(type, eventInit = {}) {
    super(type, eventInit);
    this.detail = eventInit.detail;
  }
}

function installBrowserRuntime() {
  const windowTarget = new EventTarget();
  windowTarget.confirm = () => true;
  windowTarget.open = () => null;
  windowTarget.localStorage = new MemoryLocalStorage();

  const body = {
    appendChild: () => undefined,
    removeChild: () => undefined,
  };
  const document = {
    body,
    createElement: () => {
      const anchor = {
        href: '',
        download: '',
        click: () => {
          browserDownloadRequests.push({
            href: anchor.href,
            download: anchor.download,
          });
        },
      };
      return anchor;
    },
  };

  globalThis.window = windowTarget;
  globalThis.document = document;
  globalThis.localStorage = windowTarget.localStorage;
  globalThis.CustomEvent = AutoCutCustomEvent;
  const navigator = {
    clipboard: {
      writeText: async (text) => {
        clipboardWrites.push(text);
      },
    },
  };
  windowTarget.navigator = navigator;
  Object.defineProperty(globalThis, 'navigator', {
    value: navigator,
    configurable: true,
  });

  URL.createObjectURL = (source) => {
    const url = `blob:autocut-contract-${Math.random().toString(16).slice(2)}`;
    objectUrlSources.set(url, source);
    return url;
  };
  URL.revokeObjectURL = (url) => {
    revokedObjectUrls.push(url);
    objectUrlSources.delete(url);
  };
}

async function readObjectUrlText(url) {
  const source = objectUrlSources.get(url);
  if (source && typeof source.text === 'function') {
    return await source.text();
  }
  return '';
}

async function flushMicrotasks(iterations = 30) {
  for (let index = 0; index < iterations; index += 1) {
    await Promise.resolve();
  }
}

async function withImmediateTimers(action) {
  globalThis.setTimeout = (handler, _timeout, ...args) => {
    if (typeof handler === 'function') {
      handler(...args);
    }
    return 0;
  };
  globalThis.clearTimeout = () => undefined;

  try {
    const result = await action();
    await flushMicrotasks();
    return result;
  } finally {
    globalThis.setTimeout = nativeSetTimeout;
    globalThis.clearTimeout = nativeClearTimeout;
  }
}

function resetStorage() {
  localStorage.clear();
}

function readStoredArray(storageKey) {
  return JSON.parse(localStorage.getItem(storageKey) ?? '[]');
}

function readScopedStoredArray(services, key) {
  return readStoredArray(services.createAutoCutStorageKey(key));
}

function readScopedStoredObject(services, key) {
  return JSON.parse(localStorage.getItem(services.createAutoCutStorageKey(key)) ?? '{}');
}

function assertNativeTaskOutputArtifact(
  result,
  expectedTaskUuid,
  expectedFileName,
  message,
  outputRootDir = 'D:/autocut/media',
) {
  const expectedTaskOutputDir = `${outputRootDir}/tasks/${expectedTaskUuid}`;
  assertEqual(result?.taskOutputDir, expectedTaskOutputDir, `${message} exposes the native task output directory`);
  assertEqual(
    result?.artifactPath,
    `${expectedTaskOutputDir}/${expectedFileName}`,
    `${message} stores the artifact inside its task output directory`,
  );
}

function createNativeTaskOutputArtifact(taskUuid, fileName, outputRootDir = 'D:/autocut/media') {
  const taskOutputDir = `${outputRootDir}/tasks/${taskUuid}`;
  return {
    taskOutputDir,
    artifactPath: `${taskOutputDir}/${fileName}`,
  };
}

const defaultConfiguredOutputDirectory = 'D:/autocut-configured-output';

function createNativeTaskEvidenceWriteResult(workflowTaskId, relativePath, outputRootDir = defaultConfiguredOutputDirectory, seed = '0') {
  const output = createNativeTaskOutputArtifact(workflowTaskId, relativePath, outputRootDir);
  return {
    taskUuid: workflowTaskId,
    taskOutputDir: output.taskOutputDir,
    artifactPath: output.artifactPath,
    relativePath,
    byteSize: 1,
    contentSha256: seed.repeat(64).slice(0, 64),
  };
}

function createNativeTaskCoverArtifact(taskUuid, fileName, outputRootDir = 'D:/autocut/media') {
  const output = createNativeTaskOutputArtifact(taskUuid, `cover/${fileName}`, outputRootDir);
  return output;
}

function createNativeVideoSliceTranscriptResult(request, taskUuid, outputRootDir, segments) {
  const output = createNativeTaskOutputArtifact(taskUuid, 'transcript.json', outputRootDir);
  return {
    artifactUuid: `${taskUuid}-artifact`,
    taskUuid,
    sourceAssetUuid: request.assetUuid,
    transcriptPath: output.artifactPath,
    taskOutputDir: output.taskOutputDir,
    language: request.language ?? 'auto',
    text: segments.map((segment) => segment.text).join(' '),
    segments,
    ffmpegExecutable: 'ffmpeg',
    speechExecutable: request.executablePath,
  };
}

function createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
  request,
  taskUuid,
  outputRootDir = 'D:/autocut/media',
) {
  return createNativeVideoSliceTranscriptResult(request, taskUuid, outputRootDir, [
    {
      startMs: 0,
      endMs: 8_500,
      text:
        'Why native validation matters is simple. Because bad native validation can break review, the native validation problem must be caught before assets are saved.',
      speaker: 'Speaker 1',
    },
    {
      startMs: 8_520,
      endMs: 19_000,
      text:
        'So native validation verifies every result and the native validation workflow stays safe.',
      speaker: 'Speaker 1',
    },
    {
      startMs: 40_000,
      endMs: 48_500,
      text:
        'What makes artifact checks reliable is clear. The artifact check shows missing artifact thumbnails before publishing, creating an artifact conflict for reviewers.',
      speaker: 'Speaker 1',
    },
    {
      startMs: 48_520,
      endMs: 59_000,
      text:
        'So the artifact check rejects the bad artifact before persistence and keeps artifact storage clean.',
      speaker: 'Speaker 1',
    },
  ]);
}

function createDialogueTwoQuestionNativeVideoSliceTranscriptResult(
  request,
  taskUuid,
  outputRootDir = 'D:/autocut/media',
) {
  return createNativeVideoSliceTranscriptResult(request, taskUuid, outputRootDir, [
    {
      startMs: 0,
      endMs: 8_500,
      text:
        'Why does native validation matter before assets are saved?',
      speaker: 'Host',
    },
    {
      startMs: 8_520,
      endMs: 19_000,
      text:
        'Native validation verifies every result and the native validation workflow stays safe before review artifacts are saved.',
      speaker: 'Guest',
    },
    {
      startMs: 40_000,
      endMs: 48_500,
      text:
        'What makes artifact checks reliable before publishing?',
      speaker: 'Host',
    },
    {
      startMs: 48_520,
      endMs: 59_000,
      text:
        'The artifact check rejects the bad artifact before persistence and keeps artifact storage clean.',
      speaker: 'Guest',
    },
  ]);
}

function createNativeVideoSliceAudioActivityAnalysisResult(request) {
  const analysisFilter = request.applyNoiseReduction === false
    ? 'silencedetect=noise=-35dB:d=0.08'
    : 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08';
  return {
    assetUuid: request.assetUuid,
    profile: request.profile ?? 'smart-slice-speech-denoise-v1',
    analyses: (request.clips ?? []).map((clip, index) => ({
      index,
      startMs: clip.startMs,
      durationMs: clip.durationMs,
      sourceStartMs: clip.sourceStartMs ?? clip.startMs,
      sourceEndMs: clip.sourceEndMs ?? clip.startMs + clip.durationMs,
      audioActivityStartMs: clip.speechStartMs,
      audioActivityEndMs: clip.speechEndMs,
      leadingSilenceMs: Math.max(0, (clip.speechStartMs ?? clip.startMs) - clip.startMs),
      trailingSilenceMs: Math.max(0, clip.startMs + clip.durationMs - (clip.speechEndMs ?? clip.startMs + clip.durationMs)),
      confidence: 0.97,
      analysisFilter,
    })),
  };
}

function createNativeVideoSliceAudioActivityAnalysisFilter(noiseReductionApplied) {
  return noiseReductionApplied === false
    ? 'silencedetect=noise=-35dB:d=0.08'
    : 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08';
}

function copyNativeVideoSliceClipEvidence(clip, renderNoiseReduction) {
  const renderedDurationMs = Array.isArray(clip.sourceSegments) && clip.sourceSegments.length > 1
    ? clip.sourceSegments.reduce((durationMs, segment) => durationMs + Math.max(0, segment.endMs - segment.startMs), 0)
    : clip.renderedDurationMs ?? clip.durationMs;
  const noiseReductionApplied = typeof renderNoiseReduction === 'boolean'
    ? renderNoiseReduction
    : clip.noiseReductionApplied;
  return {
    startMs: clip.startMs,
    durationMs: renderedDurationMs,
    label: clip.label,
    sourceSegments: clip.sourceSegments,
    renderedDurationMs,
    removedSilenceMs: clip.removedSilenceMs,
    internalSilenceTrimCount: clip.internalSilenceTrimCount,
    sourceStartMs: clip.sourceStartMs,
    sourceEndMs: clip.sourceEndMs,
    speechStartMs: clip.speechStartMs,
    speechEndMs: clip.speechEndMs,
    boundaryPaddingBeforeMs: clip.boundaryPaddingBeforeMs,
    boundaryPaddingAfterMs: clip.boundaryPaddingAfterMs,
    transcriptText: clip.transcriptText,
    transcriptSegments: clip.transcriptSegments,
    transcriptSegmentCount: clip.transcriptSegmentCount,
    transcriptCoverageScore: clip.transcriptCoverageScore,
    speechContinuityGrade: clip.speechContinuityGrade,
    risks: clip.risks,
    audioCleanupProfile: clip.audioCleanupProfile,
    noiseReductionApplied,
    boundaryDecisionSource: clip.boundaryDecisionSource,
    audioActivityStartMs: clip.audioActivityStartMs,
    audioActivityEndMs: clip.audioActivityEndMs,
    audioActivityConfidence: clip.audioActivityConfidence,
    audioActivityAnalysisFilter: typeof noiseReductionApplied === 'boolean'
      ? createNativeVideoSliceAudioActivityAnalysisFilter(noiseReductionApplied)
      : clip.audioActivityAnalysisFilter,
    leadingSilenceMs: clip.leadingSilenceMs,
    trailingSilenceMs: clip.trailingSilenceMs,
    leadingSilenceTrimMs: clip.leadingSilenceTrimMs,
    trailingSilenceTrimMs: clip.trailingSilenceTrimMs,
    tailTreatment: clip.tailTreatment,
  };
}

function createSuccessfulNativeVideoSliceResult(request, taskUuid, outputRootDir = defaultConfiguredOutputDirectory) {
  const slices = request.clips.map((clip, index) => {
    const sliceNumber = index + 1;
    const output = createNativeTaskOutputArtifact(
      taskUuid,
      clip.outputFileName ?? `slice-${String(sliceNumber).padStart(3, '0')}.mp4`,
      outputRootDir,
    );
    const thumbnail = createNativeTaskCoverArtifact(
      taskUuid,
      `slice-${String(sliceNumber).padStart(3, '0')}.jpg`,
      outputRootDir,
    );
    return {
      artifactUuid: `${taskUuid}-artifact-${sliceNumber}`,
      artifactPath: output.artifactPath,
      thumbnailArtifactUuid: `${taskUuid}-thumbnail-${sliceNumber}`,
      thumbnailArtifactPath: thumbnail.artifactPath,
      taskOutputDir: output.taskOutputDir,
      byteSize: 234567 + index,
      thumbnailByteSize: 12345 + index,
      format: 'mp4',
      ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
    };
  });

  return {
    taskUuid,
    sourceAssetUuid: request.assetUuid,
    taskOutputDir: slices[0]?.taskOutputDir ?? createNativeTaskOutputArtifact(taskUuid, 'slice-001.mp4', outputRootDir).taskOutputDir,
    ffmpegExecutable: 'ffmpeg',
    slices,
  };
}

function createRecoveredSmartSliceAudioCleanupEvidence(overrides = {}) {
  return {
    audioCleanupProfile: 'smart-slice-speech-denoise-v1',
    noiseReductionApplied: true,
    boundaryDecisionSource: 'combined',
    audioActivityStartMs: 1200,
    audioActivityEndMs: 15750,
    audioActivityConfidence: 0.97,
    audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
    leadingSilenceMs: 200,
    trailingSilenceMs: 250,
    leadingSilenceTrimMs: 0,
    trailingSilenceTrimMs: 0,
    tailTreatment: 'none',
    ...overrides,
  };
}

function configureAutoCutNativeHostClient(services, client, options = {}) {
  services.configureAutoCutNativeHostClient({
    ...client,
    getCapabilities: async () => {
      const capabilities = await client.getCapabilities();
      if (!capabilities?.videoSliceCommandReady) {
        return capabilities;
      }

      return {
        ...capabilities,
        taskEvidenceWriteCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        supportedCommands: Array.from(new Set([
          ...(capabilities.supportedCommands ?? []),
          'autocut_write_task_evidence_json',
          'autocut_analyze_video_slice_audio_activity',
        ])),
      };
    },
    analyzeVideoSliceAudioActivity:
      client.analyzeVideoSliceAudioActivity ??
      (async (request) => createNativeVideoSliceAudioActivityAnalysisResult(request)),
    writeTaskEvidenceJson:
      client.writeTaskEvidenceJson ??
      (async (request) => {
        options.taskEvidenceCommands?.push({ kind: 'task-evidence', request });
        const result = createNativeTaskEvidenceWriteResult(
          request.workflowTaskId,
          request.relativePath,
          request.outputRootDir ?? defaultConfiguredOutputDirectory,
        );
        return {
          ...result,
          byteSize: JSON.stringify(request.contentJson ?? {}).length,
        };
      }),
  });
}

function createAudioStreamProbeNativeHostClient(commands, operation = 'speech transcription') {
  return {
    getCapabilities: async () => ({
      mediaImportCommandReady: true,
      audioExtractionFromAssetReady: true,
      speechTranscriptionCommandReady: true,
      speechTranscriptionToolchainReady: true,
      speechTranscriptionProbeCommandReady: true,
    }),
    importMediaFile: async (request) => {
      commands.push({ kind: 'import', request });
      return {
        assetUuid: `${operation.replace(/\s+/gu, '-')}-no-audio-asset`,
        sandboxPath: `D:/autocut-configured-output/inputs/${operation.replace(/\s+/gu, '-')}-no-audio.mp4`,
        byteSize: 456000,
        name: `${operation.replace(/\s+/gu, '-')}-no-audio.mp4`,
        mediaType: 'video',
        hasAudioStream: false,
        hasVideoStream: true,
        mimeType: 'video/mp4',
        durationMs: 9000,
      };
    },
    extractAudio: async (request) => {
      commands.push({ kind: 'extract', request });
      throw new Error('audio extraction must not run for video without an audio stream');
    },
    transcribeMedia: async (request) => {
      commands.push({ kind: 'transcribe', request });
      throw new Error('speech transcription must not run for video without an audio stream');
    },
    probeSpeechTranscription: async (request) => ({
      ready: true,
      executablePath: request.executablePath,
      modelPath: request.modelPath,
      sourceKind: request.sourceKind ?? 'execution-preflight',
      diagnostics: [],
      versionLine: `${operation} audio stream preflight contract`,
    }),
    createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
  };
}

function createVideoStreamProbeNativeHostClient(commands, operation = 'video processing') {
  const nativeTaskOutput = createNativeTaskOutputArtifact(
    `${operation.replace(/\s+/gu, '-')}-unexpected-task`,
    `${operation.replace(/\s+/gu, '-')}-unexpected.mp4`,
    'D:/autocut-configured-output',
  );

  const rejectUnexpectedVideoCommand = async (request) => {
    commands.push({ kind: 'video-command', request });
    throw new Error('video processing must not run for media without a video stream');
  };

  return {
    getCapabilities: async () => ({
      mediaImportCommandReady: true,
      videoGifCommandReady: true,
      videoCompressCommandReady: true,
      videoConvertCommandReady: true,
      videoEnhanceCommandReady: true,
    }),
    importMediaFile: async (request) => {
      commands.push({ kind: 'import', request });
      return {
        assetUuid: `${operation.replace(/\s+/gu, '-')}-no-video-asset`,
        sandboxPath: `D:/autocut-configured-output/inputs/${operation.replace(/\s+/gu, '-')}-no-video.flac`,
        byteSize: 456000,
        name: `${operation.replace(/\s+/gu, '-')}-no-video.flac`,
        mediaType: 'audio',
        hasAudioStream: true,
        hasVideoStream: false,
        mimeType: 'audio/flac',
        durationMs: 9000,
      };
    },
    generateGif: rejectUnexpectedVideoCommand,
    compressVideo: rejectUnexpectedVideoCommand,
    convertVideo: rejectUnexpectedVideoCommand,
    enhanceVideo: rejectUnexpectedVideoCommand,
    runAudioSmoke: async () => ({
      artifactUuid: 'native-audio-smoke',
      taskUuid: 'native-audio-smoke-task',
      sourceAssetUuid: '',
      artifactPath: nativeTaskOutput.artifactPath,
      taskOutputDir: nativeTaskOutput.taskOutputDir,
      byteSize: 1,
      format: 'wav',
      ffmpegExecutable: 'ffmpeg',
    }),
    createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
  };
}

function createStandardNativeVideoSliceTranscriptResult(
  request,
  taskUuid,
  outputRootDir = 'D:/autocut/media',
) {
  return createNativeVideoSliceTranscriptResult(request, taskUuid, outputRootDir, [
    {
      startMs: 0,
      endMs: 19_000,
      text: 'Why native validation matters is simple. Because bad artifacts can break review, the problem must be caught before assets are saved. So verify every result and the workflow stays safe.',
      speaker: 'Speaker 1',
    },
    {
      startMs: 40_000,
      endMs: 59_000,
      text: 'What makes artifact checks reliable is clear. The case shows missing thumbnails before publishing, so the solution is to reject the task and keep storage clean.',
      speaker: 'Speaker 1',
    },
  ]);
}

function decodeEscapedText(value) {
  return JSON.parse(`"${value}"`);
}

function readWenan5RealSpeechTranscriptSegments() {
  const transcriptPath = resolveWenan5RealSpeechTranscriptFixturePath();
  const transcript = JSON.parse(readFileSync(transcriptPath, 'utf8'));
  const entries = Array.isArray(transcript.transcription) ? transcript.transcription : [];
  const segments = entries
    .map((entry) => {
      const startMs = Number(entry?.offsets?.from);
      const endMs = Number(entry?.offsets?.to);
      const text = typeof entry?.text === 'string' ? entry.text.trim() : '';
      if (!Number.isFinite(startMs) || !Number.isFinite(endMs) || endMs <= startMs || !text) {
        return undefined;
      }
      return {
        startMs: Math.round(startMs),
        endMs: Math.round(endMs),
        text,
        speaker: 'Speaker 1',
      };
    })
    .filter(Boolean);
  if (segments.length === 0) {
    throw new Error(`wenan5 real speech transcript fixture contains no timestamped speech: ${transcriptPath}`);
  }
  return segments;
}

function resolveWenan5RealSpeechTranscriptFixturePath() {
  const candidates = [
    process.env.SDKWORK_AUTOCUT_WENAN5_TRANSCRIPT_FIXTURE,
    path.join(rootDir, 'artifacts/autocut-diagnostics/wenan5/speech-transcript.json'),
    path.join(rootDir, 'scripts/fixtures/autocut/wenan5/speech-transcript.json'),
  ].filter((candidate) => typeof candidate === 'string' && candidate.trim().length > 0);

  const transcriptPath = candidates.find((candidate) => existsSync(candidate));
  if (!transcriptPath) {
    throw new Error(
      `missing wenan5 real speech transcript fixture. Checked: ${candidates.join(', ')}`,
    );
  }
  return transcriptPath;
}

function captureEvents(services, eventName) {
  const details = [];
  const stop = services.listenAutoCutEvent(eventName, (detail) => details.push(detail));
  return { details, stop };
}

function toPosixPath(value) {
  return value.replaceAll(path.sep, '/');
}

function outputModulePath(sourcePath) {
  return path.join(moduleLoaderOutDir, path.relative(rootDir, sourcePath)).replace(/\.(tsx?|jsx?)$/u, '.mjs');
}

function resolveLocalModulePath(fromSourcePath, specifier) {
  const basePath = specifier.startsWith('@sdkwork/autocut-')
    ? path.join(
        rootDir,
        'packages',
        `sdkwork-autocut-${specifier.slice('@sdkwork/autocut-'.length)}`,
        'src',
        'index.ts',
      )
    : path.resolve(path.dirname(fromSourcePath), specifier);

  const candidates = [
    basePath,
    `${basePath}.ts`,
    `${basePath}.tsx`,
    path.join(basePath, 'index.ts'),
    path.join(basePath, 'index.tsx'),
  ];

  for (const candidate of candidates) {
    try {
      readFileSync(candidate);
      return candidate;
    } catch {
      // Continue scanning extension candidates.
    }
  }

  return undefined;
}

function findPackageRoot(sourcePath) {
  const relativePath = path.relative(rootDir, sourcePath).split(path.sep);
  if (relativePath[0] === 'packages' && relativePath[1]) {
    return path.join(rootDir, 'packages', relativePath[1]);
  }

  return rootDir;
}

function isNodeBuiltinSpecifier(specifier) {
  const normalizedSpecifier = specifier.startsWith('node:') ? specifier.slice(5) : specifier;
  return builtinModules.includes(normalizedSpecifier);
}

function resolveExternalModuleSpecifier(sourcePath, specifier) {
  if (isNodeBuiltinSpecifier(specifier)) {
    return undefined;
  }

  try {
    return pathToFileURL(createRequire(path.join(findPackageRoot(sourcePath), 'package.json')).resolve(specifier)).href;
  } catch {
    return undefined;
  }
}

function rewriteLocalModuleSpecifiers(sourcePath, jsSource) {
  return jsSource.replace(
    /((?:from\s*|import\s*\()\s*['"])([^'"]+)(['"])/gu,
    (match, prefix, specifier, suffix) => {
      const localModulePath = specifier.startsWith('.') || specifier.startsWith('@sdkwork/autocut-')
        ? resolveLocalModulePath(sourcePath, specifier)
        : undefined;
      if (!localModulePath) {
        const externalModuleSpecifier = !specifier.startsWith('.') && !specifier.startsWith('@sdkwork/autocut-')
          ? resolveExternalModuleSpecifier(sourcePath, specifier)
          : undefined;
        return externalModuleSpecifier
          ? `${prefix}${externalModuleSpecifier}${suffix}`
          : match;
      }

      const fromOutputPath = outputModulePath(sourcePath);
      const toOutputPath = outputModulePath(localModulePath);
      let relativeSpecifier = toPosixPath(path.relative(path.dirname(fromOutputPath), toOutputPath));
      if (!relativeSpecifier.startsWith('.')) {
        relativeSpecifier = `./${relativeSpecifier}`;
      }

      return `${prefix}${relativeSpecifier}${suffix}`;
    },
  );
}

function collectLocalModuleGraph(entryPath, seen = new Set()) {
  if (seen.has(entryPath)) {
    return seen;
  }

  seen.add(entryPath);
  const source = readFileSync(entryPath, 'utf8');
  const importPattern = /(?:from\s*|import\s*\(\s*)['"]([^'"]+)['"]/gu;
  for (const match of source.matchAll(importPattern)) {
    const specifier = match[1];
    if (!specifier || (!specifier.startsWith('.') && !specifier.startsWith('@sdkwork/autocut-'))) {
      continue;
    }

    const resolvedPath = resolveLocalModulePath(entryPath, specifier);
    if (resolvedPath) {
      collectLocalModuleGraph(resolvedPath, seen);
    }
  }

  return seen;
}

function transpileLocalModule(sourcePath) {
  const tsSource = readFileSync(sourcePath, 'utf8');
  const transpiled = ts.transpileModule(tsSource, {
    compilerOptions: {
      target: ts.ScriptTarget.ES2022,
      module: ts.ModuleKind.ESNext,
      jsx: ts.JsxEmit.ReactJSX,
      useDefineForClassFields: false,
      experimentalDecorators: true,
      isolatedModules: true,
      moduleDetection: ts.ModuleDetectionKind.Force,
    },
    fileName: sourcePath,
  });
  const rewrittenSource = rewriteLocalModuleSpecifiers(sourcePath, transpiled.outputText);
  const outPath = outputModulePath(sourcePath);
  mkdirSync(path.dirname(outPath), { recursive: true });
  writeFileSync(outPath, rewrittenSource);
}

async function loadModule(_server, relativePath) {
  const entryPath = path.join(rootDir, relativePath);
  const moduleGraph = collectLocalModuleGraph(entryPath);
  if (!moduleLoaderOutDirPrepared) {
    rmSync(moduleLoaderOutDir, { recursive: true, force: true });
    mkdirSync(moduleLoaderOutDir, { recursive: true });
    moduleLoaderOutDirPrepared = true;
  }

  for (const sourcePath of moduleGraph) {
    transpileLocalModule(sourcePath);
  }

  return import(pathToFileURL(outputModulePath(entryPath)).href);
}

async function assertProcessingWorkflow({ services, types, workflow }) {
  resetStorage();
  const processingTaskUpdates = captureEvents(services, 'taskUpdated');
  const processingAssets = captureEvents(services, 'assetAdded');
  const processingMessages = captureEvents(services, 'messageAdded');
  const processingResult = await withImmediateTimers(async () => workflow.process(workflow.params));
  processingTaskUpdates.stop();
  processingAssets.stop();
  processingMessages.stop();
  const taskStorage = readScopedStoredArray(services, 'tasks');
  const processedTask = taskStorage.find((storedTask) => storedTask.id === processingResult.taskId);
  assertEqual(processingResult.success, true, `${workflow.name} workflow reports success`);
  assertRule(Boolean(processingResult.taskId), `${workflow.name} workflow returns a task id`);
  assertEqual(processedTask?.status, types.AUTOCUT_TASK_STATUS.completed, `${workflow.name} workflow completes the persisted task`);
  assertEqual(processedTask?.progress, 100, `${workflow.name} workflow persists 100 percent progress`);
  for (const field of workflow.taskFields) {
    assertRule(Boolean(processedTask?.[field]), `${workflow.name} workflow stores ${field} on the task`);
  }
  if (workflow.params.fileId) {
    assertEqual(
      processedTask?.sourceFileId,
      workflow.params.fileId,
      `${workflow.name} workflow stores the selected source asset id on the task`,
    );
  }
  if (workflow.expectedTaskNamePart) {
    assertRule(
      processedTask?.name.includes(workflow.expectedTaskNamePart),
      `${workflow.name} workflow reflects the submitted source in the task name`,
    );
  }
  const expectedSourceName = workflow.expectedTaskNamePart ?? resolveWorkflowSourceName(workflow.params);
  if (expectedSourceName) {
    assertTaskNameUsesSourceAndSecondTimestamp(
      processedTask,
      expectedSourceName,
      `${workflow.name} workflow task name`,
    );
  }
  assertRule(
    processingTaskUpdates.details.some((updatedTask) => updatedTask.id === processingResult.taskId),
    `${workflow.name} workflow dispatches task updates`,
  );
  assertRule(processingAssets.details.length >= workflow.minAssets, `${workflow.name} workflow creates generated assets`);
  const generatedAssetIds = Array.isArray(processedTask?.generatedAssetIds) ? processedTask.generatedAssetIds : [];
  const generatedAssetIdSet = new Set(generatedAssetIds);
  const createdAssetIds = processingAssets.details.map((asset) => asset.id);
  const createdAssetIdSet = new Set(createdAssetIds);
  assertRule(
    Array.isArray(processedTask?.generatedAssetIds),
    `${workflow.name} workflow stores generatedAssetIds on the completed task`,
  );
  assertEqual(
    generatedAssetIds.length,
    processingAssets.details.length,
    `${workflow.name} workflow task generatedAssetIds matches generated asset count`,
  );
  for (const createdAssetId of createdAssetIds) {
    assertRule(
      generatedAssetIdSet.has(createdAssetId),
      `${workflow.name} workflow task generatedAssetIds includes generated asset ${createdAssetId}`,
    );
  }
  for (const assetId of generatedAssetIds) {
    assertRule(
      createdAssetIdSet.has(assetId),
      `${workflow.name} workflow generatedAssetIds only references assets created by the workflow`,
    );
  }
  const storedAssets = readScopedStoredArray(services, 'assets');
  for (const asset of processingAssets.details) {
    assertEqual(asset.sourceTaskId, processingResult.taskId, `${workflow.name} workflow event asset ${asset.id} records sourceTaskId`);
    assertEqual(asset.sourceTaskType, processedTask?.type, `${workflow.name} workflow event asset ${asset.id} records sourceTaskType`);
    const storedAsset = storedAssets.find((candidate) => candidate.id === asset.id);
    assertEqual(storedAsset?.sourceTaskId, processingResult.taskId, `${workflow.name} workflow persisted asset ${asset.id} records sourceTaskId`);
    assertEqual(storedAsset?.sourceTaskType, processedTask?.type, `${workflow.name} workflow persisted asset ${asset.id} records sourceTaskType`);
  }
  assertRule(processingMessages.details.length >= 1, `${workflow.name} workflow creates a completion message`);
}

async function assertRejectedProcessingWorkflow({ services, workflow }) {
  resetStorage();
  const taskAddedEvents = captureEvents(services, 'taskAdded');
  const taskUpdatedEvents = captureEvents(services, 'taskUpdated');
  const assetAddedEvents = captureEvents(services, 'assetAdded');
  const messageAddedEvents = captureEvents(services, 'messageAdded');
  let rejectedError = null;

  try {
    await withImmediateTimers(async () => workflow.process(workflow.params));
  } catch (error) {
    rejectedError = error;
  } finally {
    taskAddedEvents.stop();
    taskUpdatedEvents.stop();
    assetAddedEvents.stop();
    messageAddedEvents.stop();
  }

  const rejectedMessage = rejectedError instanceof Error ? rejectedError.message : '';
  assertRule(rejectedError instanceof Error, `${workflow.name} rejects invalid processing source`);
    assertRule(
      rejectedMessage.toLowerCase().includes((workflow.expectedMessagePart ?? 'source media').toLowerCase()),
      `${workflow.name} rejection explains ${workflow.expectedMessagePart ?? 'source media'}`,
    );
  const storedTasks = readScopedStoredArray(services, 'tasks');
  if (workflow.expectFailedTask) {
    assertEqual(storedTasks.length, 1, `${workflow.name} rejection persists one failed task for user-visible diagnostics`);
    assertEqual(storedTasks[0]?.status, 'failed', `${workflow.name} rejection persists failed task status`);
    assertRule(
      String(storedTasks[0]?.errorMessage ?? '').toLowerCase().includes(workflow.expectedMessagePart ?? 'source media'),
      `${workflow.name} rejection stores the actionable failure reason on the task`,
    );
    const expectedSourceName = workflow.expectedTaskNamePart ?? resolveWorkflowSourceName(workflow.params);
    if (expectedSourceName) {
      assertTaskNameUsesSourceAndSecondTimestamp(
        storedTasks[0],
        expectedSourceName,
        `${workflow.name} failed task name`,
      );
    }
    assertEqual(taskAddedEvents.details.length, 1, `${workflow.name} rejection dispatches taskAdded for the failed task`);
    assertRule(taskUpdatedEvents.details.length >= 1, `${workflow.name} rejection dispatches taskUpdated for failed task state`);
  } else {
    assertEqual(storedTasks.length, 0, `${workflow.name} rejection does not persist tasks`);
    assertEqual(taskAddedEvents.details.length, 0, `${workflow.name} rejection does not dispatch taskAdded`);
    assertEqual(taskUpdatedEvents.details.length, 0, `${workflow.name} rejection does not dispatch taskUpdated`);
  }
  assertEqual(readScopedStoredArray(services, 'assets').length, 0, `${workflow.name} rejection does not persist generated assets`);
  assertEqual(readScopedStoredArray(services, 'messages').length, 0, `${workflow.name} rejection does not persist messages`);
  assertEqual(assetAddedEvents.details.length, 0, `${workflow.name} rejection does not dispatch assetAdded`);
  assertEqual(messageAddedEvents.details.length, 0, `${workflow.name} rejection does not dispatch messageAdded`);
}

async function run() {
  assertRule(
    packageJson.scripts?.test?.includes('node scripts/check-autocut-service-behavior.mjs'),
    'root test runs the AutoCut service behavior contract',
  );

  installBrowserRuntime();

  const server = null;

  try {
    const services = await loadModule(server, 'packages/sdkwork-autocut-services/src/index.ts');
    const types = await loadModule(server, 'packages/sdkwork-autocut-types/src/index.ts');
    const {
      processVideoSlice,
      analyzeVideoSlicePlan,
      renderVideoSlicePlan,
      saveVideoSliceReviewDraft,
      assertSmartSliceResultsMeetProfessionalStandard,
      assertSmartSlicePlanReadyForNativeRender,
      refreshSmartSlicePlanTranscriptEvidence,
      repairSmartSlicePlanForNativeRender,
      normalizeSmartSliceRenderableSourceSegments,
    } = await loadModule(
      server,
      'packages/sdkwork-autocut-slicer/src/service/slicerService.ts',
    );
    const { createStudioClipTimelineFromReviewSession } = await loadModule(
      server,
      'packages/sdkwork-autocut-slicer/src/service/clipWorkflow.ts',
    );
    const { processExtractorText } = await loadModule(
      server,
      'packages/sdkwork-autocut-extractor-text/src/service/extractorTextService.ts',
    );
    const { processAudioExtraction } = await loadModule(
      server,
      'packages/sdkwork-autocut-extractor-audio/src/service/audioExtractorService.ts',
    );
    const { processVideoGif } = await loadModule(
      server,
      'packages/sdkwork-autocut-video-gif/src/service/videoGifService.ts',
    );
    const { processVideoCompress } = await loadModule(
      server,
      'packages/sdkwork-autocut-video-compress/src/service/videoCompressService.ts',
    );
    const { processVideoConvert } = await loadModule(
      server,
      'packages/sdkwork-autocut-video-convert/src/service/videoConvertService.ts',
    );
    const { processVideoEnhance } = await loadModule(
      server,
      'packages/sdkwork-autocut-video-enhance/src/service/videoEnhanceService.ts',
    );
    const { processSubtitleTranslate } = await loadModule(
      server,
      'packages/sdkwork-autocut-subtitle-translate/src/service/subtitleTranslateService.ts',
    );
    const { processVoiceTranslate } = await loadModule(
      server,
      'packages/sdkwork-autocut-voice-translate/src/service/voiceTranslateService.ts',
    );
    await assertRejects(
      async () => services.assertAutoCutMediaHasAudioStream(
        { mediaType: 'audio', hasAudioStream: false },
        'audio evidence contract',
      ),
      'requires source media with an audio stream',
      'audio stream evidence assertion rejects audio-labeled media without verified audio stream',
    );
    await assertRejects(
      async () => services.assertAutoCutMediaHasVideoStream(
        { mediaType: 'video', hasVideoStream: false },
        'video evidence contract',
      ),
      'requires source media with a video stream',
      'video stream evidence assertion rejects video-labeled media without verified video stream',
    );
    resetStorage();
    services.resetAutoCutNativeHostClient();
    const emptyBrowserTasks = await withImmediateTimers(() => services.getTasks());
    assertEqual(emptyBrowserTasks.length, 0, 'getTasks does not seed browser fallback storage with mock tasks');
    assertDeepEqual(
      types.AUTOCUT_TASK_TYPE,
      {
        videoSlice: 'video-slice',
        textExtraction: 'text-extraction',
        audioExtraction: 'audio-extraction',
        videoGif: 'video-gif',
        videoCompress: 'video-compress',
        videoConvert: 'video-convert',
        videoEnhance: 'video-enhance',
        videoDedup: 'video-dedup',
        subtitleTranslate: 'subtitle-translate',
        voiceTranslate: 'voice-translate',
      },
      'TaskType is a stable code enum instead of localized display text',
    );
    assertRule(
      types.AUTOCUT_TASK_TYPES.every((taskType) => /^[a-z][a-z0-9-]*$/u.test(taskType)),
      'AUTOCUT_TASK_TYPES contains only stable ASCII task type codes',
    );
    assertRule(
      typeof services.getAutoCutTaskTypeLabel === 'function',
      'services exports the i18n task type label resolver',
    );
    assertRule(
      typeof services.getAutoCutI18n === 'function',
      'services exports the standard i18next instance accessor',
    );
    assertRule(
      typeof services.initializeAutoCutI18n === 'function',
      'services exports the standard i18next initializer',
    );
    assertRule(
      typeof services.createAutoCutTaskPackageArchive === 'function',
      'services exports the standard task package ZIP archive builder',
    );
    assertRule(
      typeof services.createAutoCutTaskId === 'function',
      'services exports the standard UUIDv7 task id factory',
    );
    if (typeof services.createAutoCutTaskId === 'function') {
      const firstSliceTaskId = services.createAutoCutTaskId('slice');
      const secondSliceTaskId = services.createAutoCutTaskId('slice');
      assertAutoCutTaskIdUsesUuidV7(firstSliceTaskId, 'slice', 'createAutoCutTaskId(slice)');
      assertAutoCutTaskIdUsesUuidV7(secondSliceTaskId, 'slice', 'createAutoCutTaskId(slice) second call');
      assertRule(firstSliceTaskId !== secondSliceTaskId, 'createAutoCutTaskId produces unique task ids across adjacent calls');
      assertAutoCutTaskIdUsesUuidV7(
        services.createAutoCutTaskId('Voice Translate'),
        'voice-translate',
        'createAutoCutTaskId normalizes multi-word task types',
      );
    }
    assertRule(
      typeof services.createNativeTaskExecutionProjection === 'function',
      'services exports native task execution step projection for task detail progress logs',
    );
    assertRule(
      typeof services.projectNativeTaskProgressEventToTask === 'function',
      'services exports native progress event projection for workflow task lifecycle control',
    );
    assertSourceIncludes(
      tasksServiceSource,
      'shouldProjectNativeTaskProgressEventBeforeStorage',
      'native progress projection gates high-frequency progress ticks before reading local task storage',
    );
    assertSourceIncludes(
      tasksServiceSource,
      'hasSeenNativeTaskProgressEventUuidBeforeStorage',
      'native progress projection drops duplicate native event ids before reading local task storage',
    );
    assertRule(
      typeof services.registerAutoCutTaskResumeHandler === 'function',
      'services exports a task resume handler registry for resumable workflow steps',
    );
    assertRule(
      typeof services.resumeTaskFromStep === 'function',
      'services exports a workflow task step resume API',
    );
    assertRule(
      typeof services.registerAutoCutTaskCancelHandler === 'function',
      'services exports a workflow task cancel handler registry for non-native workflow stages',
    );
    assertRule(
      typeof services.isAutoCutTaskCancellationRequested === 'function',
      'services exports a workflow task cancellation request predicate for cooperative step guards',
    );
    assertSourceIncludes(
      desktopNativeHostSource,
      "listen<AutoCutNativeTaskProgressEvent>",
      'desktop native host registers a typed native task progress event bridge',
    );
    assertSourceIncludes(
      desktopNativeHostSource,
      "dispatchAutoCutEvent('nativeTaskProgress'",
      'desktop native host redispatches native task progress into the shared service event bus',
    );
    assertSourceIncludes(
      desktopNativeHostSource,
      'projectNativeTaskProgressEventToTask(progress)',
      'desktop native host projects native progress into workflow task state before refreshing task UI',
    );
    assertRule(
      !tasksPageSource.includes("listenAutoCutEvent('nativeTaskProgress'"),
      'tasks page does not fetch tasks from raw native progress events; it consumes projected taskUpdated state to avoid large-file UI stalls',
    );
    assertSourceIncludes(
      tasksPageSource,
      'handleTaskUpdated',
      'tasks page applies projected taskUpdated events directly instead of querying native snapshots on every progress tick',
    );
    assertRule(
      !taskDetailPageSource.includes("listenAutoCutEvent('nativeTaskProgress'"),
      'task detail page does not refetch task snapshots from raw native progress events; it consumes projected taskUpdated state to keep logs responsive',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'function TaskExecutionPanel',
      'task detail page renders a queryable execution step and log panel',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'cancelTasks([task.id])',
      'task detail page can request cancellation for the active task',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'resumeTaskFromStep(task.id, step.id)',
      'task detail page can resume a failed workflow task from a selected checkpoint step',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'selectedExecutionStepId',
      'task detail page tracks the selected execution step for queryable step logs',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'visibleExecutionLogs',
      'task detail page derives a visible execution log stream from the selected step',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'data-task-execution-step-id',
      'task detail page exposes clickable execution step controls for log drilldown',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'data-task-execution-log-step-id',
      'task detail page renders queryable execution log rows with step correlation',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'formatTaskExecutionLogProviderProgress',
      'task detail page surfaces native provider progress from execution log details',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'normalizeTaskDetailDisplayText',
      'task detail page normalizes escaped diagnostic text before rendering or copying task details',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'createTaskExecutionLogClipboardText',
      'task detail page builds a complete execution log clipboard payload for operator debugging',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'data-task-execution-log-copy',
      'task detail page exposes a copy control on every execution log row',
    );
    assertRule(
      !/[\u3400-\u9FFF]/u.test(taskDetailPageSource),
      'task detail page keeps fallback UI copy ASCII or i18n-driven so source encoding cannot render mojibake',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'setShowAllExecutionLogs',
      'task detail page lets users expand beyond the latest execution logs',
    );
    assertSourceIncludes(
      taskDetailPageSource,
      'lastExecutionTaskIdRef',
      'task detail page resets execution log selection when navigating between tasks',
    );
    assertSourceIncludes(
      desktopMediaRuntimeSource,
      'AUTOCUT_NATIVE_TASK_PROGRESS_EVENT',
      'native runtime defines the authoritative native task progress event channel',
    );
    assertSourceIncludes(
      desktopMediaRuntimeSource,
      'record_ops_task_progress_for_app',
      'native runtime emits progress events only after native task progress is recorded',
    );
    assertSourceIncludes(
      desktopMediaRuntimeSource,
      '"phase": "local-whisper-started"',
      'native runtime emits a key local Whisper start step for speech-to-text monitoring',
    );
    assertSourceIncludes(
      desktopMediaRuntimeSource,
      '"phase": "transcript-parsed"',
      'native runtime emits a key transcript parsed step for speech-to-text monitoring',
    );
    assertSourceIncludes(
      desktopMediaRuntimeSource,
      'read_ops_task_input_string_field(connection, &normalized_task_uuid, "workflowTaskId")',
      'native runtime carries workflow task correlation across all subsequent progress events',
    );
    if (typeof services.createNativeTaskExecutionProjection === 'function') {
      const nativeExecutionProjection = services.createNativeTaskExecutionProjection({
        uuid: 'native-progress-task',
        taskType: 7,
        status: 1,
        progress: 75,
        sourceAssetUuid: 'native-progress-asset',
        inputJson: '{}',
        outputJson: '{}',
        createdAt: '2026-05-10T01:00:00.000Z',
        updatedAt: '2026-05-10T01:01:00.000Z',
        stages: [],
        events: [
          {
            uuid: 'native-progress-event-1',
            eventType: 8,
            payload: {
              stepId: 'extract-audio',
              phase: 'speech-audio-extracted',
              operation: 'speechTranscription',
              source: 'native',
              progress: 40,
              message: 'Audio extracted for speech-to-text.',
            },
            payloadJson: '{}',
            createdAt: '2026-05-10T01:00:10.000Z',
            updatedAt: '2026-05-10T01:00:10.000Z',
          },
          {
            uuid: 'native-progress-event-2',
            eventType: 8,
            payload: {
              stepId: 'speech-to-text',
              phase: 'local-whisper-started',
              operation: 'speechTranscription',
              source: 'native',
              progress: 45,
              message: 'Whisper transcription started.',
              workflowTaskId: 'workflow-progress-task',
            },
            payloadJson: '{}',
            createdAt: '2026-05-10T01:00:15.000Z',
            updatedAt: '2026-05-10T01:00:15.000Z',
          },
          {
            uuid: 'native-progress-event-3',
            eventType: 8,
            payload: {
              stepId: 'speech-to-text',
              phase: 'local-whisper-completed',
              operation: 'speechTranscription',
              source: 'native',
              progress: 75,
              message: 'Whisper transcription completed.',
            },
            payloadJson: '{}',
            createdAt: '2026-05-10T01:01:00.000Z',
            updatedAt: '2026-05-10T01:01:00.000Z',
          },
        ],
        workerLeases: [],
      });
      assertEqual(
        nativeExecutionProjection.currentStepId,
        'speech-to-text',
        'native task execution projection exposes the current running speech-to-text step',
      );
      assertRule(
        nativeExecutionProjection.executionSteps.some((step) =>
          step.id === 'extract-audio' &&
            step.status === 'running' &&
            step.progress === 40 &&
            step.canResumeFromHere === false
        ),
        'native task execution projection creates an extract-audio step from native progress events',
      );
      assertRule(
        nativeExecutionProjection.executionSteps.some((step) =>
          step.id === 'speech-to-text' &&
            step.status === 'running' &&
            step.progress === 75 &&
            step.message === 'Whisper transcription completed.'
        ),
        'native task execution projection updates repeated speech-to-text progress events on one step',
      );
      assertRule(
        nativeExecutionProjection.executionLogs.some((log) =>
          log.stepId === 'speech-to-text' &&
            log.phase === 'local-whisper-started' &&
            log.source === 'native' &&
            log.progress === 45 &&
            log.details?.workflowTaskId === 'workflow-progress-task'
        ),
        'native task execution projection preserves queryable progress logs with phase source progress and workflow task correlation',
      );
    }
    if (typeof services.projectNativeTaskProgressEventToTask === 'function') {
      resetStorage();
      await withImmediateTimers(async () => {
        await services.addTask({
          id: 'workflow-native-progress-task',
          type: types.AUTOCUT_TASK_TYPE.videoSlice,
          name: 'workflow-native-progress.mp4',
          status: types.AUTOCUT_TASK_STATUS.processing,
          progress: 50,
          progressMessage: 'Running local speech-to-text for Smart Slice...',
          currentStepId: 'speech-to-text',
          createdAt: '2026-05-10T02:00:00.000Z',
          executionSteps: [
            {
              id: 'speech-to-text',
              label: 'Run speech-to-text',
              status: 'running',
              progress: 50,
              startedAt: '2026-05-10T02:00:00.000Z',
              attempts: 1,
              canResumeFromHere: false,
              checkpointKey: 'smart-slice:speech-to-text',
              message: 'Running local speech-to-text for Smart Slice...',
            },
          ],
          executionLogs: [],
        });
      });
      const nativeProgressTaskUpdateEvents = captureEvents(services, 'taskUpdated');
      const projectedNativeProgressTask = await services.projectNativeTaskProgressEventToTask({
        taskUuid: 'native-progress-event-task',
        nativeTaskId: 'native-progress-event-task',
        workflowTaskId: 'workflow-native-progress-task',
        eventUuid: 'native-progress-event-uuid',
        eventType: 8,
        operation: 'speechTranscription',
        phase: 'local-whisper-started',
        stepId: 'speech-to-text',
        progress: 45,
        message: 'Whisper transcription started.',
        severity: 'info',
        source: 'native',
        timestamp: '2026-05-10T02:00:15.000Z',
        payload: {
          workflowTaskId: 'workflow-native-progress-task',
          nativeTaskId: 'native-progress-event-task',
          providerId: 'local-whisper',
        },
      });
      nativeProgressTaskUpdateEvents.stop();
      const projectedNativeProgressStoredTask = readScopedStoredArray(services, 'tasks')
        .find((task) => task.id === 'workflow-native-progress-task');
      assertEqual(
        projectedNativeProgressTask?.nativeTaskId,
        'native-progress-event-task',
        'native progress projection returns the workflow task with the native task id immediately attached',
      );
      assertEqual(
        projectedNativeProgressStoredTask?.nativeTaskId,
        'native-progress-event-task',
        'native progress projection persists nativeTaskId onto the workflow task before the native command resolves',
      );
      assertEqual(
        projectedNativeProgressStoredTask?.progress,
        50,
        'native progress projection does not regress the workflow task progress when native subtask progress is lower',
      );
      assertRule(
        projectedNativeProgressStoredTask?.executionLogs?.some((log) =>
          log.id === 'native-progress-event-uuid' &&
            log.taskId === 'workflow-native-progress-task' &&
            log.stepId === 'speech-to-text' &&
            log.phase === 'local-whisper-started' &&
            log.source === 'native' &&
            log.details?.providerId === 'local-whisper'
        ),
        'native progress projection persists queryable native execution logs on the workflow task',
      );
      assertRule(
        projectedNativeProgressStoredTask?.executionSteps?.some((step) =>
          step.id === 'speech-to-text' &&
            step.status === 'running' &&
            step.progress === 45 &&
            step.message === 'Whisper transcription started.'
        ),
        'native progress projection updates the matching workflow execution step from native progress events',
      );
      assertEqual(
        nativeProgressTaskUpdateEvents.details.at(-1)?.nativeTaskId,
        'native-progress-event-task',
        'native progress projection dispatches a taskUpdated event with the native task mapping',
      );
      const duplicateNativeProgressTaskUpdateEvents = captureEvents(services, 'taskUpdated');
      const duplicateNativeProgressTask = await services.projectNativeTaskProgressEventToTask({
        taskUuid: 'native-progress-event-task',
        nativeTaskId: 'native-progress-event-task',
        workflowTaskId: 'workflow-native-progress-task',
        eventUuid: 'native-progress-event-uuid',
        eventType: 8,
        operation: 'speechTranscription',
        phase: 'local-whisper-started',
        stepId: 'speech-to-text',
        progress: 45,
        message: 'Whisper transcription started.',
        severity: 'info',
        source: 'native',
        timestamp: '2026-05-10T02:00:15.000Z',
        payload: {
          workflowTaskId: 'workflow-native-progress-task',
          nativeTaskId: 'native-progress-event-task',
          providerId: 'local-whisper',
        },
      });
      duplicateNativeProgressTaskUpdateEvents.stop();
      const projectedNativeProgressStoredTaskAfterDuplicate = readScopedStoredArray(services, 'tasks')
        .find((task) => task.id === 'workflow-native-progress-task');
      assertEqual(
        duplicateNativeProgressTask,
        undefined,
        'native progress projection drops duplicate native progress events before reading local task storage',
      );
      assertEqual(
        duplicateNativeProgressTaskUpdateEvents.details.length,
        0,
        'native progress projection skips taskUpdated dispatch for duplicate native progress event ids',
      );
      assertEqual(
        projectedNativeProgressStoredTaskAfterDuplicate?.executionLogs?.filter((log) => log.id === 'native-progress-event-uuid').length,
        1,
        'native progress projection keeps duplicate native progress event ids idempotent in execution logs',
      );
      const minorNativeProgressTaskUpdateEvents = captureEvents(services, 'taskUpdated');
      const minorNativeProgressTask = await services.projectNativeTaskProgressEventToTask({
        taskUuid: 'native-progress-event-task',
        nativeTaskId: 'native-progress-event-task',
        workflowTaskId: 'workflow-native-progress-task',
        eventUuid: 'native-progress-event-uuid-46',
        eventType: 8,
        operation: 'speechTranscription',
        phase: 'local-whisper-started',
        stepId: 'speech-to-text',
        progress: 46,
        message: 'Whisper transcription progress 46%.',
        severity: 'info',
        source: 'native',
        timestamp: '2026-05-10T02:00:16.000Z',
        payload: {
          workflowTaskId: 'workflow-native-progress-task',
          nativeTaskId: 'native-progress-event-task',
          providerId: 'local-whisper',
        },
      });
      minorNativeProgressTaskUpdateEvents.stop();
      const projectedNativeProgressStoredTaskAfterMinorProgress = readScopedStoredArray(services, 'tasks')
        .find((task) => task.id === 'workflow-native-progress-task');
      assertEqual(
        minorNativeProgressTask,
        undefined,
        'native progress projection drops same-bucket sub-threshold progress before reading local task storage',
      );
      assertEqual(
        minorNativeProgressTaskUpdateEvents.details.length,
        0,
        'native progress projection skips sub-threshold progress ticks so large files do not flood local task storage',
      );
      assertEqual(
        projectedNativeProgressStoredTaskAfterMinorProgress?.executionLogs?.some((log) => log.id === 'native-progress-event-uuid-46'),
        false,
        'native progress projection does not persist sub-threshold progress tick logs',
      );
      const milestoneNativeProgressTaskUpdateEvents = captureEvents(services, 'taskUpdated');
      await services.projectNativeTaskProgressEventToTask({
        taskUuid: 'native-progress-event-task',
        nativeTaskId: 'native-progress-event-task',
        workflowTaskId: 'workflow-native-progress-task',
        eventUuid: 'native-progress-event-uuid-50',
        eventType: 8,
        operation: 'speechTranscription',
        phase: 'local-whisper-started',
        stepId: 'speech-to-text',
        progress: 50,
        message: 'Whisper transcription progress 50%.',
        severity: 'info',
        source: 'native',
        timestamp: '2026-05-10T02:00:20.000Z',
        payload: {
          workflowTaskId: 'workflow-native-progress-task',
          nativeTaskId: 'native-progress-event-task',
          providerId: 'local-whisper',
        },
      });
      milestoneNativeProgressTaskUpdateEvents.stop();
      const projectedNativeProgressStoredTaskAfterMilestoneProgress = readScopedStoredArray(services, 'tasks')
        .find((task) => task.id === 'workflow-native-progress-task');
      assertEqual(
        milestoneNativeProgressTaskUpdateEvents.details.length,
        1,
        'native progress projection persists milestone progress ticks for queryable task progress',
      );
      assertRule(
        projectedNativeProgressStoredTaskAfterMilestoneProgress?.executionLogs?.some((log) =>
          log.id === 'native-progress-event-uuid-50' &&
            log.stepId === 'speech-to-text' &&
            log.progress === 50
        ),
        'native progress projection keeps milestone progress logs with step and percent evidence',
      );

      const nativeProgressCancelCommands = [];
      configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
        nativeProgressCancelCommands.push({ command, args });
        if (command === 'autocut_host_capabilities') {
          return {
            contractVersion: 'native-progress-cancel-test',
            hostKind: 'native-host',
            nativeTaskQueryCommandReady: false,
            nativeTaskCancelCommandReady: true,
            supportedCommands: ['autocut_host_capabilities', 'autocut_cancel_native_task'],
          };
        }
        if (command === 'autocut_cancel_native_task') {
          return {
            taskUuid: args.request.taskUuid,
            status: 4,
            canceled: true,
            message: 'cancel requested',
          };
        }
        throw new Error(`Unexpected native progress cancel command: ${command}`);
      }));
      const nativeProgressCancelResult = await services.cancelTasks(['workflow-native-progress-task']);
      assertDeepEqual(
        nativeProgressCancelResult.canceledTaskIds,
        ['workflow-native-progress-task'],
        'workflow task can be canceled immediately after native progress projection attaches nativeTaskId',
      );
      assertDeepEqual(
        nativeProgressCancelCommands
          .filter((entry) => entry.command === 'autocut_cancel_native_task')
          .map((entry) => entry.args.request.taskUuid),
        ['native-progress-event-task'],
        'cancelTasks uses the native task id learned from native progress events',
      );
      services.resetAutoCutNativeHostClient();
    }
    assertRule(
      typeof services.downloadAutoCutTaskPackage === 'function',
      'services exports the standard task package download action',
    );
    assertRule(
      typeof services.hasAutoCutTaskPackageDownloadables === 'function',
      'services exports a reusable task package availability predicate for task-list UI state',
    );
    const originalFetch = globalThis.fetch;
    const taskPackageFetchPayloads = new Map([
      ['https://assets.example/slice-a.mp4', 'slice-a-video'],
      ['https://assets.example/slice-b.mp4', 'slice-b-video'],
      ['https://assets.example/slice-a.jpg', 'slice-a-thumbnail'],
      ['https://assets.example/slice-b.jpg', 'slice-b-thumbnail'],
      ['https://assets.example/slice-a.srt', '1\n00:00:00,000 --> 00:00:01,000\nfirst slice'],
      ['https://assets.example/audio.mp3', 'audio-output'],
    ]);
    const taskPackageFetchRequests = [];
    globalThis.fetch = async (resource) => {
      const url = String(resource);
      taskPackageFetchRequests.push(url);
      if (!taskPackageFetchPayloads.has(url)) {
        return new Response('missing fixture', { status: 404, statusText: 'Not Found' });
      }
      return new Response(taskPackageFetchPayloads.get(url), { status: 200 });
    };
    try {
      const packageableTasks = [
        {
          id: 'task-smart-slice',
          type: types.AUTOCUT_TASK_TYPE.videoSlice,
          name: 'Launch Review / Smart Slice',
          status: types.AUTOCUT_TASK_STATUS.completed,
          progress: 100,
          createdAt: '2026-05-10T01:02:03.000Z',
          completedAt: '2026-05-10T01:03:03.000Z',
          sourceFileId: 'source-video-001',
          generatedAssetIds: ['slice-a', 'slice-b'],
          resultCount: 2,
          sliceResults: [
            {
              id: 'slice-a',
              name: 'clip.mp4',
              duration: 12,
              size: 12,
              resolution: '1080P',
              thumbnailUrl: 'https://assets.example/slice-a.jpg',
              url: 'https://assets.example/slice-a.mp4',
              subtitleUrl: 'https://assets.example/slice-a.srt',
              subtitleFormat: 'srt',
              transcriptText: 'first slice',
              transcriptSegments: [
                { startMs: 0, endMs: 1000, text: 'first slice', speaker: 'Speaker 1' },
              ],
            },
            {
              id: 'slice-b',
              name: 'clip.mp4',
              duration: 14,
              size: 12,
              resolution: '1080P',
              thumbnailUrl: 'https://assets.example/slice-b.jpg',
              url: 'https://assets.example/slice-b.mp4',
              subtitleUrl: 'https://assets.example/missing-subtitle.srt',
              subtitleFormat: 'srt',
              transcriptText: 'second slice',
              transcriptSegments: [
                { startMs: 1_200, endMs: 2_000, text: 'second slice', speaker: 'Speaker 1' },
              ],
            },
          ],
        },
        {
          id: 'task-text',
          type: types.AUTOCUT_TASK_TYPE.textExtraction,
          name: 'Interview Transcript',
          status: types.AUTOCUT_TASK_STATUS.completed,
          progress: 100,
          createdAt: '2026-05-10T02:00:00.000Z',
          completedAt: '2026-05-10T02:01:00.000Z',
          resultCount: 1,
          extractedText: [
            { time: '00:00', speaker: 'Speaker 1', text: 'usable transcript text' },
          ],
        },
        {
          id: 'task-audio',
          type: types.AUTOCUT_TASK_TYPE.audioExtraction,
          name: 'Audio Output',
          status: types.AUTOCUT_TASK_STATUS.completed,
          progress: 100,
          createdAt: '2026-05-10T02:10:00.000Z',
          completedAt: '2026-05-10T02:11:00.000Z',
          resultCount: 1,
          audioUrl: 'https://assets.example/audio.mp3',
        },
      ];
      assertEqual(
        services.hasAutoCutTaskPackageDownloadables(packageableTasks[0]),
        true,
        'hasAutoCutTaskPackageDownloadables accepts completed smart-slice tasks with generated outputs',
      );
      assertEqual(
        services.hasAutoCutTaskPackageDownloadables({
          ...packageableTasks[0],
          status: types.AUTOCUT_TASK_STATUS.processing,
        }),
        false,
        'hasAutoCutTaskPackageDownloadables rejects unfinished tasks for package download UI state',
      );
      const archive = await services.createAutoCutTaskPackageArchive(packageableTasks, {
        exportedAt: '2026-05-10T03:00:00.000Z',
        filename: 'selected-tasks.zip',
      });
      assertEqual(archive.filename, 'selected-tasks.zip', 'createAutoCutTaskPackageArchive preserves the requested ZIP filename');
      assertEqual(archive.blob.type, 'application/zip', 'createAutoCutTaskPackageArchive emits an application/zip Blob');
      assertEqual(archive.taskCount, 3, 'createAutoCutTaskPackageArchive counts packaged completed tasks');
      assertEqual(archive.includedFileCount >= 11, true, 'createAutoCutTaskPackageArchive includes task metadata, media outputs, thumbnails, subtitles, and transcript files');
      assertEqual(archive.skippedFileCount, 1, 'createAutoCutTaskPackageArchive records failed optional asset fetches without aborting the package');
      assertArrayIncludes(
        taskPackageFetchRequests,
        'https://assets.example/missing-subtitle.srt',
        'createAutoCutTaskPackageArchive attempts every advertised downloadable task artifact',
      );
      const zipEntries = await readStoredZipEntries(archive.blob);
      assertRule(zipEntries.has('manifest.json'), 'task package ZIP includes a root manifest');
      assertRule(zipEntries.has('tasks/task-smart-slice/task.json'), 'task package ZIP includes task metadata JSON for each task');
      assertRule(zipEntries.has('tasks/task-smart-slice/smart-slice-task.json'), 'task package ZIP includes smart-slice evidence JSON for video slicing tasks');
      assertRule(zipEntries.has('tasks/task-smart-slice/clip.mp4'), 'task package ZIP includes the first generated smart-slice video output directly under the task directory');
      assertRule(zipEntries.has('tasks/task-smart-slice/clip-2.mp4'), 'task package ZIP makes duplicate output filenames unique instead of overwriting artifacts');
      assertRule(zipEntries.has('tasks/task-smart-slice/cover/clip-thumbnail.jpg'), 'task package ZIP includes generated smart-slice thumbnails under the task cover directory');
      assertRule(zipEntries.has('tasks/task-smart-slice/clip-subtitle.srt'), 'task package ZIP includes generated smart-slice subtitle files directly under the task directory');
      assertRule(zipEntries.has('tasks/task-smart-slice/transcripts/clip-transcript.txt'), 'task package ZIP includes editable smart-slice transcript text');
      assertRule(zipEntries.has('tasks/task-text/transcript.txt'), 'task package ZIP includes extracted text outputs as readable transcript text');
      assertRule(zipEntries.has('tasks/task-audio/Audio Output-audio.mp3'), 'task package ZIP includes top-level audio extraction outputs directly under the task directory');
      assertEqual(
        zipEntries.get('tasks/task-smart-slice/clip-2.mp4')?.text,
        'slice-b-video',
        'task package ZIP stores distinct bytes for duplicate-named generated outputs',
      );
      const manifest = JSON.parse(zipEntries.get('manifest.json')?.text ?? '{}');
      assertEqual(manifest.schemaVersion, '2026-05-10.autocut-task-package.v1', 'task package manifest uses the standard schema version');
      assertEqual(manifest.taskCount, 3, 'task package manifest records packaged task count');
      assertEqual(manifest.skippedFiles?.[0]?.url, 'https://assets.example/missing-subtitle.srt', 'task package manifest records skipped optional artifact URLs');
      assertRule(
        manifest.includedFiles.some((file) => file.path === 'tasks/task-smart-slice/clip-2.mp4' && file.taskId === 'task-smart-slice'),
        'task package manifest indexes included files by archive path and task id',
      );
      browserDownloadRequests.length = 0;
      revokedObjectUrls.length = 0;
      const downloadResult = await services.downloadAutoCutTaskPackage([packageableTasks[1]], {
        exportedAt: '2026-05-10T03:05:00.000Z',
        filename: 'single-task.zip',
      });
      assertEqual(downloadResult.filename, 'single-task.zip', 'downloadAutoCutTaskPackage returns the downloaded ZIP filename');
      assertEqual(
        browserDownloadRequests.at(-1)?.download,
        'single-task.zip',
        'downloadAutoCutTaskPackage triggers a browser download with the package filename',
      );
      assertRule(
        browserDownloadRequests.at(-1)?.href?.startsWith('blob:autocut-contract-'),
        'downloadAutoCutTaskPackage downloads through a temporary object URL',
      );
      assertIncludes(
        revokedObjectUrls,
        browserDownloadRequests.at(-1)?.href,
        'downloadAutoCutTaskPackage revokes the temporary object URL after triggering download',
      );
      await assertRejects(
        () => services.createAutoCutTaskPackageArchive([{
          ...packageableTasks[0],
          status: types.AUTOCUT_TASK_STATUS.failed,
          errorMessage: 'failed task',
        }]),
        'No completed task outputs are available for package download',
        'createAutoCutTaskPackageArchive rejects selections without completed downloadable task outputs',
      );
    } finally {
      if (originalFetch === undefined) {
        delete globalThis.fetch;
      } else {
        globalThis.fetch = originalFetch;
      }
    }
    services.clearAutoCutDiagnostics();
    const consoleDiagnosticCalls = captureConsoleDiagnostics(() => {
      services.reportAutoCutDiagnostic(
        'error',
        'slicer.submit',
        'Smart Slice failed before native dispatch',
        new Error('AutoCut minimum slice duration must be less than or equal to the maximum slice duration.'),
      );
    });
    assertEqual(
      consoleDiagnosticCalls.length,
      1,
      'reportAutoCutDiagnostic writes user-visible diagnostics to the browser console',
    );
    assertEqual(
      consoleDiagnosticCalls[0]?.level,
      'error',
      'reportAutoCutDiagnostic uses console.error for error-level diagnostics',
    );
    assertRule(
      String(consoleDiagnosticCalls[0]?.args?.[0] ?? '').includes('[AutoCut:slicer.submit] Smart Slice failed before native dispatch'),
      'reportAutoCutDiagnostic console prefix includes the AutoCut source and diagnostic message',
    );
    assertEqual(
      consoleDiagnosticCalls[0]?.args?.[1]?.errorMessage,
      'AutoCut minimum slice duration must be less than or equal to the maximum slice duration.',
      'reportAutoCutDiagnostic console payload includes the underlying error message',
    );
    assertRule(
      consoleDiagnosticCalls[0]?.args?.some((arg) => arg instanceof Error),
      'reportAutoCutDiagnostic console output preserves the original Error object for stack inspection',
    );
    const storedConsoleDiagnostics = services.getAutoCutDiagnostics();
    assertEqual(
      storedConsoleDiagnostics[0]?.errorMessage,
      'AutoCut minimum slice duration must be less than or equal to the maximum slice duration.',
      'reportAutoCutDiagnostic still stores diagnostics after writing to console',
    );
    assertRule(
      typeof assertSmartSliceResultsMeetProfessionalStandard === 'function',
      'slicer service exports the smart slice professional completion gate',
    );
    assertRule(
      typeof assertSmartSlicePlanReadyForNativeRender === 'function',
      'slicer service exports the smart slice native-render readiness gate',
    );
    assertRule(
      typeof refreshSmartSlicePlanTranscriptEvidence === 'function',
      'slicer service exports the smart slice transcript evidence refresh gate',
    );
    assertRule(
      typeof repairSmartSlicePlanForNativeRender === 'function',
      'slicer service exports the smart slice render-ready transcript coverage repair gate',
    );
    assertRule(
      typeof normalizeSmartSliceRenderableSourceSegments === 'function',
      'slicer service exports the smart slice renderable sourceSegments normalizer',
    );
    if (typeof normalizeSmartSliceRenderableSourceSegments === 'function') {
      const nonSpanningRenderableSourceSegments = normalizeSmartSliceRenderableSourceSegments(
        [
          { startMs: 3800, endMs: 8000 },
          { startMs: 12000, endMs: 20000 },
        ],
        3800,
        20250,
      );
      assertEqual(
        nonSpanningRenderableSourceSegments,
        undefined,
        'smart slice result normalization drops sourceSegments that do not span the final rendered source range',
      );
      const spanningRenderableSourceSegments = normalizeSmartSliceRenderableSourceSegments(
        [
          { startMs: 3600, endMs: 8000 },
          { startMs: 12000, endMs: 20400 },
        ],
        3800,
        20250,
      );
      assertDeepEqual(
        spanningRenderableSourceSegments,
        [
          { startMs: 3800, endMs: 8000 },
          { startMs: 12000, endMs: 20250 },
        ],
        'smart slice result normalization trims sourceSegments while preserving complete final source range span',
      );
    }
    const slicerServiceSource = readFileSync(
      path.join(rootDir, 'packages/sdkwork-autocut-slicer/src/service/slicerService.ts'),
      'utf8',
    );
    assertIncludes(
      slicerServiceSource,
      'const renderReadyPlannedClips = repairSmartSlicePlanForNativeRender',
      'processVideoSlice repairs planned smart slice transcript coverage before native readiness validation',
    );
    assertIncludes(
      slicerServiceSource,
      'const renderClip = normalizeSmartSlicePlannedClipSourceSegmentsForContinuousFallback',
      'Smart Slice normalizes stale sourceSegments before dispatching clips to native rendering',
    );
    assertIncludes(
      slicerServiceSource,
      'planRenderTranscriptSegments',
      'processVideoSlice validates semantic smart slices only when render-ready transcript evidence exists',
    );
    assertIncludes(
      slicerServiceSource,
      'mergedRenderTranscriptSegments',
      'processVideoSlice dispatches native rendering with the resolved render transcript evidence',
    );
    assertIncludes(
      slicerServiceSource,
      'isRepairableSmartSliceRenderTimelineReadinessError(readinessError)',
      'Smart Slice treats transcript-refresh-expanded post-cleanup render timeline overlap as repairable before failing native rendering',
    );
    assertIncludes(
      slicerServiceSource,
      'isRepairableSmartSlicePlannedSpeechPaddingReadinessError(secondReadinessError)',
      'Smart Slice treats post-reanalysis planned speech padding as repairable before failing native rendering',
    );
    assertRule(
      !/throw new Error\(\s*['"]AutoCut transcript speech has no renderable timestamped segment\./u.test(slicerServiceSource),
      'smart-slice non-renderable transcript failures must use SmartSlicePlanningError so desktop errors expose runtime params',
    );
    assertIncludes(
      slicerServiceSource,
      'shouldRecordSmartSliceLongRunningStageProgress',
      'smart-slice service samples long-running native progress before writing task logs to prevent large-file UI stalls',
    );
    if (typeof assertSmartSliceResultsMeetProfessionalStandard === 'function') {
      const professionalSliceResult = {
        id: 'professional-slice-1',
        name: 'professional-slice-1.mp4',
        duration: 17,
        size: 123456,
        resolution: '1080P',
        thumbnailUrl: 'asset://localhost/professional-slice-1.jpg',
        url: 'asset://localhost/professional-slice-1.mp4',
        sourceStartMs: 3800,
        sourceEndMs: 20250,
        speechStartMs: 4000,
        speechEndMs: 20000,
        boundaryPaddingBeforeMs: 200,
        boundaryPaddingAfterMs: 250,
        audioCleanupProfile: 'smart-slice-speech-denoise-v1',
        noiseReductionApplied: true,
        boundaryDecisionSource: 'combined',
        audioActivityStartMs: 4000,
        audioActivityEndMs: 20000,
        audioActivityConfidence: 0.95,
        audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
        leadingSilenceMs: 200,
        trailingSilenceMs: 250,
        leadingSilenceTrimMs: 0,
        trailingSilenceTrimMs: 0,
        tailTreatment: 'none',
        transcriptText: 'The hook starts immediately. The payoff ends cleanly.',
        transcriptSegments: [
          { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
          { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
        ],
        transcriptSegmentCount: 2,
        transcriptCoverageScore: 0.96,
        speechContinuityGrade: 'strong',
      };
      let acceptedProfessionalSlice = true;
      try {
        assertSmartSliceResultsMeetProfessionalStandard([professionalSliceResult]);
      } catch {
        acceptedProfessionalSlice = false;
      }
      assertRule(
        acceptedProfessionalSlice,
        'smart slice professional completion gate accepts slices with structured STT and bounded speech padding',
      );
      let acceptedAudioCorrectedProfessionalSlice = true;
      try {
        assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            sourceStartMs: 4920,
            sourceEndMs: 24130,
            speechStartMs: 5120,
            speechEndMs: 23880,
            boundaryPaddingBeforeMs: 200,
            boundaryPaddingAfterMs: 250,
            audioActivityStartMs: 5120,
            audioActivityEndMs: 23880,
            audioActivityConfidence: 0.94,
            audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
            leadingSilenceMs: 200,
            trailingSilenceMs: 250,
            leadingSilenceTrimMs: 4920,
            trailingSilenceTrimMs: 5870,
            tailTreatment: 'fade-out',
            transcriptSegments: [
              { startMs: 5000, endMs: 12000, text: 'The hook starts immediately.' },
              { startMs: 12000, endMs: 24000, text: 'The payoff ends cleanly.' },
            ],
          },
        ]);
      } catch {
        acceptedAudioCorrectedProfessionalSlice = false;
      }
      assertRule(
        acceptedAudioCorrectedProfessionalSlice,
        'smart slice professional completion gate accepts high-confidence audio boundaries that tighten STT segment edges',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            audioActivityStartMs: undefined,
            audioActivityEndMs: undefined,
            audioActivityConfidence: undefined,
            audioActivityAnalysisFilter: undefined,
          },
        ]),
        'trusted audio activity evidence',
        'smart slice professional completion gate rejects completed slices without trusted audio activity evidence',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            audioActivityConfidence: 0.1,
          },
        ]),
        'trusted audio activity evidence',
        'smart slice professional completion gate rejects low-confidence audio activity evidence before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            leadingSilenceMs: 999,
          },
        ]),
        'audio silence evidence to match trusted audio activity padding',
        'smart slice professional completion gate rejects stale leading silence evidence before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            trailingSilenceMs: 999,
          },
        ]),
        'audio silence evidence to match trusted audio activity padding',
        'smart slice professional completion gate rejects stale trailing silence evidence before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            sourceStartMs: 0,
            speechStartMs: 1200,
            boundaryPaddingBeforeMs: 1200,
          },
        ]),
        'no more than 200ms leading and 250ms trailing silence',
        'smart slice professional completion gate rejects excessive leading silence',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            transcriptSegmentCount: 3,
          },
        ]),
        'transcriptSegmentCount to match structured transcriptSegments',
        'smart slice professional completion gate rejects transcript segment count mismatches',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            transcriptText: [
              'This starts outside the rendered source range.',
              'The hook starts immediately.',
              'The payoff ends cleanly.',
            ].join(' '),
            transcriptSegments: [
              { startMs: 3600, endMs: 4100, text: 'This starts outside the rendered source range.' },
              ...professionalSliceResult.transcriptSegments,
            ],
            transcriptSegmentCount: 3,
          },
        ]),
        'inside its rendered source range',
        'smart slice professional completion gate rejects transcript segments outside the source range',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            transcriptText: 'A stale AI summary is not valid speech-to-text evidence.',
          },
        ]),
        'transcriptText to match structured transcriptSegments',
        'smart slice professional completion gate rejects transcript text that does not match structured STT segments',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            sourceStartMs: 3700,
            speechStartMs: 3900,
            audioActivityStartMs: 3900,
          },
        ]),
        'speech range to stay covered by structured transcript segment boundaries',
        'smart slice professional completion gate rejects speechStartMs that is not covered by the first structured STT segment',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            sourceEndMs: 20400,
            speechEndMs: 20150,
            audioActivityEndMs: 20150,
          },
        ]),
        'speech range to stay covered by structured transcript segment boundaries',
        'smart slice professional completion gate rejects speechEndMs that is not covered by the final structured STT segment',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            transcriptText: 'The payoff ends cleanly. The hook starts immediately.',
            transcriptSegments: [
              { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
              { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
            ],
          },
        ]),
        'transcript segments to be ordered and non-overlapping',
        'smart slice professional completion gate rejects out-of-order structured STT segments',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            transcriptCoverageScore: 0.79,
          },
        ]),
        'transcriptCoverageScore to be at least 0.8',
        'smart slice professional completion gate rejects low transcript coverage before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            speechContinuityGrade: 'weak',
          },
        ]),
        'speechContinuityGrade to be strong or repaired',
        'smart slice professional completion gate rejects weak speech continuity before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            speechContinuityGrade: undefined,
          },
        ]),
        'speechContinuityGrade to be strong or repaired',
        'smart slice professional completion gate rejects missing speech continuity grade before task completion',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            audioCleanupProfile: undefined,
          },
        ]),
        'audio cleanup profile evidence',
        'smart slice professional completion gate rejects completed slices without audio cleanup profile evidence',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            noiseReductionApplied: undefined,
          },
        ]),
        'noise reduction decision evidence',
        'smart slice professional completion gate rejects completed slices without noise reduction decision evidence',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            boundaryDecisionSource: undefined,
          },
        ]),
        'boundary decision evidence',
        'smart slice professional completion gate rejects completed slices without boundary decision evidence',
      );
      await assertRejects(
        () => assertSmartSliceResultsMeetProfessionalStandard([
          {
            ...professionalSliceResult,
            tailTreatment: undefined,
          },
        ]),
        'tail treatment evidence',
        'smart slice professional completion gate rejects completed slices without tail treatment evidence',
      );
      assertSmartSliceResultsMeetProfessionalStandard([
        {
          ...professionalSliceResult,
          noiseReductionApplied: false,
          audioActivityStartMs: 4000,
          audioActivityEndMs: 20000,
          audioActivityConfidence: 0.95,
          audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
        },
      ]);
    }
    if (typeof assertSmartSlicePlanReadyForNativeRender === 'function') {
      const professionalTranscriptSegments = [
        { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
        { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
        { startMs: 20450, endMs: 29800, text: 'The second clip starts after the first rendered window.' },
      ];
      const professionalPlanClip = {
        index: 0,
        startMs: 3800,
        durationMs: 16450,
        label: 'Professional clip',
        sourceStartMs: 3800,
        sourceEndMs: 20250,
        speechStartMs: 4000,
        speechEndMs: 20000,
        boundaryPaddingBeforeMs: 200,
        boundaryPaddingAfterMs: 250,
        transcriptText: 'The hook starts immediately. The payoff ends cleanly.',
        transcriptSegmentCount: 2,
        transcriptCoverageScore: 0.96,
        speechContinuityGrade: 'strong',
      };
      const professionalSecondPlanClip = {
        index: 1,
        startMs: 20250,
        durationMs: 9800,
        label: 'Professional second clip',
        sourceStartMs: 20250,
        sourceEndMs: 30050,
        speechStartMs: 20450,
        speechEndMs: 29800,
        boundaryPaddingBeforeMs: 200,
        boundaryPaddingAfterMs: 250,
        transcriptText: 'The second clip starts after the first rendered window.',
        transcriptSegmentCount: 1,
        transcriptCoverageScore: 1,
        speechContinuityGrade: 'strong',
      };
      let acceptedProfessionalPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [professionalPlanClip, professionalSecondPlanClip],
          professionalTranscriptSegments,
          60000,
        );
      } catch {
        acceptedProfessionalPlan = false;
      }
      assertRule(
        acceptedProfessionalPlan,
        'smart slice native-render readiness gate accepts chronological transcript-backed planned clips',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [professionalPlanClip],
          professionalTranscriptSegments,
          60000,
        ),
        'cover every eligible transcript speech segment',
        'smart slice native-render readiness gate rejects plans that leave eligible speech outside every output clip',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [professionalPlanClip],
          professionalTranscriptSegments,
          60000,
        ),
        'segment=3 startMs=20450 endMs=29800',
        'smart slice native-render readiness gate reports the exact uncovered transcript segment timing',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            professionalPlanClip,
            {
              ...professionalSecondPlanClip,
              startMs: 20050,
              durationMs: 10000,
              sourceStartMs: 20050,
              sourceEndMs: 30050,
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'previousRenderedEndMs=20250',
        'smart slice native-render readiness gate reports previous rendered end timing for overlapping planned clips',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            professionalPlanClip,
            {
              ...professionalSecondPlanClip,
              startMs: 20050,
              durationMs: 10000,
              sourceStartMs: 20050,
              sourceEndMs: 30050,
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'Clip runtime params:',
        'smart slice native-render readiness gate includes clip runtime params for overlapping planned clips',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            professionalPlanClip,
            {
              ...professionalSecondPlanClip,
              sourceStartMs: 20250,
              sourceEndMs: 23000,
              speechStartMs: 20450,
              speechEndMs: 23000,
              durationMs: 2750,
              transcriptText: 'The second clip starts after the first rendered window.',
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'segment=3 startMs=20450 endMs=29800',
        'smart slice native-render readiness gate rejects plans that only partially cover an eligible transcript segment',
      );
      let acceptedTailRepairedProfessionalPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalSecondPlanClip,
              startMs: 43000,
              durationMs: 17000,
              sourceStartMs: 43000,
              sourceEndMs: 60000,
              speechStartMs: 43200,
              speechEndMs: 60000,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 0,
              transcriptText: 'The final payoff still belongs inside the imported source after tail timestamp repair.',
            },
          ],
          [
            {
              startMs: 43200,
              endMs: 60900,
              text: 'The final payoff still belongs inside the imported source after tail timestamp repair.',
            },
          ],
          60000,
        );
      } catch {
        acceptedTailRepairedProfessionalPlan = false;
      }
      assertRule(
        acceptedTailRepairedProfessionalPlan,
        'smart slice native-render readiness gate repairs bounded final STT tail timestamp drift consistently with clip planning',
      );
      let acceptedDroppedLongTailFillerProfessionalPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalSecondPlanClip,
              startMs: 43000,
              durationMs: 16250,
              sourceStartMs: 43000,
              sourceEndMs: 59250,
              speechStartMs: 43200,
              speechEndMs: 59000,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
              transcriptText: 'The final payoff ends before the imported source tail filler.',
              transcriptSegmentCount: 1,
            },
          ],
          [
            {
              startMs: 43200,
              endMs: 59000,
              text: 'The final payoff ends before the imported source tail filler.',
            },
            {
              startMs: 59980,
              endMs: 68000,
              text: 'um',
            },
          ],
          60000,
        );
      } catch {
        acceptedDroppedLongTailFillerProfessionalPlan = false;
      }
      assertRule(
        acceptedDroppedLongTailFillerProfessionalPlan,
        'smart slice native-render readiness gate drops long low-information tail filler that exceeds imported media duration',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              boundaryDecisionSource: 'transcript',
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          [
            { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'trusted audio activity evidence',
        'smart slice native-render readiness gate rejects post-cleanup plans without trusted audio activity evidence',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 4000,
              audioActivityEndMs: 20000,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 999,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          [
            { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'audio silence evidence to match trusted audio activity padding',
        'smart slice native-render readiness gate rejects stale audio silence evidence after cleanup',
      );
      let acceptedAudioCorrectedPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 4920,
              durationMs: 19210,
              sourceStartMs: 4920,
              sourceEndMs: 24130,
              speechStartMs: 5120,
              speechEndMs: 23880,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 5120,
              audioActivityEndMs: 23880,
              audioActivityConfidence: 0.94,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 200,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 4920,
              trailingSilenceTrimMs: 5870,
              tailTreatment: 'fade-out',
            },
          ],
          [
            { startMs: 5000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 24000, text: 'The payoff ends cleanly.' },
          ],
          60000,
        );
      } catch {
        acceptedAudioCorrectedPlan = false;
      }
      assertRule(
        acceptedAudioCorrectedPlan,
        'smart slice native-render readiness gate accepts audio boundaries that tighten STT segment edges',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 4800,
              durationMs: 19450,
              sourceStartMs: 4800,
              sourceEndMs: 24250,
              speechStartMs: 5000,
              speechEndMs: 24000,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
              boundaryDecisionSource: 'transcript',
              audioActivityStartMs: 9000,
              audioActivityEndMs: 18000,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 4200,
              trailingSilenceMs: 6250,
              leadingSilenceTrimMs: 4800,
              trailingSilenceTrimMs: 5750,
              tailTreatment: 'fade-out',
              risks: ['audio-boundary-refined', 'audio-transcript-boundary-conflict'],
            },
          ],
          [
            { startMs: 5000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 24000, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'audio activity padding',
        'smart slice native-render readiness gate rejects clips whose audible activity still has excessive leading or trailing silence',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 6000,
              durationMs: 12000,
              sourceStartMs: 6000,
              sourceEndMs: 18000,
              speechStartMs: 5000,
              speechEndMs: 17800,
              sourceSegments: [
                { startMs: 6000, endMs: 9000 },
                { startMs: 12000, endMs: 18000 },
              ],
              renderedDurationMs: 9000,
              removedSilenceMs: 3000,
              internalSilenceTrimCount: 1,
              transcriptText: 'The hook starts immediately. The payoff ends cleanly.',
              transcriptSegmentCount: 2,
            },
          ],
          [
            { startMs: 5000, endMs: 9000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 17800, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'Clip runtime params: clipNumber=1',
        'smart slice native-render readiness gate prints clip runtime params when speech range falls outside compacted source range',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 6000,
              durationMs: 12000,
              sourceStartMs: 6000,
              sourceEndMs: 18000,
              speechStartMs: 5000,
              speechEndMs: 17800,
              sourceSegments: [
                { startMs: 6000, endMs: 9000 },
                { startMs: 12000, endMs: 18000 },
              ],
              renderedDurationMs: 9000,
              removedSilenceMs: 3000,
              internalSilenceTrimCount: 1,
              transcriptText: 'The hook starts immediately. The payoff ends cleanly.',
              transcriptSegmentCount: 2,
            },
          ],
          [
            { startMs: 5000, endMs: 9000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 17800, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'sourceSegments=6000-9000,12000-18000',
        'smart slice native-render readiness gate includes compacted sourceSegments in clip runtime params',
      );
      let acceptedTrustedAudioCompactedCoarseTranscriptPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 1500,
              durationMs: 8000,
              sourceStartMs: 1500,
              sourceEndMs: 9500,
              speechStartMs: 1500,
              speechEndMs: 9500,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 0,
              sourceSegments: [
                { startMs: 1500, endMs: 4000 },
                { startMs: 6000, endMs: 9500 },
              ],
              renderedDurationMs: 6000,
              removedSilenceMs: 2000,
              internalSilenceTrimCount: 1,
              transcriptText: 'The coarse transcript starts before audio activity but the spoken words are retained.',
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'repaired',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 1500,
              audioActivityEndMs: 9500,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 0,
              leadingSilenceTrimMs: 500,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'fade-out',
              risks: ['internal-silence-trimmed', 'audio-boundary-refined'],
            },
          ],
          [
            {
              startMs: 1000,
              endMs: 9500,
              text: 'The coarse transcript starts before audio activity but the spoken words are retained.',
            },
          ],
          12000,
        );
      } catch {
        acceptedTrustedAudioCompactedCoarseTranscriptPlan = false;
      }
      assertRule(
        acceptedTrustedAudioCompactedCoarseTranscriptPlan,
        'smart slice native-render readiness gate accepts trusted audio-compacted sourceSegments for coarse STT timestamps without reporting uncovered speech',
      );
      let acceptedTrustedAudioTrimmedOpeningCoarseTranscriptPlan = true;
      try {
        assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 3035,
              durationMs: 6865,
              sourceStartMs: 3035,
              sourceEndMs: 9900,
              speechStartMs: 3035,
              speechEndMs: 9900,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 0,
              sourceSegments: [
                { startMs: 3035, endMs: 3556 },
                { startMs: 5815, endMs: 9900 },
              ],
              renderedDurationMs: 4606,
              removedSilenceMs: 2259,
              internalSilenceTrimCount: 1,
              transcriptText: 'The first coarse STT segment starts with removable silence before speech.',
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'repaired',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 3035,
              audioActivityEndMs: 9900,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 0,
              leadingSilenceTrimMs: 3035,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'fade-out',
              risks: ['internal-silence-trimmed', 'audio-boundary-refined'],
            },
          ],
          [
            {
              startMs: 0,
              endMs: 9900,
              text: 'The first coarse STT segment starts with removable silence before speech.',
            },
          ],
          12000,
        );
      } catch {
        acceptedTrustedAudioTrimmedOpeningCoarseTranscriptPlan = false;
      }
      assertRule(
        acceptedTrustedAudioTrimmedOpeningCoarseTranscriptPlan,
        'smart slice native-render readiness gate accepts trusted audio-trimmed opening silence inside a coarse STT segment',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              sourceSegments: [
                { startMs: 3800, endMs: 6000 },
                { startMs: 16000, endMs: 20250 },
              ],
              renderedDurationMs: 6450,
              removedSilenceMs: 10000,
              internalSilenceTrimCount: 1,
              risks: ['internal-silence-trimmed'],
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'retained sourceSegments to cover every structured speech-to-text transcript segment',
        'smart slice native-render readiness gate rejects sourceSegments that cut through STT-backed speech',
      );
      const nonSpanningSourceSegmentsTranscriptSegments = [
        { startMs: 3800, endMs: 8000, text: 'The first retained speech segment starts at the rendered source start.' },
        { startMs: 12000, endMs: 20000, text: 'The second retained speech segment ends before the rendered source tail.' },
        { startMs: 20450, endMs: 29800, text: 'The second clip starts after the first rendered window.' },
      ];
      const nonSpanningSourceSegmentsPlan = [
        {
          ...professionalPlanClip,
          sourceStartMs: 3800,
          sourceEndMs: 20250,
          speechStartMs: 3800,
          speechEndMs: 20000,
          boundaryPaddingBeforeMs: 0,
          boundaryPaddingAfterMs: 250,
          sourceSegments: [
            { startMs: 3800, endMs: 8000 },
            { startMs: 12000, endMs: 20000 },
          ],
          renderedDurationMs: 12200,
          removedSilenceMs: 4250,
          internalSilenceTrimCount: 1,
          transcriptText:
            'The first retained speech segment starts at the rendered source start. The second retained speech segment ends before the rendered source tail.',
          transcriptSegmentCount: 2,
          transcriptCoverageScore: 1,
          speechContinuityGrade: 'repaired',
          risks: ['internal-silence-trimmed'],
        },
        {
          ...professionalSecondPlanClip,
          transcriptText: 'The second clip starts after the first rendered window.',
          transcriptSegmentCount: 1,
          transcriptCoverageScore: 1,
          speechContinuityGrade: 'strong',
        },
      ];
      let acceptedStaleNonSpanningSourceSegmentsPlan = true;
      let staleNonSpanningSourceSegmentsPlanError = '';
      try {
        assertSmartSlicePlanReadyForNativeRender(
          nonSpanningSourceSegmentsPlan,
          nonSpanningSourceSegmentsTranscriptSegments,
          60000,
        );
      } catch (error) {
        acceptedStaleNonSpanningSourceSegmentsPlan = false;
        staleNonSpanningSourceSegmentsPlanError = error instanceof Error ? error.message : String(error);
      }
      assertRule(
        acceptedStaleNonSpanningSourceSegmentsPlan,
        `smart slice native-render readiness gate degrades stale sourceSegments that do not span the rendered source range instead of failing slicing${staleNonSpanningSourceSegmentsPlanError ? `: ${staleNonSpanningSourceSegmentsPlanError}` : ''}`,
      );
      const repairedNonSpanningSourceSegmentsPlan = repairSmartSlicePlanForNativeRender(
        nonSpanningSourceSegmentsPlan,
        nonSpanningSourceSegmentsTranscriptSegments,
        {
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
          sourceDurationMs: 60000,
        },
      );
      assertRule(
        !Array.isArray(repairedNonSpanningSourceSegmentsPlan[0]?.sourceSegments) ||
          repairedNonSpanningSourceSegmentsPlan[0].sourceSegments.length === 0 ||
          (
            repairedNonSpanningSourceSegmentsPlan[0].sourceSegments[0]?.startMs ===
              repairedNonSpanningSourceSegmentsPlan[0].sourceStartMs &&
            repairedNonSpanningSourceSegmentsPlan[0].sourceSegments.at(-1)?.endMs ===
              repairedNonSpanningSourceSegmentsPlan[0].sourceEndMs
          ),
        'smart slice render-ready repair either removes stale sourceSegments or rebuilds them to span the final rendered source range',
      );
      let acceptedRepairedNonSpanningSourceSegmentsPlan = true;
      let repairedNonSpanningSourceSegmentsPlanError = '';
      try {
        assertSmartSlicePlanReadyForNativeRender(
          repairedNonSpanningSourceSegmentsPlan,
          nonSpanningSourceSegmentsTranscriptSegments,
          60000,
        );
      } catch (error) {
        acceptedRepairedNonSpanningSourceSegmentsPlan = false;
        repairedNonSpanningSourceSegmentsPlanError = error instanceof Error ? error.message : String(error);
      }
      assertRule(
        acceptedRepairedNonSpanningSourceSegmentsPlan,
        `smart slice render-ready repair removes stale sourceSegments that do not span the rendered source range${repairedNonSpanningSourceSegmentsPlanError ? `: ${repairedNonSpanningSourceSegmentsPlanError}` : ''}`,
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            professionalPlanClip,
            {
              ...professionalSecondPlanClip,
              startMs: 20000,
              sourceStartMs: 20000,
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'planned clip 2 to start after the previous rendered clip ends',
        'smart slice native-render readiness gate rejects overlapping planned render windows',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            professionalSecondPlanClip,
            professionalPlanClip,
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'planned clip 2 to start after the previous rendered clip ends',
        'smart slice native-render readiness gate rejects out-of-order planned render windows',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              startMs: 55000,
              durationMs: 10000,
              sourceStartMs: 55000,
              sourceEndMs: 65000,
              speechStartMs: 55200,
              speechEndMs: 64750,
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'stay inside the imported media duration',
        'smart slice native-render readiness gate rejects planned clips outside imported media duration',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalSecondPlanClip,
              durationMs: 39750,
              sourceEndMs: 60000,
              speechEndMs: 59800,
              transcriptText: 'The second clip starts after the first rendered window. This transcript should never extend past the imported media.',
              transcriptSegmentCount: 2,
            },
          ],
          [
            { startMs: 20450, endMs: 29800, text: 'The second clip starts after the first rendered window.' },
            { startMs: 59000, endMs: 65000, text: 'This transcript should never extend past the imported media.' },
          ],
          60000,
        ),
        'transcript segment 2 to stay inside the imported media duration',
        'smart slice native-render readiness gate rejects STT segments that extend beyond imported media duration',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              transcriptSegmentCount: 3,
            },
          ],
          professionalTranscriptSegments,
          60000,
        ),
        'transcriptSegmentCount to match structured speech-to-text coverage',
        'smart slice native-render readiness gate rejects transcript count mismatches before native rendering',
      );
      if (typeof refreshSmartSlicePlanTranscriptEvidence === 'function') {
        const staleCheckpointPlanClip = {
          ...professionalPlanClip,
          sourceStartMs: 3800,
          sourceEndMs: 20250,
          speechStartMs: 4000,
          speechEndMs: 20000,
          transcriptText: 'The hook starts immediately. The payoff ends cleanly. Stale checkpoint text.',
          transcriptSegmentCount: 3,
          transcriptCoverageScore: 0.4,
          speechContinuityGrade: 'weak',
        };
        const refreshedPlan = refreshSmartSlicePlanTranscriptEvidence(
          [staleCheckpointPlanClip, professionalSecondPlanClip],
          professionalTranscriptSegments,
          60000,
        );
        assertEqual(
          refreshedPlan[0]?.transcriptSegmentCount,
          2,
          'smart slice transcript evidence refresh recalculates stale checkpoint transcriptSegmentCount from final source coverage',
        );
        assertEqual(
          refreshedPlan[0]?.transcriptText,
          'The hook starts immediately. The payoff ends cleanly.',
          'smart slice transcript evidence refresh recalculates stale checkpoint transcriptText from final source coverage',
        );
        assertRule(
          (refreshedPlan[0]?.transcriptCoverageScore ?? 0) >= 0.8,
          'smart slice transcript evidence refresh recalculates transcriptCoverageScore from final source coverage',
        );
        assertRule(
          refreshedPlan[0]?.speechContinuityGrade === 'strong' ||
            refreshedPlan[0]?.speechContinuityGrade === 'repaired',
          'smart slice transcript evidence refresh repairs stale weak speechContinuityGrade before native rendering',
        );
        let acceptedRefreshedStalePlan = true;
        try {
          assertSmartSlicePlanReadyForNativeRender(
            refreshedPlan,
            professionalTranscriptSegments,
            60000,
          );
        } catch {
          acceptedRefreshedStalePlan = false;
        }
        assertRule(
          acceptedRefreshedStalePlan,
          'smart slice native-render readiness gate accepts refreshed checkpoint transcript evidence',
        );
        const uncoveredTailTranscriptSegments = [
          { startMs: 4_000, endMs: 12_000, text: 'Opening explains why this application path fits experienced operators.' },
          { startMs: 12_200, endMs: 20_000, text: 'Middle evidence maps preparation material and industry history to the requirement.' },
          { startMs: 93_300, endMs: 98_700, text: 'Late uncovered transcript speech should force coverage repair.' },
        ];
        const audioRefinedPlanWithUncoveredTail = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 3_800,
              durationMs: 16_450,
              sourceStartMs: 3_800,
              sourceEndMs: 20_250,
              speechStartMs: 4_000,
              speechEndMs: 20_000,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
              boundaryDecisionSource: 'combined',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              audioActivityStartMs: 4_000,
              audioActivityEndMs: 20_000,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 200,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          uncoveredTailTranscriptSegments,
          120_000,
        );
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            audioRefinedPlanWithUncoveredTail,
            uncoveredTailTranscriptSegments,
            120_000,
          ),
          'cover every eligible transcript speech segment',
          'smart slice native-render readiness reproduces uncovered late transcript speech before render-ready coverage repair',
        );
        const repairedTailCoveragePlan = repairSmartSlicePlanForNativeRender(
          audioRefinedPlanWithUncoveredTail,
          uncoveredTailTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 120_000,
          },
        );
        assertRule(
          repairedTailCoveragePlan.length > audioRefinedPlanWithUncoveredTail.length,
          'smart slice render-ready coverage repair appends a transcript-backed clip for uncovered late speech',
        );
        assertRule(
          repairedTailCoveragePlan.some((clip) =>
            (clip.sourceStartMs ?? clip.startMs) <= 93_300 &&
              (clip.sourceEndMs ?? clip.startMs + clip.durationMs) >= 98_700 &&
              clip.transcriptText?.includes('Late uncovered transcript speech should force coverage repair.')
          ),
          'smart slice render-ready coverage repair covers the uncovered Chinese transcript segment before native rendering',
        );
        let acceptedRepairedTailCoveragePlan = true;
        let repairedTailCoveragePlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedTailCoveragePlan,
            uncoveredTailTranscriptSegments,
            120_000,
          );
        } catch (error) {
          acceptedRepairedTailCoveragePlan = false;
          repairedTailCoveragePlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedTailCoveragePlan,
          `smart slice native-render readiness accepts render-ready coverage repaired plans with late transcript speech${repairedTailCoveragePlanError ? `: ${repairedTailCoveragePlanError}` : ''}`,
        );
        const openingAndTailMissingTranscriptSegments = [
          { startMs: 0, endMs: 9_900, text: 'First STT speech starts at the source boundary and must stay included.' },
          ...Array.from({ length: 29 }, (_, middleIndex) => {
            const startMs = 12_000 + middleIndex * 5_000;
            return {
              startMs,
              endMs: startMs + 4_900,
              text: `Core clip text ${middleIndex + 1}.`,
            };
          }),
          { startMs: 158_820, endMs: 162_400, text: 'Final speech near the media tail must also produce a fallback clip.' },
        ];
        const postCleanupPlanMissingOpeningAndTail = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              index: 0,
              startMs: 11_800,
              durationMs: 50_350,
              sourceStartMs: 11_800,
              sourceEndMs: 62_150,
              speechStartMs: 12_000,
              speechEndMs: 61_900,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
            },
            {
              ...professionalSecondPlanClip,
              index: 1,
              startMs: 61_800,
              durationMs: 50_350,
              sourceStartMs: 61_800,
              sourceEndMs: 112_150,
              speechStartMs: 62_000,
              speechEndMs: 111_900,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
            },
            {
              ...professionalSecondPlanClip,
              index: 2,
              startMs: 111_800,
              durationMs: 45_350,
              label: 'Professional third clip',
              sourceStartMs: 111_800,
              sourceEndMs: 157_150,
              speechStartMs: 112_000,
              speechEndMs: 156_900,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
            },
          ],
          openingAndTailMissingTranscriptSegments,
          165_000,
        );
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            postCleanupPlanMissingOpeningAndTail,
            openingAndTailMissingTranscriptSegments,
            165_000,
          ),
          'planned clip 2 to start after the previous rendered clip ends',
          'smart slice native-render readiness rejects the refreshed plan before final repair normalizes overlapping clip timing',
        );
        const repairedOpeningAndTailCoveragePlan = repairSmartSlicePlanForNativeRender(
          postCleanupPlanMissingOpeningAndTail,
          openingAndTailMissingTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 165_000,
          },
        );
        assertRule(
          repairedOpeningAndTailCoveragePlan.some((clip) =>
            (clip.sourceStartMs ?? clip.startMs) <= 0 &&
              (clip.sourceEndMs ?? clip.startMs + clip.durationMs) >= 9_900 &&
              clip.transcriptText?.includes('First STT speech starts at the source boundary')
          ),
          'smart slice render-ready repair creates a transcript-backed opening clip when clip planning skips the first STT segment',
        );
        assertRule(
          repairedOpeningAndTailCoveragePlan.some((clip) =>
            (clip.sourceStartMs ?? clip.startMs) <= 158_820 &&
              (clip.sourceEndMs ?? clip.startMs + clip.durationMs) >= 162_400 &&
              clip.transcriptText?.includes('Final speech near the media tail')
          ),
          'smart slice render-ready repair creates a transcript-backed tail clip when clip planning skips the final STT segment',
        );
        assertRule(
          repairedOpeningAndTailCoveragePlan.every((clip) => typeof clip.transcriptCoverageScore === 'number'),
          'smart slice render-ready repair keeps transcriptCoverageScore numeric for repaired and retained clips',
        );
        assertRule(
          repairedOpeningAndTailCoveragePlan.every((clip) =>
            Number.isFinite(clip.transcriptCoverageScore) &&
              clip.transcriptCoverageScore >= 0.8
          ),
          'smart slice render-ready repair keeps transcriptCoverageScore above the native readiness floor for repaired and retained clips',
        );
        let acceptedRepairedOpeningAndTailCoveragePlan = true;
        let repairedOpeningAndTailCoveragePlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedOpeningAndTailCoveragePlan,
            openingAndTailMissingTranscriptSegments,
            165_000,
          );
        } catch (error) {
          acceptedRepairedOpeningAndTailCoveragePlan = false;
          repairedOpeningAndTailCoveragePlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedOpeningAndTailCoveragePlan,
          `smart slice native-render readiness accepts final repair for skipped opening and tail STT speech${repairedOpeningAndTailCoveragePlanError ? `: ${repairedOpeningAndTailCoveragePlanError}` : ''}`,
        );
        const smartCutEngineSemanticStoryTranscriptSegments = [
          { startMs: 0, endMs: 12_000, text: 'Watch this case background.', speaker: 'Speaker 1' },
          { startMs: 12_000, endMs: 26_000, text: 'Then the real spike comes from concentrated user pain.', speaker: 'Speaker 1' },
          { startMs: 26_000, endMs: 41_000, text: 'So this is the complete short-video payoff.', speaker: 'Speaker 1' },
        ];
        const repairedSemanticStoryPlan = repairSmartSlicePlanForNativeRender(
          [
            {
              index: 0,
              candidateId: 'semantic-story-1-3',
              planningEngine: 'smart-cut-engine',
              risks: ['smart-cut-engine', 'semantic-story-merged', 'connector-repaired'],
              title: 'Watch this case background.',
              label: 'Watch this case background.',
              summary:
                'Speech-to-text window: Watch this case background. Then the real spike comes from concentrated user pain. So this is the complete short-video payoff.',
              reason:
                'Planner selected a continuous speech-to-text window and aligned slice boundaries to transcript segment edges for complete voice content.',
              startMs: 0,
              durationMs: 41_250,
              sourceStartMs: 0,
              sourceEndMs: 41_250,
              speechStartMs: 0,
              speechEndMs: 41_000,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 250,
              transcriptText:
                'Watch this case background. Then the real spike comes from concentrated user pain. So this is the complete short-video payoff.',
              transcriptSegments: smartCutEngineSemanticStoryTranscriptSegments,
              transcriptSegmentCount: 3,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
              qualityScore: 0.9,
              continuityScore: 0.9,
              storyShape: 'complete',
              publishabilityScore: 0.9,
              publishabilityGrade: 'good',
              contentArcScore: 0.9,
              contentArcGrade: 'complete',
              contentArcStages: ['hook', 'setup', 'conflict', 'payoff'],
              contentArcMissingStages: [],
              topicCoherenceScore: 0.9,
              topicCoherenceGrade: 'strong',
              topicShiftCount: 0,
              platformReadinessScore: 0.9,
              platformReadinessGrade: 'ready',
              platformReadinessIssues: [],
              sentenceBoundaryIntegrityScore: 0.9,
              sentenceBoundaryIntegrityGrade: 'clean',
            },
          ],
          smartCutEngineSemanticStoryTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 62_000,
          },
        );
        assertEqual(
          repairedSemanticStoryPlan[0]?.candidateId,
          'semantic-story-1-3',
          'smart slice render-ready repair is idempotent for Smart Cut Engine semantic-story candidate ids',
        );
        assertEqual(
          repairedSemanticStoryPlan[0]?.title,
          'Watch this case background.',
          'smart slice render-ready repair is idempotent for Smart Cut Engine semantic-story transcript titles',
        );
        const refreshExpandedOverlapTranscriptSegments = [
          {
            startMs: 0,
            endMs: 5_600,
            text: 'Opening speech ends before the next rendered clip.',
          },
          {
            startMs: 5_894,
            endMs: 43_740,
            text: 'The refreshed transcript boundary expands this clip back before the previous render window ends.',
          },
        ];
        const refreshedOverlappingPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              candidateId: 'speech-semantic-candidate-0',
              startMs: 0,
              durationMs: 10_145,
              sourceStartMs: 0,
              sourceEndMs: 5_850,
              speechStartMs: 0,
              speechEndMs: 5_600,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 250,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: false,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 0,
              audioActivityEndMs: 5_600,
              audioActivityConfidence: 0.96,
              audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
            {
              ...professionalSecondPlanClip,
              candidateId: 'speech-semantic-candidate-1',
              index: 1,
              startMs: 10_145,
              durationMs: 33_830,
              sourceStartMs: 10_145,
              sourceEndMs: 43_975,
              speechStartMs: 10_145,
              speechEndMs: 43_740,
              transcriptText:
                'The refreshed transcript boundary expands this clip back before the previous render window ends.',
              transcriptSegments: [refreshExpandedOverlapTranscriptSegments[1]],
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 235,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: false,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 6_094,
              audioActivityEndMs: 43_725,
              audioActivityConfidence: 0.96,
              audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 200,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          refreshExpandedOverlapTranscriptSegments,
          60_000,
        );
        assertRule(
          refreshedOverlappingPlan.some((clip, index, clips) =>
            index > 0 &&
              clip.startMs < ((clips[index - 1]?.startMs ?? 0) + (clips[index - 1]?.durationMs ?? 0))
          ),
          'smart slice transcript evidence refresh can expand post-cleanup clips into overlapping render windows',
        );
        const repairedRefreshExpandedOverlapPlan = repairSmartSlicePlanForNativeRender(
          refreshedOverlappingPlan,
          refreshExpandedOverlapTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 60_000,
          },
        );
        let acceptedRepairedRefreshExpandedOverlapPlan = true;
        let repairedRefreshExpandedOverlapPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedRefreshExpandedOverlapPlan,
            refreshExpandedOverlapTranscriptSegments,
            60_000,
          );
        } catch (error) {
          acceptedRepairedRefreshExpandedOverlapPlan = false;
          repairedRefreshExpandedOverlapPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedRefreshExpandedOverlapPlan,
          `smart slice render-ready repair normalizes transcript-refresh-expanded overlapping post-cleanup clips${repairedRefreshExpandedOverlapPlanError ? `: ${repairedRefreshExpandedOverlapPlanError}` : ''}`,
        );
        const renderOverlapRuntimeTranscriptSegments = [
          {
            startMs: 0,
            endMs: 5_894,
            text: 'The first rendered clip introduces the setup and should not be duplicated in the next output.',
          },
          {
            startMs: 5_894,
            endMs: 43_740,
            text: 'The second resumed clip starts inside the first rendered range and must be normalized before native rendering.',
          },
        ];
        const renderOverlapRuntimePlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              candidateId: 'speech-semantic-candidate-0',
              index: 0,
              startMs: 0,
              durationMs: 10_150,
              sourceStartMs: 0,
              sourceEndMs: 10_150,
              speechStartMs: 0,
              speechEndMs: 10_150,
              transcriptText:
                'The first rendered clip introduces the setup and should not be duplicated in the next output.',
              transcriptSegments: renderOverlapRuntimeTranscriptSegments,
              transcriptSegmentCount: 2,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 0,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: false,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 0,
              audioActivityEndMs: 10_150,
              audioActivityConfidence: 0.96,
              audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 0,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
            {
              ...professionalSecondPlanClip,
              candidateId: 'speech-semantic-candidate-1',
              index: 1,
              startMs: 5_894,
              durationMs: 38_081,
              sourceStartMs: 5_894,
              sourceEndMs: 43_975,
              speechStartMs: 5_894,
              speechEndMs: 43_740,
              transcriptText:
                'The second resumed clip starts inside the first rendered range and must be normalized before native rendering.',
              transcriptSegments: [renderOverlapRuntimeTranscriptSegments[1]],
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 235,
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: false,
              boundaryDecisionSource: 'combined',
              audioActivityStartMs: 6_094,
              audioActivityEndMs: 43_725,
              audioActivityConfidence: 0.96,
              audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 200,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          renderOverlapRuntimeTranscriptSegments,
          60_000,
        );
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            renderOverlapRuntimePlan,
            renderOverlapRuntimeTranscriptSegments,
            60_000,
          ),
          'previousRenderedEndMs=10150',
          'smart slice native-render readiness reproduces resumed real-overlap clip timing before render-ready repair',
        );
        const repairedRenderOverlapRuntimePlan = repairSmartSlicePlanForNativeRender(
          renderOverlapRuntimePlan,
          renderOverlapRuntimeTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 60_000,
          },
        );
        let acceptedRepairedRenderOverlapRuntimePlan = true;
        let repairedRenderOverlapRuntimePlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedRenderOverlapRuntimePlan,
            renderOverlapRuntimeTranscriptSegments,
            60_000,
          );
        } catch (error) {
          acceptedRepairedRenderOverlapRuntimePlan = false;
          repairedRenderOverlapRuntimePlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedRenderOverlapRuntimePlan,
          `smart slice render-ready repair merges resumed real-overlap clips before native rendering${repairedRenderOverlapRuntimePlanError ? `: ${repairedRenderOverlapRuntimePlanError}` : ''}`,
        );
        assertRule(
          repairedRenderOverlapRuntimePlan.some((clip) =>
            (clip.sourceStartMs ?? clip.startMs) === 0 &&
              (clip.sourceEndMs ?? clip.startMs + clip.durationMs) >= 43_975 &&
              clip.transcriptText?.includes('The second resumed clip starts inside the first rendered range')
          ),
          'smart slice render-ready repair preserves resumed overlap transcript content in the normalized output clip',
        );
        const repairedRenderOverlapRuntimeMergedClip = repairedRenderOverlapRuntimePlan.find((clip) =>
          clip.risks?.includes('timeline-overlap-merged')
        );
        assertRule(
          repairedRenderOverlapRuntimeMergedClip !== undefined,
          'smart slice render-ready repair records overlap-merge provenance on resumed overlap clips',
        );
        assertRule(
          repairedRenderOverlapRuntimeMergedClip?.candidateId === undefined,
          'smart slice render-ready repair clears single-candidate ids after merging multiple planned candidates',
        );
        const excessiveSpeechPaddingTranscriptSegments = [
          { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
          { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
          { startMs: 20450, endMs: 29800, text: 'The second clip starts after the first rendered window.' },
        ];
        const excessiveSpeechPaddingPlan = [
          {
            ...professionalPlanClip,
            durationMs: 16200,
            sourceEndMs: 20000,
            speechEndMs: 20000,
            boundaryPaddingAfterMs: 0,
          },
          {
            ...professionalSecondPlanClip,
            startMs: 20100,
            durationMs: 10250,
            sourceStartMs: 20100,
            sourceEndMs: 30350,
            speechStartMs: 20450,
            speechEndMs: 29800,
            boundaryPaddingBeforeMs: 350,
            boundaryPaddingAfterMs: 550,
            audioCleanupProfile: 'smart-slice-speech-denoise-v1',
            noiseReductionApplied: false,
            boundaryDecisionSource: 'combined',
            audioActivityStartMs: 20450,
            audioActivityEndMs: 29800,
            audioActivityConfidence: 0.96,
            audioActivityAnalysisFilter: 'silencedetect=noise=-35dB:d=0.08',
            leadingSilenceMs: 350,
            trailingSilenceMs: 550,
            leadingSilenceTrimMs: 0,
            trailingSilenceTrimMs: 0,
            tailTreatment: 'none',
          },
        ];
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            excessiveSpeechPaddingPlan,
            excessiveSpeechPaddingTranscriptSegments,
            60_000,
          ),
          'planned clip 2 to keep no more than 200ms leading and 250ms trailing silence around speech',
          'smart slice native-render readiness reproduces excessive planned speech padding before render-ready repair',
        );
        const repairedExcessiveSpeechPaddingPlan = repairSmartSlicePlanForNativeRender(
          excessiveSpeechPaddingPlan,
          excessiveSpeechPaddingTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 60_000,
          },
        );
        const repairedExcessiveSpeechPaddingClip = repairedExcessiveSpeechPaddingPlan.find((clip) =>
          clip.transcriptText?.includes('The second clip starts after the first rendered window.')
        );
        assertRule(
          repairedExcessiveSpeechPaddingClip !== undefined &&
            (repairedExcessiveSpeechPaddingClip.speechStartMs ?? 0) -
              (repairedExcessiveSpeechPaddingClip.sourceStartMs ?? repairedExcessiveSpeechPaddingClip.startMs) <= 200 &&
            (repairedExcessiveSpeechPaddingClip.sourceEndMs ??
              repairedExcessiveSpeechPaddingClip.startMs + repairedExcessiveSpeechPaddingClip.durationMs) -
              (repairedExcessiveSpeechPaddingClip.speechEndMs ?? 0) <= 250,
          'smart slice render-ready repair trims excessive planned speech padding before native rendering',
        );
        let acceptedRepairedExcessiveSpeechPaddingPlan = true;
        let repairedExcessiveSpeechPaddingPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedExcessiveSpeechPaddingPlan,
            excessiveSpeechPaddingTranscriptSegments,
            60_000,
          );
        } catch (error) {
          acceptedRepairedExcessiveSpeechPaddingPlan = false;
          repairedExcessiveSpeechPaddingPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedExcessiveSpeechPaddingPlan,
          `smart slice native-render readiness accepts speech-padding repaired plans${repairedExcessiveSpeechPaddingPlanError ? `: ${repairedExcessiveSpeechPaddingPlanError}` : ''}`,
        );
        const excessiveAudioActivityPaddingTranscriptSegments = [
          {
            startMs: 0,
            endMs: 10_000,
            text: 'The recognizer keeps a coarse opening span while trusted audio activity starts later.',
          },
        ];
        const excessiveAudioActivityPaddingPlan = [
          {
            ...professionalPlanClip,
            startMs: 0,
            durationMs: 10_250,
            sourceStartMs: 0,
            sourceEndMs: 10_250,
            speechStartMs: 0,
            speechEndMs: 10_000,
            boundaryPaddingBeforeMs: 0,
            boundaryPaddingAfterMs: 250,
            audioCleanupProfile: 'smart-slice-speech-denoise-v1',
            noiseReductionApplied: true,
            boundaryDecisionSource: 'combined',
            audioActivityStartMs: 1_500,
            audioActivityEndMs: 9_500,
            audioActivityConfidence: 0.96,
            audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
            leadingSilenceMs: 1_500,
            trailingSilenceMs: 750,
            leadingSilenceTrimMs: 0,
            trailingSilenceTrimMs: 0,
            tailTreatment: 'none',
            transcriptText: 'The recognizer keeps a coarse opening span while trusted audio activity starts later.',
            transcriptSegmentCount: 1,
            transcriptCoverageScore: 1,
            speechContinuityGrade: 'strong',
          },
        ];
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            excessiveAudioActivityPaddingPlan,
            excessiveAudioActivityPaddingTranscriptSegments,
            60_000,
          ),
          'audio activity padding',
          'smart slice native-render readiness reproduces excessive trusted audio activity padding before render-ready repair',
        );
        const repairedExcessiveAudioActivityPaddingPlan = repairSmartSlicePlanForNativeRender(
          excessiveAudioActivityPaddingPlan,
          excessiveAudioActivityPaddingTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 60_000,
          },
        );
        const repairedExcessiveAudioActivityPaddingClip = repairedExcessiveAudioActivityPaddingPlan[0];
        assertEqual(
          repairedExcessiveAudioActivityPaddingClip?.sourceStartMs,
          1_300,
          'smart slice render-ready repair trims sourceStartMs to trusted audio activity leading padding',
        );
        assertEqual(
          repairedExcessiveAudioActivityPaddingClip?.sourceEndMs,
          9_750,
          'smart slice render-ready repair trims sourceEndMs to trusted audio activity trailing padding',
        );
        assertEqual(
          repairedExcessiveAudioActivityPaddingClip?.leadingSilenceMs,
          200,
          'smart slice render-ready repair refreshes leading audio activity silence evidence',
        );
        assertEqual(
          repairedExcessiveAudioActivityPaddingClip?.trailingSilenceMs,
          250,
          'smart slice render-ready repair refreshes trailing audio activity silence evidence',
        );
        let acceptedRepairedExcessiveAudioActivityPaddingPlan = true;
        let repairedExcessiveAudioActivityPaddingPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedExcessiveAudioActivityPaddingPlan,
            excessiveAudioActivityPaddingTranscriptSegments,
            60_000,
          );
        } catch (error) {
          acceptedRepairedExcessiveAudioActivityPaddingPlan = false;
          repairedExcessiveAudioActivityPaddingPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedExcessiveAudioActivityPaddingPlan,
          `smart slice native-render readiness accepts audio-activity-padding repaired plans${repairedExcessiveAudioActivityPaddingPlanError ? `: ${repairedExcessiveAudioActivityPaddingPlanError}` : ''}`,
        );
        const shortMinimumFloorPaddingPlan = [
          {
            ...professionalPlanClip,
            startMs: 0,
            durationMs: 10_000,
            sourceStartMs: 0,
            sourceEndMs: 10_000,
            speechStartMs: 200,
            speechEndMs: 10_000,
            boundaryPaddingBeforeMs: 200,
            boundaryPaddingAfterMs: 0,
            transcriptText: 'An earlier clip keeps the timeline in order before the short repaired slice.',
            transcriptSegmentCount: 1,
            transcriptCoverageScore: 1,
          },
          {
            ...professionalSecondPlanClip,
            startMs: 10_000,
            durationMs: 5_000,
            sourceStartMs: 10_000,
            sourceEndMs: 15_000,
            speechStartMs: 10_200,
            speechEndMs: 14_720,
            boundaryPaddingBeforeMs: 200,
            boundaryPaddingAfterMs: 280,
            transcriptText: 'The short clip keeps only a small amount of silence around the speech.',
            transcriptSegmentCount: 1,
            transcriptCoverageScore: 1,
          },
        ];
        const shortMinimumFloorPaddingTranscriptSegments = [
          { startMs: 200, endMs: 10_000, text: 'An earlier clip keeps the timeline in order before the short repaired slice.' },
          { startMs: 10_200, endMs: 14_720, text: 'The short clip keeps only a small amount of silence around the speech.' },
        ];
        await assertRejects(
          () => assertSmartSlicePlanReadyForNativeRender(
            shortMinimumFloorPaddingPlan,
            shortMinimumFloorPaddingTranscriptSegments,
            60_000,
          ),
          'planned clip 2 to keep no more than 200ms leading and 250ms trailing silence around speech',
          'smart slice native-render readiness reproduces short minimum-floor trailing silence before render-ready repair',
        );
        const repairedShortMinimumFloorPaddingPlan = repairSmartSlicePlanForNativeRender(
          shortMinimumFloorPaddingPlan,
          shortMinimumFloorPaddingTranscriptSegments,
          {
            mode: 'contract-mode',
            llmModel: 'gemini-3-flash-preview',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'keyword',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
            sourceDurationMs: 60_000,
          },
        );
        const repairedShortMinimumFloorPaddingClip = repairedShortMinimumFloorPaddingPlan.find((clip) =>
          clip.transcriptText?.includes('The short clip keeps only a small amount of silence around the speech.')
        );
        assertEqual(
          repairedShortMinimumFloorPaddingClip?.sourceEndMs,
          14_970,
          'smart slice render-ready repair trims short clips below the planning duration floor when native silence limits require it',
        );
        let acceptedRepairedShortMinimumFloorPaddingPlan = true;
        let repairedShortMinimumFloorPaddingPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedShortMinimumFloorPaddingPlan,
            shortMinimumFloorPaddingTranscriptSegments,
            60_000,
          );
        } catch (error) {
          acceptedRepairedShortMinimumFloorPaddingPlan = false;
          repairedShortMinimumFloorPaddingPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedShortMinimumFloorPaddingPlan,
          `smart slice native-render readiness accepts short minimum-floor padding repairs${repairedShortMinimumFloorPaddingPlanError ? `: ${repairedShortMinimumFloorPaddingPlanError}` : ''}`,
        );
        const refreshedSilenceCompactedPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 4000,
              durationMs: 18000,
              sourceStartMs: 4000,
              sourceEndMs: 22000,
              speechStartMs: 4000,
              speechEndMs: 22000,
              sourceSegments: [
                { startMs: 4000, endMs: 12000 },
                { startMs: 20000, endMs: 22000 },
              ],
              renderedDurationMs: 10000,
              removedSilenceMs: 8000,
              internalSilenceTrimCount: 1,
              transcriptText: 'Stale compacted transcript metadata.',
              transcriptSegmentCount: 99,
              transcriptCoverageScore: 0.1,
              speechContinuityGrade: 'weak',
              risks: ['internal-silence-trimmed'],
            },
          ],
          [
            { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 20000, endMs: 22000, text: 'The compressed payoff remains readable.' },
          ],
          60000,
        );
        assertRule(
          (refreshedSilenceCompactedPlan[0]?.transcriptCoverageScore ?? 0) >= 0.95,
          'smart slice transcript evidence refresh scores silence-compacted clips against retained source speech instead of removed internal silence',
        );
        const refreshedInvalidCheckpointSourceSegmentsPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              sourceSegments: [
                { startMs: 3800, endMs: 6000 },
                { startMs: 16000, endMs: 20250 },
              ],
              renderedDurationMs: 6450,
              removedSilenceMs: 10000,
              internalSilenceTrimCount: 1,
              risks: ['internal-silence-trimmed'],
            },
            professionalSecondPlanClip,
          ],
          professionalTranscriptSegments,
          60000,
        );
        let acceptedRefreshedInvalidCheckpointSourceSegments = true;
        try {
          assertSmartSlicePlanReadyForNativeRender(
            refreshedInvalidCheckpointSourceSegmentsPlan,
            professionalTranscriptSegments,
            60000,
          );
        } catch {
          acceptedRefreshedInvalidCheckpointSourceSegments = false;
        }
        assertRule(
          acceptedRefreshedInvalidCheckpointSourceSegments,
          'smart slice transcript evidence refresh repairs stale checkpoint sourceSegments that cut through STT-backed speech',
        );
        const refreshedStaleCompactedSpeechBoundaryPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 10000,
              durationMs: 10000,
              sourceStartMs: 12000,
              sourceEndMs: 20000,
              speechStartMs: 10000,
              speechEndMs: 19800,
              boundaryPaddingBeforeMs: 2000,
              boundaryPaddingAfterMs: 200,
              sourceSegments: [
                { startMs: 12000, endMs: 15000 },
                { startMs: 17000, endMs: 20000 },
              ],
              renderedDurationMs: 6000,
              removedSilenceMs: 2000,
              internalSilenceTrimCount: 1,
              transcriptText: 'Stale compacted transcript metadata.',
              transcriptSegmentCount: 99,
              transcriptCoverageScore: 0.1,
              speechContinuityGrade: 'weak',
              risks: ['internal-silence-trimmed'],
            },
          ],
          [
            { startMs: 12050, endMs: 14800, text: 'The retained first speech island starts after compaction.' },
            { startMs: 17100, endMs: 19800, text: 'The retained second speech island remains complete.' },
          ],
          30000,
        );
        let acceptedRefreshedStaleCompactedSpeechBoundaryPlan = true;
        try {
          assertSmartSlicePlanReadyForNativeRender(
            refreshedStaleCompactedSpeechBoundaryPlan,
            [
              { startMs: 12050, endMs: 14800, text: 'The retained first speech island starts after compaction.' },
              { startMs: 17100, endMs: 19800, text: 'The retained second speech island remains complete.' },
            ],
            30000,
          );
        } catch {
          acceptedRefreshedStaleCompactedSpeechBoundaryPlan = false;
        }
        assertRule(
          acceptedRefreshedStaleCompactedSpeechBoundaryPlan,
          'smart slice transcript evidence refresh repairs stale compacted speech boundaries before native rendering',
        );
        assertRule(
          (refreshedStaleCompactedSpeechBoundaryPlan[0]?.speechStartMs ?? 0) >=
            (refreshedStaleCompactedSpeechBoundaryPlan[0]?.sourceStartMs ?? Number.POSITIVE_INFINITY),
          'smart slice transcript evidence refresh keeps speechStartMs inside the final retained source range',
        );
        assertRule(
          (refreshedStaleCompactedSpeechBoundaryPlan[0]?.speechEndMs ?? Number.POSITIVE_INFINITY) <=
            (refreshedStaleCompactedSpeechBoundaryPlan[0]?.sourceEndMs ?? 0),
          'smart slice transcript evidence refresh keeps speechEndMs inside the final retained source range',
        );
        const audioTrimmedBoundaryPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 120,
              durationMs: 9800,
              sourceStartMs: 120,
              sourceEndMs: 9920,
              speechStartMs: 120,
              speechEndMs: 9920,
              boundaryDecisionSource: 'combined',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              audioActivityStartMs: 120,
              audioActivityEndMs: 9920,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              transcriptText: 'Stale boundary-trimmed transcript.',
              transcriptSegmentCount: 99,
              transcriptCoverageScore: 0.1,
              speechContinuityGrade: 'weak',
            },
          ],
          [
            { startMs: 0, endMs: 9900, text: 'The recognizer segment starts exactly at the source boundary.' },
          ],
          60000,
        );
        assertEqual(
          audioTrimmedBoundaryPlan[0]?.sourceStartMs,
          0,
          'smart slice transcript evidence refresh expands audio-trimmed sourceStartMs back to the eligible STT segment boundary',
        );
        assertEqual(
          audioTrimmedBoundaryPlan[0]?.speechStartMs,
          0,
          'smart slice transcript evidence refresh expands audio-trimmed speechStartMs back to the eligible STT segment boundary',
        );
        let acceptedAudioTrimmedBoundaryPlan = true;
        let audioTrimmedBoundaryPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            audioTrimmedBoundaryPlan,
            [
              { startMs: 0, endMs: 9900, text: 'The recognizer segment starts exactly at the source boundary.' },
            ],
            60000,
          );
        } catch (error) {
          acceptedAudioTrimmedBoundaryPlan = false;
          audioTrimmedBoundaryPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedAudioTrimmedBoundaryPlan,
          `smart slice native-render readiness accepts refreshed clips whose audio cleanup originally trimmed an eligible STT segment boundary${audioTrimmedBoundaryPlanError ? `: ${audioTrimmedBoundaryPlanError}` : ''}`,
        );
        const audioPaddingProtectedBoundaryPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 1500,
              durationMs: 8300,
              sourceStartMs: 1500,
              sourceEndMs: 9800,
              speechStartMs: 1500,
              speechEndMs: 9800,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 0,
              boundaryDecisionSource: 'combined',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              audioActivityStartMs: 1700,
              audioActivityEndMs: 9550,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 0,
              leadingSilenceTrimMs: 1500,
              trailingSilenceTrimMs: 250,
              transcriptText: 'Stale audio-padding-protected transcript.',
              transcriptSegmentCount: 99,
              transcriptCoverageScore: 0.1,
              speechContinuityGrade: 'weak',
            },
          ],
          [
            {
              startMs: 0,
              endMs: 9800,
              text: 'The coarse recognizer segment starts long before trusted audio activity, but the rendered clip must not keep audible leading silence.',
            },
          ],
          60000,
        );
        assertEqual(
          audioPaddingProtectedBoundaryPlan[0]?.sourceStartMs,
          1500,
          'smart slice transcript evidence refresh does not expand trusted audio-trimmed sourceStartMs beyond the native leading padding standard',
        );
        assertEqual(
          audioPaddingProtectedBoundaryPlan[0]?.leadingSilenceMs,
          200,
          'smart slice transcript evidence refresh keeps trusted leading audio activity padding render-ready after coarse STT expansion',
        );
        assertEqual(
          audioPaddingProtectedBoundaryPlan[0]?.transcriptSegments?.[0]?.startMs,
          1500,
          'smart slice transcript evidence refresh clamps cached transcriptSegments to the preserved audio-trimmed sourceStartMs',
        );
        let acceptedAudioPaddingProtectedBoundaryPlan = true;
        let audioPaddingProtectedBoundaryPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            audioPaddingProtectedBoundaryPlan,
            [
              {
                startMs: 0,
                endMs: 9800,
                text: 'The coarse recognizer segment starts long before trusted audio activity, but the rendered clip must not keep audible leading silence.',
              },
            ],
            60000,
          );
        } catch (error) {
          acceptedAudioPaddingProtectedBoundaryPlan = false;
          audioPaddingProtectedBoundaryPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedAudioPaddingProtectedBoundaryPlan,
          `smart slice native-render readiness accepts trusted audio-trimmed clips even when coarse STT starts before the allowed audio padding${audioPaddingProtectedBoundaryPlanError ? `: ${audioPaddingProtectedBoundaryPlanError}` : ''}`,
        );
        const repairedShortPaddingRefreshPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalSecondPlanClip,
              startMs: 10_000,
              durationMs: 4_970,
              sourceStartMs: 10_000,
              sourceEndMs: 14_970,
              speechStartMs: 10_200,
              speechEndMs: 14_720,
              boundaryPaddingBeforeMs: 200,
              boundaryPaddingAfterMs: 250,
              transcriptText: 'The repaired short clip already keeps valid speech padding.',
              transcriptSegmentCount: 1,
              transcriptCoverageScore: 1,
              speechContinuityGrade: 'strong',
              risks: ['excess-trailing-silence-trimmed'],
            },
          ],
          [
            {
              startMs: 10_150,
              endMs: 14_800,
              text: 'The repaired short clip already keeps valid speech padding.',
            },
          ],
          60_000,
        );
        assertEqual(
          repairedShortPaddingRefreshPlan[0]?.sourceStartMs,
          10_000,
          'smart slice transcript evidence refresh preserves repaired sourceStartMs when padding is already native-ready',
        );
        assertEqual(
          repairedShortPaddingRefreshPlan[0]?.sourceEndMs,
          14_970,
          'smart slice transcript evidence refresh preserves repaired sourceEndMs when padding is already native-ready',
        );
        assertEqual(
          repairedShortPaddingRefreshPlan[0]?.boundaryPaddingBeforeMs,
          200,
          'smart slice transcript evidence refresh keeps repaired leading padding at the native standard',
        );
        assertEqual(
          repairedShortPaddingRefreshPlan[0]?.boundaryPaddingAfterMs,
          250,
          'smart slice transcript evidence refresh keeps repaired trailing padding at the native standard',
        );
        let acceptedRepairedShortPaddingRefreshPlan = true;
        let repairedShortPaddingRefreshPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            repairedShortPaddingRefreshPlan,
            [
              {
                startMs: 10_150,
                endMs: 14_800,
                text: 'The repaired short clip already keeps valid speech padding.',
              },
            ],
            60_000,
          );
        } catch (error) {
          acceptedRepairedShortPaddingRefreshPlan = false;
          repairedShortPaddingRefreshPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedRepairedShortPaddingRefreshPlan,
          `smart slice native-render readiness accepts repaired clips after transcript evidence refresh without padding regression${repairedShortPaddingRefreshPlanError ? `: ${repairedShortPaddingRefreshPlanError}` : ''}`,
        );
        const trustedAudioOpeningTrimmedBoundaryPlan = refreshSmartSlicePlanTranscriptEvidence(
          [
            {
              ...professionalPlanClip,
              startMs: 3035,
              durationMs: 6865,
              sourceStartMs: 3035,
              sourceEndMs: 9900,
              speechStartMs: 3035,
              speechEndMs: 9900,
              boundaryPaddingBeforeMs: 0,
              boundaryPaddingAfterMs: 0,
              sourceSegments: [
                { startMs: 3035, endMs: 3556 },
                { startMs: 5815, endMs: 9900 },
              ],
              renderedDurationMs: 4606,
              removedSilenceMs: 2259,
              internalSilenceTrimCount: 1,
              boundaryDecisionSource: 'combined',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              audioActivityStartMs: 3035,
              audioActivityEndMs: 9900,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 0,
              leadingSilenceTrimMs: 3035,
              trailingSilenceTrimMs: 0,
              transcriptText: 'Stale opening-trimmed transcript.',
              transcriptSegmentCount: 99,
              transcriptCoverageScore: 0.1,
              speechContinuityGrade: 'weak',
              risks: ['internal-silence-trimmed', 'audio-boundary-refined'],
            },
          ],
          [
            { startMs: 0, endMs: 9900, text: 'The first coarse STT segment starts with removable silence before speech.' },
          ],
          60000,
        );
        assertEqual(
          trustedAudioOpeningTrimmedBoundaryPlan[0]?.sourceStartMs,
          3035,
          'smart slice transcript evidence refresh preserves trusted audio-trimmed opening silence instead of expanding to coarse STT start',
        );
        assertEqual(
          trustedAudioOpeningTrimmedBoundaryPlan[0]?.speechStartMs,
          3035,
          'smart slice transcript evidence refresh preserves trusted audio-trimmed opening speechStartMs',
        );
        let acceptedTrustedAudioOpeningTrimmedBoundaryPlan = true;
        let trustedAudioOpeningTrimmedBoundaryPlanError = '';
        try {
          assertSmartSlicePlanReadyForNativeRender(
            trustedAudioOpeningTrimmedBoundaryPlan,
            [
              { startMs: 0, endMs: 9900, text: 'The first coarse STT segment starts with removable silence before speech.' },
            ],
            60000,
          );
        } catch (error) {
          acceptedTrustedAudioOpeningTrimmedBoundaryPlan = false;
          trustedAudioOpeningTrimmedBoundaryPlanError = error instanceof Error ? error.message : String(error);
        }
        assertRule(
          acceptedTrustedAudioOpeningTrimmedBoundaryPlan,
          `smart slice native-render readiness accepts refreshed trusted audio-trimmed opening silence inside a coarse STT segment${trustedAudioOpeningTrimmedBoundaryPlanError ? `: ${trustedAudioOpeningTrimmedBoundaryPlanError}` : ''}`,
        );
      }
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [
            {
              ...professionalPlanClip,
              speechStartMs: undefined,
              speechEndMs: undefined,
              boundaryDecisionSource: 'audio',
              audioCleanupProfile: 'smart-slice-speech-denoise-v1',
              noiseReductionApplied: true,
              audioActivityStartMs: 4000,
              audioActivityEndMs: 20000,
              audioActivityConfidence: 0.95,
              audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
              leadingSilenceMs: 0,
              trailingSilenceMs: 250,
              leadingSilenceTrimMs: 0,
              trailingSilenceTrimMs: 0,
              tailTreatment: 'none',
            },
          ],
          [
            { startMs: 4000, endMs: 12000, text: 'The hook starts immediately.' },
            { startMs: 12000, endMs: 20000, text: 'The payoff ends cleanly.' },
          ],
          60000,
        ),
        'explicit STT speechStartMs and speechEndMs',
        'smart slice native-render readiness gate rejects audio-only post-cleanup plans without explicit STT speech boundaries',
      );
      await assertRejects(
        () => assertSmartSlicePlanReadyForNativeRender(
          [professionalPlanClip],
          [],
          60000,
        ),
        'structured speech-to-text transcript segments',
        'smart slice native-render readiness gate rejects planned clips without real STT coverage before native rendering',
      );
    }
    if (typeof services.getAutoCutI18n === 'function') {
      const i18n = services.getAutoCutI18n();
      assertRule(
        typeof i18n?.t === 'function' && typeof i18n?.changeLanguage === 'function',
        'getAutoCutI18n returns an i18next-compatible runtime instance',
      );
      services.initializeAutoCutI18n('zh-CN');
      let redundantLanguageChangedEvents = 0;
      const stopLanguageChanged = services.listenAutoCutI18nLanguageChanged?.(() => {
        redundantLanguageChangedEvents += 1;
      });
      services.initializeAutoCutI18n('zh-CN');
      services.getAutoCutI18n();
      services.getAutoCutI18nText('layout.sidebar.home');
      services.getAutoCutTaskTypeLabel(types.AUTOCUT_TASK_TYPE.videoSlice);
      stopLanguageChanged?.();
      assertEqual(
        redundantLanguageChangedEvents,
        0,
        'reading active i18next text does not emit redundant languageChanged events',
      );
    }
    if (typeof services.getAutoCutTaskTypeLabel === 'function') {
      assertEqual(
        services.getAutoCutTaskTypeLabel(types.AUTOCUT_TASK_TYPE.textExtraction, 'zh-CN'),
        '\u6587\u6848\u63d0\u53d6',
        'getAutoCutTaskTypeLabel resolves the zh-CN task type display label from i18n resources',
      );
      assertEqual(
        services.getAutoCutTaskTypeLabel(types.AUTOCUT_TASK_TYPE.textExtraction, 'en-US'),
        'Text extraction',
        'getAutoCutTaskTypeLabel resolves the en-US task type display label from i18n resources',
      );
    }
    if (typeof services.getTools === 'function') {
      const localizedTools = await services.getTools();
      const textExtractionTool = localizedTools.find((tool) => tool.id === 'extractor-text');
      assertEqual(
        textExtractionTool?.nameKey,
        'tool.extractorText.name',
        'getTools returns stable i18n name keys for tool display labels',
      );
      assertEqual(
        textExtractionTool?.descriptionKey,
        'tool.extractorText.description',
        'getTools returns stable i18n description keys for tool display labels',
      );
      assertEqual(
        textExtractionTool?.name,
        '\u6587\u6848\u63d0\u53d6',
        'getTools resolves localized tool display names through i18next resources',
      );
      assertRule(
        localizedTools.every((tool) => /^[a-z][a-z0-9-]*$/u.test(tool.id)),
        'getTools keeps tool ids as stable ASCII codes instead of localized display text',
      );
    }
    assertRule(typeof services.createAutoCutTaskName === 'function', 'services exports the standard AutoCut task naming helper');
    if (typeof services.createAutoCutTaskName === 'function') {
      const deterministicTaskTimestamp = new Date(2026, 4, 7, 8, 9, 10);
      assertEqual(
        services.createAutoCutTaskName({
          sourceName: 'meeting.final.mp4',
          createdAt: deterministicTaskTimestamp,
        }),
        'meeting.final.mp4 20260507-080910',
        'createAutoCutTaskName keeps the original file name and appends a local second-precision timestamp',
      );
      assertEqual(
        services.createAutoCutTaskName({
          url: 'https://media.example.com/uploads/source%20clip.mp4?download=1',
          createdAt: deterministicTaskTimestamp,
        }),
        'source clip.mp4 20260507-080910',
        'createAutoCutTaskName derives the original file name from URL paths',
      );
      assertEqual(
        services.createAutoCutTaskName({
          sourceName: 'meeting.final.mp4 20260507-080910',
          createdAt: deterministicTaskTimestamp,
        }),
        'meeting.final.mp4 20260507-080910',
        'createAutoCutTaskName does not append a duplicate timestamp to already-standard task names',
      );
    }
    assertEqual(
      services.getAutoCutTimestampMs('2026-05-09 04:12:00'),
      services.getAutoCutTimestampMs('2026-05-09T04:12:00.000Z'),
      'datetime service treats native SQLite UTC timestamps without timezone suffix as UTC instants',
    );
    assertEqual(
      services.formatAutoCutDateTime('2026-05-09 04:12:00'),
      services.formatAutoCutDateTime('2026-05-09T04:12:00.000Z'),
      'datetime service renders native SQLite UTC timestamps consistently with ISO UTC timestamps',
    );
    assertRule(
      /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/u.test(services.formatAutoCutDateTime('2026-05-09T04:12:00.000Z')),
      'formatAutoCutDateTime returns stable local YYYY-MM-DD HH:mm:ss text for task lists',
    );
    assertRule(
      /^\d{2}:\d{2}:\d{2}$/u.test(services.formatAutoCutTimeOfDay('2026-05-09T04:12:00.000Z')),
      'formatAutoCutTimeOfDay returns stable local HH:mm:ss text for compact task previews',
    );

    resetStorage();
    const nativeTaskListCommands = [];
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      nativeTaskListCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-contract',
              sourceName: 'native-source.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              name: 'slice-001.mp4',
              assetUuid: 'asset-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-contract',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-contract',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/cover/slice-001.jpg',
                  subtitleArtifactUuid: 'slice-subtitle-contract',
                  subtitleArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/slice-001.srt',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-contract',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  subtitleByteSize: 234,
                  subtitleFormat: 'srt',
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  speechStartMs: 1180,
                  speechEndMs: 15760,
                  boundaryPaddingBeforeMs: 180,
                  boundaryPaddingAfterMs: 240,
                  audioCleanupProfile: 'smart-slice-speech-denoise-v1',
                  noiseReductionApplied: true,
                  boundaryDecisionSource: 'combined',
                  audioActivityStartMs: 1180,
                  audioActivityEndMs: 15760,
                  audioActivityConfidence: 0.94,
                  audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
                  leadingSilenceMs: 180,
                  trailingSilenceMs: 240,
                  leadingSilenceTrimMs: 60,
                  trailingSilenceTrimMs: 130,
                  tailTreatment: 'fade-out',
                  transcriptText: 'Native transcript text survives recovered task projection.',
                  transcriptSegments: [
                    {
                      startMs: 1200,
                      endMs: 15750,
                      text: 'Native transcript text survives recovered task projection.',
                      speaker: 'Speaker 1',
                    },
                  ],
                  transcriptSegmentCount: 1,
                  transcriptCoverageScore: 0.96,
                  speechContinuityGrade: 'strong',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-slice-progress-contract',
                eventType: 8,
                payload: {
                  operation: 'videoSlice',
                  phase: 'ffmpeg-progress-streamed',
                  source: 'ffmpeg-progress',
                  progress: 88,
                },
                payloadJson: '{"operation":"videoSlice","phase":"ffmpeg-progress-streamed","source":"ffmpeg-progress","progress":88}',
                createdAt: '2026-05-05T00:04:00.000Z',
                updatedAt: '2026-05-05T00:04:00.000Z',
              },
            ],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-transcript-contract',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-transcript-contract',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-transcript-contract',
              language: 'zh',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-transcript-contract',
              artifactUuid: 'transcript-artifact-contract',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-transcript-contract/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-transcript-contract',
              language: 'zh',
              segmentCount: 2,
              segments: [
                { startMs: 0, endMs: 1400, text: 'hello', speaker: 'Speaker 1' },
                { startMs: 1500, endMs: 3200, text: 'world', speaker: 'Speaker 2' },
              ],
              text: 'hello world',
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:01:00.000Z',
            updatedAt: '2026-05-05T00:06:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-transcript-corrupt-contract',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-transcript-corrupt-contract',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-transcript-corrupt-contract',
              language: 'en',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-transcript-corrupt-contract',
              artifactUuid: 'transcript-artifact-corrupt-contract',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-transcript-corrupt-contract/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-transcript-corrupt-contract',
              language: 'en',
              segmentCount: 2,
              segments: [
                { startMs: 0, endMs: 1400, text: 'valid segment', speaker: 'Speaker 1' },
                { startMs: 1500, endMs: 1400, text: 'invalid timing', speaker: 'Speaker 2' },
              ],
              text: 'valid segment invalid timing',
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:01:30.000Z',
            updatedAt: '2026-05-05T00:06:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-compress-contract',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-compress-contract',
            inputJson: JSON.stringify({
              operation: 'videoCompress',
              assetUuid: 'asset-compress-contract',
              compressionMode: 'balanced',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-compress-contract',
              artifactUuid: 'compress-artifact-contract',
              artifactPath: 'D:/autocut/media/tasks/ops-task-compress-contract/compressed.mp4',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-contract',
              format: 'mp4',
              byteSize: 2500,
              originalByteSize: 10000,
            }),
            createdAt: '2026-05-05T00:02:00.000Z',
            updatedAt: '2026-05-05T00:07:00.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-compress-completed-contract',
                eventType: 2,
                payload: {
                  operation: 'videoCompress',
                  artifactUuid: 'compress-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-compress-contract/compressed.mp4',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-contract',
                  byteSize: 2500,
                  originalByteSize: 10000,
                  progress: 100,
                  phase: 'completed',
                  source: 'native-host',
                },
                payloadJson: '{}',
                createdAt: '2026-05-05T00:07:00.000Z',
                updatedAt: '2026-05-05T00:07:00.000Z',
              },
            ],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-failed-contract',
            taskType: 1,
            status: 3,
            progress: 42,
            sourceAssetUuid: 'asset-failed-contract',
            inputJson: JSON.stringify({
              operation: 'audioExtraction',
              assetUuid: 'asset-failed-contract',
              outputFormat: 'wav',
            }),
            outputJson: '{}',
            errorCode: 'FFMPEG_AUDIO_EXTRACTION_FAILED',
            errorMessage: 'ffmpeg exited with status 1',
            createdAt: '2026-05-05T00:03:00.000Z',
            updatedAt: '2026-05-05T00:08:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-corrupt-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-corrupt-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-corrupt-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-corrupt-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract',
              sliceCount: 2,
              sliceResults: [
                {
                  artifactUuid: 'corrupt-slice-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract/slice-001.mp4',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract',
                  byteSize: 0,
                  startMs: 0,
                  durationMs: 15000,
                  label: 'Corrupt opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:04:00.000Z',
            updatedAt: '2026-05-05T00:09:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-empty-completed-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-empty-completed-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-empty-completed-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-empty-completed-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-empty-completed-slice-contract',
              sliceCount: 0,
              sliceResults: [],
            }),
            createdAt: '2026-05-05T00:04:30.000Z',
            updatedAt: '2026-05-05T00:09:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-processing-slice-contract',
            taskType: 6,
            status: 1,
            progress: 48,
            sourceAssetUuid: 'asset-processing-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-processing-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: '{}',
            createdAt: '2026-05-05T00:04:45.000Z',
            updatedAt: '2026-05-05T00:09:45.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-processing-slice-progress-contract',
                eventType: 8,
                payload: {
                  operation: 'videoSlice',
                  phase: 'rendering',
                  source: 'ffmpeg-progress',
                  progress: 48,
                },
                payloadJson: '{"operation":"videoSlice","phase":"rendering","source":"ffmpeg-progress","progress":48}',
                createdAt: '2026-05-05T00:09:40.000Z',
                updatedAt: '2026-05-05T00:09:40.000Z',
              },
            ],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasks = await withImmediateTimers(() => services.getTasks());
    assertIncludes(
      nativeTaskListCommands.map((entry) => entry.command),
      'autocut_list_native_tasks',
      'getTasks queries persisted native ops_task snapshots when the native task bridge is ready',
    );
    assertEqual(
      nativeTaskListCommands.find((entry) => entry.command === 'autocut_list_native_tasks')?.args?.request?.limit,
      100,
      'getTasks bounds the native task list query to the native database contract limit',
    );
    assertEqual(nativeTasks.length, 6, 'getTasks maps visible native ops_task snapshots and hides implementation-only speech transcription tasks');
    const nativeSliceTask = nativeTasks.find((task) => task.id === 'ops-task-slice-contract');
    assertEqual(nativeSliceTask?.type, types.AUTOCUT_TASK_TYPES[0], 'native video slice tasks map to the AppTask slice type');
    assertEqual(nativeSliceTask?.status, types.AUTOCUT_TASK_STATUS.completed, 'native completed status maps to completed AppTask status');
    assertTaskNameUsesSourceAndSecondTimestamp(
      nativeSliceTask,
      'native-source.mp4',
      'native video slice task list projection name',
    );
    assertEqual(nativeSliceTask?.completedAt, '2026-05-05T00:05:00.000Z', 'native completed tasks expose updatedAt as completedAt');
    assertEqual(nativeSliceTask?.resultCount, 1, 'native slice output_json sliceCount maps to AppTask resultCount');
    assertEqual(nativeSliceTask?.generatedAssetIds?.[0], 'slice-artifact-contract', 'native slice output maps generated asset ids from artifact UUIDs');
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.url,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fops-task-slice-contract%2Fslice-001.mp4',
      'native slice output artifactPath is converted to a safe desktop asset URL',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.thumbnailUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fops-task-slice-contract%2Fcover%2Fslice-001.jpg',
      'native slice thumbnail artifactPath is converted to a safe desktop asset URL',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.subtitleUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fops-task-slice-contract%2Fslice-001.srt',
      'native slice subtitle artifactPath is converted to a safe desktop asset URL',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.transcriptText,
      'Native transcript text survives recovered task projection.',
      'native completed slice task exposes recovered speech-to-text transcript text',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Native transcript text survives recovered task projection.',
      'native completed slice task exposes recovered structured speech-to-text transcript segments',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.speechStartMs,
      1180,
      'native completed slice task accepts audio-refined speech start covered by structured transcript segments',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.speechEndMs,
      15760,
      'native completed slice task accepts audio-refined speech end covered by structured transcript segments',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.audioCleanupProfile,
      'smart-slice-speech-denoise-v1',
      'native completed slice task exposes recovered Smart Slice audio cleanup profile evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.noiseReductionApplied,
      true,
      'native completed slice task exposes recovered denoise application evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.boundaryDecisionSource,
      'combined',
      'native completed slice task exposes recovered boundary decision source evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.audioActivityStartMs,
      1180,
      'native completed slice task exposes recovered audio activity start evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.audioActivityEndMs,
      15760,
      'native completed slice task exposes recovered audio activity end evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.audioActivityConfidence,
      0.94,
      'native completed slice task exposes recovered audio activity confidence evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.audioActivityAnalysisFilter,
      'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
      'native completed slice task exposes recovered audio analysis filter evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.leadingSilenceMs,
      180,
      'native completed slice task exposes recovered leading silence evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.trailingSilenceMs,
      240,
      'native completed slice task exposes recovered trailing silence evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.leadingSilenceTrimMs,
      60,
      'native completed slice task exposes recovered leading silence trim evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.trailingSilenceTrimMs,
      130,
      'native completed slice task exposes recovered trailing silence trim evidence',
    );
    assertEqual(
      nativeSliceTask?.sliceResults?.[0]?.tailTreatment,
      'fade-out',
      'native completed slice task exposes recovered tail treatment evidence',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-input-clip-transcript-recovery-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        const recoveredInputClip = {
          startMs: 1000,
          durationMs: 15000,
          label: 'Opening',
          sourceStartMs: 1000,
          sourceEndMs: 16000,
          speechStartMs: 1200,
          speechEndMs: 15750,
          boundaryPaddingBeforeMs: 200,
          boundaryPaddingAfterMs: 250,
          transcriptText: 'Input clip transcript evidence restores recovered native output.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 15750,
              text: 'Input clip transcript evidence restores recovered native output.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptSegmentCount: 1,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'strong',
          ...createRecoveredSmartSliceAudioCleanupEvidence({
            sourceSegments: [
              { startMs: 2000, endMs: 9000 },
              { startMs: 12000, endMs: 15000 },
            ],
            renderedDurationMs: 10000,
            removedSilenceMs: 5000,
            internalSilenceTrimCount: 1,
          }),
        };
        const lowCoverageInputClip = {
          ...recoveredInputClip,
          label: 'Low coverage opening',
          transcriptText: 'Low transcript coverage should not recover as a completed slice.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 15750,
              text: 'Low transcript coverage should not recover as a completed slice.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptCoverageScore: 0.79,
          speechContinuityGrade: 'strong',
          ...createRecoveredSmartSliceAudioCleanupEvidence(),
        };
        const weakContinuityInputClip = {
          ...recoveredInputClip,
          label: 'Weak continuity opening',
          transcriptText: 'Weak speech continuity should not recover as a completed slice.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 15750,
              text: 'Weak speech continuity should not recover as a completed slice.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'weak',
          ...createRecoveredSmartSliceAudioCleanupEvidence(),
        };
        const sourceSegmentGapInputClip = {
          ...recoveredInputClip,
          label: 'Source segment gap opening',
          transcriptText:
            'First recovered speech remains. Middle recovered speech must not be cut. Final recovered speech remains.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 5000,
              text: 'First recovered speech remains.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 9500,
              endMs: 11000,
              text: 'Middle recovered speech must not be cut.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 13000,
              endMs: 15750,
              text: 'Final recovered speech remains.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptSegmentCount: 3,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'repaired',
          ...createRecoveredSmartSliceAudioCleanupEvidence({
            sourceSegments: [
              { startMs: 1000, endMs: 9000 },
              { startMs: 12000, endMs: 16000 },
            ],
            renderedDurationMs: 12000,
            removedSilenceMs: 3000,
            internalSilenceTrimCount: 1,
          }),
        };
        const timingOnlyInputClip = {
          startMs: 1000,
          durationMs: 15000,
          label: 'Timing only opening',
          sourceStartMs: 1000,
          sourceEndMs: 16000,
        };
        return [
          {
            uuid: 'ops-task-slice-input-clip-transcript-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-input-clip-transcript-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-input-clip-transcript-recovery',
              sourceName: 'input-clip-recovery.mp4',
              outputFormat: 'mp4',
              clips: [recoveredInputClip],
              requestedClips: [recoveredInputClip],
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-input-clip-transcript-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-transcript-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-input-clip-transcript-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-transcript-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-input-clip-transcript-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-transcript-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-transcript-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-input-clip-source-segment-gap-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-input-clip-source-segment-gap-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-input-clip-source-segment-gap-recovery',
              sourceName: 'input-clip-source-segment-gap-recovery.mp4',
              outputFormat: 'mp4',
              clips: [sourceSegmentGapInputClip],
              requestedClips: [sourceSegmentGapInputClip],
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-input-clip-source-segment-gap-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-source-segment-gap-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-input-clip-source-segment-gap-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-source-segment-gap-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-input-clip-source-segment-gap-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-source-segment-gap-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-source-segment-gap-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Source segment gap opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:15.000Z',
            updatedAt: '2026-05-05T00:05:15.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-speech-transcript-backfill-recovery',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-speech-task-recovery',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-slice-speech-task-recovery',
              language: 'auto',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-speech-task-recovery',
              artifactUuid: 'transcript-artifact-speech-task-recovery',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-speech-transcript-backfill-recovery/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-speech-transcript-backfill-recovery',
              language: 'auto',
              segmentCount: 1,
              text: 'Sibling speech transcription evidence restores recovered native slices.',
              segments: [
                {
                  startMs: 1200,
                  endMs: 15750,
                  text: 'Sibling speech transcription evidence restores recovered native slices.',
                  speaker: 'Speaker 1',
                },
              ],
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:00:30.000Z',
            updatedAt: '2026-05-05T00:04:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-speech-transcript-fallback-after-timing-clip',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-timing-clip-speech-fallback',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-slice-timing-clip-speech-fallback',
              language: 'auto',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-timing-clip-speech-fallback',
              artifactUuid: 'transcript-artifact-timing-clip-speech-fallback',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-speech-transcript-fallback-after-timing-clip/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-speech-transcript-fallback-after-timing-clip',
              language: 'auto',
              segmentCount: 1,
              text: 'Sibling speech transcript must recover when input clips only preserve timing.',
              segments: [
                {
                  startMs: 1200,
                  endMs: 15750,
                  text: 'Sibling speech transcript must recover when input clips only preserve timing.',
                  speaker: 'Speaker 1',
                },
              ],
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:00:45.000Z',
            updatedAt: '2026-05-05T00:04:45.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-speech-task-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-speech-task-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-speech-task-recovery',
              sourceName: 'speech-task-recovery.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-speech-task-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-speech-task-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-speech-task-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-speech-task-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-speech-task-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-speech-task-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-speech-task-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Sibling STT recovery opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  ...createRecoveredSmartSliceAudioCleanupEvidence(),
                },
              ],
            }),
            createdAt: '2026-05-05T00:01:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-timing-clip-speech-fallback',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-timing-clip-speech-fallback',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-timing-clip-speech-fallback',
              sourceName: 'timing-clip-speech-fallback.mp4',
              outputFormat: 'mp4',
              clips: [timingOnlyInputClip],
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-timing-clip-speech-fallback',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-timing-clip-speech-fallback',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-timing-clip-speech-fallback',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-timing-clip-speech-fallback/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-timing-clip-speech-fallback',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-timing-clip-speech-fallback/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-timing-clip-speech-fallback',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Timing clip STT fallback opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  ...createRecoveredSmartSliceAudioCleanupEvidence(),
                },
              ],
            }),
            createdAt: '2026-05-05T00:01:15.000Z',
            updatedAt: '2026-05-05T00:05:15.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-basic-stale-source-segments-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-basic-stale-source-segments-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-basic-stale-source-segments-recovery',
              sourceName: 'basic-stale-source-segments-recovery.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-basic-stale-source-segments-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-basic-stale-source-segments-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-basic-stale-source-segments-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-basic-stale-source-segments-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-basic-stale-source-segments-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-basic-stale-source-segments-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-basic-stale-source-segments-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Basic stale sourceSegments opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  sourceSegments: [
                    { startMs: 2000, endMs: 9000 },
                    { startMs: 12000, endMs: 15000 },
                  ],
                  removedSilenceMs: 5000,
                  internalSilenceTrimCount: 1,
                },
              ],
            }),
            createdAt: '2026-05-05T00:01:30.000Z',
            updatedAt: '2026-05-05T00:05:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-input-clip-low-coverage-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-input-clip-low-coverage-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-input-clip-low-coverage-recovery',
              sourceName: 'input-clip-low-coverage-recovery.mp4',
              outputFormat: 'mp4',
              clips: [lowCoverageInputClip],
              requestedClips: [lowCoverageInputClip],
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-input-clip-low-coverage-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-low-coverage-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-input-clip-low-coverage-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-low-coverage-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-input-clip-low-coverage-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-low-coverage-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-low-coverage-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Low coverage opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-input-clip-weak-continuity-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-input-clip-weak-continuity-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-input-clip-weak-continuity-recovery',
              sourceName: 'input-clip-weak-continuity-recovery.mp4',
              outputFormat: 'mp4',
              clips: [weakContinuityInputClip],
              requestedClips: [weakContinuityInputClip],
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-input-clip-weak-continuity-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-weak-continuity-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-input-clip-weak-continuity-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-weak-continuity-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-input-clip-weak-continuity-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-input-clip-weak-continuity-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-input-clip-weak-continuity-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Weak continuity opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected input clip transcript recovery native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksRecoveredFromInputClipEvidence = await withImmediateTimers(() => services.getTasks());
    const nativeSliceTaskRecoveredFromInputClipEvidence = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-input-clip-transcript-recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromInputClipEvidence?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task recovers speech-to-text transcript evidence from persisted input clip requests',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromInputClipEvidence?.sliceResults?.[0]?.transcriptText,
      'Input clip transcript evidence restores recovered native output.',
      'native completed slice task exposes input clip transcript text when output_json omits it',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromInputClipEvidence?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Input clip transcript evidence restores recovered native output.',
      'native completed slice task exposes input clip structured transcript segments when output_json omits them',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromInputClipEvidence?.generatedAssetIds?.[0],
      'slice-artifact-input-clip-transcript-recovery',
      'native completed slice task keeps generated asset ids after input clip transcript evidence recovery',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredFromInputClipEvidence?.sliceResults?.[0],
      'sourceSegments',
      'native completed slice task drops stale recovered sourceSegments that do not span the final source range',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredFromInputClipEvidence?.sliceResults?.[0],
      'removedSilenceMs',
      'native completed slice task drops stale recovered removedSilenceMs when sourceSegments are not renderable',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredFromInputClipEvidence?.sliceResults?.[0],
      'internalSilenceTrimCount',
      'native completed slice task drops stale recovered internalSilenceTrimCount when sourceSegments are not renderable',
    );
    const nativeSliceTaskRecoveredWithSourceSegmentGap = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-input-clip-source-segment-gap-recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredWithSourceSegmentGap?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task stays completed when recovered sourceSegments cut through transcript speech',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredWithSourceSegmentGap?.sliceResults?.[0],
      'sourceSegments',
      'native completed slice task drops recovered sourceSegments that cut through structured transcript speech',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredWithSourceSegmentGap?.sliceResults?.[0],
      'removedSilenceMs',
      'native completed slice task drops recovered removedSilenceMs when sourceSegments cut through transcript speech',
    );
    assertNotOwnProperty(
      nativeSliceTaskRecoveredWithSourceSegmentGap?.sliceResults?.[0],
      'internalSilenceTrimCount',
      'native completed slice task drops recovered internalSilenceTrimCount when sourceSegments cut through transcript speech',
    );
    const nativeSliceTaskRecoveredFromSiblingSpeechTask = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-speech-task-recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task recovers speech-to-text transcript evidence from a completed sibling STT task for the same source asset',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTask?.sliceResults?.[0]?.transcriptText,
      'Sibling speech transcription evidence restores recovered native slices.',
      'native completed slice task backfills transcript text from the same-asset STT task when output_json and input_json omit slice evidence',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTask?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Sibling speech transcription evidence restores recovered native slices.',
      'native completed slice task backfills structured transcript segments from the same-asset STT task',
    );
    const nativeSliceTaskRecoveredFromSiblingSpeechTaskAfterTimingOnlyInputClip = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-timing-clip-speech-fallback',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTaskAfterTimingOnlyInputClip?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task keeps searching for speech-to-text evidence when persisted input clips only contain timing metadata',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTaskAfterTimingOnlyInputClip?.sliceResults?.[0]?.transcriptText,
      'Sibling speech transcript must recover when input clips only preserve timing.',
      'native completed slice task uses sibling STT transcript text after timing-only input clip recovery evidence',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromSiblingSpeechTaskAfterTimingOnlyInputClip?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Sibling speech transcript must recover when input clips only preserve timing.',
      'native completed slice task uses sibling structured STT segments after timing-only input clip recovery evidence',
    );
    const nativeBasicSliceTaskWithStaleSourceSegments = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-basic-stale-source-segments-recovery',
    );
    assertEqual(
      nativeBasicSliceTaskWithStaleSourceSegments?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed basic slice task ignores stale sourceSegments instead of failing recovered task listing',
    );
    assertNotOwnProperty(
      nativeBasicSliceTaskWithStaleSourceSegments?.sliceResults?.[0],
      'sourceSegments',
      'native completed basic slice task drops stale sourceSegments that do not span the final source range',
    );
    assertNotOwnProperty(
      nativeBasicSliceTaskWithStaleSourceSegments?.sliceResults?.[0],
      'removedSilenceMs',
      'native completed basic slice task drops stale removedSilenceMs when sourceSegments are not renderable',
    );
    assertNotOwnProperty(
      nativeBasicSliceTaskWithStaleSourceSegments?.sliceResults?.[0],
      'internalSilenceTrimCount',
      'native completed basic slice task drops stale internalSilenceTrimCount when sourceSegments are not renderable',
    );
    const nativeSliceTaskRejectedForLowCoverage = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-input-clip-low-coverage-recovery',
    );
    assertEqual(
      nativeSliceTaskRejectedForLowCoverage?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice task fails closed when recovered transcript coverage is below the professional threshold',
    );
    assertIncludes(
      nativeSliceTaskRejectedForLowCoverage?.errorMessage ?? '',
      'transcriptCoverageScore to be at least 0.8',
      'native completed slice task explains recovered low transcript coverage',
    );
    assertEqual(
      nativeSliceTaskRejectedForLowCoverage?.sliceResults,
      undefined,
      'native completed slice task does not expose generated slices with low recovered transcript coverage',
    );
    const nativeSliceTaskRejectedForWeakContinuity = nativeTasksRecoveredFromInputClipEvidence.find(
      (task) => task.id === 'ops-task-slice-input-clip-weak-continuity-recovery',
    );
    assertEqual(
      nativeSliceTaskRejectedForWeakContinuity?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice task fails closed when recovered speech continuity is weak',
    );
    assertIncludes(
      nativeSliceTaskRejectedForWeakContinuity?.errorMessage ?? '',
      'speechContinuityGrade to be strong or repaired',
      'native completed slice task explains recovered weak speech continuity',
    );
    assertEqual(
      nativeSliceTaskRejectedForWeakContinuity?.sliceResults,
      undefined,
      'native completed slice task does not expose generated slices with weak recovered speech continuity',
    );

    resetStorage();
    await services.addTask({
      id: 'local-click-slice-task-before-native-uuid',
      type: types.AUTOCUT_TASK_TYPES[0],
      name: 'click recovered local smart slice transcript sidecar',
      status: types.AUTOCUT_TASK_STATUS.completed,
      progress: 100,
      createdAt: '2026-05-05T00:00:00.000Z',
      sourceFileId: 'asset-click-slice-local-transcript-recovery',
      generatedAssetIds: ['slice-artifact-click-local-transcript-recovery'],
      sliceResults: [
        {
          id: 'slice-artifact-click-local-transcript-recovery',
          name: 'Click recovered opening.mp4',
          duration: 15,
          size: 1234567,
          resolution: '1080P',
          thumbnailUrl: '',
          url: '',
          title: 'Click recovered opening',
          summary: 'The click-created local task keeps the speech transcript sidecar for native recovery.',
          sourceStartMs: 1000,
          sourceEndMs: 16000,
          speechStartMs: 1200,
          speechEndMs: 15750,
          boundaryPaddingBeforeMs: 200,
          boundaryPaddingAfterMs: 250,
          transcriptText: 'Click slicing local transcript evidence restores recovered native output.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 15750,
              text: 'Click slicing local transcript evidence restores recovered native output.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptSegmentCount: 1,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'strong',
          ...createRecoveredSmartSliceAudioCleanupEvidence(),
        },
      ],
    });
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-click-local-transcript-recovery-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-click-slice-native-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-click-slice-local-transcript-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-click-slice-local-transcript-recovery',
              sourceName: 'click-local-transcript-recovery.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-click-slice-local-transcript-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-click-slice-native-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-click-local-transcript-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-click-slice-native-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-click-local-transcript-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-click-slice-native-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-click-slice-native-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Click recovered opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  ...createRecoveredSmartSliceAudioCleanupEvidence(),
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected click local transcript recovery native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksRecoveredFromClickLocalEvidence = await withImmediateTimers(() => services.getTasks());
    const nativeSliceTaskRecoveredFromClickLocalEvidence = nativeTasksRecoveredFromClickLocalEvidence.find(
      (task) => task.id === 'ops-task-click-slice-native-recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromClickLocalEvidence?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task recovers speech-to-text evidence from a local click-created task whose id differs from the native uuid',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromClickLocalEvidence?.sliceResults?.[0]?.transcriptText,
      'Click slicing local transcript evidence restores recovered native output.',
      'native completed slice task exposes local click-created transcript text after native uuid recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromClickLocalEvidence?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Click slicing local transcript evidence restores recovered native output.',
      'native completed slice task exposes local click-created structured transcript segments after native uuid recovery',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromClickLocalEvidence?.generatedAssetIds?.[0],
      'slice-artifact-click-local-transcript-recovery',
      'native completed slice task keeps generated asset ids after local click-created transcript recovery',
    );

    resetStorage();
    await services.deleteTask('ops-task-hidden-speech-transcript-recovery');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-hidden-stt-transcript-recovery-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-hidden-speech-transcript-recovery',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-hidden-speech-transcript-recovery',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-hidden-speech-transcript-recovery',
              language: 'auto',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-hidden-speech-transcript-recovery',
              artifactUuid: 'transcript-artifact-hidden-speech-recovery',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-hidden-speech-transcript-recovery/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-hidden-speech-transcript-recovery',
              language: 'auto',
              segmentCount: 1,
              text: 'Hidden speech transcription evidence still restores recovered smart slices.',
              segments: [
                {
                  startMs: 1200,
                  endMs: 15750,
                  text: 'Hidden speech transcription evidence still restores recovered smart slices.',
                  speaker: 'Speaker 1',
                },
              ],
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:00:30.000Z',
            updatedAt: '2026-05-05T00:04:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-slice-hidden-stt-recovery',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-hidden-speech-transcript-recovery',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-hidden-speech-transcript-recovery',
              sourceName: 'hidden-stt-recovery.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-hidden-speech-transcript-recovery',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-hidden-stt-recovery',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-hidden-stt-recovery',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-hidden-stt-recovery/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-hidden-stt-recovery',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-hidden-stt-recovery/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-hidden-stt-recovery',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Hidden STT recovery opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  ...createRecoveredSmartSliceAudioCleanupEvidence(),
                },
              ],
            }),
            createdAt: '2026-05-05T00:01:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected hidden STT transcript recovery native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksRecoveredFromHiddenSpeechEvidence = await withImmediateTimers(() => services.getTasks());
    const hiddenSpeechTranscriptTask = nativeTasksRecoveredFromHiddenSpeechEvidence.find(
      (task) => task.id === 'ops-task-hidden-speech-transcript-recovery',
    );
    const nativeSliceTaskRecoveredFromHiddenSpeechEvidence = nativeTasksRecoveredFromHiddenSpeechEvidence.find(
      (task) => task.id === 'ops-task-slice-hidden-stt-recovery',
    );
    assertEqual(
      hiddenSpeechTranscriptTask,
      undefined,
      'deleteTask keeps a dismissed native speech transcription task hidden from the visible task list',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromHiddenSpeechEvidence?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task still recovers speech-to-text evidence from a dismissed same-asset STT task',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromHiddenSpeechEvidence?.sliceResults?.[0]?.transcriptText,
      'Hidden speech transcription evidence still restores recovered smart slices.',
      'native completed slice task backfills transcript text from hidden same-asset STT evidence',
    );
    assertEqual(
      nativeSliceTaskRecoveredFromHiddenSpeechEvidence?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Hidden speech transcription evidence still restores recovered smart slices.',
      'native completed slice task backfills structured transcript segments from hidden same-asset STT evidence',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-missing-transcript-evidence-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-missing-transcript-evidence',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-missing-transcript-evidence',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-missing-transcript-evidence',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-missing-transcript-evidence',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-missing-transcript-evidence',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-missing-transcript-evidence',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-missing-transcript-evidence/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-missing-transcript-evidence',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-missing-transcript-evidence/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-missing-transcript-evidence',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening without transcript evidence',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected missing transcript evidence native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksMissingTranscriptEvidence = await withImmediateTimers(() => services.getTasks());
    const missingTranscriptEvidenceTask = nativeTasksMissingTranscriptEvidence.find(
      (task) => task.id === 'ops-task-slice-missing-transcript-evidence',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task recovers completed timeline slices when speech-to-text transcript evidence is missing',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.errorMessage,
      undefined,
      'native completed timeline-only slice task does not surface a transcript evidence failure',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.failureDiagnostics,
      undefined,
      'native completed timeline-only slice task does not store transcript evidence failure diagnostics',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.sliceResults?.[0]?.id,
      'slice-artifact-missing-transcript-evidence',
      'native completed timeline-only slice task exposes generated slices without transcript evidence',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.sliceResults?.[0]?.transcriptSegments,
      undefined,
      'native completed timeline-only slice task keeps missing transcript evidence absent',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.sliceResults?.[0]?.sourceStartMs,
      1000,
      'native completed timeline-only slice task preserves sourceStartMs',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.sliceResults?.[0]?.sourceEndMs,
      16000,
      'native completed timeline-only slice task preserves sourceEndMs',
    );
    assertEqual(
      missingTranscriptEvidenceTask?.generatedAssetIds?.[0],
      'slice-artifact-missing-transcript-evidence',
      'native completed timeline-only slice task exposes generated asset ids without transcript evidence',
    );
    await services.deleteTask('ops-task-slice-missing-transcript-evidence');
    const nativeTasksAfterDeletingMissingTranscriptEvidence = await withImmediateTimers(() => services.getTasks());
    assertRule(
      nativeTasksAfterDeletingMissingTranscriptEvidence.every(
        (task) => task.id !== 'ops-task-slice-missing-transcript-evidence',
      ),
      'deleteTask tombstones recovered native tasks so invalid legacy smart-slice failures do not reappear after deletion',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-missing-audio-cleanup-evidence-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-missing-audio-cleanup-evidence',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-missing-audio-cleanup-evidence',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-missing-audio-cleanup-evidence',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-missing-audio-cleanup-evidence',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-missing-audio-cleanup-evidence',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-missing-audio-cleanup-evidence',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-missing-audio-cleanup-evidence/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-missing-audio-cleanup-evidence',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-missing-audio-cleanup-evidence/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-missing-audio-cleanup-evidence',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening without audio cleanup evidence',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  speechStartMs: 1200,
                  speechEndMs: 15750,
                  boundaryPaddingBeforeMs: 200,
                  boundaryPaddingAfterMs: 250,
                  transcriptText: 'Transcript evidence is present but cleanup evidence is missing.',
                  transcriptSegments: [
                    {
                      startMs: 1200,
                      endMs: 15750,
                      text: 'Transcript evidence is present but cleanup evidence is missing.',
                      speaker: 'Speaker 1',
                    },
                  ],
                  transcriptSegmentCount: 1,
                  transcriptCoverageScore: 0.96,
                  speechContinuityGrade: 'strong',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected missing audio cleanup evidence native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksMissingAudioCleanupEvidence = await withImmediateTimers(() => services.getTasks());
    const missingAudioCleanupEvidenceTask = nativeTasksMissingAudioCleanupEvidence.find(
      (task) => task.id === 'ops-task-slice-missing-audio-cleanup-evidence',
    );
    assertEqual(
      missingAudioCleanupEvidenceTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task stays completed when recovered audio cleanup evidence is missing',
    );
    assertEqual(
      missingAudioCleanupEvidenceTask?.sliceResults?.length,
      1,
      'native completed slice task exposes the recovered generated slice results',
    );
    assertEqual(
      missingAudioCleanupEvidenceTask?.generatedAssetIds?.length,
      1,
      'native completed slice task exposes the recovered generated asset ids',
    );

    resetStorage();
    const missingRawSilenceNativeTaskCommands = [];
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      missingRawSilenceNativeTaskCommands.push(command);
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-missing-raw-silence-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRetryCommandReady: true,
          speechTranscriptionModelDownloadCommandReady: true,
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-missing-raw-silence-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-missing-raw-silence-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-missing-raw-silence-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-missing-raw-silence-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-missing-raw-silence-contract',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'missing-raw-silence-artifact',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-missing-raw-silence-contract/slice-001.mp4',
                  thumbnailArtifactUuid: 'missing-raw-silence-thumb',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-missing-raw-silence-contract/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-missing-raw-silence-contract',
                  byteSize: 123456,
                  thumbnailByteSize: 12345,
                  startMs: 1_000,
                  durationMs: 15_000,
                  sourceStartMs: 1_000,
                  sourceEndMs: 16_000,
                  speechStartMs: 1_200,
                  speechEndMs: 15_750,
                  audioCleanupProfile: 'smart-slice-speech-denoise-v1',
                  noiseReductionApplied: true,
                  boundaryDecisionSource: 'combined',
                  audioActivityStartMs: 1_200,
                  audioActivityEndMs: 15_750,
                  audioActivityConfidence: 0.94,
                  audioActivityAnalysisFilter: 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08',
                  leadingSilenceTrimMs: 60,
                  trailingSilenceTrimMs: 130,
                  tailTreatment: 'fade-out',
                  transcriptText: 'Raw silence evidence must be present.',
                  transcriptSegments: [
                    {
                      startMs: 1_200,
                      endMs: 15_750,
                      text: 'Raw silence evidence must be present.',
                    },
                  ],
                  transcriptSegmentCount: 1,
                  transcriptCoverageScore: 0.94,
                  speechContinuityGrade: 'strong',
                },
              ],
            }),
            createdAt: '2026-05-05T00:04:20.000Z',
            updatedAt: '2026-05-05T00:09:20.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected missing raw silence native task command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const missingRawSilenceNativeTasks = await withImmediateTimers(() => services.getTasks());
    const missingRawSilenceTask = missingRawSilenceNativeTasks.find((task) => task.id === 'ops-task-missing-raw-silence-contract');
    assertIncludes(
      missingRawSilenceNativeTaskCommands,
      'autocut_list_native_tasks',
      'getTasks queries native completed task output before validating recovered raw silence evidence',
    );
    assertEqual(
      missingRawSilenceTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'native completed slice task stays completed when recovered raw leading/trailing silence evidence is missing',
    );
    assertEqual(
      missingRawSilenceTask?.sliceResults?.length,
      1,
      'native completed slice task exposes the recovered raw silence slice results',
    );
    assertEqual(
      missingRawSilenceTask?.generatedAssetIds?.length,
      1,
      'native completed slice task exposes the recovered raw silence generated asset ids',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-asset-url-failure-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-asset-url-failure',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-asset-url-failure',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-asset-url-failure',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-asset-url-failure',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-asset-url-failure',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-asset-url-failure',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-asset-url-failure/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-asset-url-failure',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-asset-url-failure/cover/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-asset-url-failure',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-compress-asset-url-failure',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-compress-asset-url-failure',
            inputJson: JSON.stringify({
              operation: 'videoCompress',
              assetUuid: 'asset-compress-asset-url-failure',
              compressionMode: 'balanced',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-compress-asset-url-failure',
              artifactUuid: 'compress-artifact-asset-url-failure',
              artifactPath: 'D:/autocut/media/tasks/ops-task-compress-asset-url-failure/compressed.mp4',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-asset-url-failure',
              format: 'mp4',
              byteSize: 2500,
              originalByteSize: 10000,
            }),
            createdAt: '2026-05-05T00:01:00.000Z',
            updatedAt: '2026-05-05T00:06:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list asset URL failure command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => {
        throw new Error(`asset scope denied for ${artifactPath}`);
      },
    }));
    const nativeTasksWithAssetUrlFailure = await withImmediateTimers(() => services.getTasks());
    const failedAssetUrlSliceTask = nativeTasksWithAssetUrlFailure.find(
      (task) => task.id === 'ops-task-slice-asset-url-failure',
    );
    const failedAssetUrlCompressTask = nativeTasksWithAssetUrlFailure.find(
      (task) => task.id === 'ops-task-compress-asset-url-failure',
    );
    assertEqual(
      failedAssetUrlSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice task fails closed when asset URL conversion fails',
    );
    assertIncludes(
      failedAssetUrlSliceTask?.errorMessage ?? '',
      'asset URL conversion failed',
      'native completed slice task explains asset URL conversion failures instead of exposing local paths',
    );
    assertEqual(
      failedAssetUrlSliceTask?.sliceResults,
      undefined,
      'native completed slice task does not expose slice results when asset URL conversion fails',
    );
    assertEqual(
      failedAssetUrlCompressTask?.videoUrl,
      undefined,
      'native completed non-slice task does not expose raw artifactPath when asset URL conversion fails',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-path-containment-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-outside-output-dir',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-outside-output-dir',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-outside-output-dir',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-outside-output-dir',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-outside-output-dir',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-outside-output-dir',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-outside-output-dir/../slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-outside-output-dir',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-outside-output-dir/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-outside-output-dir',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list path containment command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksWithOutsideOutputPath = await withImmediateTimers(() => services.getTasks());
    const outsideOutputSliceTask = nativeTasksWithOutsideOutputPath.find(
      (task) => task.id === 'ops-task-slice-outside-output-dir',
    );
    assertEqual(
      outsideOutputSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice task fails closed when an artifact escapes its task output directory',
    );
    assertIncludes(
      outsideOutputSliceTask?.errorMessage ?? '',
      'is outside its task output directory',
      'native completed slice task explains task output directory containment failures',
    );
    assertEqual(
      outsideOutputSliceTask?.sliceResults,
      undefined,
      'native completed slice task does not expose escaped artifact URLs',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-cover-directory-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-thumbnail-outside-cover-dir',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-thumbnail-outside-cover-dir',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-thumbnail-outside-cover-dir',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-thumbnail-outside-cover-dir',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-thumbnail-outside-cover-dir',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-thumbnail-outside-cover-dir',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-thumbnail-outside-cover-dir/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-thumbnail-outside-cover-dir',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-thumbnail-outside-cover-dir/slice-001.jpg',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-thumbnail-outside-cover-dir',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Cover directory enforcement opening',
                  sourceStartMs: 1000,
                  sourceEndMs: 16000,
                  speechStartMs: 1260,
                  speechEndMs: 15620,
                  boundaryPaddingBeforeMs: 260,
                  boundaryPaddingAfterMs: 380,
                  transcriptText: 'Cover thumbnails must live under the dedicated cover directory.',
                  transcriptSegments: [
                    {
                      startMs: 1120,
                      endMs: 15880,
                      text: 'Cover thumbnails must live under the dedicated cover directory.',
                      speaker: 'Speaker 1',
                    },
                  ],
                  transcriptSegmentCount: 1,
                  transcriptCoverageScore: 0.96,
                  speechContinuityGrade: 'strong',
                  ...createRecoveredSmartSliceAudioCleanupEvidence({
                    audioActivityEndMs: 12800,
                    trailingSilenceMs: 200,
                  }),
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list cover directory command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksWithThumbnailOutsideCoverPath = await withImmediateTimers(() => services.getTasks());
    const thumbnailOutsideCoverSliceTask = nativeTasksWithThumbnailOutsideCoverPath.find(
      (task) => task.id === 'ops-task-slice-thumbnail-outside-cover-dir',
    );
    assertEqual(
      thumbnailOutsideCoverSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice task fails closed when a thumbnail stays in the task output root instead of cover',
    );
    assertIncludes(
      thumbnailOutsideCoverSliceTask?.errorMessage ?? '',
      'is outside its task cover directory',
      'native completed slice task explains dedicated cover directory containment failures',
    );
    assertEqual(
      thumbnailOutsideCoverSliceTask?.sliceResults,
      undefined,
      'native completed slice task does not expose thumbnails that bypass the cover directory',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-non-slice-path-containment-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-compress-outside-output-dir',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-compress-outside-output-dir',
            inputJson: JSON.stringify({
              operation: 'videoCompress',
              assetUuid: 'asset-compress-outside-output-dir',
              compressionMode: 'balanced',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-compress-outside-output-dir',
              artifactUuid: 'compress-artifact-outside-output-dir',
              artifactPath: 'D:/autocut/media/tasks/ops-task-compress-outside-output-dir/../compressed.mp4',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-outside-output-dir',
              format: 'mp4',
              byteSize: 2500,
              originalByteSize: 10000,
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list non-slice path containment command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksWithOutsideNonSliceOutputPath = await withImmediateTimers(() => services.getTasks());
    const outsideOutputCompressTask = nativeTasksWithOutsideNonSliceOutputPath.find(
      (task) => task.id === 'ops-task-compress-outside-output-dir',
    );
    assertEqual(
      outsideOutputCompressTask?.videoUrl,
      undefined,
      'native completed non-slice task does not expose artifact URLs that escape the task output directory',
    );
    assertEqual(
      outsideOutputCompressTask?.generatedAssetIds,
      undefined,
      'native completed non-slice task does not expose generated assets for escaped artifact paths',
    );

    resetStorage();
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-dot-segment-path-containment-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-compress-dot-segment-escape',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-compress-dot-segment-escape',
            inputJson: JSON.stringify({
              operation: 'videoCompress',
              assetUuid: 'asset-compress-dot-segment-escape',
              compressionMode: 'balanced',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-compress-dot-segment-escape',
              artifactUuid: 'compress-artifact-dot-segment-escape',
              artifactPath: 'D:/autocut/media/tasks/ops-task-compress-dot-segment-escape/../compressed.mp4',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-dot-segment-escape',
              format: 'mp4',
              byteSize: 2500,
              originalByteSize: 10000,
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list dot-segment containment command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    const nativeTasksWithDotSegmentEscapedOutputPath = await withImmediateTimers(() => services.getTasks());
    const dotSegmentEscapedOutputTask = nativeTasksWithDotSegmentEscapedOutputPath.find(
      (task) => task.id === 'ops-task-compress-dot-segment-escape',
    );
    assertEqual(
      dotSegmentEscapedOutputTask?.videoUrl,
      undefined,
      'native completed non-slice task does not expose artifact URLs that escape via dot segments',
    );
    assertEqual(
      dotSegmentEscapedOutputTask?.generatedAssetIds,
      undefined,
      'native completed non-slice task does not expose generated assets for dot-segment escaped artifact paths',
    );
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      nativeTaskListCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'task-list-contract',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: ['autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-contract',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'slice-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/slice-001.mp4',
                  thumbnailArtifactUuid: 'slice-thumb-contract',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/cover/slice-001.jpg',
                  subtitleArtifactUuid: 'slice-subtitle-contract',
                  subtitleArtifactPath: 'D:/autocut/media/tasks/ops-task-slice-contract/slice-001.srt',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-slice-contract',
                  byteSize: 1234567,
                  thumbnailByteSize: 12345,
                  subtitleByteSize: 234,
                  subtitleFormat: 'srt',
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 15000,
                  label: 'Opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-slice-progress-contract',
                eventType: 8,
                payload: {
                  operation: 'videoSlice',
                  phase: 'ffmpeg-progress-streamed',
                  source: 'ffmpeg-progress',
                  progress: 88,
                },
                payloadJson: '{"operation":"videoSlice","phase":"ffmpeg-progress-streamed","source":"ffmpeg-progress","progress":88}',
                createdAt: '2026-05-05T00:04:00.000Z',
                updatedAt: '2026-05-05T00:04:00.000Z',
              },
            ],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-transcript-contract',
            taskType: 7,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-transcript-contract',
            inputJson: JSON.stringify({
              operation: 'speechTranscription',
              assetUuid: 'asset-transcript-contract',
              language: 'zh',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-transcript-contract',
              artifactUuid: 'transcript-artifact-contract',
              transcriptPath: 'D:/autocut/media/tasks/ops-task-transcript-contract/transcript.json',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-transcript-contract',
              language: 'zh',
              segmentCount: 2,
              segments: [
                { startMs: 0, endMs: 1400, text: 'hello', speaker: 'Speaker 1' },
                { startMs: 1500, endMs: 3200, text: 'world', speaker: 'Speaker 2' },
              ],
              text: 'hello world',
              byteSize: 4567,
            }),
            createdAt: '2026-05-05T00:01:00.000Z',
            updatedAt: '2026-05-05T00:06:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-compress-contract',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-compress-contract',
            inputJson: JSON.stringify({
              operation: 'videoCompress',
              assetUuid: 'asset-compress-contract',
              compressionMode: 'balanced',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-compress-contract',
              artifactUuid: 'compress-artifact-contract',
              artifactPath: 'D:/autocut/media/tasks/ops-task-compress-contract/compressed.mp4',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-contract',
              format: 'mp4',
              byteSize: 2500,
              originalByteSize: 10000,
            }),
            createdAt: '2026-05-05T00:02:00.000Z',
            updatedAt: '2026-05-05T00:07:00.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-compress-completed-contract',
                eventType: 2,
                payload: {
                  operation: 'videoCompress',
                  artifactUuid: 'compress-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-compress-contract/compressed.mp4',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-compress-contract',
                  byteSize: 2500,
                  originalByteSize: 10000,
                  progress: 100,
                  phase: 'completed',
                  source: 'native-host',
                },
                payloadJson: '{}',
                createdAt: '2026-05-05T00:07:00.000Z',
                updatedAt: '2026-05-05T00:07:00.000Z',
              },
            ],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-failed-contract',
            taskType: 1,
            status: 3,
            progress: 42,
            sourceAssetUuid: 'asset-failed-contract',
            inputJson: JSON.stringify({
              operation: 'audioExtraction',
              assetUuid: 'asset-failed-contract',
              outputFormat: 'wav',
            }),
            outputJson: '{}',
            errorCode: 'FFMPEG_AUDIO_EXTRACTION_FAILED',
            errorMessage: 'ffmpeg exited with status 1',
            createdAt: '2026-05-05T00:03:00.000Z',
            updatedAt: '2026-05-05T00:08:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-corrupt-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-corrupt-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-corrupt-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-corrupt-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract',
              sliceCount: 2,
              sliceResults: [
                {
                  artifactUuid: 'corrupt-slice-artifact-contract',
                  artifactPath: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract/slice-001.mp4',
                  taskOutputDir: 'D:/autocut/media/tasks/ops-task-corrupt-slice-contract',
                  byteSize: 0,
                  startMs: 0,
                  durationMs: 15000,
                  label: 'Corrupt opening',
                },
              ],
            }),
            createdAt: '2026-05-05T00:04:00.000Z',
            updatedAt: '2026-05-05T00:09:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-empty-completed-slice-contract',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-empty-completed-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-empty-completed-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              assetUuid: 'asset-empty-completed-slice-contract',
              taskOutputDir: 'D:/autocut/media/tasks/ops-task-empty-completed-slice-contract',
              sliceCount: 0,
              sliceResults: [],
            }),
            createdAt: '2026-05-05T00:04:30.000Z',
            updatedAt: '2026-05-05T00:09:30.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'ops-task-processing-slice-contract',
            taskType: 6,
            status: 1,
            progress: 48,
            sourceAssetUuid: 'asset-processing-slice-contract',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'asset-processing-slice-contract',
              outputFormat: 'mp4',
            }),
            outputJson: '{}',
            createdAt: '2026-05-05T00:04:45.000Z',
            updatedAt: '2026-05-05T00:09:45.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-event-processing-slice-progress-contract',
                eventType: 8,
                payload: {
                  operation: 'videoSlice',
                  phase: 'rendering',
                  source: 'ffmpeg-progress',
                  progress: 48,
                },
                payloadJson: '{"operation":"videoSlice","phase":"rendering","source":"ffmpeg-progress","progress":48}',
                createdAt: '2026-05-05T00:09:40.000Z',
                updatedAt: '2026-05-05T00:09:40.000Z',
              },
            ],
            workerLeases: [],
          },
        ];
      }

      throw new Error(`Unexpected task list native host command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));

    resetStorage();
    await services.addTask({
      id: 'ops-task-slice-contract',
      type: types.AUTOCUT_TASK_TYPES[0],
      name: 'local smart slice metadata sidecar',
      status: types.AUTOCUT_TASK_STATUS.completed,
      progress: 100,
      createdAt: '2026-05-05T00:00:00.000Z',
      sliceResults: [
        {
          id: 'slice-artifact-contract',
          name: 'Opening.mp4',
          duration: 15,
          size: 1234567,
          resolution: '1080P',
          thumbnailUrl: '',
          url: '',
          title: 'Opening hook',
          summary: 'Local sidecar summary survives native task projection.',
          reason: 'The deterministic candidate has a complete hook and payoff.',
          qualityScore: 0.91,
          continuityScore: 0.87,
          storyShape: 'complete',
          publishabilityScore: 0.9,
          publishabilityGrade: 'excellent',
          publishabilityIssues: ['needs-cover-title'],
          boundaryQualityScore: 0.88,
          hookStrength: 'strong',
          endingCompleteness: 'complete',
          contentArcScore: 0.94,
          contentArcGrade: 'complete',
          contentArcStages: ['hook', 'setup', 'conflict', 'payoff'],
          contentArcMissingStages: [],
          topicCoherenceScore: 0.91,
          topicCoherenceGrade: 'strong',
          topicShiftCount: 0,
          topicKeywords: ['opening', 'pain', 'payoff'],
          platformReadinessScore: 0.89,
          platformReadinessGrade: 'ready',
          platformReadinessIssues: ['platform-ready'],
          sentenceBoundaryIntegrityScore: 0.93,
          sentenceBoundaryIntegrityGrade: 'clean',
          sentenceBoundaryIssues: ['sentence-clean'],
          risks: ['needs-cover-title'],
          sourceStartMs: 1000,
          sourceEndMs: 16000,
          speechStartMs: 1200,
          speechEndMs: 15750,
          boundaryPaddingBeforeMs: 200,
          boundaryPaddingAfterMs: 250,
          transcriptText: 'Opening transcript text survives projection.',
          transcriptSegments: [
            {
              startMs: 1200,
              endMs: 15750,
              text: 'Opening transcript text survives projection.',
              speaker: 'Speaker 1',
            },
          ],
          transcriptSegmentCount: 1,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'strong',
          ...createRecoveredSmartSliceAudioCleanupEvidence(),
        },
      ],
    });
    const nativeTasksWithSidecarMetadata = await withImmediateTimers(() => services.getTasks());
    const nativeSliceTaskWithSidecarMetadata = nativeTasksWithSidecarMetadata.find((task) => task.id === 'ops-task-slice-contract');
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.summary,
      'Local sidecar summary survives native task projection.',
      'native task projection merges local smart slice summary metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.qualityScore,
      0.91,
      'native task projection merges local smart slice quality score metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.storyShape,
      'complete',
      'native task projection merges local smart slice story-shape metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.publishabilityScore,
      0.9,
      'native task projection merges local smart slice publishability score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.publishabilityGrade,
      'excellent',
      'native task projection merges local smart slice publishability grade sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.publishabilityIssues?.[0],
      'needs-cover-title',
      'native task projection merges local smart slice publishability issue sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.boundaryQualityScore,
      0.88,
      'native task projection merges local smart slice boundary quality score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.hookStrength,
      'strong',
      'native task projection merges local smart slice hook strength sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.endingCompleteness,
      'complete',
      'native task projection merges local smart slice ending completeness sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.contentArcScore,
      0.94,
      'native task projection merges local smart slice content-arc score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.contentArcGrade,
      'complete',
      'native task projection merges local smart slice content-arc grade sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.contentArcStages?.[2],
      'conflict',
      'native task projection merges local smart slice content-arc stage sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.contentArcMissingStages?.length,
      0,
      'native task projection merges local smart slice missing content-arc stage sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.topicCoherenceScore,
      0.91,
      'native task projection merges local smart slice topic coherence score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.topicCoherenceGrade,
      'strong',
      'native task projection merges local smart slice topic coherence grade sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.topicShiftCount,
      0,
      'native task projection merges local smart slice topic shift count sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.topicKeywords?.[0],
      'opening',
      'native task projection merges local smart slice topic keyword sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.platformReadinessScore,
      0.89,
      'native task projection merges local smart slice platform readiness score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.platformReadinessGrade,
      'ready',
      'native task projection merges local smart slice platform readiness grade sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.platformReadinessIssues?.[0],
      'platform-ready',
      'native task projection merges local smart slice platform readiness issue sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.sentenceBoundaryIntegrityScore,
      0.93,
      'native task projection merges local smart slice sentence boundary integrity score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.sentenceBoundaryIntegrityGrade,
      'clean',
      'native task projection merges local smart slice sentence boundary integrity grade sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.sentenceBoundaryIssues?.[0],
      'sentence-clean',
      'native task projection merges local smart slice sentence boundary issue sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.sourceEndMs,
      16000,
      'native task projection merges local smart slice repaired source range metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.speechStartMs,
      1200,
      'native task projection merges local smart slice unpadded speech start metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.speechEndMs,
      15750,
      'native task projection merges local smart slice unpadded speech end metadata sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.boundaryPaddingBeforeMs,
      200,
      'native task projection merges local smart slice leading boundary padding sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.boundaryPaddingAfterMs,
      250,
      'native task projection merges local smart slice trailing boundary padding sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.transcriptText,
      'Opening transcript text survives projection.',
      'native task projection merges local smart slice transcript text sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Opening transcript text survives projection.',
      'native task projection merges local smart slice structured transcript segments',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.transcriptSegmentCount,
      1,
      'native task projection merges local smart slice structured transcript segment counts',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.transcriptCoverageScore,
      0.96,
      'native task projection merges local smart slice transcript coverage score sidecars',
    );
    assertEqual(
      nativeSliceTaskWithSidecarMetadata?.sliceResults?.[0]?.speechContinuityGrade,
      'strong',
      'native task projection merges local smart slice speech continuity grade sidecars',
    );

    resetStorage();
    await services.addTask({
      id: 'ops-task-slice-contract',
      type: types.AUTOCUT_TASK_TYPES[0],
      name: 'stale local smart slice metadata sidecar',
      status: types.AUTOCUT_TASK_STATUS.completed,
      progress: 100,
      createdAt: '2026-05-05T00:00:00.000Z',
      sliceResults: [
        {
          id: 'stale-local-artifact-contract',
          name: 'Wrong clip.mp4',
          duration: 15,
          size: 1234567,
          resolution: '1080P',
          thumbnailUrl: '',
          url: '',
          summary: 'This stale summary must not be attached to a different native artifact.',
          qualityScore: 0.99,
          publishabilityScore: 0.99,
          publishabilityGrade: 'excellent',
          sourceStartMs: 80000,
          sourceEndMs: 95000,
          speechStartMs: 80200,
          speechEndMs: 94750,
        },
      ],
    });
    const nativeTasksWithMismatchedSidecarMetadata = await withImmediateTimers(() => services.getTasks());
    const nativeSliceTaskWithMismatchedSidecarMetadata = nativeTasksWithMismatchedSidecarMetadata.find((task) => task.id === 'ops-task-slice-contract');
    assertEqual(
      nativeSliceTaskWithMismatchedSidecarMetadata?.sliceResults?.[0]?.summary,
      undefined,
      'native task projection does not merge stale smart slice metadata by index when artifact ids and source windows differ',
    );
    assertEqual(
      nativeSliceTaskWithMismatchedSidecarMetadata?.sliceResults?.[0]?.qualityScore,
      undefined,
      'native task projection does not attach stale quality scores to a different native slice',
    );
    resetStorage();
    const nativeTranscriptTask = nativeTasks.find((task) => task.id === 'ops-task-transcript-contract');
    assertEqual(nativeTranscriptTask, undefined, 'getTasks hides native speech transcription implementation tasks from the user-facing task list');
    const corruptNativeTranscriptTask = nativeTasks.find((task) => task.id === 'ops-task-transcript-corrupt-contract');
    assertEqual(
      corruptNativeTranscriptTask,
      undefined,
      'getTasks hides corrupt native speech transcription implementation tasks instead of surfacing them as text extraction tasks',
    );
    const nativeCompressTask = nativeTasks.find((task) => task.id === 'ops-task-compress-contract');
    assertEqual(nativeCompressTask?.type, types.AUTOCUT_TASK_TYPES[4], 'native video compress tasks map to the AppTask compression type');
    assertEqual(nativeCompressTask?.videoUrl, 'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fops-task-compress-contract%2Fcompressed.mp4', 'native compression artifactPath maps to videoUrl');
    assertEqual(nativeCompressTask?.fileSizeStats?.originalSize, 10000, 'native compression originalByteSize maps to fileSizeStats');
    assertEqual(nativeCompressTask?.fileSizeStats?.newSize, 2500, 'native compression byteSize maps to fileSizeStats');
    assertEqual(nativeCompressTask?.fileSizeStats?.compressionRatio, 0.75, 'native compression ratio is derived from real output sizes');
    const nativeFailedTask = nativeTasks.find((task) => task.id === 'ops-task-failed-contract');
    assertEqual(nativeFailedTask?.type, types.AUTOCUT_TASK_TYPES[2], 'native audio extraction tasks map to the AppTask audio extraction type');
    assertEqual(nativeFailedTask?.status, types.AUTOCUT_TASK_STATUS.failed, 'native failed status maps to failed AppTask status');
    assertEqual(nativeFailedTask?.errorMessage, 'ffmpeg exited with status 1', 'native task errorMessage is preserved');
    const corruptNativeSliceTask = nativeTasks.find((task) => task.id === 'ops-task-corrupt-slice-contract');
    assertEqual(
      corruptNativeSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice tasks with corrupt output are recovered as failed AppTasks',
    );
    assertIncludes(
      corruptNativeSliceTask?.errorMessage ?? '',
      'returned 1 slice artifacts for 2 declared slices',
      'native completed slice task recovery explains corrupt slice count mismatches',
    );
    assertEqual(
      corruptNativeSliceTask?.resultCount,
      undefined,
      'native corrupt slice task recovery does not expose a result count',
    );
    assertEqual(
      corruptNativeSliceTask?.generatedAssetIds,
      undefined,
      'native corrupt slice task recovery does not expose partial generated asset ids',
    );
    assertEqual(
      corruptNativeSliceTask?.sliceResults,
      undefined,
      'native corrupt slice task recovery does not expose partial slice results',
    );
    assertEqual(
      corruptNativeSliceTask?.completedAt,
      undefined,
      'native corrupt slice task recovery does not expose a completed timestamp',
    );
    assertEqual(
      corruptNativeSliceTask?.sourceFileId,
      'asset-corrupt-slice-contract',
      'native corrupt slice task recovery preserves the source asset id for diagnostics',
    );
    const emptyCompletedNativeSliceTask = nativeTasks.find((task) => task.id === 'ops-task-empty-completed-slice-contract');
    assertEqual(
      emptyCompletedNativeSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'native completed slice tasks with zero declared outputs are recovered as failed AppTasks',
    );
    assertIncludes(
      emptyCompletedNativeSliceTask?.errorMessage ?? '',
      'has invalid sliceCount',
      'native completed slice task recovery explains invalid empty slice counts',
    );
    assertEqual(
      emptyCompletedNativeSliceTask?.resultCount,
      undefined,
      'native empty completed slice task recovery does not expose a zero result count',
    );
    assertEqual(
      emptyCompletedNativeSliceTask?.sliceResults,
      undefined,
      'native empty completed slice task recovery does not expose empty slice results',
    );
    const processingNativeSliceTask = nativeTasks.find((task) => task.id === 'ops-task-processing-slice-contract');
    assertEqual(
      processingNativeSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.processing,
      'native processing slice tasks stay processing during recovery',
    );
    assertEqual(
      processingNativeSliceTask?.resultCount,
      undefined,
      'native processing slice tasks do not expose result counts before completion',
    );
    assertEqual(
      processingNativeSliceTask?.generatedAssetIds,
      undefined,
      'native processing slice tasks do not expose generated asset ids before completion',
    );
    assertEqual(
      processingNativeSliceTask?.sliceResults,
      undefined,
      'native processing slice tasks do not expose slice results before completion',
    );
    services.resetAutoCutNativeHostClient();
    const commons = await loadModule(server, 'packages/sdkwork-autocut-commons/src/index.ts');
    const normalizedTaskDetailTextSample = decodeEscapedText('\\u4e2d\\u6587\\u4efb\\u52a1\\u8be6\\u60c5');
    const mojibakeTaskDetailTextSample = Buffer.from(normalizedTaskDetailTextSample, 'utf8').toString('latin1');
    assertEqual(
      commons.normalizeAutoCutTaskDetailDisplayText(mojibakeTaskDetailTextSample),
      normalizedTaskDetailTextSample,
      'shared task detail text normalization repairs UTF-8 mojibake task detail text',
    );
    assertEqual(
      commons.normalizeAutoCutTaskDetailDisplayText('plain diagnostic text'),
      'plain diagnostic text',
      'shared task detail text normalization preserves ordinary ASCII diagnostics',
    );
    assertEqual(
      commons.normalizeAutoCutTaskDetailDisplayText('Runtime params:\\nsourceAssetUuid=asset-1'),
      'Runtime params:\nsourceAssetUuid=asset-1',
      'shared task detail text normalization decodes escaped diagnostic line breaks',
    );
    assertEqual(
      commons.validateAutoCutTrustedFileRequiredStreams(
        createTrustedLocalMediaFile(
          commons,
          'D:/media/mislabeled-video-without-video-stream.wav',
          'mislabeled-video-without-video-stream.wav',
          { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: true, hasVideoStream: false },
        ),
        { video: true },
      ),
      'AutoCut selected media requires a video stream.',
      'trusted upload validation rejects video-labeled files when native probe evidence reports no video stream',
    );
    assertEqual(
      commons.validateAutoCutTrustedFileRequiredStreams(
        createTrustedLocalMediaFile(
          commons,
          'D:/media/mislabeled-audio-without-audio-stream.mp4',
          'mislabeled-audio-without-audio-stream.mp4',
          { mimeType: 'audio/mp4', mediaType: 'audio', hasAudioStream: false, hasVideoStream: true },
        ),
        { audio: true },
      ),
      'AutoCut selected media requires an audio stream.',
      'trusted upload validation rejects audio-labeled files when native probe evidence reports no audio stream',
    );
    assertEqual(
      commons.validateAutoCutTrustedFileRequiredStreams(
        createTrustedLocalMediaFile(
          commons,
          'D:/media/audio-bearing-source.flac',
          'audio-bearing-source.flac',
          { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
        ),
        { audio: true },
      ),
      null,
      'trusted upload validation accepts audio-bearing files for audio-stream workflows',
    );
    assertEqual(
      commons.validateAutoCutTrustedFileRequiredStreams(
        createTrustedLocalMediaFile(
          commons,
          'D:/media/video-bearing-source.mp4',
          'video-bearing-source.mp4',
          { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: true, hasVideoStream: true },
        ),
        { audio: true, video: true },
      ),
      null,
      'trusted upload validation accepts files with both streams for hardcoded subtitle video workflows',
    );

    assertEqual(services.createAutoCutStorageKey('tasks'), 'autocut_release_tasks', 'storage key factory namespaces release task data');
    assertEqual(services.createAutoCutStorageKey('assets'), 'autocut_release_assets', 'storage key factory namespaces release asset data');
    assertEqual(services.createAutoCutStorageKey('messages'), 'autocut_release_messages', 'storage key factory namespaces release message data');
    assertEqual(services.createAutoCutStorageKey('settings'), 'autocut_release_settings', 'storage key factory namespaces release settings data');
    assertEqual(
      services.createAutoCutStorageKey('workflowPreferences'),
      'autocut_release_workflow_preferences',
      'storage key factory namespaces release workflow parameter preferences',
    );
    services.configureAutoCutRuntimeEnvironment('dev');
    assertEqual(services.createAutoCutStorageKey('settings'), 'autocut_dev_settings', 'storage key factory isolates dev settings data');
    assertEqual(
      services.createAutoCutStorageKey('workflowPreferences'),
      'autocut_dev_workflow_preferences',
      'storage key factory isolates dev workflow parameter preferences',
    );
    services.configureAutoCutRuntimeEnvironment('release');
    assertEqual(services.createAutoCutStorageKey('settings'), 'autocut_release_settings', 'storage key factory restores release settings data');

    assertRule(
      typeof services.getAutoCutWorkflowPreferences === 'function',
      'services exports workflow parameter preference loading',
    );
    assertRule(
      typeof services.saveAutoCutVideoSlicePreferences === 'function',
      'services exports video slice parameter preference persistence',
    );
    assertRule(
      typeof services.saveAutoCutTextExtractionPreferences === 'function',
      'services exports text extraction parameter preference persistence',
    );
    if (
      typeof services.getAutoCutWorkflowPreferences === 'function' &&
      typeof services.saveAutoCutVideoSlicePreferences === 'function' &&
      typeof services.saveAutoCutTextExtractionPreferences === 'function'
    ) {
      const defaultWorkflowPreferences = await withImmediateTimers(() => services.getAutoCutWorkflowPreferences());
      assertEqual(
        defaultWorkflowPreferences.videoSlice.targetPlatform,
        'douyin',
        'workflow parameter preferences provide a canonical video slice target platform default',
      );
      assertEqual(
        defaultWorkflowPreferences.videoSlice.enableSubtitles,
        false,
        'workflow parameter preferences default video slicing to transcript-assisted planning without subtitle rendering',
      );
    assertEqual(
      defaultWorkflowPreferences.videoSlice.sttPresetId,
      'smart-slice-cloud-stt',
      'workflow parameter preferences default Smart Slice to the commercial cloud STT workflow preset',
    );
      assertEqual(
        defaultWorkflowPreferences.videoSlice.subtitleMode,
        'both',
        'workflow parameter preferences retain a canonical subtitle publishing mode for when subtitle rendering is enabled',
      );
      assertEqual(
        defaultWorkflowPreferences.textExtraction.language,
        'auto',
        'workflow parameter preferences provide a canonical text extraction language default',
      );

      const savedVideoSlicePreferences = await withImmediateTimers(() =>
        services.saveAutoCutVideoSlicePreferences({
          mode: 'contract-mode',
          targetPlatform: 'bilibili',
          targetAspectRatio: '16:9',
          videoObjectFit: 'contain',
          idealDuration: 2,
          continuityLevel: 'strict',
          sttPresetId: 'smart-slice-fast-preview',
          customKeywordsInput: ' hook, payoff ',
          minDuration: 2,
          maxDuration: 9999,
          llmModel: 'gpt-5.4',
          baseAlgorithm: 'pause',
          highlightEngine: 'motion',
          enableNoiseReduction: false,
          enableCoughFilter: false,
          enableRepeatFilter: true,
          enableSubtitles: true,
          subtitleMode: 'srt',
          subtitleStyleId: 'minimal',
        }),
      );
      const savedTextExtractionPreferences = await withImmediateTimers(() =>
        services.saveAutoCutTextExtractionPreferences({
          language: 'ja_JP',
          separateSpeakers: false,
          filterWords: false,
        }),
      );
      const reloadedWorkflowPreferences = await withImmediateTimers(() => services.getAutoCutWorkflowPreferences());
      const storedWorkflowPreferences = readScopedStoredObject(services, 'workflowPreferences');
      assertNotOwnProperty(
        savedVideoSlicePreferences.videoSlice,
        'targetSliceCount',
        'video slice parameter preferences do not persist target clip counts because Smart Slice derives clips from content',
      );
      assertNotOwnProperty(
        savedVideoSlicePreferences.videoSlice,
        'sliceCountMode',
        'video slice parameter preferences do not persist count strategies because Smart Slice derives clips from content',
      );
      assertEqual(
        savedVideoSlicePreferences.videoSlice.minDuration,
        5,
        'video slice parameter preferences clamp minimum duration before persistence',
      );
      assertEqual(
        savedVideoSlicePreferences.videoSlice.maxDuration,
        600,
        'video slice parameter preferences clamp maximum duration before persistence',
      );
      assertEqual(
        savedVideoSlicePreferences.videoSlice.customKeywordsInput,
        'hook, payoff',
        'video slice parameter preferences normalize keyword input before persistence',
      );
      assertEqual(
        savedVideoSlicePreferences.videoSlice.enableNoiseReduction,
        false,
        'video slice parameter preferences preserve disabled broadband denoise for clean source audio',
      );
      assertEqual(
        savedTextExtractionPreferences.textExtraction.language,
        'ja-JP',
        'text extraction parameter preferences normalize BCP-47 language tags before persistence',
      );
      assertEqual(
        reloadedWorkflowPreferences.videoSlice.subtitleMode,
        'srt',
        'workflow parameter preferences reload the selected subtitle publishing mode without promotion',
      );
      assertEqual(
        reloadedWorkflowPreferences.videoSlice.enableNoiseReduction,
        false,
        'workflow parameter preferences reload disabled broadband denoise after disabled denoise input',
      );
      assertEqual(
        reloadedWorkflowPreferences.videoSlice.sttPresetId,
        'smart-slice-fast-preview',
        'workflow parameter preferences reload the selected Smart Slice STT workflow preset',
      );
      assertEqual(
        savedVideoSlicePreferences.videoSlice.subtitleMode,
        'srt',
        'video slice parameter preferences preserve SRT-only subtitle publishing mode',
      );
      const disabledVideoSlicePreferences = await withImmediateTimers(() =>
        services.saveAutoCutVideoSlicePreferences({
          enableSubtitles: false,
          subtitleMode: 'none',
        }),
      );
      assertEqual(
        disabledVideoSlicePreferences.videoSlice.enableSubtitles,
        false,
        'video slice parameter preferences allow subtitle rendering to remain disabled while smart slicing still uses speech-to-text',
      );
      assertEqual(
        disabledVideoSlicePreferences.videoSlice.subtitleMode,
        'none',
        'video slice parameter preferences preserve disabled subtitle mode',
      );
      assertEqual(
        reloadedWorkflowPreferences.textExtraction.separateSpeakers,
        false,
        'workflow parameter preferences reload the last text extraction speaker setting',
      );
      assertEqual(
        storedWorkflowPreferences.videoSlice.llmModel,
        'gpt-5.4',
        'workflow parameter preferences persist video slice model selection in scoped storage',
      );
      assertEqual(
        storedWorkflowPreferences.videoSlice.enableNoiseReduction,
        false,
        'workflow parameter preferences store disabled broadband denoise after disabled denoise input',
      );
    assertEqual(
      storedWorkflowPreferences.videoSlice.sttPresetId,
      'smart-slice-fast-preview',
      'workflow parameter preferences persist the selected Smart Slice STT workflow preset',
    );
      assertRule(
        Boolean(storedWorkflowPreferences.updatedAt),
        'workflow parameter preferences persist an updatedAt timestamp',
      );
    }

    services.resetAutoCutNativeHostClient();
    const unsupportedNativeHostClient = services.getAutoCutNativeHostClient();
    const unsupportedCapabilities = await unsupportedNativeHostClient.getCapabilities();
    const unsupportedDatabaseHealth = await unsupportedNativeHostClient.getDatabaseHealth();
    const unsupportedFfmpegProbe = await unsupportedNativeHostClient.probeFfmpeg();
    assertEqual(unsupportedCapabilities.hostKind, 'browser', 'native host fallback reports browser host kind');
    assertEqual(
      unsupportedCapabilities.mediaImportCommandReady,
      false,
      'native host fallback does not claim media import readiness',
    );
    assertEqual(
      unsupportedCapabilities.llmSecretStoreReady,
      false,
      'native host fallback does not claim LLM secret store readiness',
    );
    assertEqual(
      unsupportedCapabilities.mediaFileDescribeCommandReady,
      false,
      'native host fallback does not claim local file describe readiness',
    );
    assertEqual(
      unsupportedCapabilities.localMediaFileSelectCommandReady,
      false,
      'native host fallback does not claim local media chooser readiness',
    );
    assertEqual(
      unsupportedCapabilities.localVideoFileSelectCommandReady,
      false,
      'native host fallback does not claim local video chooser readiness',
    );
    assertEqual(
      unsupportedCapabilities.localDirectorySelectCommandReady,
      false,
      'native host fallback does not claim local directory chooser readiness',
    );
    assertEqual(
      unsupportedCapabilities.localMediaPreviewDirectoryScopeCommandReady,
      false,
      'native host fallback does not claim local media preview directory authorization readiness',
    );
    assertEqual(
      unsupportedCapabilities.audioExtractionFromAssetReady,
      false,
      'native host fallback does not claim assetUuid extraction readiness',
    );
    assertEqual(
      unsupportedCapabilities.videoGifCommandReady,
      false,
      'native host fallback does not claim video GIF command readiness',
    );
    assertEqual(
      unsupportedCapabilities.videoCompressCommandReady,
      false,
      'native host fallback does not claim video compression command readiness',
    );
    assertEqual(
      unsupportedCapabilities.videoConvertCommandReady,
      false,
      'native host fallback does not claim video conversion command readiness',
    );
    assertEqual(
      unsupportedCapabilities.videoEnhanceCommandReady,
      false,
      'native host fallback does not claim video enhancement command readiness',
    );
    assertEqual(
      unsupportedCapabilities.videoSliceCommandReady,
      false,
      'native host fallback does not claim video slice command readiness',
    );
    assertEqual(
      unsupportedCapabilities.speechTranscriptionCommandReady,
      false,
      'native host fallback does not claim speech transcription command readiness',
    );
    assertEqual(
      unsupportedCapabilities.speechTranscriptionToolchainReady,
      false,
      'native host fallback does not claim speech transcription toolchain readiness',
    );
    assertEqual(
      unsupportedCapabilities.speechTranscriptionProbeCommandReady,
      false,
      'native host fallback does not claim speech transcription probe readiness',
    );
    assertEqual(
      unsupportedCapabilities.speechTranscriptionFileSelectCommandReady,
      false,
      'native host fallback does not claim speech transcription file chooser readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeTaskQueryCommandReady,
      false,
      'native host fallback does not claim native task query readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeTaskCancelCommandReady,
      false,
      'native host fallback does not claim native task cancel readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeTaskRecoveryCommandReady,
      false,
      'native host fallback does not claim native task recovery readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeTaskRetryCommandReady,
      false,
      'native host fallback does not claim native task retry readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeTaskProgressEventsReady,
      false,
      'native host fallback does not claim native task progress event readiness',
    );
    assertEqual(
      unsupportedCapabilities.nativeWorkerLeaseReady,
      false,
      'native host fallback does not claim native worker lease readiness',
    );
    assertEqual(unsupportedDatabaseHealth.ready, false, 'native host fallback database health is not ready');
    assertEqual(unsupportedFfmpegProbe.available, false, 'native host fallback FFmpeg probe is unavailable');
    assertEqual(
      unsupportedFfmpegProbe.manifestReady,
      false,
      'native host fallback FFmpeg probe does not claim manifest readiness',
    );
    assertEqual(
      unsupportedFfmpegProbe.bundledReady,
      false,
      'native host fallback FFmpeg probe does not claim bundled readiness',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.importMediaFile({ sourcePath: 'D:/media/source.mp4' }),
      'Tauri desktop host',
      'native host fallback media import',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.describeLocalMediaFile({ sourcePath: 'D:/media/source.mp4' }),
      'Tauri desktop host',
      'native host fallback local file describe',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.selectLocalDirectory(),
      'Tauri desktop host',
      'native host fallback local directory selection',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.allowLocalMediaPreviewDirectory({ directoryPath: 'D:/media' }),
      'Tauri desktop host',
      'native host fallback local media preview directory authorization',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.listNativeTasks({ limit: 10 }),
      'Tauri desktop host',
      'native host fallback task query',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.cancelNativeTask({ taskUuid: 'ops-task-contract' }),
      'Tauri desktop host',
      'native host fallback task cancellation',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.recoverNativeTasks({ limit: 10 }),
      'Tauri desktop host',
      'native host fallback task recovery',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.retryNativeTask({ taskUuid: 'ops-task-contract' }),
      'Tauri desktop host',
      'native host fallback task retry',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.extractAudio({ assetUuid: 'media-asset-1', outputFormat: 'wav' }),
      'assetUuid',
      'native host fallback assetUuid audio extraction',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.generateGif({
          assetUuid: 'media-asset-1',
          fps: '15',
          resolution: '480p',
          dither: true,
        }),
      'assetUuid',
      'native host fallback assetUuid video GIF generation',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.compressVideo({
          assetUuid: 'media-asset-1',
          compressionMode: 'balanced',
        }),
      'assetUuid',
      'native host fallback assetUuid video compression',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.convertVideo({
          assetUuid: 'media-asset-1',
          targetFormat: 'mp4',
          videoCodec: 'h264',
          audioCodec: 'aac',
          resolution: 'original',
        }),
      'assetUuid',
      'native host fallback assetUuid video conversion',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.enhanceVideo({
          assetUuid: 'media-asset-1',
          targetResolution: '1080p',
          enhanceMode: 'real',
          frameRate: 'original',
        }),
      'assetUuid',
      'native host fallback assetUuid video enhancement',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.sliceVideo({
          assetUuid: 'media-asset-1',
          clips: [{ startMs: 0, durationMs: 15000, label: 'Highlight 1' }],
          outputFormat: 'mp4',
        }),
      'assetUuid',
      'native host fallback assetUuid video slicing',
    );
    const invalidNativeVideoSliceInvocations = [];
    const invalidNativeVideoSliceClient = services.createAutoCutNativeHostClient(async (command, args) => {
      invalidNativeVideoSliceInvocations.push({ command, args });
      return {
        taskUuid: 'unexpected-native-video-slice-task',
        sourceAssetUuid: args?.request?.assetUuid ?? '',
        taskOutputDir: 'D:/autocut/media/tasks/unexpected-native-video-slice-task',
        slices: [],
        ffmpegExecutable: 'ffmpeg',
      };
    });
    await invalidNativeVideoSliceClient.sliceVideo({
      assetUuid: 'media-asset-1',
      clips: [{ startMs: 0, durationMs: 45000, label: 'Fixed interval without STT' }],
      outputFormat: 'mp4',
    });
    assertEqual(
      invalidNativeVideoSliceInvocations.length,
      1,
      'native host client invokes Tauri video slicing for basic timeline clips without STT evidence',
    );
    const audioRefinedNativeVideoSliceInvocations = [];
    const audioRefinedNativeVideoSliceClient = services.createAutoCutNativeHostClient(async (command, args) => {
      audioRefinedNativeVideoSliceInvocations.push({ command, args });
      return {
        taskUuid: 'audio-refined-native-video-slice-task',
        sourceAssetUuid: args?.request?.assetUuid ?? '',
        taskOutputDir: 'D:/autocut/media/tasks/audio-refined-native-video-slice-task',
        ffmpegExecutable: 'ffmpeg',
        slices: [],
      };
    });
    await audioRefinedNativeVideoSliceClient.sliceVideo({
      assetUuid: 'media-asset-1',
      clips: [
        {
          startMs: 1000,
          durationMs: 10000,
          label: 'Audio refined speech range',
          sourceStartMs: 1000,
          sourceEndMs: 11000,
          speechStartMs: 1200,
          speechEndMs: 10800,
          boundaryPaddingBeforeMs: 200,
          boundaryPaddingAfterMs: 200,
          transcriptText: 'Opening context. Closing context.',
          transcriptSegments: [
            { startMs: 1000, endMs: 5600, text: 'Opening context.', speaker: 'Speaker 1' },
            { startMs: 5700, endMs: 11000, text: 'Closing context.', speaker: 'Speaker 1' },
          ],
          transcriptSegmentCount: 2,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'strong',
        },
      ],
      outputFormat: 'mp4',
    });
    assertEqual(
      audioRefinedNativeVideoSliceInvocations.length,
      1,
      'native host client accepts audio-refined smart slice speech ranges covered by transcript segment boundaries',
    );
    assertEqual(
      audioRefinedNativeVideoSliceInvocations[0]?.args?.request?.clips?.[0]?.speechStartMs,
      1200,
      'native host client forwards audio-refined speechStartMs after transcript coverage validation',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.transcribeMedia({
          assetUuid: 'media-asset-1',
          language: 'zh',
        }),
      'speech transcription',
      'native host fallback speech transcription',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.probeSpeechTranscription({
          executablePath: 'D:/tools/whisper-cli.exe',
          modelPath: 'D:/models/ggml.bin',
        }),
      'Tauri desktop host',
      'native host fallback speech transcription probe',
    );
    await assertRejects(
      () => unsupportedNativeHostClient.selectSpeechTranscriptionFile({ kind: 'model' }),
      'Tauri desktop host',
      'native host fallback speech transcription file selection',
    );
    await assertRejects(
      () =>
        unsupportedNativeHostClient.saveLlmSecret({
          secretName: 'default',
          secretValue: 'sk-browser-secret',
        }),
      'secret store',
      'native host fallback LLM secret save',
    );

    const invokedCommands = [];
    let nativeLlmSecretValue;
    const nativeHostClient = services.createAutoCutNativeHostClient(async (command, args) => {
      invokedCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'contract-test',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localMediaFileSelectCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          localDirectorySelectCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: [
            'autocut_import_media_file',
            'autocut_select_local_directory',
            'autocut_allow_local_media_preview_directory',
            'autocut_extract_audio',
            'autocut_generate_gif',
            'autocut_compress_video',
            'autocut_convert_video',
            'autocut_enhance_video',
            'autocut_transcribe_media',
            'autocut_probe_speech_transcription',
            'autocut_select_speech_transcription_file',
            'autocut_list_native_tasks',
            'autocut_cancel_native_task',
            'autocut_recover_native_tasks',
            'autocut_retry_native_task',
            'autocut_llm_http_request',
            'autocut_save_llm_secret',
            'autocut_get_llm_secret',
            'autocut_delete_llm_secret',
          ],
        };
      }
      if (command === 'autocut_database_health') {
        return {
          ready: true,
          databasePath: 'memory',
          appliedMigrations: ['baseline'],
          verifiedTables: ['media_asset'],
          missingTables: [],
          diagnostics: [],
        };
      }
      if (command === 'autocut_ffmpeg_probe') {
        return {
          available: true,
          executable: 'ffmpeg',
          sourceKind: 'system-path',
          manifestReady: true,
          bundledReady: false,
          versionLine: 'ffmpeg contract',
          diagnostics: [],
        };
      }
      if (command === 'autocut_import_media_file') {
        return {
          assetUuid: 'media-asset-contract',
          sandboxPath: 'D:/autocut/media/inputs/media-asset-contract.mp4',
          byteSize: 123,
          name: 'source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 100000,
        };
      }
      if (command === 'autocut_describe_local_media_file') {
        return {
          sourcePath: args.request.sourcePath,
          byteSize: 123,
          name: 'source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 100000,
        };
      }
      if (command === 'autocut_select_local_video_file') {
        return {
          sourcePath: 'D:/media/selected-source.mp4',
          byteSize: 789,
          name: 'selected-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 78000,
        };
      }
      if (command === 'autocut_select_local_media_file') {
        return {
          sourcePath: 'D:/media/selected-audio.wav',
          byteSize: 456,
          name: 'selected-audio.wav',
          mediaType: 'audio',
          hasAudioStream: true,
          hasVideoStream: false,
          mimeType: 'audio/wav',
        };
      }
      if (command === 'autocut_select_local_directory') {
        return 'D:/media/selected-output-root';
      }
      if (command === 'autocut_allow_local_media_preview_directory') {
        return {
          directoryPath: args.request.directoryPath,
          allowed: true,
        };
      }
      if (command === 'autocut_probe_speech_transcription') {
        return {
          ready: true,
          executablePath: args.request.executablePath,
          modelPath: args.request.modelPath,
          sourceKind: 'settings',
          diagnostics: [],
          versionLine: 'whisper.cpp contract',
        };
      }
      if (command === 'autocut_select_speech_transcription_file') {
        return args.request.kind === 'executable'
          ? 'D:/tools/whisper-cli.exe'
          : 'D:/models/ggml-large-v3-turbo.bin';
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'ops-task-contract',
            taskType: 1,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'media-asset-contract',
            inputJson: '{}',
            outputJson: '{}',
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:00:00.000Z',
            stages: [],
            events: [
              {
                uuid: 'ops-task-event-progress-contract',
                eventType: 8,
                payload: {
                  operation: 'audioExtraction',
                  phase: 'ffmpeg-progress-streamed',
                  source: 'ffmpeg-progress',
                  progress: 42,
                },
                payloadJson:
                  '{"operation":"audioExtraction","phase":"ffmpeg-progress-streamed","source":"ffmpeg-progress","progress":42}',
                createdAt: '2026-05-05T00:00:00.000Z',
                updatedAt: '2026-05-05T00:00:00.000Z',
              },
            ],
            workerLeases: [
              {
                uuid: 'ops-worker-lease-contract',
                workerId: 'autocut-native-media-worker',
                leaseStatus: 2,
                leaseToken: 'ops-worker-lease-token-contract',
                acquiredAt: '2026-05-05T00:00:00.000Z',
                heartbeatAt: '2026-05-05T00:00:01.000Z',
                expiresAt: '2026-05-05T00:02:01.000Z',
                releasedAt: '2026-05-05T00:00:02.000Z',
                diagnosticsJson: '{"releaseReason":"completed","source":"native-host"}',
                createdAt: '2026-05-05T00:00:00.000Z',
                updatedAt: '2026-05-05T00:00:02.000Z',
              },
            ],
          },
        ];
      }
      if (command === 'autocut_cancel_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          status: 4,
          canceled: true,
          message: 'cancel requested',
        };
      }
      if (command === 'autocut_recover_native_tasks') {
        return {
          recovered: 2,
          interrupted: 1,
          canceled: 1,
          expiredLeases: 2,
          deferred: 1,
          inspected: args.request.limit,
          taskUuids: ['ops-task-interrupted', 'ops-task-canceled'],
        };
      }
      if (command === 'autocut_retry_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          retryTaskUuid: 'ops-task-retry-contract',
          status: 1,
          retried: true,
          message: 'retry started',
        };
      }
      if (command === 'autocut_extract_audio') {
        const output = createNativeTaskOutputArtifact('ops-task-contract', 'audio.wav');
        return {
          artifactUuid: 'media-artifact-contract',
          taskUuid: 'ops-task-contract',
          sourceAssetUuid: args.request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 456,
          format: args.request.outputFormat,
          ffmpegExecutable: 'ffmpeg',
        };
      }
      if (command === 'autocut_generate_gif') {
        const output = createNativeTaskOutputArtifact('ops-task-gif-contract', 'source.gif');
        return {
          artifactUuid: 'media-artifact-gif-contract',
          taskUuid: 'ops-task-gif-contract',
          sourceAssetUuid: args.request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 567,
          format: 'gif',
          ffmpegExecutable: 'ffmpeg',
        };
      }
      if (command === 'autocut_compress_video') {
        const output = createNativeTaskOutputArtifact('ops-task-compress-contract', 'source-compressed.mp4');
        return {
          artifactUuid: 'media-artifact-compress-contract',
          taskUuid: 'ops-task-compress-contract',
          sourceAssetUuid: args.request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 234,
          originalByteSize: 123,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      }
      if (command === 'autocut_convert_video') {
        const output = createNativeTaskOutputArtifact('ops-task-convert-contract', 'source-converted.mp4');
        return {
          artifactUuid: 'media-artifact-convert-contract',
          taskUuid: 'ops-task-convert-contract',
          sourceAssetUuid: args.request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 345,
          format: args.request.targetFormat,
          ffmpegExecutable: 'ffmpeg',
        };
      }
      if (command === 'autocut_enhance_video') {
        const output = createNativeTaskOutputArtifact('ops-task-enhance-contract', 'source-enhanced.mp4');
        return {
          artifactUuid: 'media-artifact-enhance-contract',
          taskUuid: 'ops-task-enhance-contract',
          sourceAssetUuid: args.request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 456,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      }
      if (command === 'autocut_transcribe_media') {
        const output = createNativeTaskOutputArtifact('ops-task-transcript-contract', 'transcript.json');
        return {
          artifactUuid: 'media-artifact-transcript-contract',
          taskUuid: 'ops-task-transcript-contract',
          sourceAssetUuid: args.request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: args.request.language,
          text: 'First key transcript segment. Second turning transcript segment.',
          segments: [
            {
              startMs: 12000,
              endMs: 27000,
              text: 'First key transcript segment.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 45000,
              endMs: 62000,
              text: 'Second turning transcript segment.',
              speaker: 'Speaker 1',
            },
          ],
          qualityGuard: {
            schema: 'smart-slice.stt-quality-guard.v1',
            status: 'passed-after-retry',
            passed: true,
            scope: 'chunked-local-whisper',
            chunkId: '',
            retryCount: 1,
            riskCount: 0,
            risks: [],
            metrics: {
              segmentCount: 2,
              textLength: 58,
              uniqueCharacterRatio: 0.63,
              replacementCharacterCount: 0,
              repeatedPhraseRunCount: 0,
              duplicateWindowRatio: 0,
              tinySegmentRatio: 0,
            },
          },
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      }
      if (command === 'autocut_llm_http_request') {
        return {
          status: 200,
          statusText: 'OK',
          headers: {
            'content-type': 'application/json',
          },
          bodyText: '{"id":"native-llm-http","choices":[{"message":{"content":"pong"}}]}',
        };
      }
      if (command === 'autocut_save_llm_secret') {
        nativeLlmSecretValue = args.request.secretValue;
        return {
          secretName: args.request.secretName,
          saved: true,
        };
      }
      if (command === 'autocut_get_llm_secret') {
        return {
          secretName: args.request.secretName,
          configured: Boolean(nativeLlmSecretValue),
          ...(nativeLlmSecretValue ? { secretValue: nativeLlmSecretValue } : {}),
        };
      }
      if (command === 'autocut_delete_llm_secret') {
        const deleted = Boolean(nativeLlmSecretValue);
        nativeLlmSecretValue = undefined;
        return {
          secretName: args.request.secretName,
          deleted,
        };
      }
      const output = createNativeTaskOutputArtifact('ops-task-smoke', 'smoke.wav');
      return {
        artifactUuid: 'media-artifact-smoke',
        taskUuid: 'ops-task-smoke',
        sourceAssetUuid: '',
        artifactPath: output.artifactPath,
        taskOutputDir: output.taskOutputDir,
        byteSize: 789,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      };
    });
    configureAutoCutNativeHostClient(services, nativeHostClient);
    const configuredNativeHostClient = services.getAutoCutNativeHostClient();
    const configuredCapabilities = await configuredNativeHostClient.getCapabilities();
    const configuredDatabaseHealth = await configuredNativeHostClient.getDatabaseHealth();
    const configuredFfmpegProbe = await configuredNativeHostClient.probeFfmpeg();
    const importedNativeMedia = await configuredNativeHostClient.importMediaFile({
      sourcePath: 'D:/media/source.mp4',
    });
    const describedNativeMedia = await configuredNativeHostClient.describeLocalMediaFile({
      sourcePath: 'D:/media/source.mp4',
    });
    const selectedNativeMedia = await configuredNativeHostClient.selectLocalMediaFile({
      mediaTypes: ['audio', 'video'],
    });
    const selectedNativeVideo = await configuredNativeHostClient.selectLocalVideoFile();
    const selectedNativeDirectory = await configuredNativeHostClient.selectLocalDirectory();
    const previewDirectoryAuthorization = await configuredNativeHostClient.allowLocalMediaPreviewDirectory({
      directoryPath: 'D:/media',
    });
    const selectedSpeechExecutable = await configuredNativeHostClient.selectSpeechTranscriptionFile({ kind: 'executable' });
    const probedSpeechTranscription = await configuredNativeHostClient.probeSpeechTranscription({
      executablePath: 'D:/tools/whisper-cli.exe',
      modelPath: 'D:/models/ggml-large-v3-turbo.bin',
    });
    const extractedNativeAudio = await configuredNativeHostClient.extractAudio({
      assetUuid: importedNativeMedia.assetUuid,
      outputFormat: 'wav',
    });
    const generatedNativeGif = await configuredNativeHostClient.generateGif({
      assetUuid: importedNativeMedia.assetUuid,
      fps: '15',
      resolution: '480p',
      dither: true,
    });
    const compressedNativeVideo = await configuredNativeHostClient.compressVideo({
      assetUuid: importedNativeMedia.assetUuid,
      compressionMode: 'balanced',
    });
    const convertedNativeVideo = await configuredNativeHostClient.convertVideo({
      assetUuid: importedNativeMedia.assetUuid,
      targetFormat: 'mp4',
      videoCodec: 'h264',
      audioCodec: 'aac',
      resolution: 'original',
    });
    const enhancedNativeVideo = await configuredNativeHostClient.enhanceVideo({
      assetUuid: importedNativeMedia.assetUuid,
      targetResolution: '1080p',
      enhanceMode: 'real',
      frameRate: 'original',
    });
    const transcribedNativeMedia = await configuredNativeHostClient.transcribeMedia({
      assetUuid: importedNativeMedia.assetUuid,
      language: 'zh',
    });
    const nativeTaskSnapshots = await configuredNativeHostClient.listNativeTasks({
      taskUuid: 'ops-task-contract',
      limit: 5,
    });
    const canceledNativeTask = await configuredNativeHostClient.cancelNativeTask({
      taskUuid: 'ops-task-contract',
    });
    const recoveredNativeTasks = await configuredNativeHostClient.recoverNativeTasks({
      limit: 10,
    });
    const retriedNativeTask = await configuredNativeHostClient.retryNativeTask({
      taskUuid: 'ops-task-contract',
    });
    const llmHttpResponse = await configuredNativeHostClient.sendLlmHttpRequest({
      url: 'https://api.deepseek.com/chat/completions',
      method: 'POST',
      headers: {
        'content-type': 'application/json',
      },
      bodyText: '{"model":"deepseek-v4-flash"}',
    });
    const savedNativeLlmSecret = await configuredNativeHostClient.saveLlmSecret({
      secretName: 'default',
      secretValue: 'sk-native-stored-secret',
    });
    const loadedNativeLlmSecret = await configuredNativeHostClient.getLlmSecret({
      secretName: 'default',
    });
    const deletedNativeLlmSecret = await configuredNativeHostClient.deleteLlmSecret({
      secretName: 'default',
    });
    const audioSmoke = await configuredNativeHostClient.runAudioSmoke();
    assertEqual(configuredCapabilities.hostKind, 'native-host', 'configured native host client returns host capabilities');
    assertEqual(
      configuredCapabilities.nativeWorkerLeaseReady,
      true,
      'configured native host client reports native worker lease readiness',
    );
    assertEqual(configuredDatabaseHealth.ready, true, 'configured native host client returns database health');
    assertEqual(configuredFfmpegProbe.available, true, 'configured native host client returns FFmpeg probe');
    assertEqual(importedNativeMedia.assetUuid, 'media-asset-contract', 'configured native host client imports media through assetUuid');
    assertEqual(describedNativeMedia.sourcePath, 'D:/media/source.mp4', 'configured native host client describes a trusted local file source');
    assertEqual(selectedNativeMedia?.sourcePath, 'D:/media/selected-audio.wav', 'configured native host client selects a trusted local audio or video source');
    const selectedTrustedMedia = await services.selectAutoCutTrustedLocalMediaFile(['audio', 'video']);
    assertEqual(selectedTrustedMedia?.mediaType, 'audio', 'trusted local media chooser helper returns selected native audio descriptions');
    assertEqual(selectedNativeVideo?.sourcePath, 'D:/media/selected-source.mp4', 'configured native host client selects a trusted local video source');
    assertEqual(selectedNativeDirectory, 'D:/media/selected-output-root', 'configured native host client selects a trusted local directory');
    assertEqual(previewDirectoryAuthorization.allowed, true, 'configured native host client authorizes a trusted local media preview directory');
    assertEqual(previewDirectoryAuthorization.directoryPath, 'D:/media', 'configured native host client passes preview directory path through the Tauri request');
    assertEqual(selectedSpeechExecutable, 'D:/tools/whisper-cli.exe', 'configured native host client selects a trusted local speech executable');
    assertEqual(probedSpeechTranscription.ready, true, 'configured native host client probes local speech-to-text toolchain readiness');
    assertEqual(extractedNativeAudio.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client extracts audio by assetUuid');
    assertNativeTaskOutputArtifact(
      extractedNativeAudio,
      'ops-task-contract',
      'audio.wav',
      'configured native host client audio extraction result',
    );
    assertEqual(generatedNativeGif.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client generates video GIF by assetUuid');
    assertNativeTaskOutputArtifact(
      generatedNativeGif,
      'ops-task-gif-contract',
      'source.gif',
      'configured native host client video GIF result',
    );
    assertEqual(compressedNativeVideo.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client compresses video by assetUuid');
    assertNativeTaskOutputArtifact(
      compressedNativeVideo,
      'ops-task-compress-contract',
      'source-compressed.mp4',
      'configured native host client video compression result',
    );
    assertEqual(convertedNativeVideo.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client converts video by assetUuid');
    assertNativeTaskOutputArtifact(
      convertedNativeVideo,
      'ops-task-convert-contract',
      'source-converted.mp4',
      'configured native host client video conversion result',
    );
    assertEqual(enhancedNativeVideo.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client enhances video by assetUuid');
    assertNativeTaskOutputArtifact(
      enhancedNativeVideo,
      'ops-task-enhance-contract',
      'source-enhanced.mp4',
      'configured native host client video enhancement result',
    );
    assertEqual(transcribedNativeMedia.sourceAssetUuid, importedNativeMedia.assetUuid, 'configured native host client transcribes media by assetUuid');
    assertEqual(transcribedNativeMedia.segments[0]?.startMs, 12000, 'configured native host client returns speech transcription segment timing');
    assertEqual(transcribedNativeMedia.speechExecutable, 'whisper-cli', 'configured native host client returns the local speech executable used');
    assertEqual(
      transcribedNativeMedia.qualityGuard?.status,
      'passed-after-retry',
      'configured native host client preserves STT quality guard status for Smart Slice auditability',
    );
    assertEqual(
      transcribedNativeMedia.qualityGuard?.retryCount,
      1,
      'configured native host client preserves STT quality guard retry count',
    );
    assertEqual(nativeTaskSnapshots[0]?.uuid, 'ops-task-contract', 'configured native host client lists native task snapshots');
    assertEqual(
      nativeTaskSnapshots[0]?.events[0]?.payload?.operation,
      'audioExtraction',
      'configured native host client exposes structured native task event payload operation',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.events[0]?.payload?.phase,
      'ffmpeg-progress-streamed',
      'configured native host client exposes structured native task event payload phase',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.events[0]?.payload?.source,
      'ffmpeg-progress',
      'configured native host client exposes structured native task event payload source',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.events[0]?.payload?.progress,
      42,
      'configured native host client exposes structured native task event payload progress',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.events[0]?.payloadJson.includes('"progress":42'),
      true,
      'configured native host client keeps raw native task event payloadJson for audits',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.workerLeases[0]?.workerId,
      'autocut-native-media-worker',
      'configured native host client exposes native worker lease workerId',
    );
    assertEqual(
      nativeTaskSnapshots[0]?.workerLeases[0]?.leaseStatus,
      2,
      'configured native host client exposes native worker lease status',
    );
    assertEqual(canceledNativeTask.taskUuid, 'ops-task-contract', 'configured native host client cancels native task by taskUuid');
    assertEqual(canceledNativeTask.canceled, true, 'configured native host client returns cancel acknowledgement');
    assertEqual(recoveredNativeTasks.recovered, 2, 'configured native host client recovers interrupted native tasks');
    assertEqual(recoveredNativeTasks.interrupted, 1, 'configured native host client reports interrupted task count');
    assertEqual(recoveredNativeTasks.canceled, 1, 'configured native host client reports recovered canceled task count');
    assertEqual(recoveredNativeTasks.expiredLeases, 2, 'configured native host client reports expired worker lease count');
    assertEqual(recoveredNativeTasks.deferred, 1, 'configured native host client reports deferred active lease task count');
    assertEqual(retriedNativeTask.taskUuid, 'ops-task-contract', 'configured native host client retries native task by taskUuid');
    assertEqual(retriedNativeTask.retryTaskUuid, 'ops-task-retry-contract', 'configured native host client returns the retry task uuid');
    assertEqual(retriedNativeTask.retried, true, 'configured native host client returns retry acknowledgement');
    assertEqual(llmHttpResponse.status, 200, 'configured native host client sends LLM HTTP requests through the native host');
    assertEqual(llmHttpResponse.bodyText.includes('native-llm-http'), true, 'configured native host client returns native LLM HTTP response body text');
    assertEqual(savedNativeLlmSecret.saved, true, 'configured native host client saves LLM secrets through the native secret store');
    assertEqual(loadedNativeLlmSecret.configured, true, 'configured native host client loads LLM secret configured state from the native secret store');
    assertEqual(loadedNativeLlmSecret.secretValue, 'sk-native-stored-secret', 'configured native host client returns LLM secret values only through explicit secret reads');
    assertEqual(deletedNativeLlmSecret.deleted, true, 'configured native host client deletes LLM secrets through the native secret store');
    assertEqual(audioSmoke.format, 'wav', 'configured native host client runs audio smoke');
    assertNativeTaskOutputArtifact(audioSmoke, 'ops-task-smoke', 'smoke.wav', 'configured native host client audio smoke result');
    assertEqual(importedNativeMedia.durationMs, 100000, 'native host client exposes imported media duration for source-bounded slicing');
    assertEqual(describedNativeMedia.durationMs, 100000, 'native host client exposes local media description duration for preflight planning');
    assertEqual(selectedNativeVideo?.durationMs, 78000, 'native host client exposes selected local video duration for UI planning defaults');
    assertEqual(invokedCommands[0]?.command, 'autocut_host_capabilities', 'native host client invokes capabilities command first');
    assertEqual(
      invokedCommands[3]?.command,
      'autocut_import_media_file',
      'native host client invokes the media import command',
    );
    assertEqual(
      invokedCommands[3]?.args?.request?.sourcePath,
      'D:/media/source.mp4',
      'native host client passes sourcePath under the Tauri request argument',
    );
    assertEqual(
      invokedCommands[4]?.command,
      'autocut_describe_local_media_file',
      'native host client invokes the local file describe command',
    );
    assertEqual(
      invokedCommands[4]?.args?.request?.sourcePath,
      'D:/media/source.mp4',
      'native host client passes sourcePath to the local file describe command',
    );
    assertEqual(
      invokedCommands.some((entry) => entry.command === 'autocut_select_local_video_file'),
      true,
      'native host client invokes the local video chooser command',
    );
    assertEqual(
      invokedCommands.some((entry) => entry.command === 'autocut_select_local_directory'),
      true,
      'native host client invokes the local directory chooser command',
    );
    assertEqual(
      invokedCommands.some((entry) => entry.command === 'autocut_allow_local_media_preview_directory'),
      true,
      'native host client invokes the local media preview directory authorization command',
    );
    const localMediaPreviewDirectoryCommand = invokedCommands.find(
      (entry) => entry.command === 'autocut_allow_local_media_preview_directory',
    );
    assertEqual(
      localMediaPreviewDirectoryCommand?.command,
      'autocut_allow_local_media_preview_directory',
      'native host client invokes the local media preview directory authorization command',
    );
    assertEqual(
      localMediaPreviewDirectoryCommand?.args?.request?.directoryPath,
      'D:/media',
      'native host client passes directoryPath under the Tauri preview authorization request argument',
    );
    const audioExtractionCommand = invokedCommands.find((entry) => entry.command === 'autocut_extract_audio');
    const localMediaSelectCommand = invokedCommands.find((entry) => entry.command === 'autocut_select_local_media_file');
    const videoGifCommand = invokedCommands.find((entry) => entry.command === 'autocut_generate_gif');
    const videoCompressCommand = invokedCommands.find((entry) => entry.command === 'autocut_compress_video');
    const videoConvertCommand = invokedCommands.find((entry) => entry.command === 'autocut_convert_video');
    const videoEnhanceCommand = invokedCommands.find((entry) => entry.command === 'autocut_enhance_video');
    const speechTranscriptionCommand = invokedCommands.find((entry) => entry.command === 'autocut_transcribe_media');
    const speechTranscriptionProbeCommand = invokedCommands.find((entry) => entry.command === 'autocut_probe_speech_transcription');
    const speechTranscriptionFileSelectCommand = invokedCommands.find((entry) => entry.command === 'autocut_select_speech_transcription_file');
    const nativeTaskQueryCommand = invokedCommands.find((entry) => entry.command === 'autocut_list_native_tasks');
    const nativeTaskCancelCommand = invokedCommands.find((entry) => entry.command === 'autocut_cancel_native_task');
    const nativeTaskRecoveryCommand = invokedCommands.find((entry) => entry.command === 'autocut_recover_native_tasks');
    const nativeTaskRetryCommand = invokedCommands.find((entry) => entry.command === 'autocut_retry_native_task');
    const llmHttpCommand = invokedCommands.find((entry) => entry.command === 'autocut_llm_http_request');
    const llmSecretSaveCommand = invokedCommands.find((entry) => entry.command === 'autocut_save_llm_secret');
    const llmSecretGetCommand = invokedCommands.find((entry) => entry.command === 'autocut_get_llm_secret');
    const llmSecretDeleteCommand = invokedCommands.find((entry) => entry.command === 'autocut_delete_llm_secret');
    assertEqual(
      localMediaSelectCommand?.command,
      'autocut_select_local_media_file',
      'native host client invokes the trusted local audio/video chooser command',
    );
    assertDeepEqual(
      localMediaSelectCommand?.args?.request?.mediaTypes,
      ['audio', 'video'],
      'native host client passes mediaTypes under the trusted local media chooser request',
    );
    assertEqual(
      audioExtractionCommand?.command,
      'autocut_extract_audio',
      'native host client invokes the assetUuid audio extraction command',
    );
    assertEqual(
      audioExtractionCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri request argument',
    );
    assertEqual(
      videoGifCommand?.command,
      'autocut_generate_gif',
      'native host client invokes the assetUuid video GIF command',
    );
    assertEqual(
      videoGifCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri video GIF request argument',
    );
    assertEqual(
      videoCompressCommand?.command,
      'autocut_compress_video',
      'native host client invokes the assetUuid video compression command',
    );
    assertEqual(
      videoCompressCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri video compression request argument',
    );
    assertEqual(
      videoCompressCommand?.args?.request?.compressionMode,
      'balanced',
      'native host client passes compressionMode under the Tauri video compression request argument',
    );
    assertEqual(
      videoConvertCommand?.command,
      'autocut_convert_video',
      'native host client invokes the assetUuid video conversion command',
    );
    assertEqual(
      videoConvertCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri video conversion request argument',
    );
    assertEqual(
      videoConvertCommand?.args?.request?.targetFormat,
      'mp4',
      'native host client passes targetFormat under the Tauri video conversion request argument',
    );
    assertEqual(
      videoEnhanceCommand?.command,
      'autocut_enhance_video',
      'native host client invokes the assetUuid video enhancement command',
    );
    assertEqual(
      videoEnhanceCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri video enhancement request argument',
    );
    assertEqual(
      videoEnhanceCommand?.args?.request?.targetResolution,
      '1080p',
      'native host client passes targetResolution under the Tauri video enhancement request argument',
    );
    assertEqual(
      speechTranscriptionCommand?.command,
      'autocut_transcribe_media',
      'native host client invokes the assetUuid speech transcription command',
    );
    assertEqual(
      speechTranscriptionCommand?.args?.request?.assetUuid,
      importedNativeMedia.assetUuid,
      'native host client passes assetUuid under the Tauri speech transcription request argument',
    );
    assertEqual(
      speechTranscriptionCommand?.args?.request?.language,
      'zh',
      'native host client passes language under the Tauri speech transcription request argument',
    );
    assertEqual(
      speechTranscriptionProbeCommand?.args?.request?.executablePath,
      'D:/tools/whisper-cli.exe',
      'native host client passes executablePath under the Tauri speech transcription probe request argument',
    );
    assertEqual(
      speechTranscriptionProbeCommand?.args?.request?.modelPath,
      'D:/models/ggml-large-v3-turbo.bin',
      'native host client passes modelPath under the Tauri speech transcription probe request argument',
    );
    assertEqual(
      speechTranscriptionFileSelectCommand?.args?.request?.kind,
      'executable',
      'native host client passes file kind under the Tauri speech transcription file chooser request argument',
    );
    assertEqual(
      nativeTaskQueryCommand?.command,
      'autocut_list_native_tasks',
      'native host client invokes the native task query command',
    );
    assertEqual(
      nativeTaskQueryCommand?.args?.request?.taskUuid,
      'ops-task-contract',
      'native host client passes taskUuid under the Tauri native task query request argument',
    );
    assertEqual(
      nativeTaskCancelCommand?.command,
      'autocut_cancel_native_task',
      'native host client invokes the native task cancel command',
    );
    assertEqual(
      nativeTaskCancelCommand?.args?.request?.taskUuid,
      'ops-task-contract',
      'native host client passes taskUuid under the Tauri native task cancel request argument',
    );
    assertEqual(
      nativeTaskRecoveryCommand?.command,
      'autocut_recover_native_tasks',
      'native host client invokes the native task recovery command',
    );
    assertEqual(
      nativeTaskRecoveryCommand?.args?.request?.limit,
      10,
      'native host client passes limit under the Tauri native task recovery request argument',
    );
    assertEqual(
      nativeTaskRetryCommand?.command,
      'autocut_retry_native_task',
      'native host client invokes the native task retry command',
    );
    assertEqual(
      nativeTaskRetryCommand?.args?.request?.taskUuid,
      'ops-task-contract',
      'native host client passes taskUuid under the Tauri native task retry request argument',
    );
    assertEqual(
      llmHttpCommand?.command,
      'autocut_llm_http_request',
      'native host client invokes the LLM HTTP request command',
    );
    assertEqual(
      llmHttpCommand?.args?.request?.url,
      'https://api.deepseek.com/chat/completions',
      'native host client passes the LLM HTTP request URL',
    );
    assertEqual(
      llmHttpCommand?.args?.request?.bodyText,
      '{"model":"deepseek-v4-flash"}',
      'native host client passes the LLM HTTP request body text',
    );
    assertEqual(
      llmSecretSaveCommand?.command,
      'autocut_save_llm_secret',
      'native host client invokes the LLM secret save command',
    );
    assertEqual(
      llmSecretGetCommand?.command,
      'autocut_get_llm_secret',
      'native host client invokes the LLM secret get command',
    );
    assertEqual(
      llmSecretDeleteCommand?.command,
      'autocut_delete_llm_secret',
      'native host client invokes the LLM secret delete command',
    );

    resetStorage();
    const nativeAudioWorkflowCommands = [];
    const configuredOutputDirectory = 'D:/autocut-configured-output';
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          audioExtractionCommandReady: true,
        audioExtractionFromAssetReady: true,
        videoGifCommandReady: false,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_extract_audio'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeAudioWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-audio-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/native-audio-asset.mp4`,
          byteSize: 321000,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 321000,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async (request) => {
        nativeAudioWorkflowCommands.push({ kind: 'extract', request });
        const output = createNativeTaskOutputArtifact(
          'native-audio-task',
          'native-audio-artifact.wav',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'native-audio-artifact',
          taskUuid: 'native-audio-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 654321,
          format: request.outputFormat,
          ffmpegExecutable: 'ffmpeg',
        };
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for the native audio workflow contract');
      },
      compressVideo: async () => {
        throw new Error('video compression command is not configured for the native audio workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for the native audio workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for the native audio workflow contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const trustedFileSourceEvents = [];
    const stopTrustedFileSourceDrop = commons.listenAutoCutTrustedFileSourceDrop((detail) => {
      trustedFileSourceEvents.push(detail);
    });
    const trustedLocalFile = commons.createAutoCutTrustedLocalFile({
      sourcePath: 'D:/media/native-source.mp4',
      name: 'native-source.mp4',
      byteSize: 321000,
      mimeType: 'video/mp4',
      mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
    });
    commons.dispatchAutoCutTrustedFileSourceDrop({
      files: [trustedLocalFile],
    });
    stopTrustedFileSourceDrop();
    assertEqual(
      commons.resolveAutoCutTrustedSourcePath(trustedLocalFile),
      'D:/media/native-source.mp4',
      'trusted file source bridge creates a File-compatible trusted local file',
    );
    const forgedSourcePathFile = new File(['video'], 'forged-source-path.mp4', { type: 'video/mp4' });
    Object.defineProperty(forgedSourcePathFile, 'sourcePath', {
      configurable: true,
      enumerable: true,
      value: 'D:/media/forged-source-path.mp4',
    });
    assertEqual(
      commons.resolveAutoCutTrustedSourcePath(forgedSourcePathFile),
      null,
      'trusted file source bridge rejects browser-forged sourcePath properties',
    );
    const forgedPathFile = new File(['video'], 'forged-path.mp4', { type: 'video/mp4' });
    Object.defineProperty(forgedPathFile, 'path', {
      configurable: true,
      enumerable: true,
      value: 'D:/media/forged-path.mp4',
    });
    assertEqual(
      commons.resolveAutoCutTrustedSourcePath(forgedPathFile),
      null,
      'trusted file source bridge rejects browser-forged path properties',
    );
    assertEqual(
      commons.resolveAutoCutTrustedSourcePath(structuredClone(trustedLocalFile)),
      null,
      'browser route state cloning does not preserve custom trusted File sourcePath metadata',
    );
    assertEqual(
      commons.resolveAutoCutTrustedSourcePath(commons.createAutoCutTrustedLocalFile(structuredClone({
        sourcePath: 'D:/media/native-source.mp4',
        name: 'native-source.mp4',
        byteSize: 321000,
        mimeType: 'video/mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
      }))),
      'D:/media/native-source.mp4',
      'trusted desktop source descriptors survive router state cloning and can rebuild File-compatible trusted inputs',
    );
    assertEqual(trustedLocalFile.size, 321000, 'trusted file source bridge preserves native byte size on the File-compatible value');
    assertEqual(trustedFileSourceEvents[0]?.files?.[0]?.name, 'native-source.mp4', 'trusted file source bridge dispatches trusted local files');
    const nativeAudioSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeAudioWorkflowResult = await withImmediateTimers(async () =>
      processAudioExtraction({
        fileId: 'asset-source-native-audio',
        file: nativeAudioSourceFile,
        format: 'wav',
        quality: '320',
        channel: 'stereo',
      }),
    );
    const nativeAudioWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeAudioWorkflowTask = nativeAudioWorkflowTasks.find(
      (task) => task.id === nativeAudioWorkflowResult.taskId,
    );
    const nativeAudioWorkflowAssets = readScopedStoredArray(services, 'assets');
    assertEqual(nativeAudioWorkflowResult.success, true, 'extractor audio native workflow reports success');
    assertEqual(
      nativeAudioWorkflowCommands[0]?.kind,
      'import',
      'extractor audio native workflow imports local media before extraction',
    );
    assertEqual(
      nativeAudioWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'extractor audio native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeAudioWorkflowCommands[0]?.request?.outputRootDir,
      configuredOutputDirectory,
      'extractor audio native workflow passes the configured output directory to media import',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.kind,
      'extract',
      'extractor audio native workflow extracts audio after import',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.assetUuid,
      'native-audio-asset',
      'extractor audio native workflow extracts audio by imported assetUuid',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputRootDir,
      configuredOutputDirectory,
      'extractor audio native workflow passes the configured output directory to audio extraction',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputQuality,
      '320',
      'extractor audio native workflow forwards the selected output quality to native extraction',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputChannel,
      'stereo',
      'extractor audio native workflow forwards the selected channel mode to native extraction',
    );
    assertEqual(
      nativeAudioWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'extractor audio native workflow completes the persisted task',
    );
    assertEqual(
      nativeAudioWorkflowTask?.audioUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fnative-audio-task%2Fnative-audio-artifact.wav',
      'extractor audio native workflow converts native artifact paths to safe asset URLs',
    );
    assertEqual(
      nativeAudioWorkflowAssets[0]?.size,
      654321,
      'extractor audio native workflow stores the native artifact byte size on the generated asset',
    );
    assertEqual(
      nativeAudioWorkflowAssets[0]?.url,
      nativeAudioWorkflowTask?.audioUrl,
      'extractor audio native workflow stores the safe asset URL on the generated asset',
    );

    resetStorage();
    nativeAudioWorkflowCommands.length = 0;
    const trustedAudioExtractionSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/native-audio-source.flac',
      'native-audio-source.flac',
      { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
    );
    const nativeAudioInputExtractionResult = await withImmediateTimers(async () =>
      processAudioExtraction({
        fileId: 'asset-source-native-audio-input',
        file: trustedAudioExtractionSourceFile,
        format: 'mp3',
        quality: '256',
        channel: 'smart-stereo',
      }),
    );
    const nativeAudioInputTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === nativeAudioInputExtractionResult.taskId,
    );
    assertEqual(nativeAudioInputExtractionResult.success, true, 'extractor audio accepts trusted local audio sources');
    assertEqual(
      nativeAudioWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-audio-source.flac',
      'extractor audio imports trusted audio files instead of requiring a video container',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputFormat,
      'mp3',
      'extractor audio transcodes audio input to the requested output format',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputQuality,
      '256',
      'extractor audio transcodes audio input with the requested quality preset',
    );
    assertEqual(
      nativeAudioWorkflowCommands[1]?.request?.outputChannel,
      'smart-stereo',
      'extractor audio applies the requested smart stereo channel preset',
    );
    assertEqual(
      nativeAudioInputTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'extractor audio trusted audio-source workflow completes the persisted task',
    );
    assertRule(
      Boolean(nativeAudioInputTask?.audioUrl),
      'extractor audio trusted audio-source workflow stores a downloadable audio URL',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoGifWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: true,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_generate_gif'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoGifWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-gif-asset',
          sandboxPath: 'D:/autocut/media/inputs/native-gif-asset.mp4',
          byteSize: 321000,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 321000,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the native video GIF workflow contract');
      },
      generateGif: async (request) => {
        nativeVideoGifWorkflowCommands.push({ kind: 'gif', request });
        const output = createNativeTaskOutputArtifact('native-gif-task', 'native-gif-artifact.gif');
        return {
          artifactUuid: 'native-gif-artifact',
          taskUuid: 'native-gif-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 765432,
          format: 'gif',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      compressVideo: async () => {
        throw new Error('video compression command is not configured for the native video GIF workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for the native video GIF workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for the native video GIF workflow contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const nativeVideoGifSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeVideoGifWorkflowResult = await withImmediateTimers(async () =>
      processVideoGif({
        fileId: 'asset-source-native-video-gif',
        file: nativeVideoGifSourceFile,
        fps: '15',
        resolution: '480p',
        dither: true,
      }),
    );
    const nativeVideoGifWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeVideoGifWorkflowTask = nativeVideoGifWorkflowTasks.find(
      (task) => task.id === nativeVideoGifWorkflowResult.taskId,
    );
    const nativeVideoGifWorkflowAssets = readScopedStoredArray(services, 'assets');
    assertEqual(nativeVideoGifWorkflowResult.success, true, 'video GIF native workflow reports success');
    assertEqual(
      nativeVideoGifWorkflowCommands[0]?.kind,
      'import',
      'video GIF native workflow imports local media before generation',
    );
    assertEqual(
      nativeVideoGifWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'video GIF native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeVideoGifWorkflowCommands[1]?.kind,
      'gif',
      'video GIF native workflow generates GIF after import',
    );
    assertEqual(
      nativeVideoGifWorkflowCommands[1]?.request?.assetUuid,
      'native-gif-asset',
      'video GIF native workflow generates GIF by imported assetUuid',
    );
    assertEqual(
      nativeVideoGifWorkflowCommands[1]?.request?.fps,
      '15',
      'video GIF native workflow forwards the selected FPS',
    );
    assertEqual(
      nativeVideoGifWorkflowCommands[1]?.request?.resolution,
      '480p',
      'video GIF native workflow forwards the selected resolution',
    );
    assertEqual(
      nativeVideoGifWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video GIF native workflow completes the persisted task',
    );
    assertEqual(
      nativeVideoGifWorkflowTask?.gifUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fnative-gif-task%2Fnative-gif-artifact.gif',
      'video GIF native workflow converts native artifact paths to safe asset URLs',
    );
    assertEqual(
      nativeVideoGifWorkflowAssets[0]?.type,
      'image',
      'video GIF native workflow stores the generated GIF as an image asset',
    );
    assertEqual(
      nativeVideoGifWorkflowAssets[0]?.size,
      765432,
      'video GIF native workflow stores the native artifact byte size on the generated asset',
    );
    assertEqual(
      nativeVideoGifWorkflowAssets[0]?.url,
      nativeVideoGifWorkflowTask?.gifUrl,
      'video GIF native workflow stores the safe asset URL on the generated asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoSliceWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionFileSelectCommandReady: true,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_transcribe_media', 'autocut_slice_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoSliceWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/native-slice-asset.mp4',
          byteSize: 654000,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 100000,
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 654000,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the native video slice workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for the native video slice workflow contract');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'native video slice workflow contract',
      }),
      selectSpeechTranscriptionFile: async () => null,
      transcribeMedia: async (request) => {
        nativeVideoSliceWorkflowCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'native-slice-transcript-task',
          'native-slice-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'native-slice-transcript-artifact',
          taskUuid: 'native-slice-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text:
            'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works. What fixes weak conversion is clear. The case shows pricing pain before users choose annual plans. So show the solution and the final answer improves signups.',
          segments: [
            {
              startMs: 22000,
              endMs: 30000,
              text:
                'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 32400,
              endMs: 41000,
              text:
                'So lead with the result and the retention payoff works.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 72000,
              endMs: 93000,
              text:
                'What fixes weak conversion is clear. The case shows pricing pain before users choose annual plans. So show the solution and the final answer improves signups.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        invalidLlmVideoSliceWorkflowCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        nativeVideoSliceWorkflowCommands.push({ kind: 'slice', request });
        const subtitlesRequested = Array.isArray(request.subtitleSegments) && request.subtitleSegments.length > 0;
        const slices = request.clips.map((clip, index) => {
          const output = createNativeTaskOutputArtifact(
            'native-slice-task',
            clip.outputFileName ?? `native-slice-artifact-${index + 1}.mp4`,
            configuredOutputDirectory,
          );
          const thumbnail = createNativeTaskCoverArtifact(
            'native-slice-task',
            `native-slice-thumb-${index + 1}.jpg`,
            configuredOutputDirectory,
          );
          const subtitle = createNativeTaskOutputArtifact(
            'native-slice-task',
            `native-slice-subtitle-${index + 1}.srt`,
            configuredOutputDirectory,
          );
          return {
            artifactUuid: `native-slice-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `native-slice-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567 + index,
            thumbnailByteSize: 12345 + index,
            ...(subtitlesRequested
              ? {
                  subtitleArtifactUuid: `native-slice-subtitle-${index + 1}`,
                  subtitleArtifactPath: subtitle.artifactPath,
                  subtitleByteSize: 1234 + index,
                  subtitleFormat: 'srt',
                }
              : {}),
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          };
        });
        return {
          taskUuid: 'native-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: slices[0]?.taskOutputDir ?? configuredOutputDirectory,
          ffmpegExecutable: 'ffmpeg',
          slices,
        };
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        nativeVideoSliceWorkflowCommands.push({ kind: 'analyze-audio-boundaries', request });
        const analysisFilter = request.applyNoiseReduction === false
          ? 'silencedetect=noise=-35dB:d=0.08'
          : 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08';
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: request.clips.map((clip, index) => {
            const speechStartMs = clip.speechStartMs ?? clip.startMs;
            const speechEndMs = clip.speechEndMs ?? clip.startMs + clip.durationMs;
            void index;
            const audioActivityStartMs = speechStartMs + 100;
            const audioActivityEndMs = speechEndMs - 100;
            return {
              index,
              startMs: clip.startMs,
              durationMs: clip.durationMs,
              sourceStartMs: clip.sourceStartMs ?? clip.startMs,
              sourceEndMs: clip.sourceEndMs ?? clip.startMs + clip.durationMs,
              audioActivityStartMs,
              audioActivityEndMs,
              leadingSilenceMs: Math.max(0, audioActivityStartMs - clip.startMs),
              trailingSilenceMs: Math.max(0, clip.startMs + clip.durationMs - audioActivityEndMs),
              ...(index === 0
                ? {
                    internalSilenceIntervals: [
                      {
                        startMs: 30000,
                        endMs: 32400,
                      },
                    ],
                  }
                : {}),
              confidence: 0.97,
              analysisFilter,
            };
          }),
        };
      },
      compressVideo: async () => {
        throw new Error('video compression command is not configured for the native video slice workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for the native video slice workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for the native video slice workflow contract');
      },
      sendLlmHttpRequest: async () => {
        throw new Error('LLM HTTP is not configured for the native video slice workflow contract');
      },
      saveLlmSecret: async (request) => ({
        secretName: request.secretName,
        saved: false,
      }),
      getLlmSecret: async (request) => ({
        secretName: request.secretName,
        configured: false,
      }),
      deleteLlmSecret: async (request) => ({
        secretName: request.secretName,
        deleted: false,
      }),
      listNativeTasks: async () => [],
      cancelNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        status: 0,
        canceled: false,
        message: 'not configured',
      }),
      recoverNativeTasks: async () => ({
        inspected: 0,
        recovered: 0,
        interrupted: 0,
        canceled: 0,
        expiredLeases: 0,
        deferred: 0,
        taskUuids: [],
      }),
      retryNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        retryTaskUuid: '',
        status: 0,
        retried: false,
        message: 'not configured',
      }),
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const nativeVideoSliceSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeVideoSliceWorkflowTaskUpdates = captureEvents(services, 'taskUpdated');
    let nativeVideoSliceWorkflowResult;
    const nativeVideoSliceWorkflowDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      nativeVideoSliceWorkflowResult = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-native-video-slice',
          file: nativeVideoSliceSourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
              highlightEngine: 'emotion',
              enableCoughFilter: true,
              enableRepeatFilter: true,
              enableSubtitles: false,
            }),
          );
    });
    nativeVideoSliceWorkflowTaskUpdates.stop();
    const nativeVideoSliceWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeVideoSliceWorkflowTask = nativeVideoSliceWorkflowTasks.find(
      (task) => task.id === nativeVideoSliceWorkflowResult.taskId,
    );
    const nativeVideoSliceWorkflowAssets = readScopedStoredArray(services, 'assets');
    const nativeVideoSliceGeneratedAssets = nativeVideoSliceWorkflowAssets.filter(
      (asset) => asset.sourceTaskId === nativeVideoSliceWorkflowResult.taskId,
    );
    const nativeVideoSliceFirstAsset = nativeVideoSliceGeneratedAssets.find(
      (asset) => asset.id === nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.id,
    );
    assertEqual(nativeVideoSliceWorkflowResult.success, true, 'video slice native workflow reports success');
    assertAutoCutTaskIdUsesUuidV7(
      nativeVideoSliceWorkflowResult.taskId,
      'slice',
      'video slice native workflow returns the stable local UUIDv7 task id used by UI routing',
    );
    assertEqual(
      nativeVideoSliceWorkflowResult.nativeTaskId,
      'native-slice-task',
      'video slice native workflow exposes the native ops_task UUID separately from the local task id',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.id,
      nativeVideoSliceWorkflowResult.taskId,
      'video slice native workflow persists the completed task under the stable local task id',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.nativeTaskId,
      'native-slice-task',
      'video slice native workflow records the native ops_task UUID on the persisted task',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.id === nativeVideoSliceWorkflowTask?.nativeTaskId,
      false,
      'video slice native workflow does not replace the local task id with the native ops_task UUID',
    );
    assertRule(
      nativeVideoSliceWorkflowTasks.every((task) => task.id !== 'native-slice-task'),
      'video slice native workflow does not persist a duplicate task row under the native ops_task UUID',
    );
    assertRule(
      nativeVideoSliceGeneratedAssets.every((asset) => asset.sourceTaskId === nativeVideoSliceWorkflowResult.taskId),
      'video slice native workflow links generated assets to the stable local task id',
    );
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        nativeTaskQueryCommandReady: true,
      }),
      listNativeTasks: async () => [
        {
          uuid: 'native-slice-task',
          taskType: 6,
          status: 2,
          progress: 100,
          sourceAssetUuid: 'native-slice-asset',
          inputJson: JSON.stringify({
            operation: 'videoSlice',
            assetUuid: 'native-slice-asset',
            sourceName: 'native-source.mp4',
            outputFormat: 'mp4',
          }),
          outputJson: JSON.stringify({
            assetUuid: 'native-slice-asset',
            taskOutputDir: `${configuredOutputDirectory}/tasks/native-slice-task`,
            sliceCount: 2,
            sliceResults: nativeVideoSliceWorkflowTask?.sliceResults?.map((slice) => ({
              artifactUuid: slice.id,
              artifactPath: slice.artifactPath,
              thumbnailArtifactUuid: `${slice.id}-thumbnail`,
              thumbnailArtifactPath: slice.thumbnailUrl
                ? decodeURIComponent(slice.thumbnailUrl.replace(/^asset:\/\/localhost\//u, ''))
                : undefined,
              taskOutputDir: slice.taskOutputDir,
              byteSize: slice.size,
              thumbnailByteSize: 12345,
              format: 'mp4',
              startMs: slice.sourceStartMs,
              durationMs: (slice.sourceEndMs ?? 0) - (slice.sourceStartMs ?? 0),
              label: slice.title ?? slice.name?.replace(/\.mp4$/u, ''),
              sourceStartMs: slice.sourceStartMs,
              sourceEndMs: slice.sourceEndMs,
              speechStartMs: slice.speechStartMs,
              speechEndMs: slice.speechEndMs,
              boundaryPaddingBeforeMs: slice.boundaryPaddingBeforeMs,
              boundaryPaddingAfterMs: slice.boundaryPaddingAfterMs,
              transcriptText: slice.transcriptText,
              transcriptSegments: slice.transcriptSegments,
              transcriptSegmentCount: slice.transcriptSegmentCount,
              transcriptCoverageScore: slice.transcriptCoverageScore,
              speechContinuityGrade: slice.speechContinuityGrade,
              audioCleanupProfile: slice.audioCleanupProfile,
              noiseReductionApplied: slice.noiseReductionApplied,
              boundaryDecisionSource: slice.boundaryDecisionSource,
              audioActivityStartMs: slice.audioActivityStartMs,
              audioActivityEndMs: slice.audioActivityEndMs,
              audioActivityConfidence: slice.audioActivityConfidence,
              audioActivityAnalysisFilter: slice.audioActivityAnalysisFilter,
              leadingSilenceMs: slice.leadingSilenceMs,
              trailingSilenceMs: slice.trailingSilenceMs,
              leadingSilenceTrimMs: slice.leadingSilenceTrimMs,
              trailingSilenceTrimMs: slice.trailingSilenceTrimMs,
              tailTreatment: slice.tailTreatment,
            })),
          }),
          createdAt: '2026-05-05T00:00:00.000Z',
          updatedAt: '2026-05-05T00:05:00.000Z',
          stages: [],
          events: [],
          workerLeases: [],
        },
      ],
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const mergedNativeVideoSliceWorkflowTasks = await withImmediateTimers(() => services.getTasks());
    assertEqual(
      mergedNativeVideoSliceWorkflowTasks.filter((task) =>
        task.id === nativeVideoSliceWorkflowResult.taskId || task.id === 'native-slice-task'
      ).length,
      1,
      'video slice native workflow task list merges the local task with its recovered native ops_task snapshot',
    );
    assertEqual(
      mergedNativeVideoSliceWorkflowTasks.find((task) => task.id === nativeVideoSliceWorkflowResult.taskId)?.nativeTaskId,
      'native-slice-task',
      'video slice native workflow task list keeps nativeTaskId after local/native recovery merge',
    );
    const nativeVideoSliceWorkflowProgressValues = nativeVideoSliceWorkflowTaskUpdates.details
      .map((task) => task?.progress)
      .filter((progress) => typeof progress === 'number');
    const nativeVideoSliceWorkflowProgressRegressionIndex = nativeVideoSliceWorkflowProgressValues.findIndex(
      (progress, index) => index > 0 && progress < nativeVideoSliceWorkflowProgressValues[index - 1],
    );
    assertRule(
      nativeVideoSliceWorkflowProgressRegressionIndex === -1,
      `video slice native workflow reports monotonic Smart Slice progress updates (${JSON.stringify(nativeVideoSliceWorkflowProgressValues)})`,
    );
    assertRule(
      !nativeVideoSliceWorkflowProgressValues.includes(35),
      'video slice native workflow never reports the obsolete sticky 35 percent speech-to-text progress',
    );
    assertEqual(
      nativeVideoSliceWorkflowProgressValues.at(-1),
      100,
      'video slice native workflow emits a final 100 percent completed task update',
    );
    assertRule(
      !nativeVideoSliceWorkflowTaskUpdates.details.some((task) =>
        String(task?.progressMessage ?? '').includes('AutoCut is still working') ||
          String(task?.progressMessage ?? '').includes('heartbeat')
      ),
      'video slice native workflow does not publish fake JavaScript heartbeat progress while speech-to-text is running',
    );
    assertRule(
      nativeVideoSliceWorkflowDiagnostics.some((call) =>
        call.level === 'info' &&
        String(call.args?.[0] ?? '').includes('Smart Slice execution plan')
      ),
      'video slice native workflow logs the full Smart Slice execution plan to the console',
    );
    for (const stepId of [
      'prepare-source',
      'speech-to-text',
      'plan-clips',
      'native-render',
      'verify-artifacts',
      'persist-results',
    ]) {
      assertRule(
        nativeVideoSliceWorkflowDiagnostics.some((call) =>
          call.level === 'info' &&
          String(call.args?.[0] ?? '').includes(`Smart Slice ${stepId} started`)
        ),
        `video slice native workflow logs Smart Slice ${stepId} start to the console`,
      );
      assertRule(
        nativeVideoSliceWorkflowDiagnostics.some((call) =>
          call.level === 'info' &&
          String(call.args?.[0] ?? '').includes(`Smart Slice ${stepId} completed`)
        ),
        `video slice native workflow logs Smart Slice ${stepId} completion to the console`,
      );
    }
    assertEqual(
      nativeVideoSliceWorkflowCommands[0]?.kind,
      'import',
      'video slice native workflow imports local media before slicing',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'video slice native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[0]?.request?.outputRootDir,
      configuredOutputDirectory,
      'video slice native workflow passes the configured output directory to media import',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.kind,
      'transcribe',
      'video slice native workflow transcribes local media before slicing when local speech toolchain is ready',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.request?.assetUuid,
      'native-slice-asset',
      'video slice native workflow transcribes by imported assetUuid',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.request?.outputRootDir,
      configuredOutputDirectory,
      'video slice native workflow passes the configured output directory to speech transcription',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.request?.executablePath,
      'D:/tools/whisper-cli.exe',
      'video slice native workflow passes the configured local speech executable to transcription',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.request?.modelPath,
      'D:/models/ggml-large-v3-turbo.bin',
      'video slice native workflow passes the configured local speech model to transcription',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[1]?.request?.dedupeRepeatedSpeech,
      true,
      'video slice native workflow enables STT repeated-speech dedupe when Smart Slice repeat filtering is enabled',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.kind,
      'analyze-audio-boundaries',
      'video slice native workflow analyzes raw audio activity boundaries before rendering clean default audio',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.profile,
      'smart-slice-speech-denoise-v1',
      'video slice native workflow uses the canonical Smart Slice audio cleanup profile for boundary analysis',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.applyNoiseReduction,
      false,
      'video slice native workflow defaults clean audio boundary analysis to raw audio before applying destructive denoise',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.kind,
      'slice',
      'video slice native workflow slices video after import',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.assetUuid,
      'native-slice-asset',
      'video slice native workflow slices by imported assetUuid',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.outputRootDir,
      configuredOutputDirectory,
      'video slice native workflow passes the configured output directory to video slicing',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.outputFormat,
      'mp4',
      'video slice native workflow requests MP4 slice output',
    );
    assertRule(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.every((clip) =>
        clip.startMs + clip.durationMs <= 100000,
      ),
      'video slice native workflow keeps planned clips inside imported media duration before native rendering',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.subtitleFormat,
      undefined,
      'video slice native workflow omits subtitle output by default while still using speech-to-text for planning',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.subtitleMode,
      undefined,
      'video slice native workflow does not burn or write subtitles unless the user enables subtitles',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.subtitleStyleId,
      undefined,
      'video slice native workflow omits subtitle style when no subtitle style was selected',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.subtitleSegments,
      undefined,
      'video slice native workflow keeps transcript segments out of native subtitle rendering when subtitles are disabled',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.transcriptText,
      'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works.',
      'video slice native workflow embeds slice transcript text in each native clip request independent of subtitle rendering',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.transcriptSegments?.map((segment) => segment.text).join(' '),
      'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works.',
      'video slice native workflow embeds structured slice transcript segments in each native clip request',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.transcriptSegmentCount,
      2,
      'video slice native workflow embeds transcript segment counts in each native clip request',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.sourceSegments?.length,
      2,
      'video slice native workflow sends retained source speech islands to native rendering when internal silence is excessive',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.sourceSegments?.[0]?.endMs,
      30175,
      'video slice native workflow uses acoustic internal silence intervals instead of relying only on coarse speech-to-text segment gaps',
    );
    assertRule(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.sourceSegments?.some((segment) =>
        segment.startMs <= 32400 && segment.endMs >= 41000
      ),
      'video slice native workflow never accepts acoustic silence compaction that drops a transcript-backed speech segment',
    );
    assertRule(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.transcriptSegments?.every((transcriptSegment) =>
        nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.sourceSegments?.some((sourceSegment) =>
          sourceSegment.startMs <= transcriptSegment.startMs &&
            sourceSegment.endMs >= transcriptSegment.endMs
        )
      ),
      'video slice native workflow sends sourceSegments that fully cover every embedded transcript segment',
    );
    assertRule(
      (nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.removedSilenceMs ?? 0) >= 1_000,
      'video slice native workflow records removed internal silence before native rendering',
    );
    assertArrayIncludes(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.risks,
      'internal-silence-trimmed',
      'video slice native workflow marks source-segment silence compaction for review evidence',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.noiseReduction,
      true,
      'video slice native workflow enables post-cut denoise for generated slice artifacts by default',
    );
    assertRule(
      (nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.speechStartMs ?? 0) >= 22000,
      'video slice native workflow keeps the first speech boundary at or after the STT speech start when trusted audio trims opening silence',
    );
    assertRule(
      (nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.speechStartMs ?? Number.POSITIVE_INFINITY) <= 22200,
      'video slice native workflow does not trim past the trusted opening audio activity start',
    );
    assertRule(
      (nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.boundaryPaddingBeforeMs ?? Number.POSITIVE_INFINITY) <= 200,
      'video slice native workflow keeps leading silence padding within the professional Smart Slice limit',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.audioCleanupProfile,
      'smart-slice-speech-denoise-v1',
      'video slice native workflow embeds canonical audio cleanup evidence in each native clip request',
    );
    assertArrayIncludes(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.risks,
      'audio-boundary-refined',
      'video slice native workflow sends successful audio boundary refinement evidence to native rendering',
    );
    assertArrayIncludes(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.risks,
      'smart-cut-engine',
      'video slice native workflow sends Smart Cut Engine planning evidence to native rendering',
    );
    assertArrayNotIncludes(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.[0]?.risks,
      'fallback-plan',
      'video slice native workflow does not use legacy fallback-plan risk after Smart Cut Engine planning',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.noiseReductionApplied,
      true,
      'video slice native workflow records default post-cut denoise on task slice results',
    );
    assertArrayIncludes(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.risks,
      'audio-boundary-refined',
      'video slice native workflow records successful audio boundary refinement evidence on task slice results',
    );
    assertArrayIncludes(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.risks,
      'smart-cut-engine',
      'video slice native workflow records Smart Cut Engine planning evidence on task slice results',
    );
    assertArrayNotIncludes(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.risks,
      'fallback-plan',
      'video slice native workflow does not persist legacy fallback-plan risk after Smart Cut Engine planning',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.sourceSegments?.length,
      2,
      'video slice native workflow persists retained source speech islands on completed task slice results',
    );
    assertRule(
      (nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.removedSilenceMs ?? 0) >= 1_000,
      'video slice native workflow persists the amount of internal silence removed from completed task slice results',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.transcriptText,
      'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works.',
      'video slice native workflow still exposes speech-to-text transcript text on each generated slice when subtitles are disabled',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.transcriptSegments?.map((segment) => segment.text).join(' '),
      'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works.',
      'video slice native workflow records slice-level structured transcript segments when subtitles are disabled',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const staleNativeResultSourceSegmentsCommands = [];
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-stale-native-result-source-segments-regression',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        staleNativeResultSourceSegmentsCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'stale-native-result-source-segments-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/stale-native-result-source-segments-source.mp4`,
          byteSize: 654000,
          name: 'stale-native-result-source-segments-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'stale native result sourceSegments regression',
      }),
      transcribeMedia: async (request) => {
        staleNativeResultSourceSegmentsCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'stale-native-result-source-segments-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8000,
              endMs: 24000,
              text:
                'Why stale native result evidence should not block slicing is simple. The rendered artifact can be correct while optional source segment metadata is old.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        staleNativeResultSourceSegmentsCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        staleNativeResultSourceSegmentsCommands.push({ kind: 'slice', request });
        const clip = request.clips[0];
        const sourceStartMs = clip?.sourceStartMs ?? clip?.startMs ?? 0;
        const sourceEndMs = clip?.sourceEndMs ?? (clip?.startMs ?? 0) + (clip?.durationMs ?? 1);
        const staleSourceSegments = [
          {
            startMs: sourceStartMs,
            endMs: Math.max(sourceStartMs + 1, Math.min(sourceEndMs - 2, sourceStartMs + 4000)),
          },
          {
            startMs: Math.max(sourceStartMs + 2, sourceEndMs - 4000),
            endMs: sourceEndMs,
          },
        ].filter((segment) => segment.endMs > segment.startMs);
        const staleRenderedDurationMs = staleSourceSegments.reduce(
          (durationMs, segment) => durationMs + Math.max(0, segment.endMs - segment.startMs),
          0,
        );
        const output = createNativeTaskOutputArtifact(
          'stale-native-result-source-segments-render-task',
          clip?.outputFileName ?? 'stale-native-result-source-segments.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'stale-native-result-source-segments-render-task',
          'stale-native-result-source-segments-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'stale-native-result-source-segments-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'stale-native-result-source-segments-artifact',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'stale-native-result-source-segments-thumb',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000,
              thumbnailByteSize: 12000,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
              durationMs: Math.max(1, sourceEndMs - sourceStartMs),
              sourceSegments: staleSourceSegments,
              renderedDurationMs: staleRenderedDurationMs,
              removedSilenceMs: Math.max(0, sourceEndMs - sourceStartMs - staleRenderedDurationMs),
              internalSilenceTrimCount: staleSourceSegments.length - 1,
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    let staleNativeResultSourceSegmentsResult;
    let staleNativeResultSourceSegmentsError = null;
    try {
      staleNativeResultSourceSegmentsResult = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-stale-native-result-source-segments',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/stale-native-result-source-segments-source.mp4',
            'stale-native-result-source-segments-source.mp4',
          ),
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          targetPlatform: 'generic',
          idealDuration: 20,
          continuityLevel: 'standard',
          minDuration: 10,
          maxDuration: 25,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: false,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      );
    } catch (error) {
      staleNativeResultSourceSegmentsError = error;
    }
    const staleNativeResultSourceSegmentsTask = staleNativeResultSourceSegmentsResult
      ? readScopedStoredArray(services, 'tasks').find((task) =>
          task.id === staleNativeResultSourceSegmentsResult.taskId
        )
      : undefined;
    assertEqual(
      staleNativeResultSourceSegmentsError instanceof Error
        ? staleNativeResultSourceSegmentsError.message
        : undefined,
      undefined,
      'Smart Slice ignores stale native-result sourceSegments instead of failing a usable render artifact',
    );
    assertEqual(
      staleNativeResultSourceSegmentsTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes when native result sourceSegments are stale but the continuous artifact timing is valid',
    );
    assertEqual(
      staleNativeResultSourceSegmentsTask?.sliceResults?.[0]?.sourceSegments,
      undefined,
      'Smart Slice drops stale native-result sourceSegments from completed slice results',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const reviewBeforeRenderSmartSliceCommands = [];
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.addAsset({
      id: 'asset-source-review-before-render-smart-slice',
      name: 'review-before-render-source.mp4',
      type: 'video',
      size: 654000,
      url: 'asset://localhost/review-before-render-source.mp4',
      createdAt: '2026-05-05T00:00:00.000Z',
      updatedAt: '2026-05-05T00:00:00.000Z',
    });
    await services.addAsset({
      id: 'asset-reference-review-before-render-smart-slice',
      name: 'review-before-render-source.mp4',
      type: 'video',
      size: 654000,
      url: 'asset://localhost/review-before-render-reference.mp4',
      createdAt: '2026-05-05T00:00:01.000Z',
      updatedAt: '2026-05-05T00:00:01.000Z',
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-human-review-contract',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        reviewBeforeRenderSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'review-before-render-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/review-before-render-smart-slice-asset.mp4`,
          byteSize: 654000,
          name: 'review-before-render-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 100_000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'smart slice human review contract',
      }),
      transcribeMedia: async (request) => {
        reviewBeforeRenderSmartSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'review-before-render-smart-slice-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 8_500,
              text:
                'Why review before rendering matters is simple. Because automatic slicing can miss operator intent, the first plan must be inspectable.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8_520,
              endMs: 19_000,
              text:
                'So the operator can split the plan, keep the useful opening, and render only the selected part.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40_000,
              endMs: 48_500,
              text:
                'What duplicate removal protects is clear. The second repeated segment should not be exported again.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 48_520,
              endMs: 59_000,
              text:
                'So duplicate deletion keeps the reviewed export clean and preserves audit evidence.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        reviewBeforeRenderSmartSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        reviewBeforeRenderSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'review-before-render-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/review-before-render-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'review-before-render-smart-slice-render-task',
              clip.outputFileName ?? `review-before-render-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'review-before-render-smart-slice-render-task',
              `review-before-render-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `review-before-render-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `review-before-render-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000 + index,
              thumbnailByteSize: 12000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }, { taskEvidenceCommands: reviewBeforeRenderSmartSliceCommands });
    const reviewBeforeRenderAnalyzeResult = await withImmediateTimers(async () =>
      analyzeVideoSlicePlan({
        fileId: 'asset-source-review-before-render-smart-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/review-before-render-source.mp4',
          'review-before-render-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        idealDuration: 25,
        continuityLevel: 'standard',
        minDuration: 10,
        maxDuration: 25,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSmartDedup: true,
        videoDedupParams: {
          mode: 'slice-result-dedup',
          sourceAssetIds: ['asset-source-review-before-render-smart-slice'],
          referenceAssetIds: ['asset-reference-review-before-render-smart-slice'],
          strategies: ['exact-file-hash', 'container-normalized', 'transcript-semantic'],
          sensitivity: 'balanced',
          minMatchDurationMs: 8_000,
          ignoreIntroOutro: true,
          introOutroMaxDurationMs: 12_000,
          actionMode: 'review-before-action',
        },
        enableSubtitles: false,
      }),
    );
    const reviewBeforeRenderAnalyzedTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === reviewBeforeRenderAnalyzeResult.taskId);
    const reviewBeforeRenderSession = reviewBeforeRenderAnalyzedTask?.sliceReviewSession;
    const reviewBeforeRenderSegments = reviewBeforeRenderSession?.segments ?? [];
    const firstReviewSegment = reviewBeforeRenderSegments[0];
    const secondReviewSegment = reviewBeforeRenderSegments[1];
    assertEqual(
      reviewBeforeRenderAnalyzeResult.success,
      true,
      'Smart Slice review-before-render analysis reports success without rendering',
    );
    assertEqual(
      reviewBeforeRenderAnalyzedTask?.status,
      types.AUTOCUT_TASK_STATUS.reviewing,
      'Smart Slice review-before-render analysis persists a review-ready task',
    );
    assertEqual(
      reviewBeforeRenderAnalyzedTask?.currentStepId,
      'human-review',
      'Smart Slice review-before-render analysis stops at the human-review checkpoint',
    );
    assertRule(
      reviewBeforeRenderAnalyzedTask?.executionCheckpoint?.completedStepIds?.includes('analyze-duplicates'),
      'Smart Slice intelligent dedup runs as a checkpoint before human review when enabled',
    );
    assertRule(
      (reviewBeforeRenderAnalyzedTask?.executionCheckpoint?.artifacts?.['analyze-duplicates']?.smartDedupReport?.matchCount ?? 0) >= 1,
      'Smart Slice intelligent dedup checkpoint persists the reusable video dedup report',
    );
    assertRule(
      (reviewBeforeRenderSession?.smartDedupReport?.matchCount ?? 0) >= 1,
      'Smart Slice review session exposes the smart dedup report for the review UI',
    );
    assertRule(
      reviewBeforeRenderSession?.duplicateGroups?.some((group) => group.reason === 'smart-dedup'),
      'Smart Slice review session converts video dedup matches into reviewable duplicate groups',
    );
    const reviewBeforeRenderSmartDedupGroup = reviewBeforeRenderSession?.duplicateGroups?.find((group) => group.reason === 'smart-dedup');
    assertRule(
      (reviewBeforeRenderSmartDedupGroup?.segmentIds ?? []).every((segmentId) =>
        reviewBeforeRenderSegments.find((segment) => segment.id === segmentId)?.duplicateGroupId === reviewBeforeRenderSmartDedupGroup?.id
      ),
      'Smart Slice review session links smart dedup duplicate groups back onto each review segment for deterministic keep-target resolution',
    );
    assertEqual(
      reviewBeforeRenderSmartDedupGroup?.segmentIds?.length,
      1,
      'Smart Slice review fixture covers external smart-dedup matches that have no in-plan keep segment',
    );
    const externalSmartDedupSegmentId = reviewBeforeRenderSmartDedupGroup?.segmentIds?.[0] ?? '';
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [externalSmartDedupSegmentId].filter(Boolean),
        manualEdits: [{
          id: 'review-edit-delete-external-smart-dedup-preflight',
          kind: 'deleteDuplicate',
          segmentIds: [externalSmartDedupSegmentId].filter(Boolean),
          createdAt: '2026-05-05T00:00:00.450Z',
          reason: 'external smart-dedup duplicate should be removable without an arbitrary in-plan keep segment',
        }],
      })),
      'selected non-duplicate',
      'Smart Slice render-selected rejects an externally duplicated segment after single-segment duplicate deletion replay',
    );
    assertRule(
      reviewBeforeRenderSegments.some((segment) => segment.risks?.includes('smart-dedup-review')),
      'Smart Slice review segments keep smart dedup matches as human-review risks instead of silently deleting content',
    );
    assertRule(
      reviewBeforeRenderSegments.length >= 2,
      'Smart Slice review-before-render analysis creates multiple reviewable semantic segments',
    );
    assertRule(
      !reviewBeforeRenderSmartSliceCommands.some((entry) => entry.kind === 'slice'),
      'Smart Slice review-before-render analysis does not render native slices before human approval',
    );
    const reviewBeforeRenderReviewSessionEvidenceWrite = reviewBeforeRenderSmartSliceCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/review-session.json',
    );
    assertEqual(
      reviewBeforeRenderReviewSessionEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.review-session.v1',
      'Smart Slice review-before-render persists a versioned review-session evidence file before human approval',
    );
    assertEqual(
      reviewBeforeRenderReviewSessionEvidenceWrite?.request?.contentJson?.segmentCount,
      reviewBeforeRenderSession?.segments?.length,
      'Smart Slice review-session evidence records the reviewable segment count',
    );
    assertEqual(
      reviewBeforeRenderAnalyzedTask?.executionCheckpoint?.artifacts?.['human-review']?.reviewSessionEvidence?.relativePath,
      'evidence/review-session.json',
      'Smart Slice review-before-render checkpoint references the review-session evidence artifact',
    );
    const reviewDraftSaveEdit = {
      id: 'review-edit-draft-exclude-second',
      kind: 'exclude',
      segmentIds: [secondReviewSegment?.id].filter(Boolean),
      createdAt: '2026-05-05T00:00:00.250Z',
      reason: 'draft correction should persist before rendering',
    };
    const reviewDraftCorrectionEdit = {
      id: 'review-edit-correct-first-boundary-speaker',
      kind: 'correctSegment',
      segmentIds: [firstReviewSegment?.id].filter(Boolean),
      createdAt: '2026-05-05T00:00:00.300Z',
      reason: 'operator corrected boundary, transcript title, and speaker label during review',
      patch: {
        title: 'Operator corrected review opening',
        startMs: firstReviewSegment ? firstReviewSegment.startMs + 250 : undefined,
        endMs: firstReviewSegment ? firstReviewSegment.endMs - 250 : undefined,
        transcriptText: 'Operator corrected transcript for the selected opening.',
        speakerIds: ['speaker-operator'],
        speakerRoles: ['Host'],
        manualNotes: 'corrected while previewing the segment',
      },
    };
    const reviewDraftSaveResult = await withImmediateTimers(() =>
      saveVideoSliceReviewDraft(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [firstReviewSegment?.id].filter(Boolean),
        manualEdits: [reviewDraftSaveEdit, reviewDraftCorrectionEdit],
      }),
    );
    const reviewBeforeRenderDraftTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === reviewBeforeRenderAnalyzeResult.taskId);
    const reviewDraftManualEditsEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/manual-edits.json')
      .at(-1);
    const reviewDraftRenderSelectionEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/render-selection.json')
      .at(-1);
    const reviewDraftEventsEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/review-events.json')
      .at(-1);
    assertEqual(
      reviewDraftSaveResult.reviewSessionId,
      reviewBeforeRenderSession?.id,
      'Smart Slice review draft save returns the current review session id',
    );
    assertEqual(
      reviewBeforeRenderDraftTask?.status,
      types.AUTOCUT_TASK_STATUS.reviewing,
      'Smart Slice review draft save keeps the task waiting for human review',
    );
    assertEqual(
      reviewBeforeRenderDraftTask?.sliceReviewSession?.manualEdits?.some((edit) => edit.id === reviewDraftSaveEdit.id),
      true,
      'Smart Slice review draft save persists human corrections on the task before rendering',
    );
    assertEqual(
      reviewBeforeRenderDraftTask?.sliceReviewSession?.segments?.find((segment) => segment.id === firstReviewSegment?.id)?.title,
      'Operator corrected review opening',
      'Smart Slice review draft save applies manual segment correction edits to the task review session',
    );
    assertEqual(
      reviewBeforeRenderDraftTask?.sliceReviewSession?.segments?.find((segment) => segment.id === firstReviewSegment?.id)?.speakerRoles?.[0],
      'Host',
      'Smart Slice review draft save persists corrected speaker roles for multi-speaker review',
    );
    assertEqual(
      reviewDraftManualEditsEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.manual-edits.v1',
      'Smart Slice review draft save persists manual-edits evidence before rendering',
    );
    assertEqual(
      reviewDraftEventsEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.review-events.v1',
      'Smart Slice review draft save persists a replayable review event log before rendering',
    );
    assertEqual(
      reviewDraftEventsEvidenceWrite?.request?.contentJson?.events?.at(-1)?.editId,
      reviewDraftCorrectionEdit.id,
      'Smart Slice review event log records the latest real-time correction edit id',
    );
    assertEqual(
      reviewDraftRenderSelectionEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.render-selection.v1',
      'Smart Slice review draft save persists render-selection draft evidence before rendering',
    );
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: 'wrong-review-session',
        selectedSegmentIds: [firstReviewSegment?.id].filter(Boolean),
      })),
      'review session does not match',
      'Smart Slice render-selected rejects a review selection from the wrong review session',
    );
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [],
      })),
      'at least one selected review segment',
      'Smart Slice render-selected rejects an empty manual segment selection',
    );
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: ['missing-review-segment'],
      })),
      'unknown review segment',
      'Smart Slice render-selected rejects unknown review segment ids before native rendering',
    );
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [secondReviewSegment?.id].filter(Boolean),
        manualEdits: [{
          id: 'review-edit-delete-selected-duplicate-preflight',
          kind: 'deleteDuplicate',
          segmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
          createdAt: '2026-05-05T00:00:00.500Z',
          keepSegmentId: firstReviewSegment?.id,
          reason: 'duplicate segment must not be renderable even when selected ids include it',
        }],
      })),
      'selected non-duplicate',
      'Smart Slice render-selected rejects duplicate or excluded segment ids before native rendering',
    );
    const illegalDuplicateMergeSegmentId = [
      firstReviewSegment?.id,
      secondReviewSegment?.id,
    ].filter(Boolean).join('-');
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [illegalDuplicateMergeSegmentId].filter(Boolean),
        manualEdits: [
          {
            id: 'review-edit-delete-merge-target-duplicate-preflight',
            kind: 'deleteDuplicate',
            segmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
            createdAt: '2026-05-05T00:00:00.600Z',
            keepSegmentId: firstReviewSegment?.id,
            reason: 'duplicate segment must be restored before it can be merged',
          },
          {
            id: 'review-edit-merge-selected-with-duplicate-preflight',
            kind: 'merge',
            segmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
            createdAt: '2026-05-05T00:00:00.700Z',
            createdSegmentIds: [illegalDuplicateMergeSegmentId].filter(Boolean),
            reason: 'duplicate merge attempts must not create a renderable selected segment',
          },
        ],
      })),
      'duplicate review segment',
      'Smart Slice render-selected rejects merge edits that include duplicate review segments',
    );
    const splitReviewEdit = {
      id: 'review-edit-split-first',
      kind: 'split',
      segmentIds: [firstReviewSegment?.id].filter(Boolean),
      createdAt: '2026-05-05T00:00:00.000Z',
      splitAtMs: firstReviewSegment
        ? Math.round((firstReviewSegment.startMs + firstReviewSegment.endMs) / 2)
        : undefined,
      createdSegmentIds: ['review-segment-first-a', 'review-segment-first-b'],
      reason: 'manual split should be idempotent when the UI resubmits the same edit',
    };
    const illegalNonAdjacentMergeSegmentId = [
      'review-segment-first-a',
      secondReviewSegment?.id,
    ].filter(Boolean).join('-');
    await assertRejects(
      () => withImmediateTimers(() => renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: [illegalNonAdjacentMergeSegmentId].filter(Boolean),
        manualEdits: [
          splitReviewEdit,
          {
            id: 'review-edit-merge-non-adjacent-preflight',
            kind: 'merge',
            segmentIds: ['review-segment-first-a', secondReviewSegment?.id].filter(Boolean),
            createdAt: '2026-05-05T00:00:00.800Z',
            createdSegmentIds: [illegalNonAdjacentMergeSegmentId].filter(Boolean),
            reason: 'manual merge replay must not span over an unmerged review segment',
          },
        ],
      })),
      'adjacent review segments',
      'Smart Slice render-selected rejects non-adjacent merge edits before they can span unreviewed content',
    );
    const deleteDuplicateReviewEdit = {
      id: 'review-edit-delete-duplicate-second',
      kind: 'deleteDuplicate',
      segmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
      createdAt: '2026-05-05T00:00:01.000Z',
      keepSegmentId: firstReviewSegment?.id,
      reason: 'manual duplicate deletion should exclude the repeated segment',
    };
    const correctSplitReviewEdit = {
      id: 'review-edit-correct-split-first-a-transcript',
      kind: 'correctSegment',
      segmentIds: ['review-segment-first-a'],
      createdAt: '2026-05-05T00:00:01.500Z',
      reason: 'operator corrected the final selected split segment transcript before rendering',
      patch: {
        transcriptText: 'Operator corrected transcript for the selected split opening.',
        manualNotes: 'corrected after split before render',
      },
    };
    const reviewBeforeRenderRenderResult = await withImmediateTimers(() =>
      renderVideoSlicePlan(reviewBeforeRenderAnalyzeResult.taskId, {
        reviewSessionId: reviewBeforeRenderSession?.id ?? '',
        selectedSegmentIds: ['review-segment-first-a'],
        manualEdits: [
          splitReviewEdit,
          splitReviewEdit,
          correctSplitReviewEdit,
          deleteDuplicateReviewEdit,
          deleteDuplicateReviewEdit,
        ],
      }),
    );
    const reviewBeforeRenderRenderedTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === reviewBeforeRenderAnalyzeResult.taskId);
    const reviewBeforeRenderRenderRequest = reviewBeforeRenderSmartSliceCommands
      .find((entry) => entry.kind === 'slice')?.request;
    const reviewBeforeRenderSelectedSplitSegment = reviewBeforeRenderRenderedTask?.sliceReviewSession?.segments?.find((segment) =>
      segment.id === 'review-segment-first-a'
    );
    const reviewBeforeRenderSelectedSplitTimelineClip = reviewBeforeRenderRenderedTask?.studioClipTimeline?.clips?.find((clip) =>
      clip.metadata?.reviewSegmentId === reviewBeforeRenderSelectedSplitSegment?.id
    );
    const reviewBeforeRenderExpectedStudioClipTimeline = reviewBeforeRenderRenderedTask?.sliceReviewSession
      ? createStudioClipTimelineFromReviewSession(reviewBeforeRenderRenderedTask.sliceReviewSession)
      : null;
    assertEqual(
      reviewBeforeRenderRenderResult.nativeTaskId,
      'review-before-render-smart-slice-render-task',
      'Smart Slice render-selected resumes review-ready tasks and dispatches native rendering',
    );
    assertEqual(
      reviewBeforeRenderRenderRequest?.clips?.length,
      1,
      'Smart Slice render-selected renders only the manually selected reviewed segment',
    );
    assertEqual(
      reviewBeforeRenderRenderRequest?.clips?.[0]?.label,
      'Operator corrected review opening A',
      'Smart Slice render-selected uses the manual split segment title for the native clip',
    );
    assertEqual(
      reviewBeforeRenderRenderRequest?.clips?.[0]?.transcriptText,
      'Operator corrected transcript for the selected split opening.',
      'Smart Slice render-selected sends the operator-corrected review transcript text to native rendering',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice render-selected completes the persisted review task after rendering',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.status,
      'rendered',
      'Smart Slice render-selected marks the review session rendered after successful native output',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceResults?.length,
      1,
      'Smart Slice render-selected persists only the selected reviewed slice result',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceResults?.[0]?.transcriptText,
      'Operator corrected transcript for the selected split opening.',
      'Smart Slice render-selected persists the operator-corrected review transcript text on the completed slice result',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.studioClipTimeline?.timeline?.id,
      reviewBeforeRenderExpectedStudioClipTimeline?.timeline?.id,
      'Smart Slice render-selected persists the review session timeline alongside the completed task',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.studioClipTimeline?.clips?.length,
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.segments?.length,
      'Smart Slice render-selected keeps one StudioClip timeline clip per review segment',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.metadata?.reviewSegmentId,
      reviewBeforeRenderSelectedSplitSegment?.id,
      'Smart Slice render-selected links each StudioClip timeline clip back to its review segment id',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.preview?.sourceStartMs,
      reviewBeforeRenderSelectedSplitSegment?.startMs,
      'Smart Slice render-selected keeps the StudioClip preview start aligned with the review segment start time',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.preview?.sourceEndMs,
      reviewBeforeRenderSelectedSplitSegment?.endMs,
      'Smart Slice render-selected keeps the StudioClip preview end aligned with the review segment end time',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.sourceRefs?.find((ref) => ref.sourceType === 'content_unit')?.startMs,
      reviewBeforeRenderSelectedSplitSegment?.startMs,
      'Smart Slice render-selected keeps the StudioClip content-unit source reference start aligned with the review segment start time',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.sourceRefs?.find((ref) => ref.sourceType === 'content_unit')?.endMs,
      reviewBeforeRenderSelectedSplitSegment?.endMs,
      'Smart Slice render-selected keeps the StudioClip content-unit source reference end aligned with the review segment end time',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.sourceRefs?.filter((ref) => ref.sourceType === 'text_segment').length,
      reviewBeforeRenderSelectedSplitSegment?.transcriptSegments?.length,
      'Smart Slice render-selected keeps the StudioClip transcript source references aligned with the review segment transcript timeline',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.sourceRefs?.find((ref) => ref.sourceType === 'text_segment')?.startMs,
      reviewBeforeRenderSelectedSplitSegment?.transcriptSegments?.[0]?.startMs,
      'Smart Slice render-selected keeps the StudioClip transcript source reference start aligned with the first STT segment',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.sourceRefs?.find((ref) => ref.sourceType === 'text_segment')?.endMs,
      reviewBeforeRenderSelectedSplitSegment?.transcriptSegments?.[0]?.endMs,
      'Smart Slice render-selected keeps the StudioClip transcript source reference end aligned with the first STT segment',
    );
    assertEqual(
      reviewBeforeRenderSelectedSplitTimelineClip?.transcriptTextSnapshot,
      reviewBeforeRenderSelectedSplitSegment?.transcriptText,
      'Smart Slice render-selected keeps the StudioClip transcript snapshot aligned with the review segment text',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.manualEdits?.filter((edit) => edit.id === splitReviewEdit.id).length,
      1,
      'Smart Slice render-selected applies duplicate manual split edit ids only once',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.manualEdits?.filter((edit) => edit.id === deleteDuplicateReviewEdit.id).length,
      1,
      'Smart Slice render-selected applies duplicate manual duplicate-delete edit ids only once',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.manualEdits?.filter((edit) => edit.id === correctSplitReviewEdit.id).length,
      1,
      'Smart Slice render-selected applies split-segment transcript correction edits exactly once',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.segments?.filter((segment) =>
        segment.id === 'review-segment-first-a' || segment.id === 'review-segment-first-b'
      ).length,
      2,
      'Smart Slice render-selected keeps manual split idempotent instead of recursively splitting generated segments',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.sliceReviewSession?.segments?.find((segment) => segment.id === secondReviewSegment?.id)?.status,
      'duplicate',
      'Smart Slice render-selected preserves manual duplicate deletion on the review session',
    );
    const reviewBeforeRenderManualDuplicateGroup = reviewBeforeRenderRenderedTask?.sliceReviewSession?.duplicateGroups?.find((group) =>
      group.id === `manual-duplicate-${deleteDuplicateReviewEdit.id}`
    );
    const reviewBeforeRenderSplitSmartDedupGroup = reviewBeforeRenderRenderedTask?.sliceReviewSession?.duplicateGroups?.find((group) =>
      group.reason === 'smart-dedup' && group.segmentIds.includes('review-segment-first-b')
    );
    const reviewBeforeRenderSecondSplitSegment = reviewBeforeRenderRenderedTask?.sliceReviewSession?.segments?.find((segment) =>
      segment.id === 'review-segment-first-b'
    );
    assertEqual(
      reviewBeforeRenderManualDuplicateGroup?.keptSegmentId,
      'review-segment-first-a',
      'Smart Slice render-selected resolves a duplicate-delete keep target through the split segment alias map',
    );
    assertEqual(
      reviewBeforeRenderSplitSmartDedupGroup?.keptSegmentId,
      'review-segment-first-a',
      'Smart Slice render-selected remaps smart-dedup groups to the kept split review segment',
    );
    assertEqual(
      reviewBeforeRenderSecondSplitSegment?.duplicateOfSegmentId,
      'review-segment-first-a',
      'Smart Slice render-selected relinks non-kept split children to the active duplicate-group keep target',
    );
    const reviewBeforeRenderManualEditsEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/manual-edits.json')
      .at(-1);
    const reviewBeforeRenderRenderSelectionEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/render-selection.json')
      .at(-1);
    const reviewBeforeRenderEventsEvidenceWrite = reviewBeforeRenderSmartSliceCommands
      .filter((entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/review-events.json')
      .at(-1);
    const reviewBeforeRenderArtifactManifestEvidenceWrite = reviewBeforeRenderSmartSliceCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/render-artifact-manifest.json',
    );
    assertEqual(
      reviewBeforeRenderManualEditsEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.manual-edits.v1',
      'Smart Slice render-selected persists a versioned manual-edits evidence file for real-time correction audit',
    );
    assertEqual(
      reviewBeforeRenderManualEditsEvidenceWrite?.request?.contentJson?.editCount,
      5,
      'Smart Slice manual-edits evidence stores saved draft corrections plus idempotently applied render-time edits rather than duplicate UI submissions',
    );
    assertEqual(
      reviewBeforeRenderEventsEvidenceWrite?.request?.contentJson?.eventCount,
      5,
      'Smart Slice review-events evidence stores one replayable event per unique manual correction',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.executionCheckpoint?.artifacts?.['human-review']?.reviewEventsEvidence?.relativePath,
      'evidence/review-events.json',
      'Smart Slice render-selected checkpoint references the review event log evidence artifact',
    );
    assertEqual(
      reviewBeforeRenderRenderSelectionEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.render-selection.v1',
      'Smart Slice render-selected persists a versioned render-selection evidence file',
    );
    assertEqual(
      reviewBeforeRenderRenderSelectionEvidenceWrite?.request?.contentJson?.selectedSegmentIds?.[0],
      'review-segment-first-a',
      'Smart Slice render-selection evidence records the exact human-selected segment ids',
    );
    assertEqual(
      reviewBeforeRenderArtifactManifestEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.render-artifact-manifest.v1',
      'Smart Slice render-selected persists a versioned render artifact manifest',
    );
    assertEqual(
      reviewBeforeRenderArtifactManifestEvidenceWrite?.request?.contentJson?.slices?.[0]?.reviewSegmentIds?.[0],
      'review-segment-first-a',
      'Smart Slice render artifact manifest links output slices back to reviewed segment ids',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.executionCheckpoint?.artifacts?.['human-review']?.manualEditsEvidence?.relativePath,
      'evidence/manual-edits.json',
      'Smart Slice render-selected checkpoint references the manual-edits evidence artifact',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.executionCheckpoint?.artifacts?.['human-review']?.renderSelectionEvidence?.relativePath,
      'evidence/render-selection.json',
      'Smart Slice render-selected checkpoint references the render-selection evidence artifact',
    );
    assertEqual(
      reviewBeforeRenderRenderedTask?.executionCheckpoint?.artifacts?.['verify-artifacts']?.renderArtifactManifestEvidence?.relativePath,
      'evidence/render-artifact-manifest.json',
      'Smart Slice render-selected checkpoint references the render artifact manifest evidence',
    );
    const boundaryCorrectionReviewTaskId = `${reviewBeforeRenderAnalyzeResult.taskId}-boundary-correction-regression`;
    const boundaryCorrectionReviewSessionId = `${reviewBeforeRenderSession?.id ?? 'slice-review'}-boundary-correction-regression`;
    const boundaryCorrectionReviewTask = {
      ...structuredClone(reviewBeforeRenderAnalyzedTask),
      id: boundaryCorrectionReviewTaskId,
      status: types.AUTOCUT_TASK_STATUS.reviewing,
      progress: 70,
      progressMessage: 'Segment Review Workbench is ready. Review, select, split, merge, or remove duplicate content before rendering.',
      currentStepId: 'human-review',
      completedAt: undefined,
      errorMessage: undefined,
      nativeTaskId: undefined,
      sliceResults: [],
      sliceReviewSession: {
        ...structuredClone(reviewBeforeRenderSession),
        id: boundaryCorrectionReviewSessionId,
        taskId: boundaryCorrectionReviewTaskId,
        status: 'ready_for_review',
      },
    };
    localStorage.setItem(
      services.createAutoCutStorageKey('tasks'),
      JSON.stringify([
        ...readScopedStoredArray(services, 'tasks'),
        boundaryCorrectionReviewTask,
      ]),
    );
    const boundaryCorrectedSecondStartMs = Math.max(0, (secondReviewSegment?.startMs ?? 0) - 300);
    const boundaryCorrectedSecondEndMs = (secondReviewSegment?.endMs ?? 0) + 300;
    const boundaryCorrectionCommandStart = reviewBeforeRenderSmartSliceCommands.length;
    let boundaryCorrectionRenderError = null;
    try {
      await withImmediateTimers(() =>
        renderVideoSlicePlan(boundaryCorrectionReviewTaskId, {
          reviewSessionId: boundaryCorrectionReviewSessionId,
          selectedSegmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
          manualEdits: [{
            id: 'review-edit-expand-second-boundary-source-segments-regression',
            kind: 'correctSegment',
            segmentIds: [secondReviewSegment?.id].filter(Boolean),
            createdAt: '2026-05-05T00:00:01.750Z',
            reason: 'review boundary correction must not leave stale sourceSegments that block rendering',
            patch: {
              startMs: boundaryCorrectedSecondStartMs,
              endMs: boundaryCorrectedSecondEndMs,
            },
          }],
        }),
      );
    } catch (error) {
      boundaryCorrectionRenderError = error;
    }
    const boundaryCorrectionRenderRequest = reviewBeforeRenderSmartSliceCommands
      .slice(boundaryCorrectionCommandStart)
      .find((entry) => entry.kind === 'slice')?.request;
    const boundaryCorrectionSecondNativeClip = boundaryCorrectionRenderRequest?.clips?.[1];
    const boundaryCorrectionSecondSourceSegments = boundaryCorrectionSecondNativeClip?.sourceSegments ?? [];
    assertEqual(
      boundaryCorrectionRenderError instanceof Error ? boundaryCorrectionRenderError.message : undefined,
      undefined,
      'Smart Slice render-selected accepts review boundary corrections without stale sourceSegments blocking render',
    );
    assertEqual(
      boundaryCorrectionRenderRequest?.clips?.length,
      2,
      'Smart Slice review boundary correction regression renders both selected reviewed clips',
    );
    assertEqual(
      boundaryCorrectionSecondNativeClip?.sourceStartMs,
      boundaryCorrectedSecondStartMs,
      'Smart Slice review boundary correction native clip uses the operator-corrected source start',
    );
    assertEqual(
      boundaryCorrectionSecondNativeClip?.sourceEndMs,
      boundaryCorrectedSecondEndMs,
      'Smart Slice review boundary correction native clip uses the operator-corrected source end',
    );
    assertRule(
      boundaryCorrectionSecondSourceSegments.length === 0 ||
        (
          boundaryCorrectionSecondSourceSegments[0]?.startMs === boundaryCorrectedSecondStartMs &&
          boundaryCorrectionSecondSourceSegments.at(-1)?.endMs === boundaryCorrectedSecondEndMs
        ),
      'Smart Slice review boundary correction repairs stale sourceSegments to span the corrected source range before native rendering',
    );
    const adjacentMergeReviewTaskId = `${reviewBeforeRenderAnalyzeResult.taskId}-adjacent-merge-regression`;
    const adjacentMergeReviewSessionId = `${reviewBeforeRenderSession?.id ?? 'slice-review'}-adjacent-merge-regression`;
    const adjacentMergeReviewTask = {
      ...structuredClone(reviewBeforeRenderAnalyzedTask),
      id: adjacentMergeReviewTaskId,
      status: types.AUTOCUT_TASK_STATUS.reviewing,
      progress: 70,
      progressMessage: 'Segment Review Workbench is ready. Review, select, split, merge, or remove duplicate content before rendering.',
      currentStepId: 'human-review',
      completedAt: undefined,
      errorMessage: undefined,
      nativeTaskId: undefined,
      sliceResults: [],
      sliceReviewSession: {
        ...structuredClone(reviewBeforeRenderSession),
        id: adjacentMergeReviewSessionId,
        taskId: adjacentMergeReviewTaskId,
        status: 'ready_for_review',
      },
    };
    localStorage.setItem(
      services.createAutoCutStorageKey('tasks'),
      JSON.stringify([
        ...readScopedStoredArray(services, 'tasks'),
        adjacentMergeReviewTask,
      ]),
    );
    const adjacentMergeSegmentId = [
      firstReviewSegment?.id,
      secondReviewSegment?.id,
    ].filter(Boolean).join('-');
    const adjacentMergeCommandStart = reviewBeforeRenderSmartSliceCommands.length;
    let adjacentMergeRenderError = null;
    try {
      await withImmediateTimers(() =>
        renderVideoSlicePlan(adjacentMergeReviewTaskId, {
          reviewSessionId: adjacentMergeReviewSessionId,
          selectedSegmentIds: [adjacentMergeSegmentId].filter(Boolean),
          manualEdits: [{
            id: 'review-edit-merge-adjacent-speech-range-regression',
            kind: 'merge',
            segmentIds: [firstReviewSegment?.id, secondReviewSegment?.id].filter(Boolean),
            createdAt: '2026-05-05T00:00:02.000Z',
            createdSegmentIds: [adjacentMergeSegmentId].filter(Boolean),
            reason: 'adjacent manual merge must preserve the full merged speech range',
          }],
        }),
      );
    } catch (error) {
      adjacentMergeRenderError = error;
    }
    const adjacentMergeRenderRequest = reviewBeforeRenderSmartSliceCommands
      .slice(adjacentMergeCommandStart)
      .find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      adjacentMergeRenderError instanceof Error ? adjacentMergeRenderError.message : undefined,
      undefined,
      'Smart Slice render-selected accepts a legal adjacent merge after human review',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.length,
      1,
      'Smart Slice render-selected sends one native clip for a legal adjacent merge',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.sourceStartMs,
      firstReviewSegment?.startMs,
      'Smart Slice adjacent merge native clip starts at the first merged review segment',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.sourceEndMs,
      secondReviewSegment?.endMs,
      'Smart Slice adjacent merge native clip ends at the last merged review segment',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.speechStartMs,
      firstReviewSegment?.speechStartMs ?? firstReviewSegment?.startMs,
      'Smart Slice adjacent merge native clip preserves the first merged speech boundary',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.speechEndMs,
      secondReviewSegment?.speechEndMs ?? secondReviewSegment?.endMs,
      'Smart Slice adjacent merge native clip preserves the last merged speech boundary',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.boundaryDecisionSource,
      'transcript',
      'Smart Slice adjacent merge native clip rebuilds boundary evidence from the merged transcript range',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.audioActivityStartMs,
      firstReviewSegment?.speechStartMs ?? firstReviewSegment?.startMs,
      'Smart Slice adjacent merge native clip resets audio activity start to the merged speech start',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.audioActivityEndMs,
      secondReviewSegment?.speechEndMs ?? secondReviewSegment?.endMs,
      'Smart Slice adjacent merge native clip resets audio activity end to the merged speech end',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.audioActivityConfidence,
      0.97,
      'Smart Slice adjacent merge native clip emits trusted transcript-boundary audio activity confidence',
    );
    assertEqual(
      adjacentMergeRenderRequest?.clips?.[0]?.audioActivityAnalysisFilter,
      'silencedetect=noise=-35dB:d=0.08',
      'Smart Slice adjacent merge native clip keeps audio activity filter aligned with the source denoise decision',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const localImportSmartDedupCommands = [];
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.addAsset({
      id: 'asset-reference-local-import-smart-dedup',
      name: 'local-import-smart-dedup-source.mp4',
      type: 'video',
      size: 654000,
      url: 'asset://localhost/local-import-smart-dedup-reference.mp4',
      createdAt: '2026-05-05T00:00:01.000Z',
      updatedAt: '2026-05-05T00:00:01.000Z',
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-local-import-dedup-contract',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        localImportSmartDedupCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'local-import-smart-dedup-source-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/local-import-smart-dedup-source.mp4`,
          byteSize: 654000,
          name: 'local-import-smart-dedup-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 80_000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'local import Smart Slice dedup contract',
      }),
      transcribeMedia: async (request) => {
        localImportSmartDedupCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'local-import-smart-dedup-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 12_000,
              text:
                'The local import dedup case starts with a complete opening. The source must be registered before duplicate analysis can compare it.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 12_500,
              endMs: 24_000,
              text:
                'The local import dedup case ends with the same commercial point. Smart Slice should flag the imported source for review.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        localImportSmartDedupCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        localImportSmartDedupCommands.push({ kind: 'slice', request });
        throw new Error('local import smart dedup review test should stop before rendering');
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }, { taskEvidenceCommands: localImportSmartDedupCommands });
    const localImportSmartDedupResult = await withImmediateTimers(async () =>
      analyzeVideoSlicePlan({
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/local-import-smart-dedup-source.mp4',
          'local-import-smart-dedup-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        idealDuration: 25,
        continuityLevel: 'standard',
        minDuration: 10,
        maxDuration: 25,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSmartDedup: true,
        videoDedupParams: {
          mode: 'slice-result-dedup',
          referenceAssetIds: ['asset-reference-local-import-smart-dedup'],
          strategies: ['exact-file-hash', 'container-normalized'],
          sensitivity: 'balanced',
          minMatchDurationMs: 8_000,
          ignoreIntroOutro: true,
          introOutroMaxDurationMs: 12_000,
          actionMode: 'review-before-action',
        },
        enableSubtitles: false,
      }),
    );
    const localImportSmartDedupTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === localImportSmartDedupResult.taskId);
    const localImportSourceAsset = readScopedStoredArray(services, 'assets')
      .find((asset) => asset.id === 'local-import-smart-dedup-source-asset');
    assertEqual(
      localImportSmartDedupResult.success,
      true,
      'Smart Slice local import dedup analysis reports success without rendering',
    );
    assertEqual(
      localImportSourceAsset,
      undefined,
      'Smart Slice local import dedup uses runtime source evidence without polluting the asset library before rendering',
    );
    assertRule(
      (localImportSmartDedupTask?.sliceReviewSession?.smartDedupReport?.matchCount ?? 0) >= 1,
      'Smart Slice intelligent dedup scans the current trusted local import when the user did not select an existing asset id',
    );
    assertRule(
      localImportSmartDedupTask?.sliceReviewSession?.duplicateGroups?.some((group) => group.reason === 'smart-dedup'),
      'Smart Slice local import dedup matches are exposed as review duplicate groups',
    );
    assertRule(
      !localImportSmartDedupCommands.some((entry) => entry.kind === 'slice'),
      'Smart Slice local import dedup review analysis does not render before human approval',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const secondsTimestampSmartSliceCommands = [];
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-seconds-timestamp-ui-regression',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        secondsTimestampSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'seconds-timestamp-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/seconds-timestamp-smart-slice-asset.mp4`,
          byteSize: 654000,
          name: 'seconds-timestamp-smart-slice-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90_000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'seconds timestamp Smart Slice UI regression',
      }),
      transcribeMedia: async (request) => {
        secondsTimestampSmartSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'seconds-timestamp-smart-slice-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 8.5,
              text:
                'Why native validation matters is simple. Because bad native validation can break review, the native validation problem must be caught before assets are saved.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8.52,
              endMs: 19,
              text:
                'So native validation verifies every result and the native validation workflow stays safe.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40,
              endMs: 48.5,
              text:
                'What makes artifact checks reliable is clear. The artifact check shows missing artifact thumbnails before publishing, creating an artifact conflict for reviewers.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 48.52,
              endMs: 59,
              text:
                'So the artifact check rejects the bad artifact before persistence and keeps artifact storage clean.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        secondsTimestampSmartSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        secondsTimestampSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'seconds-timestamp-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/seconds-timestamp-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'seconds-timestamp-smart-slice-render-task',
              clip.outputFileName ?? `seconds-timestamp-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'seconds-timestamp-smart-slice-render-task',
              `seconds-timestamp-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `seconds-timestamp-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `seconds-timestamp-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000 + index,
              thumbnailByteSize: 12000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const secondsTimestampSmartSliceResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-seconds-timestamp-smart-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/seconds-timestamp-smart-slice-source.mp4',
          'seconds-timestamp-smart-slice-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        idealDuration: 25,
        continuityLevel: 'standard',
        minDuration: 10,
        maxDuration: 20,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const secondsTimestampSmartSliceTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === secondsTimestampSmartSliceResult.taskId);
    const secondsTimestampSmartSliceRenderRequest = secondsTimestampSmartSliceCommands
      .find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      secondsTimestampSmartSliceResult.success,
      true,
      'Smart Slice UI workflow normalizes STT seconds timestamps and reports success',
    );
    assertEqual(
      secondsTimestampSmartSliceRenderRequest?.clips?.length,
      2,
      'Smart Slice UI workflow renders multiple transcript-derived clips when STT timestamps arrive in seconds',
    );
    assertRule(
      secondsTimestampSmartSliceRenderRequest?.clips?.every((clip) => clip.startMs >= 0 && clip.durationMs >= 10_000),
      'Smart Slice UI workflow converts seconds timestamps into renderable millisecond clip windows before native rendering',
    );
    assertEqual(
      secondsTimestampSmartSliceRenderRequest?.clips?.[1]?.speechStartMs,
      40_000,
      'Smart Slice UI workflow preserves the second story timestamp after seconds-to-milliseconds normalization',
    );
    assertEqual(
      secondsTimestampSmartSliceTask?.sliceResults?.length,
      2,
      'Smart Slice UI workflow persists both generated clips after seconds timestamp normalization',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const unknownDurationSecondsTimestampSmartSliceCommands = [];
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-unknown-duration-seconds-timestamp-ui-regression',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        unknownDurationSecondsTimestampSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'unknown-duration-seconds-timestamp-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/unknown-duration-seconds-timestamp-smart-slice-asset.mp4`,
          byteSize: 654000,
          name: 'unknown-duration-seconds-timestamp-smart-slice-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'unknown duration seconds timestamp Smart Slice UI regression',
      }),
      transcribeMedia: async (request) => {
        unknownDurationSecondsTimestampSmartSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'unknown-duration-seconds-timestamp-smart-slice-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 8.5,
              text:
                'Why native validation matters is simple. Because bad native validation can break review, the native validation problem must be caught before assets are saved.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8.52,
              endMs: 19,
              text:
                'So native validation verifies every result and the native validation workflow stays safe.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40,
              endMs: 48.5,
              text:
                'What makes artifact checks reliable is clear. The artifact check shows missing artifact thumbnails before publishing, creating an artifact conflict for reviewers.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 48.52,
              endMs: 59,
              text:
                'So the artifact check rejects the bad artifact before persistence and keeps artifact storage clean.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        unknownDurationSecondsTimestampSmartSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        unknownDurationSecondsTimestampSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'unknown-duration-seconds-timestamp-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/unknown-duration-seconds-timestamp-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'unknown-duration-seconds-timestamp-smart-slice-render-task',
              clip.outputFileName ?? `unknown-duration-seconds-timestamp-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'unknown-duration-seconds-timestamp-smart-slice-render-task',
              `unknown-duration-seconds-timestamp-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `unknown-duration-seconds-timestamp-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `unknown-duration-seconds-timestamp-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000 + index,
              thumbnailByteSize: 12000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const unknownDurationSecondsTimestampSmartSliceResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-unknown-duration-seconds-timestamp-smart-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/unknown-duration-seconds-timestamp-smart-slice-source.mp4',
          'unknown-duration-seconds-timestamp-smart-slice-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        idealDuration: 25,
        continuityLevel: 'standard',
        minDuration: 10,
        maxDuration: 20,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const unknownDurationSecondsTimestampSmartSliceRenderRequest = unknownDurationSecondsTimestampSmartSliceCommands
      .find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      unknownDurationSecondsTimestampSmartSliceResult.success,
      true,
      'Smart Slice UI workflow normalizes STT seconds timestamps even when imported media duration is unavailable',
    );
    assertEqual(
      unknownDurationSecondsTimestampSmartSliceRenderRequest?.clips?.length,
      2,
      'Smart Slice UI workflow renders multiple transcript-derived clips when upload metadata lacks duration',
    );
    assertRule(
      unknownDurationSecondsTimestampSmartSliceRenderRequest?.clips?.every((clip) => clip.durationMs >= 10_000),
      'Smart Slice UI workflow converts unknown-duration seconds timestamps into renderable millisecond clip windows',
    );
    assertEqual(
      unknownDurationSecondsTimestampSmartSliceRenderRequest?.clips?.[1]?.speechStartMs,
      40_000,
      'Smart Slice UI workflow preserves later story timing after unknown-duration seconds-to-milliseconds normalization',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const autoInitializingSmartSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      importMediaFile: async (request) => {
        autoInitializingSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'auto-initializing-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/auto-initializing-smart-slice-asset.mp4`,
          byteSize: 654000,
          name: 'auto-initializing-smart-slice-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      downloadSpeechTranscriptionModel: async (request) => {
        autoInitializingSmartSliceCommands.push({ kind: 'download-model', request });
        services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          phase: types.AUTOCUT_SPEECH_TRANSCRIPTION_MODEL_DOWNLOAD_PHASE.downloading,
          downloadedBytes: 286785536,
          totalBytes: 573571072,
          progress: 50,
          sourceUrl: request.url,
        });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: `${configuredOutputDirectory}/models/speech/ggml-large-v3-turbo-q5_0.bin`,
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
      probeSpeechTranscription: async (request) => {
        autoInitializingSmartSliceCommands.push({ kind: 'probe', request });
        return {
          ready: Boolean(request.executablePath && request.modelPath),
          executablePath: request.executablePath || 'D:/tools/whisper-cli.exe',
          modelPath: request.modelPath || '',
          sourceKind: request.sourceKind ?? 'execution-preflight',
          diagnostics: [],
          versionLine: 'auto initializing smart slice STT',
        };
      },
      transcribeMedia: async (request) => {
        autoInitializingSmartSliceCommands.push({ kind: 'transcribe', request });
        return createStandardNativeVideoSliceTranscriptResult(
          request,
          'auto-initializing-smart-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      sliceVideo: async (request) => {
        autoInitializingSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'auto-initializing-smart-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: createNativeTaskOutputArtifact(
            'auto-initializing-smart-slice-task',
            'auto-initializing-smart-slice-artifact-1.mp4',
            configuredOutputDirectory,
          ).taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'auto-initializing-smart-slice-task',
              `auto-initializing-smart-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'auto-initializing-smart-slice-task',
              `auto-initializing-smart-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `auto-initializing-smart-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `auto-initializing-smart-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...(await services.getAutoCutSettings()).speechTranscription,
      executablePath: 'D:/tools/whisper-cli.exe',
      modelPath: '',
      language: 'auto',
    });
    const autoInitializingSmartSliceTaskUpdates = captureEvents(services, 'taskUpdated');
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-auto-initializing-smart-slice',
        file: createTrustedLocalMediaFile(commons, 'D:/media/auto-initializing-smart-slice-source.mp4'),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableCoughFilter: true,
        enableRepeatFilter: true,
      }),
    );
    autoInitializingSmartSliceTaskUpdates.stop();
    const autoInitializingSmartSliceProgressValues = autoInitializingSmartSliceTaskUpdates.details
      .map((task) => task?.progress)
      .filter((progress) => typeof progress === 'number');
    assertRule(
      autoInitializingSmartSliceTaskUpdates.details.some((task) =>
        String(task?.progressMessage ?? '').includes('Downloading speech recognition model') &&
        String(task?.progressMessage ?? '').includes('50%')
      ),
      'Smart Slice task displays local STT model download progress while automatic transcription setup is in flight',
    );
    assertRule(
      autoInitializingSmartSliceTaskUpdates.details.some((task) =>
        String(task?.progressMessage ?? '').includes('Speech recognition model is ready')
      ),
      'Smart Slice task displays local STT model initialization completion before native transcription starts',
    );
    assertRule(
      autoInitializingSmartSliceProgressValues.every((progress, index, values) =>
        index === 0 || progress >= values[index - 1]
      ),
      `Smart Slice local STT initialization progress messages remain monotonic (${JSON.stringify(autoInitializingSmartSliceProgressValues)})`,
    );
    assertEqual(
      autoInitializingSmartSliceCommands.find((entry) => entry.kind === 'transcribe')?.request?.modelPath,
      `${configuredOutputDirectory}/models/speech/ggml-large-v3-turbo-q5_0.bin`,
      'Smart Slice transcribes with the auto-initialized local STT model path after model setup completes',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const audioBoundaryFallbackCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        audioBoundaryFallbackCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'audio-boundary-fallback-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/audio-boundary-fallback-source.mp4',
          byteSize: 654000,
          name: 'audio-boundary-fallback-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'audio boundary fallback speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        audioBoundaryFallbackCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'audio-boundary-fallback-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why fallback matters is simple. Because denoised boundary analysis can fail, the transcript timing must still produce a valid slice.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        audioBoundaryFallbackCommands.push({ kind: 'analyze-audio-boundaries', request });
        throw new Error('silencedetect failed for corrupt audio stream');
      },
      sliceVideo: async (request) => {
        audioBoundaryFallbackCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'audio-boundary-fallback-slice-task',
          request.clips[0]?.outputFileName ?? 'audio-boundary-fallback-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'audio-boundary-fallback-slice-task',
          'audio-boundary-fallback-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'audio-boundary-fallback-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `audio-boundary-fallback-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `audio-boundary-fallback-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 345678 + index,
            thumbnailByteSize: 23456 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const audioBoundaryFallbackSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/audio-boundary-fallback-source.mp4',
      'audio-boundary-fallback-source.mp4',
    );
    let audioBoundaryFallbackResult;
    const audioBoundaryFallbackDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      audioBoundaryFallbackResult = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-audio-boundary-fallback',
          file: audioBoundaryFallbackSourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      );
    });
    const audioBoundaryFallbackTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === audioBoundaryFallbackResult?.taskId,
    );
    const audioBoundaryFallbackSliceCommand = audioBoundaryFallbackCommands.find((entry) => entry.kind === 'slice');
    assertEqual(
      audioBoundaryFallbackResult?.success,
      true,
      'Smart Slice reports success when audio boundary analysis falls back to transcript timing',
    );
    assertEqual(
      audioBoundaryFallbackTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes when audio boundary analysis falls back to transcript timing',
    );
    assertEqual(
      audioBoundaryFallbackCommands.some((entry) => entry.kind === 'analyze-audio-boundaries'),
      true,
      'Smart Slice still attempts audio boundary analysis before using fallback boundaries',
    );
    assertEqual(
      audioBoundaryFallbackSliceCommand?.kind,
      'slice',
      'Smart Slice renders native slices after audio boundary analysis falls back',
    );
    assertRule(
      audioBoundaryFallbackDiagnostics.some((call) =>
        String(call.args?.[0] ?? '').includes('audio boundary analysis failed; render fallback plan directly')
      ),
      'Smart Slice logs audio boundary analysis failure as a render fallback decision',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const audioPaddingDenoiseFallbackCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        audioPaddingDenoiseFallbackCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'audio-padding-denoise-fallback-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/audio-padding-denoise-fallback-source.mp4',
          byteSize: 654000,
          name: 'audio-padding-denoise-fallback-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'audio padding denoise fallback speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        audioPaddingDenoiseFallbackCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'audio-padding-denoise-fallback-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why audio padding fallback matters is simple. Because raw background noise can hide true speech boundaries, denoised analysis must repair the cut before rendering.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        audioPaddingDenoiseFallbackCommands.push({ kind: 'analyze-audio-boundaries', request });
        const analysisFilter = request.applyNoiseReduction === false
          ? 'silencedetect=noise=-35dB:d=0.08'
          : 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08';
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: request.clips.map((clip, index) => {
            const sourceStartMs = clip.sourceStartMs ?? clip.startMs;
            const sourceEndMs = clip.sourceEndMs ?? clip.startMs + clip.durationMs;
            const speechStartMs = clip.speechStartMs ?? sourceStartMs + 200;
            const speechEndMs = clip.speechEndMs ?? sourceEndMs - 250;
            const rawConflictStartMs = Math.min(sourceEndMs - 100, speechStartMs + 1_800);
            const rawConflictEndMs = Math.max(rawConflictStartMs + 100, speechEndMs - 1_800);
            const audioActivityStartMs = request.applyNoiseReduction === false
              ? rawConflictStartMs
              : speechStartMs;
            const audioActivityEndMs = request.applyNoiseReduction === false
              ? rawConflictEndMs
              : speechEndMs;
            return {
              index,
              startMs: clip.startMs,
              durationMs: clip.durationMs,
              sourceStartMs,
              sourceEndMs,
              audioActivityStartMs,
              audioActivityEndMs,
              leadingSilenceMs: Math.max(0, audioActivityStartMs - sourceStartMs),
              trailingSilenceMs: Math.max(0, sourceEndMs - audioActivityEndMs),
              confidence: 0.96,
              analysisFilter,
            };
          }),
        };
      },
      sliceVideo: async (request) => {
        audioPaddingDenoiseFallbackCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'audio-padding-denoise-fallback-slice-task',
          request.clips[0]?.outputFileName ?? 'audio-padding-denoise-fallback-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'audio-padding-denoise-fallback-slice-task',
          'audio-padding-denoise-fallback-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'audio-padding-denoise-fallback-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `audio-padding-denoise-fallback-artifact-${index + 1}`,
            artifactPath: index === 0
              ? output.artifactPath
              : createNativeTaskOutputArtifact(
                  'audio-padding-denoise-fallback-slice-task',
                  `audio-padding-denoise-fallback-slice-${index + 1}.mp4`,
                  configuredOutputDirectory,
                ).artifactPath,
            thumbnailArtifactUuid: `audio-padding-denoise-fallback-thumb-${index + 1}`,
            thumbnailArtifactPath: index === 0
              ? thumbnail.artifactPath
              : createNativeTaskCoverArtifact(
                  'audio-padding-denoise-fallback-slice-task',
                  `audio-padding-denoise-fallback-thumb-${index + 1}.jpg`,
                  configuredOutputDirectory,
                ).artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 345678 + index,
            thumbnailByteSize: 23456 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const audioPaddingDenoiseFallbackSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/audio-padding-denoise-fallback-source.mp4',
      'audio-padding-denoise-fallback-source.mp4',
    );
    let audioPaddingDenoiseFallbackResult;
    const audioPaddingDenoiseFallbackDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      audioPaddingDenoiseFallbackResult = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-audio-padding-denoise-fallback',
          file: audioPaddingDenoiseFallbackSourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      );
    });
    const audioPaddingDenoiseFallbackAnalyzeCommands = audioPaddingDenoiseFallbackCommands.filter(
      (entry) => entry.kind === 'analyze-audio-boundaries',
    );
    const audioPaddingDenoiseFallbackSliceCommand = audioPaddingDenoiseFallbackCommands.find(
      (entry) => entry.kind === 'slice',
    );
    const audioPaddingDenoiseFallbackClip = audioPaddingDenoiseFallbackSliceCommand?.request?.clips?.[0];
    const audioPaddingDenoiseFallbackTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === audioPaddingDenoiseFallbackResult?.taskId,
    );
    assertEqual(
      audioPaddingDenoiseFallbackResult?.success,
      true,
      'Smart Slice reports success after denoise repairs raw audio activity padding before native rendering',
    );
    assertEqual(
      audioPaddingDenoiseFallbackAnalyzeCommands.length,
      2,
      'Smart Slice retries denoised audio boundary analysis when raw activity padding still exceeds the professional standard',
    );
    assertEqual(
      audioPaddingDenoiseFallbackAnalyzeCommands[0]?.request?.applyNoiseReduction,
      false,
      'Smart Slice first analyzes raw audio boundaries to preserve clean source audio when possible',
    );
    assertEqual(
      audioPaddingDenoiseFallbackAnalyzeCommands[1]?.request?.applyNoiseReduction,
      true,
      'Smart Slice falls back to denoised boundary analysis after raw activity padding is not render-ready',
    );
    assertEqual(
      audioPaddingDenoiseFallbackSliceCommand?.request?.noiseReduction,
      true,
      'Smart Slice renders with denoise enabled after raw activity padding fallback succeeds',
    );
    assertEqual(
      audioPaddingDenoiseFallbackClip?.noiseReductionApplied,
      true,
      'Smart Slice native clip evidence records denoise after audio padding fallback',
    );
    assertNumberBetween(
      audioPaddingDenoiseFallbackClip?.leadingSilenceMs,
      0,
      200,
      'Smart Slice denoise fallback keeps leading audio activity padding inside the native render standard',
    );
    assertNumberBetween(
      audioPaddingDenoiseFallbackClip?.trailingSilenceMs,
      0,
      250,
      'Smart Slice denoise fallback keeps trailing audio activity padding inside the native render standard',
    );
    assertEqual(
      audioPaddingDenoiseFallbackTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes after denoise repairs raw audio activity padding before native rendering',
    );
    assertRule(
      audioPaddingDenoiseFallbackDiagnostics.some((call) =>
        call.level === 'info' &&
          String(call.args?.[0] ?? '').includes('raw audio boundary padding fallback to denoise')
      ),
      'Smart Slice logs the raw audio padding denoise fallback decision for diagnostics',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const incompleteAudioBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        incompleteAudioBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'incomplete-audio-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/incomplete-audio-boundary-source.mp4',
          byteSize: 654000,
          name: 'incomplete-audio-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'incomplete audio boundary speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        incompleteAudioBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'incomplete-audio-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why complete boundary evidence matters is simple. Because every rendered slice must prove audio timing before output is trusted.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        incompleteAudioBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: [],
        };
      },
      sliceVideo: async (request) => {
        incompleteAudioBoundaryCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'incomplete-audio-boundary-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const incompleteAudioBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/incomplete-audio-boundary-source.mp4',
      'incomplete-audio-boundary-source.mp4',
    );
    const incompleteAudioBoundaryResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-incomplete-audio-boundary',
        file: incompleteAudioBoundarySourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const incompleteAudioBoundaryTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === incompleteAudioBoundaryResult.taskId,
    );
    assertEqual(
      incompleteAudioBoundaryResult.success,
      true,
      'Smart Slice reports success when incomplete audio boundary analysis falls back to timeline rendering',
    );
    assertEqual(
      incompleteAudioBoundaryCommands.some((entry) => entry.kind === 'analyze-audio-boundaries'),
      true,
      'Smart Slice requests audio boundary analysis before validating completeness',
    );
    assertEqual(
      incompleteAudioBoundaryCommands.some((entry) => entry.kind === 'slice'),
      true,
      'Smart Slice renders native slices after incomplete audio boundary analysis fallback',
    );
    assertEqual(
      incompleteAudioBoundaryTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes after incomplete audio boundary analysis fallback',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const malformedAudioBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        malformedAudioBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'malformed-audio-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/malformed-audio-boundary-source.mp4',
          byteSize: 654000,
          name: 'malformed-audio-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'malformed audio boundary speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        malformedAudioBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'malformed-audio-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why malformed boundary evidence fails is simple. The renderer must not trust a native analysis without its filter provenance.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        malformedAudioBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        const clip = request.clips[0];
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: [
            {
              index: 0,
              startMs: clip?.startMs,
              durationMs: clip?.durationMs,
              sourceStartMs: clip?.sourceStartMs ?? clip?.startMs,
              sourceEndMs: clip?.sourceEndMs ?? (clip?.startMs ?? 0) + (clip?.durationMs ?? 0),
              audioActivityStartMs: clip?.speechStartMs,
              audioActivityEndMs: clip?.speechEndMs,
              confidence: 0.95,
            },
          ],
        };
      },
      sliceVideo: async (request) => {
        malformedAudioBoundaryCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'malformed-audio-boundary-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const malformedAudioBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/malformed-audio-boundary-source.mp4',
      'malformed-audio-boundary-source.mp4',
    );
    await assertDoesNotReject(
      () => withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-malformed-audio-boundary',
          file: malformedAudioBoundarySourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      ),
      'Smart Slice rejects malformed audio boundary analysis evidence with a standard error',
    );
    assertEqual(
      malformedAudioBoundaryCommands.some((entry) => entry.kind === 'slice'),
      true,
      'Smart Slice renders native slices after malformed audio boundary analysis fallback',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const weakAudioBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        weakAudioBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'weak-audio-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/weak-audio-boundary-source.mp4',
          byteSize: 654000,
          name: 'weak-audio-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'weak audio boundary speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        weakAudioBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'weak-audio-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Weak native audio activity evidence must not be trusted. Smart Slice requires a real denoised activity window.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        weakAudioBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        const clip = request.clips[0];
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: [
            {
              index: 0,
              startMs: clip?.startMs,
              durationMs: clip?.durationMs,
              sourceStartMs: clip?.sourceStartMs ?? clip?.startMs,
              sourceEndMs: clip?.sourceEndMs ?? (clip?.startMs ?? 0) + (clip?.durationMs ?? 0),
              audioActivityStartMs: clip?.speechStartMs,
              audioActivityEndMs: clip?.speechEndMs,
              confidence: 0.55,
              analysisFilter: 'silencedetect=noise=-35dB:d=0.08',
            },
          ],
        };
      },
      sliceVideo: async (request) => {
        weakAudioBoundaryCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'weak-audio-boundary-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const weakAudioBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/weak-audio-boundary-source.mp4',
      'weak-audio-boundary-source.mp4',
    );
    await assertDoesNotReject(
      () => withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-weak-audio-boundary',
          file: weakAudioBoundarySourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      ),
      'Smart Slice rejects weak non-audio boundary analysis evidence with a standard error',
    );
    assertEqual(
      weakAudioBoundaryCommands.some((entry) => entry.kind === 'slice'),
      true,
      'Smart Slice renders native slices after weak non-audio boundary analysis fallback',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const outOfRangeAudioBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        outOfRangeAudioBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'out-of-range-audio-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/out-of-range-audio-boundary-source.mp4',
          byteSize: 654000,
          name: 'out-of-range-audio-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'out-of-range audio boundary speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        outOfRangeAudioBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'out-of-range-audio-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Out of range native activity evidence must fail closed. The analysis cannot be clamped into a trusted slice because that would hide a native boundary bug.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        outOfRangeAudioBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        const clip = request.clips[0];
        const analysisFilter = request.applyNoiseReduction === false
          ? 'silencedetect=noise=-35dB:d=0.08'
          : 'highpass=f=80,lowpass=f=12000,afftdn=nr=10:nf=-25,silencedetect=noise=-35dB:d=0.08';
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
          analyses: [
            {
              index: 0,
              startMs: clip?.startMs,
              durationMs: clip?.durationMs,
              sourceStartMs: clip?.sourceStartMs ?? clip?.startMs,
              sourceEndMs: clip?.sourceEndMs ?? (clip?.startMs ?? 0) + (clip?.durationMs ?? 0),
              audioActivityStartMs: (clip?.sourceStartMs ?? clip?.startMs ?? 0) - 1,
              audioActivityEndMs: clip?.speechEndMs,
              confidence: 0.95,
              analysisFilter,
            },
          ],
        };
      },
      sliceVideo: async (request) => {
        outOfRangeAudioBoundaryCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'out-of-range-audio-boundary-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const outOfRangeAudioBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/out-of-range-audio-boundary-source.mp4',
      'out-of-range-audio-boundary-source.mp4',
    );
    const outOfRangeAudioBoundaryResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-out-of-range-audio-boundary',
        file: outOfRangeAudioBoundarySourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    assertEqual(
      outOfRangeAudioBoundaryResult.success,
      true,
      'Smart Slice reports success when out-of-range audio boundary analysis falls back to timeline rendering',
    );
    assertEqual(
      outOfRangeAudioBoundaryCommands.some((entry) => entry.kind === 'slice'),
      true,
      'Smart Slice renders native slices after out-of-range audio boundary analysis fallback',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const missingArrayAudioBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        missingArrayAudioBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'missing-array-audio-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/missing-array-audio-boundary-source.mp4',
          byteSize: 654000,
          name: 'missing-array-audio-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'missing analysis array speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        missingArrayAudioBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'missing-array-audio-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why missing boundary arrays fail is simple. The service requires a complete native analysis envelope before rendering.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        missingArrayAudioBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        return {
          assetUuid: request.assetUuid,
          profile: 'smart-slice-speech-denoise-v1',
        };
      },
      sliceVideo: async (request) => {
        missingArrayAudioBoundaryCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'missing-array-audio-boundary-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const missingArrayAudioBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/missing-array-audio-boundary-source.mp4',
      'missing-array-audio-boundary-source.mp4',
    );
    const missingArrayAudioBoundaryResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-missing-array-audio-boundary',
        file: missingArrayAudioBoundarySourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    assertEqual(
      missingArrayAudioBoundaryResult.success,
      true,
      'Smart Slice reports success when malformed audio boundary envelopes fall back to timeline rendering',
    );
    assertEqual(
      missingArrayAudioBoundaryCommands.some((entry) => entry.kind === 'slice'),
      true,
      'Smart Slice renders native slices after malformed audio boundary analysis envelopes fallback',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const missingAudioBoundaryCapabilityCommands = [];
    services.configureAutoCutNativeHostClient({
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        missingAudioBoundaryCapabilityCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'missing-audio-boundary-capability-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/missing-audio-boundary-capability-source.mp4',
          byteSize: 654000,
          name: 'missing-audio-boundary-capability-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'missing audio boundary capability speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        missingAudioBoundaryCapabilityCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'missing-audio-boundary-capability-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why optional analysis matters is simple. Because speech recognition and native rendering can work without boundary analysis, Smart Slice must still complete.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        missingAudioBoundaryCapabilityCommands.push({ kind: 'analyze-audio-boundaries', request });
        throw new Error('optional audio boundary analysis command must be skipped when capability is unavailable');
      },
      sliceVideo: async (request) => {
        missingAudioBoundaryCapabilityCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'missing-audio-boundary-capability-slice-task',
          request.clips[0]?.outputFileName ?? 'missing-audio-boundary-capability-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'missing-audio-boundary-capability-slice-task',
          'missing-audio-boundary-capability-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'missing-audio-boundary-capability-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `missing-audio-boundary-capability-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `missing-audio-boundary-capability-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 345678 + index,
            thumbnailByteSize: 23456 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const missingAudioBoundaryCapabilitySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/missing-audio-boundary-capability-source.mp4',
      'missing-audio-boundary-capability-source.mp4',
    );
    let missingAudioBoundaryCapabilityResult;
    const missingAudioBoundaryCapabilityDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      missingAudioBoundaryCapabilityResult = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-missing-audio-boundary-capability',
          file: missingAudioBoundaryCapabilitySourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'keyword',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        }),
      );
    });
    const missingAudioBoundaryCapabilityTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === missingAudioBoundaryCapabilityResult?.taskId,
    );
    const missingAudioBoundaryCapabilitySliceCommand = missingAudioBoundaryCapabilityCommands.find(
      (entry) => entry.kind === 'slice',
    );
    assertEqual(
      missingAudioBoundaryCapabilityTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes with transcript timing when audio boundary analysis capability is unavailable',
    );
    assertEqual(
      missingAudioBoundaryCapabilityCommands.some((entry) => entry.kind === 'analyze-audio-boundaries'),
      false,
      'Smart Slice does not call unavailable audio boundary analysis command after failed preflight',
    );
    assertEqual(
      missingAudioBoundaryCapabilitySliceCommand?.kind,
      'slice',
      'Smart Slice renders native slices without optional audio boundary analysis capability',
    );
    assertRule(
      missingAudioBoundaryCapabilityDiagnostics.some((call) =>
        call.level === 'info' &&
          String(call.args?.[0] ?? '').includes('Smart Slice audio boundary analysis skipped')
      ),
      'Smart Slice logs skipped audio boundary analysis capability before native rendering',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const disabledDenoiseBoundaryCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        disabledDenoiseBoundaryCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'disabled-denoise-boundary-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/disabled-denoise-boundary-source.mp4',
          byteSize: 654000,
          name: 'disabled-denoise-boundary-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'disabled denoise boundary speech preflight contract',
      }),
      transcribeMedia: async (request) => {
        disabledDenoiseBoundaryCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'disabled-denoise-boundary-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 8_000,
              endMs: 29_000,
              text:
                'Why noise reduction decision evidence matters is simple. Because the user can disable denoise, Smart Slice must record the real cleanup setting.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        disabledDenoiseBoundaryCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        disabledDenoiseBoundaryCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'disabled-denoise-boundary-slice-task',
          request.clips[0]?.outputFileName ?? 'disabled-denoise-boundary-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'disabled-denoise-boundary-slice-task',
          'disabled-denoise-boundary-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'disabled-denoise-boundary-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `disabled-denoise-boundary-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `disabled-denoise-boundary-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 345678 + index,
            thumbnailByteSize: 23456 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const disabledDenoiseBoundarySourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/disabled-denoise-boundary-source.mp4',
      'disabled-denoise-boundary-source.mp4',
    );
    const disabledDenoiseBoundaryResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-disabled-denoise-boundary',
        file: disabledDenoiseBoundarySourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const disabledDenoiseBoundaryTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === disabledDenoiseBoundaryResult.taskId,
    );
    const disabledDenoiseBoundaryAnalysisCommand = disabledDenoiseBoundaryCommands.find(
      (entry) => entry.kind === 'analyze-audio-boundaries',
    );
    const disabledDenoiseBoundarySliceCommand = disabledDenoiseBoundaryCommands.find(
      (entry) => entry.kind === 'slice',
    );
    assertEqual(
      disabledDenoiseBoundaryAnalysisCommand?.request?.applyNoiseReduction,
      false,
      'Smart Slice honors disabled denoise before audio boundary analysis for clean source audio',
    );
    assertEqual(
      disabledDenoiseBoundarySliceCommand?.request?.noiseReduction,
      false,
      'Smart Slice honors disabled denoise before native rendering for clean source audio',
    );
    assertEqual(
      disabledDenoiseBoundarySliceCommand?.request?.clips?.[0]?.noiseReductionApplied,
      false,
      'Smart Slice native clip evidence records skipped denoise after disabled denoise input',
    );
    assertEqual(
      disabledDenoiseBoundaryTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice completes with clean raw audio cleanup evidence when callers disable denoise',
    );
    assertEqual(
      disabledDenoiseBoundaryTask?.sliceResults?.[0]?.noiseReductionApplied,
      false,
      'Smart Slice exposes completed slice results with skipped noise reduction decision evidence after disabled denoise input',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const noiseCleanupSliceCommands = [];
    const noiseCleanupBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        noiseCleanupSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'noise-cleanup-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/noise-cleanup-source.mp4',
          byteSize: 654000,
          name: 'noise-cleanup-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 40_000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'noise cleanup smart slice contract',
      }),
      transcribeMedia: async (request) => {
        noiseCleanupSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'noise-cleanup-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 8_000,
              text: 'um, Watch the setup because activation pain matters.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8_100,
              endMs: 8_900,
              text: '[coughing]',
              speaker: 'Speaker 1',
            },
            {
              startMs: 9_000,
              endMs: 9_800,
              text: '[Music]',
              speaker: 'Speaker 1',
            },
            {
              startMs: 10_000,
              endMs: 25_000,
              text: 'So the complete payoff is the activation fix viewers can apply.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        noiseCleanupSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        noiseCleanupSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'noise-cleanup-slice-task',
          request.clips[0]?.outputFileName ?? 'noise-cleanup-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'noise-cleanup-slice-task',
          'noise-cleanup-thumb.jpg',
          configuredOutputDirectory,
        );
        const subtitle = createNativeTaskOutputArtifact(
          'noise-cleanup-slice-task',
          'noise-cleanup-subtitle.srt',
          configuredOutputDirectory,
        );
        const clip = request.clips[0];
        return {
          taskUuid: 'noise-cleanup-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'noise-cleanup-slice-artifact',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'noise-cleanup-thumb',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              ...(request.subtitleFormat === 'srt'
                ? {
                    subtitleArtifactUuid: 'noise-cleanup-subtitle',
                    subtitleArtifactPath: subtitle.artifactPath,
                    subtitleByteSize: 1234,
                    subtitleFormat: 'srt',
              }
                : {}),
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-noise-cleanup-plan',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        noiseCleanupBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        return {
          id: 'noise-cleanup-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds: prompt.candidates?.map((candidate) => candidate.id) ?? [],
            referencedUnitIds: prompt.contentUnits?.map((unit) => unit.id) ?? [],
            reviewNotes: ['Clean activation payoff'],
          }),
          runtime,
        };
      },
    });
    const noiseCleanupSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/noise-cleanup-source.mp4',
      'noise-cleanup-source.mp4',
    );
    const noiseCleanupResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-noise-cleanup-slice',
        file: noiseCleanupSourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        continuityLevel: 'standard',
        minDuration: 15,
        maxDuration: 45,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        customKeywords: ['activation', 'payoff'],
        enableNoiseReduction: true,
        enableCoughFilter: false,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'srt',
      }),
    );
    services.resetAutoCutNativeHostClient();
    const noiseCleanupPrompt = JSON.parse(
      noiseCleanupBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    const noiseCleanupSliceRequest = noiseCleanupSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const noiseCleanupClip = noiseCleanupSliceRequest?.clips?.[0];
    const noiseCleanupTask = readScopedStoredArray(services, 'tasks').find((task) => task.id === noiseCleanupResult.taskId);
    assertEqual(
      noiseCleanupResult.success,
      true,
      'noise cleanup Smart Slice regression reports success',
    );
    assertRule(
      noiseCleanupPrompt?.contentUnits?.every((unit) =>
        unit.text &&
          !/\b(?:coughing|music)\b|^\s*um\b/iu.test(unit.text)
      ),
      'noise cleanup Smart Slice regression sends only cleaned content unit text to the Smart Cut Engine LLM reviewer',
    );
    assertRule(
      !/\b(?:coughing|music)\b|^\s*um\b/iu.test(noiseCleanupClip?.transcriptText ?? ''),
      'noise cleanup Smart Slice regression removes noise and edge filler from native clip transcript text',
    );
    assertRule(
      noiseCleanupClip?.transcriptSegments?.length === 2 &&
        noiseCleanupClip.transcriptSegments.every((segment) =>
          !/\b(?:coughing|music)\b|^\s*um\b/iu.test(segment.text)
        ),
      'noise cleanup Smart Slice regression removes noise and edge filler from structured native clip transcript segments',
    );
    assertEqual(
      noiseCleanupClip?.audioMuteRanges,
      undefined,
      'noise cleanup Smart Slice regression respects the disabled cough and noise removal toggle for native audio muting',
    );
    assertRule(
      Array.isArray(noiseCleanupSliceRequest?.subtitleSegments) &&
        noiseCleanupSliceRequest.subtitleSegments.length >= 2 &&
        noiseCleanupSliceRequest.subtitleSegments.every((segment) =>
          !/\b(?:coughing|music)\b|^\s*um\b/iu.test(segment.text)
        ) &&
        joinSubtitleCueCoverageText(noiseCleanupSliceRequest.subtitleSegments).includes(
          'Watch the setup because activation pain matters.',
        ) &&
        joinSubtitleCueCoverageText(noiseCleanupSliceRequest.subtitleSegments).includes(
          'So the complete payoff is the activation fix viewers can apply.',
        ),
      'noise cleanup Smart Slice regression removes noise and edge filler from native subtitle rendering input',
    );
    assertRule(
      noiseCleanupTask?.sliceResults?.every((sliceResult) =>
        !/\b(?:coughing|music)\b|^\s*um\b/iu.test(sliceResult.transcriptText ?? '')
      ),
      'noise cleanup Smart Slice regression exposes clean transcript evidence in completed task results',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const existingAssetVideoSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        existingAssetVideoSliceCommands.push({ kind: 'import', request });
        throw new Error('existing native asset smart slice must not import media again');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'existing asset smart slice contract',
      }),
      transcribeMedia: async (request) => {
        existingAssetVideoSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'existing-asset-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 22_000,
              endMs: 41_000,
              text:
                'Why viewers scroll is simple. Because the opening hides the problem, people do not know why they should care. So lead with the result and the retention payoff works.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 72_000,
              endMs: 93_000,
              text:
                'What fixes weak conversion is clear. The case shows pricing pain before users choose annual plans. So show the solution and the final answer improves signups.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        existingAssetVideoSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        existingAssetVideoSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'existing-asset-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: createNativeTaskOutputArtifact(
            'existing-asset-slice-task',
            request.clips[0]?.outputFileName ?? 'existing-asset-slice-1.mp4',
            configuredOutputDirectory,
          ).taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'existing-asset-slice-task',
              clip.outputFileName ?? `existing-asset-slice-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'existing-asset-slice-task',
              `existing-asset-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `existing-asset-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `existing-asset-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 456789 + index,
              thumbnailByteSize: 34567 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const existingAssetVideoSliceResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'media-asset-existing-smart-slice-source',
        sourceDurationMs: 100_000,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const existingAssetVideoSliceTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === existingAssetVideoSliceResult.taskId,
    );
    assertEqual(
      existingAssetVideoSliceResult.success,
      true,
      'existing native asset smart slice rerun reports success after rebuilding transcript evidence',
    );
    assertEqual(
      existingAssetVideoSliceCommands.some((entry) => entry.kind === 'import'),
      false,
      'existing native asset smart slice rerun does not import an already registered native asset',
    );
    assertEqual(
      existingAssetVideoSliceCommands[0]?.kind,
      'transcribe',
      'existing native asset smart slice rerun transcribes before planning and rendering',
    );
    assertEqual(
      existingAssetVideoSliceCommands[0]?.request?.assetUuid,
      'media-asset-existing-smart-slice-source',
      'existing native asset smart slice rerun transcribes the selected native asset UUID',
    );
    assertEqual(
      existingAssetVideoSliceCommands[1]?.kind,
      'analyze-audio-boundaries',
      'existing native asset smart slice rerun analyzes audio activity boundaries after transcript planning',
    );
    assertEqual(
      existingAssetVideoSliceCommands[1]?.request?.assetUuid,
      'media-asset-existing-smart-slice-source',
      'existing native asset smart slice rerun analyzes boundaries by selected native asset UUID',
    );
    assertEqual(
      existingAssetVideoSliceCommands[1]?.request?.profile,
      'smart-slice-speech-denoise-v1',
      'existing native asset smart slice rerun uses the canonical cleanup profile for boundary analysis',
    );
    assertEqual(
      existingAssetVideoSliceCommands[2]?.kind,
      'slice',
      'existing native asset smart slice rerun renders only after verified transcript planning',
    );
    assertEqual(
      existingAssetVideoSliceCommands[2]?.request?.assetUuid,
      'media-asset-existing-smart-slice-source',
      'existing native asset smart slice rerun slices the selected native asset UUID',
    );
    assertRule(
      existingAssetVideoSliceCommands[2]?.request?.clips?.every((clip) =>
        clip.transcriptText?.trim() &&
          clip.transcriptSegments?.length &&
          clip.transcriptSegmentCount === clip.transcriptSegments.length
      ),
      'existing native asset smart slice rerun sends verified transcript evidence on every native clip request',
    );
    assertEqual(
      existingAssetVideoSliceTask?.sourceFileId,
      'media-asset-existing-smart-slice-source',
      'existing native asset smart slice rerun stores the selected asset UUID as task sourceFileId',
    );
    assertRule(
      existingAssetVideoSliceTask?.sliceResults?.every((sliceResult) =>
        sliceResult.transcriptText?.trim() && sliceResult.transcriptSegments?.length
      ),
      'existing native asset smart slice rerun exposes verified transcript evidence on every generated slice',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const overlappingTranscriptSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        overlappingTranscriptSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'overlap-repair-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/overlap-repair-slice-asset.mp4',
          byteSize: 654000,
          name: 'overlap-repair-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 60000,
        };
      },
      transcribeMedia: async (request) => {
        overlappingTranscriptSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'overlap-repair-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 10000,
              endMs: 25000,
              text: 'Why overlap repair matters is simple. Because speech-to-text segments can overlap, the transcript must stay ordered.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 24960,
              endMs: 41000,
              text: 'So trim the second segment boundary and the smart slice remains publishable.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'overlap repair speech preflight contract',
      }),
      sliceVideo: async (request) => {
        overlappingTranscriptSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'overlap-repair-slice-task',
          request.clips[0]?.outputFileName ?? 'overlap-repair-slice-1.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'overlap-repair-slice-task',
          'overlap-repair-slice-1.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'overlap-repair-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'overlap-repair-slice-artifact-1',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'overlap-repair-slice-thumb-1',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(request.clips[0], request.noiseReduction),
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const overlappingTranscriptSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/overlap-repair-source.mp4',
      'overlap-repair-source.mp4',
    );
    const overlappingTranscriptResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-overlap-repair-slice',
        file: overlappingTranscriptSourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const overlappingTranscriptTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === overlappingTranscriptResult.taskId,
    );
    assertEqual(
      overlappingTranscriptResult.success,
      true,
      'video slice workflow repairs lightly overlapping speech-to-text segments instead of failing after native rendering',
    );
    assertEqual(
      overlappingTranscriptSliceCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.[0]?.transcriptSegments?.[1]?.startMs,
      25000,
      'video slice workflow trims overlapped STT segment starts before sending native clip transcript evidence',
    );
    assertEqual(
      overlappingTranscriptTask?.sliceResults?.[0]?.transcriptSegments?.[1]?.startMs,
      25000,
      'video slice workflow stores non-overlapping slice transcript segments after STT overlap repair',
    );
    assertEqual(
      overlappingTranscriptTask?.sliceResults?.[0]?.transcriptText,
      'Why overlap repair matters is simple. Because speech-to-text segments can overlap, the transcript must stay ordered. So trim the second segment boundary and the smart slice remains publishable.',
      'video slice workflow preserves complete transcript text after STT overlap repair',
    );

    resetStorage();
    const explicitDisabledSubtitleSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        explicitDisabledSubtitleSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'explicit-disabled-subtitle-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/explicit-disabled-subtitle-source.mp4',
          byteSize: 654000,
          name: 'explicit-disabled-subtitle-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      transcribeMedia: async (request) => {
        explicitDisabledSubtitleSliceCommands.push({ kind: 'transcribe', request });
        return {
          artifactUuid: 'explicit-disabled-subtitle-transcript-artifact',
          taskUuid: 'explicit-disabled-subtitle-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: 'D:/autocut-configured-output/tasks/explicit-disabled-subtitle-transcript-task/transcript.json',
          taskOutputDir: 'D:/autocut-configured-output/tasks/explicit-disabled-subtitle-transcript-task',
          language: request.language ?? 'auto',
          text:
            'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
          segments: [
            {
              startMs: 5_000,
              endMs: 24_000,
              text:
                'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'explicit disabled subtitle speech preflight contract',
      }),
      sliceVideo: async (request) => {
        explicitDisabledSubtitleSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'explicit-disabled-subtitle-slice-task',
          request.clips[0]?.outputFileName ?? '01-why-subtitle-overlays-matter-is-simple-because.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'explicit-disabled-subtitle-slice-task',
          'explicit-disabled-subtitle-thumb-1.jpg',
          configuredOutputDirectory,
        );
        const subtitle = createNativeTaskOutputArtifact(
          'explicit-disabled-subtitle-slice-task',
          'explicit-disabled-subtitle-1.srt',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'explicit-disabled-subtitle-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.slice(0, 1).map((clip) => ({
            artifactUuid: 'explicit-disabled-subtitle-slice-artifact',
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: 'explicit-disabled-subtitle-thumb-artifact',
            thumbnailArtifactPath: thumbnail.artifactPath,
            subtitleArtifactUuid: 'explicit-disabled-subtitle-sidecar-artifact',
            subtitleArtifactPath: subtitle.artifactPath,
            subtitleByteSize: 345,
            subtitleFormat: 'srt',
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567,
            thumbnailByteSize: 12345,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const explicitDisabledSubtitleSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/explicit-disabled-subtitle-source.mp4',
      'explicit-disabled-subtitle-source.mp4',
    );
    await assertRejects(
      () => withImmediateTimers(async () =>
        processVideoSlice({
          fileId: 'asset-source-explicit-disabled-subtitle-slice',
          file: explicitDisabledSubtitleSourceFile,
          mode: 'contract-mode',
          llmModel: 'deepseek-v4-flash',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
          subtitleMode: 'none',
        }),
      ),
      'subtitle artifact was returned even though subtitle rendering was not requested',
      'video slice workflow rejects native subtitle artifacts when subtitles are explicitly disabled',
    );
    const explicitDisabledSubtitleSliceRequest =
      explicitDisabledSubtitleSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const explicitDisabledSubtitleFailureTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      explicitDisabledSubtitleSliceCommands[1]?.kind,
      'transcribe',
      'video slice workflow transcribes even when a stale caller submits enableSubtitles false',
    );
    assertEqual(
      explicitDisabledSubtitleSliceRequest?.subtitleMode,
      undefined,
      'video slice workflow honors disabled subtitle requests instead of promoting them to burned overlays',
    );
    assertEqual(
      explicitDisabledSubtitleSliceRequest?.subtitleSegments,
      undefined,
      'video slice workflow does not send transcript segments to native rendering when subtitles are disabled',
    );
    assertEqual(
      explicitDisabledSubtitleFailureTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice workflow marks the task failed when native returns unrequested subtitle artifacts',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length,
      0,
      'video slice workflow does not persist video assets after unrequested native subtitle artifacts',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const contradictorySubtitleSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        contradictorySubtitleSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'contradictory-subtitle-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/contradictory-subtitle-source.mp4',
          byteSize: 654000,
          name: 'contradictory-subtitle-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      transcribeMedia: async (request) => {
        contradictorySubtitleSliceCommands.push({ kind: 'transcribe', request });
        return {
          artifactUuid: 'contradictory-subtitle-transcript-artifact',
          taskUuid: 'contradictory-subtitle-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: 'D:/autocut-configured-output/tasks/contradictory-subtitle-transcript-task/transcript.json',
          taskOutputDir: 'D:/autocut-configured-output/tasks/contradictory-subtitle-transcript-task',
          language: request.language ?? 'auto',
          text:
            'Why subtitle controls must be explicit is simple. Because hidden none modes silently skip output, the request must fail before rendering.',
          segments: [
            {
              startMs: 5_000,
              endMs: 24_000,
              text:
                'Why subtitle controls must be explicit is simple. Because hidden none modes silently skip output, the request must fail before rendering.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'contradictory subtitle speech preflight contract',
      }),
      sliceVideo: async (request) => {
        contradictorySubtitleSliceCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'contradictory-subtitle-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const contradictorySubtitleSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/contradictory-subtitle-source.mp4',
      'contradictory-subtitle-source.mp4',
    );
    const contradictorySubtitleResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-contradictory-subtitle-slice',
        file: contradictorySubtitleSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'none',
      }),
    );
    const contradictorySubtitleTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      contradictorySubtitleResult.success,
      true,
      'video slice workflow reports success when contradictory subtitle parameters fall back to no subtitles',
    );
    assertRule(
      contradictorySubtitleSliceCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders when subtitle enablement parameters fall back to no subtitles',
    );
    assertEqual(
      contradictorySubtitleTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice workflow persists a completed task for contradictory subtitle enablement fallback',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length > 0,
      true,
      'video slice workflow persists assets for contradictory subtitle enablement fallback',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const enabledSubtitleSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        enabledSubtitleSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'enabled-subtitle-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/enabled-subtitle-source.mp4',
          byteSize: 654000,
          name: 'enabled-subtitle-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      transcribeMedia: async (request) => {
        enabledSubtitleSliceCommands.push({ kind: 'transcribe', request });
        return {
          artifactUuid: 'enabled-subtitle-transcript-artifact',
          taskUuid: 'enabled-subtitle-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: 'D:/autocut-configured-output/tasks/enabled-subtitle-transcript-task/transcript.json',
          taskOutputDir: 'D:/autocut-configured-output/tasks/enabled-subtitle-transcript-task',
          language: request.language ?? 'auto',
          text:
            'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
          segments: [
            {
              startMs: 5_000,
              endMs: 24_000,
              text:
                'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'enabled subtitle speech preflight contract',
      }),
      sliceVideo: async (request) => {
        enabledSubtitleSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'enabled-subtitle-slice-task',
          request.clips[0]?.outputFileName ?? '01-why-subtitle-overlays-matter-is-simple-because.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'enabled-subtitle-slice-task',
          'enabled-subtitle-thumb-1.jpg',
          configuredOutputDirectory,
        );
        const subtitle = createNativeTaskOutputArtifact(
          'enabled-subtitle-slice-task',
          'enabled-subtitle-1.srt',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'enabled-subtitle-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.slice(0, 1).map((clip) => ({
            artifactUuid: 'enabled-subtitle-slice-artifact',
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: 'enabled-subtitle-thumb-artifact',
            thumbnailArtifactPath: thumbnail.artifactPath,
            subtitleArtifactUuid: 'enabled-subtitle-sidecar-artifact',
            subtitleArtifactPath: subtitle.artifactPath,
            subtitleByteSize: 345,
            subtitleFormat: 'srt',
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567,
            thumbnailByteSize: 12345,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const enabledSubtitleSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/enabled-subtitle-source.mp4',
      'enabled-subtitle-source.mp4',
    );
    const enabledSubtitleResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-enabled-subtitle-slice',
        file: enabledSubtitleSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'srt',
      }),
    );
    const enabledSubtitleSliceRequest =
      enabledSubtitleSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const enabledSubtitleTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === enabledSubtitleResult.taskId,
    );
    assertEqual(
      enabledSubtitleSliceRequest?.subtitleFormat,
      'srt',
      'video slice workflow requests SRT subtitles only when subtitles are enabled',
    );
    assertEqual(
      enabledSubtitleSliceRequest?.subtitleMode,
      'srt',
      'video slice workflow preserves the enabled subtitle mode for native rendering',
    );
    assertEqual(
      joinSubtitleCueCoverageText(enabledSubtitleSliceRequest?.subtitleSegments),
      'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
      'video slice workflow forwards complete real speech-to-text coverage when subtitle generation is enabled',
    );
    assertRule(
      (enabledSubtitleSliceRequest?.subtitleSegments?.length ?? 0) > 1 &&
        enabledSubtitleSliceRequest?.subtitleSegments?.every((segment) =>
          normalizeSubtitleCueText(segment.text).length <= 52 &&
          segment.endMs > segment.startMs
        ),
      'video slice workflow sends paced subtitle cues instead of one long transcript overlay when subtitle generation is enabled',
    );
    assertEqual(
      enabledSubtitleTask?.sliceResults?.[0]?.subtitleUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fenabled-subtitle-slice-task%2Fenabled-subtitle-1.srt',
      'video slice workflow exposes subtitle sidecar URLs when subtitle generation is enabled',
    );
    assertEqual(
      enabledSubtitleTask?.sliceResults?.[0]?.transcriptText,
      'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
      'video slice workflow exposes speech-to-text transcript text alongside enabled subtitle artifacts',
    );
    assertEqual(
      enabledSubtitleTask?.sliceResults?.[0]?.transcriptSegments?.[0]?.text,
      'Why subtitle overlays matter is simple. Because viewers watch without sound, the problem is easy to miss. So show the result in speech text and the solution works.',
      'video slice workflow exposes structured speech-to-text transcript segments alongside enabled subtitle artifacts',
    );
    services.resetAutoCutNativeHostClient();

    const longWhisperSourceDurationMs = 480_000;
    const longWhisperSegments = [
      {
        startMs: 120,
        endMs: 12_400,
        text:
          'Why the opening must start immediately is simple. The first sentence states the result before any silence and gives viewers the clear payoff.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 12_420,
        endMs: 24_800,
        text:
          'Speaker two answers with context, explains the problem, and connects the setup so the first publishable clip stays complete.',
        speaker: 'Speaker 2',
      },
      {
        startMs: 210_000,
        endMs: 222_000,
        text:
          'Why the pricing pain matters is simple. Because retention drops before the team changes the workflow, the solution must be shown clearly.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 221_900,
        endMs: 235_000,
        text:
          'So speaker two interrupts quickly, but the answer completes the refund fix and gives the viewer the final result.',
        speaker: 'Speaker 2',
      },
      {
        startMs: 300_000,
        endMs: 314_000,
        text:
          'Why the operations example matters is simple. Because the launch had a concrete conflict, the practical result becomes easy to trust.',
        speaker: 'Speaker 3',
      },
      {
        startMs: 314_050,
        endMs: 329_000,
        text:
          'So speaker one closes that discussion with the final answer and a publishable takeaway for the edit.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 460_000,
        endMs: 472_000,
        text:
          'Why the final lesson matters is simple. Because long videos often trail off, the closing clip must keep the problem clear.',
        speaker: 'Speaker 2',
      },
      {
        startMs: 472_020,
        endMs: 479_750,
        text: 'So the last sentence lands before the video ends, gives the result, and keeps the boundary tight.',
        speaker: 'Speaker 1',
      },
    ];

    async function runLongWhisperVideoSliceRegression({ taskPrefix, enableSubtitles, subtitleMode }) {
      resetStorage();
      await services.saveAutoCutWorkspaceSettings({
        ...(await services.getAutoCutSettings()).workspace,
        outputDirectory: configuredOutputDirectory,
      });
      await saveVerifiedLocalSpeechTranscriptionSettings(services);

      const commands = [];
      const sliceTaskUuid = `${taskPrefix}-slice-task`;
      configureAutoCutNativeHostClient(services, {
        getCapabilities: async () => ({
          mediaImportCommandReady: true,
          videoSliceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          nativeTaskQueryCommandReady: true,
        }),
        importMediaFile: async (request) => {
          commands.push({ kind: 'import', request });
          return {
            assetUuid: `${taskPrefix}-asset`,
            sandboxPath: `${configuredOutputDirectory}/inputs/${taskPrefix}-asset.mp4`,
            byteSize: 4_800_000,
            name: `${taskPrefix}-source.mp4`,
            mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
            mimeType: 'video/mp4',
            durationMs: longWhisperSourceDurationMs,
          };
        },
        transcribeMedia: async (request) => {
          commands.push({ kind: 'transcribe', request });
          return createNativeVideoSliceTranscriptResult(
            request,
            `${taskPrefix}-transcript-task`,
            configuredOutputDirectory,
            longWhisperSegments,
          );
        },
        probeSpeechTranscription: async (request) => {
          commands.push({ kind: 'speech-preflight', request });
          return {
            ready: true,
            executablePath: request.executablePath,
            modelPath: request.modelPath,
            sourceKind: request.sourceKind ?? 'execution-preflight',
            diagnostics: [],
            versionLine: 'long local Whisper regression preflight contract',
          };
        },
        sliceVideo: async (request) => {
          commands.push({ kind: 'slice', request });
          const subtitlesRequested =
            request.subtitleFormat === 'srt' &&
            request.subtitleMode !== 'none' &&
            Array.isArray(request.subtitleSegments) &&
            request.subtitleSegments.length > 0;
          const slices = request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              sliceTaskUuid,
              clip.outputFileName ?? `${taskPrefix}-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              sliceTaskUuid,
              `${taskPrefix}-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            const subtitle = createNativeTaskOutputArtifact(
              sliceTaskUuid,
              `${taskPrefix}-slice-subtitle-${sliceNumber}.srt`,
              configuredOutputDirectory,
            );

            return {
              artifactUuid: `${taskPrefix}-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `${taskPrefix}-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              ...(subtitlesRequested
                ? {
                    subtitleArtifactUuid: `${taskPrefix}-slice-subtitle-artifact-${sliceNumber}`,
                    subtitleArtifactPath: subtitle.artifactPath,
                    subtitleByteSize: 1_024 + index,
                    subtitleFormat: 'srt',
                  }
                : {}),
              taskOutputDir: output.taskOutputDir,
              byteSize: 240_000 + index,
              thumbnailByteSize: 12_000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          });

          return {
            taskUuid: sliceTaskUuid,
            sourceAssetUuid: request.assetUuid,
            taskOutputDir: `${configuredOutputDirectory}/tasks/${sliceTaskUuid}`,
            ffmpegExecutable: 'ffmpeg',
            slices,
          };
        },
        createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
      });

      const sourceFile = createTrustedLocalMediaFile(
        commons,
        `D:/media/${taskPrefix}-long-whisper-source.mp4`,
        `${taskPrefix}-long-whisper-source.mp4`,
        {
          byteSize: 4_800_000,
          mimeType: 'video/mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        },
      );
      const result = await withImmediateTimers(async () =>
        processVideoSlice({
          fileId: `asset-source-${taskPrefix}-long-whisper-slice`,
          file: sourceFile,
          mode: 'contract-mode',
          llmModel: 'gemini-3-flash-preview',
          targetPlatform: 'generic',
          idealDuration: 25,
          continuityLevel: 'standard',
          minDuration: 10,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles,
          subtitleMode,
        }),
      );
      const task = readScopedStoredArray(services, 'tasks').find((storedTask) => storedTask.id === result.taskId);
      const assets = readScopedStoredArray(services, 'assets');
      services.resetAutoCutNativeHostClient();
      return { commands, result, task, assets };
    }

    const disabledLongWhisperRegression = await runLongWhisperVideoSliceRegression({
      taskPrefix: 'long-whisper-disabled',
      enableSubtitles: false,
      subtitleMode: 'none',
    });
    const disabledLongWhisperTranscribeRequest =
      disabledLongWhisperRegression.commands.find((entry) => entry.kind === 'transcribe')?.request;
    const disabledLongWhisperSliceRequest =
      disabledLongWhisperRegression.commands.find((entry) => entry.kind === 'slice')?.request;
    const disabledLongWhisperOpeningClip = disabledLongWhisperSliceRequest?.clips?.find((clip) =>
      clip.transcriptText?.includes('opening must start immediately')
    );
    const disabledLongWhisperInterruptionClip = disabledLongWhisperSliceRequest?.clips?.find((clip) =>
      clip.transcriptText?.includes('interrupts quickly')
    );
    const disabledLongWhisperClosingClip = disabledLongWhisperSliceRequest?.clips?.find((clip) =>
      clip.transcriptText?.includes('last sentence lands')
    );
    const disabledLongWhisperSpeakers = new Set(
      disabledLongWhisperSliceRequest?.clips
        ?.flatMap((clip) => clip.transcriptSegments ?? [])
        .map((segment) => segment.speaker)
        .filter(Boolean) ?? [],
    );
    assertEqual(
      disabledLongWhisperRegression.result.success,
      true,
      'long local Whisper regression reports success when subtitles are disabled',
    );
    assertEqual(
      disabledLongWhisperTranscribeRequest?.executablePath,
      'D:/tools/whisper-cli.exe',
      'long local Whisper regression uses the configured offline Whisper executable',
    );
    assertEqual(
      disabledLongWhisperTranscribeRequest?.modelPath,
      'D:/models/ggml-large-v3-turbo.bin',
      'long local Whisper regression uses the configured offline large-v3-turbo model',
    );
    assertEqual(
      disabledLongWhisperSliceRequest?.subtitleFormat,
      undefined,
      'long local Whisper regression omits subtitle format when subtitles are disabled',
    );
    assertEqual(
      disabledLongWhisperSliceRequest?.subtitleMode,
      undefined,
      'long local Whisper regression omits subtitle mode when subtitles are disabled',
    );
    assertEqual(
      disabledLongWhisperSliceRequest?.subtitleSegments,
      undefined,
      'long local Whisper regression does not send subtitle segments when subtitles are disabled',
    );
    assertRule(
      disabledLongWhisperSpeakers.has('Speaker 1') &&
        disabledLongWhisperSpeakers.has('Speaker 2') &&
        disabledLongWhisperSpeakers.has('Speaker 3'),
      'long local Whisper regression preserves multi-speaker transcript evidence in native clip requests',
    );
    assertEqual(
      disabledLongWhisperOpeningClip?.sourceStartMs,
      0,
      'long local Whisper regression clamps the opening clip to the beginning instead of adding leading silence',
    );
    assertEqual(
      disabledLongWhisperOpeningClip?.speechStartMs,
      120,
      'long local Whisper regression keeps the true opening speech boundary',
    );
    assertNumberBetween(
      disabledLongWhisperOpeningClip?.boundaryPaddingBeforeMs,
      0,
      200,
      'long local Whisper regression keeps opening leading padding inside the professional standard',
    );
    assertEqual(
      disabledLongWhisperClosingClip?.sourceEndMs,
      longWhisperSourceDurationMs,
      'long local Whisper regression clamps the closing clip to the video end without exceeding duration',
    );
    assertEqual(
      disabledLongWhisperClosingClip?.speechEndMs,
      479_750,
      'long local Whisper regression keeps the true closing speech boundary',
    );
    assertNumberBetween(
      disabledLongWhisperClosingClip?.boundaryPaddingAfterMs,
      0,
      250,
      'long local Whisper regression keeps closing trailing padding inside the professional standard',
    );
    assertSegmentsOrderedAndNonOverlapping(
      disabledLongWhisperInterruptionClip?.transcriptSegments,
      'long local Whisper regression repairs fast-interruption overlap in clip transcript evidence',
    );
    assertEqual(
      disabledLongWhisperInterruptionClip?.transcriptSegments?.find((segment) =>
        segment.text.includes('interrupts quickly')
      )?.startMs,
      222_000,
      'long local Whisper regression trims a lightly overlapping interruption segment to the previous speech end',
    );
    assertRule(
      disabledLongWhisperRegression.task?.sliceResults?.every((sliceResult) =>
        sliceResult.transcriptText?.trim() &&
        sliceResult.transcriptSegments?.length &&
        sliceResult.subtitleUrl === undefined
      ),
      'long local Whisper regression exposes transcript evidence on every slice while subtitles stay disabled',
    );

    const enabledLongWhisperRegression = await runLongWhisperVideoSliceRegression({
      taskPrefix: 'long-whisper-enabled',
      enableSubtitles: true,
      subtitleMode: 'srt',
    });
    const enabledLongWhisperSliceRequest =
      enabledLongWhisperRegression.commands.find((entry) => entry.kind === 'slice')?.request;
    const enabledLongWhisperSubtitleSpeakers = new Set(
      enabledLongWhisperSliceRequest?.subtitleSegments
        ?.map((segment) => segment.speaker)
        .filter(Boolean) ?? [],
    );
    assertEqual(
      enabledLongWhisperRegression.result.success,
      true,
      'long local Whisper regression reports success when SRT subtitles are enabled',
    );
    assertEqual(
      enabledLongWhisperSliceRequest?.subtitleFormat,
      'srt',
      'long local Whisper regression requests SRT subtitle output only after explicit enablement',
    );
    assertEqual(
      enabledLongWhisperSliceRequest?.subtitleMode,
      'srt',
      'long local Whisper regression preserves explicit SRT subtitle mode',
    );
    assertRule(
      (enabledLongWhisperSliceRequest?.subtitleSegments?.length ?? 0) >= longWhisperSegments.length &&
        longWhisperSegments.every((segment) =>
          joinSubtitleCueCoverageText(enabledLongWhisperSliceRequest?.subtitleSegments).includes(
            normalizeSubtitleCueCoverageText(segment.text),
          )
        ),
      'long local Whisper regression forwards the complete long-video Whisper transcript coverage to subtitle rendering',
    );
    assertRule(
      enabledLongWhisperSliceRequest?.subtitleSegments?.every((segment) =>
        countSubtitleCueDisplayUnits(segment.text) <= 84 &&
        segment.endMs > segment.startMs &&
        segment.endMs - segment.startMs <= 3600
      ),
      'long local Whisper regression forwards paced short subtitle cues for long-video transcript rendering',
    );
    assertRule(
      enabledLongWhisperSubtitleSpeakers.has('Speaker 1') &&
        enabledLongWhisperSubtitleSpeakers.has('Speaker 2') &&
        enabledLongWhisperSubtitleSpeakers.has('Speaker 3'),
      'long local Whisper regression preserves speaker labels in subtitle segments',
    );
    assertSegmentsOrderedAndNonOverlapping(
      enabledLongWhisperSliceRequest?.subtitleSegments,
      'long local Whisper regression repairs fast-interruption overlap before native subtitle rendering',
    );
    assertRule(
      enabledLongWhisperSliceRequest?.subtitleSegments?.some((segment) =>
        segment.startMs === 222_000 &&
        joinSubtitleCueCoverageText([segment]).includes('interrupts')
      ),
      'long local Whisper regression sends native subtitle rendering a repaired interruption boundary',
    );
    assertRule(
      enabledLongWhisperRegression.task?.sliceResults?.every((sliceResult) =>
        sliceResult.subtitleUrl?.endsWith('.srt') &&
        sliceResult.transcriptText?.trim() &&
        sliceResult.transcriptSegments?.length
      ),
      'long local Whisper regression exposes subtitle sidecars and transcript evidence on every enabled slice',
    );
    assertRule(
      enabledLongWhisperRegression.assets.length === enabledLongWhisperRegression.task?.sliceResults?.length,
      'long local Whisper regression stores one generated video asset per long-video slice',
    );

    const burnedLongWhisperRegression = await runLongWhisperVideoSliceRegression({
      taskPrefix: 'long-whisper-burned',
      enableSubtitles: true,
      subtitleMode: 'burned',
    });
    const burnedLongWhisperSliceRequest =
      burnedLongWhisperRegression.commands.find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      burnedLongWhisperRegression.result.success,
      true,
      'long local Whisper regression reports success when burned subtitles are enabled',
    );
    assertEqual(
      burnedLongWhisperSliceRequest?.subtitleFormat,
      'srt',
      'burned subtitle rendering still requests SRT subtitle sidecar generation',
    );
    assertEqual(
      burnedLongWhisperSliceRequest?.subtitleMode,
      'burned',
      'burned subtitle rendering preserves the explicit burned overlay mode',
    );
    assertRule(
      burnedLongWhisperRegression.task?.sliceResults?.every((sliceResult) =>
        sliceResult.subtitleUrl?.endsWith('.srt') &&
        sliceResult.transcriptText?.trim() &&
        sliceResult.transcriptSegments?.length
      ),
      'burned subtitle rendering exposes an editable SRT sidecar for every generated slice',
    );

    assertRule(
      nativeVideoSliceWorkflowCommands[2]?.request?.clips?.length > 0 &&
        nativeVideoSliceWorkflowCommands[2]?.request?.clips?.length <= 5,
      'video slice native workflow creates a quality-first bounded intelligent slice plan',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.clips?.[0]?.startMs,
      21800,
      'video slice native workflow adds leading boundary padding before the first intelligent clip render timing',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.clips?.[0]?.label,
      'Why viewers scroll is simple. Because the opening hides ',
      'video slice native workflow uses local speech transcript text as the semantic clip label',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.clips?.[0]?.outputFileName,
      '01-why-viewers-scroll-is-simple-because-the-opening-hides.mp4',
      'video slice native workflow sends a deterministic title-based output file name for the first smart slice',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[2]?.request?.clips?.[1]?.outputFileName,
      '02-what-fixes-weak-conversion-is-clear-the-case-shows-pric.mp4',
      'video slice native workflow sends a deterministic title-based output file name for the second smart slice',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice native workflow completes the persisted task',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.resultCount,
      2,
      'video slice native workflow records the generated slice count from native artifacts',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.length,
      2,
      'video slice native workflow persists every generated smart slice instead of collapsing to a single clip',
    );
    assertEqual(
      nativeVideoSliceWorkflowCommands[3]?.request?.clips?.length,
      2,
      'video slice native workflow sends every transcript-derived clip to native rendering instead of rendering only one',
    );
    for (const stepId of [
      'prepare-source',
      'speech-to-text',
      'plan-clips',
      'analyze-audio-boundaries',
      'native-render',
      'verify-artifacts',
      'persist-results',
    ]) {
      assertRule(
        nativeVideoSliceWorkflowTask?.executionSteps?.some((step) =>
          step.id === stepId &&
            step.status === 'completed' &&
            typeof step.startedAt === 'string' &&
            typeof step.completedAt === 'string'
        ),
        `video slice native workflow persists completed Smart Slice execution step ${stepId}`,
      );
      assertRule(
        nativeVideoSliceWorkflowTask?.executionLogs?.some((log) =>
          log.stepId === stepId && log.eventType === 'step-started'
        ) &&
          nativeVideoSliceWorkflowTask?.executionLogs?.some((log) =>
            log.stepId === stepId && log.eventType === 'step-completed'
          ),
        `video slice native workflow persists queryable Smart Slice start and completion logs for ${stepId}`,
      );
    }
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.url,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fnative-slice-task%2F01-why-viewers-scroll-is-simple-because-the-opening-hides.mp4',
      'video slice native workflow converts first native artifact path to a safe asset URL',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.artifactPath,
      'D:/autocut-configured-output/tasks/native-slice-task/01-why-viewers-scroll-is-simple-because-the-opening-hides.mp4',
      'video slice native workflow persists the first native artifact path for open-containing-folder actions',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.taskOutputDir,
      'D:/autocut-configured-output/tasks/native-slice-task',
      'video slice native workflow persists the native task output directory for artifact containment audits',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.name,
      '01-why-viewers-scroll-is-simple-because-the-opening-hides.mp4',
      'video slice native workflow names the first task slice result from clear speech content',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.thumbnailUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fnative-slice-task%2Fcover%2Fnative-slice-thumb-1.jpg',
      'video slice native workflow converts first native cover thumbnail path to a safe asset URL',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.subtitleUrl,
      undefined,
      'video slice native workflow does not expose subtitle sidecar URLs when subtitles are disabled',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.subtitleFormat,
      undefined,
      'video slice native workflow does not record subtitle format when subtitles are disabled',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[1]?.url,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fnative-slice-task%2F02-what-fixes-weak-conversion-is-clear-the-case-shows-pric.mp4',
      'video slice native workflow converts second native artifact path to a safe asset URL',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[1]?.artifactPath,
      'D:/autocut-configured-output/tasks/native-slice-task/02-what-fixes-weak-conversion-is-clear-the-case-shows-pric.mp4',
      'video slice native workflow persists the second native artifact path for open-containing-folder actions',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[1]?.name,
      '02-what-fixes-weak-conversion-is-clear-the-case-shows-pric.mp4',
      'video slice native workflow names the second task slice result from clear speech content',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[1]?.thumbnailUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fnative-slice-task%2Fcover%2Fnative-slice-thumb-2.jpg',
      'video slice native workflow converts second native cover thumbnail path to a safe asset URL',
    );
    assertEqual(
      nativeVideoSliceWorkflowTask?.sliceResults?.[1]?.subtitleUrl,
      undefined,
      'video slice native workflow does not expose second subtitle sidecar URL when subtitles are disabled',
    );
    assertEqual(
      nativeVideoSliceGeneratedAssets.length,
      2,
      'video slice native workflow stores one generated asset per native slice artifact',
    );
    assertEqual(
      nativeVideoSliceFirstAsset?.type,
      'video',
      'video slice native workflow stores generated slices as video assets',
    );
    assertEqual(
      nativeVideoSliceFirstAsset?.size,
      234567,
      'video slice native workflow stores the first native slice byte size on the generated asset',
    );
    assertEqual(
      nativeVideoSliceFirstAsset?.url,
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.url,
      'video slice native workflow stores safe asset URLs on generated assets',
    );
    assertEqual(
      nativeVideoSliceFirstAsset?.artifactPath,
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.artifactPath,
      'video slice native workflow stores native artifact paths on generated assets for task file folder opening',
    );
    assertEqual(
      nativeVideoSliceFirstAsset?.thumbnailUrl,
      nativeVideoSliceWorkflowTask?.sliceResults?.[0]?.thumbnailUrl,
      'video slice native workflow stores safe thumbnail URLs on generated assets',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-single-selection-llm-plan',
      temperature: 0.2,
      maxTokens: 4096,
      defaultSegmentationAgentId: 'dialogue-turn-agent',
    });
    const singleSelectionLlmCommands = [];
    const singleSelectionLlmBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        singleSelectionLlmCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'single-selection-llm-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/single-selection-llm-asset.mp4`,
          byteSize: 765000,
          name: 'single-selection-llm-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'single selection LLM multi clip regression',
      }),
      transcribeMedia: async (request) => {
        singleSelectionLlmCommands.push({ kind: 'transcribe', request });
        return createDialogueTwoQuestionNativeVideoSliceTranscriptResult(
          request,
          'single-selection-llm-transcript-task',
          configuredOutputDirectory,
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        singleSelectionLlmCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        singleSelectionLlmCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'single-selection-llm-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/single-selection-llm-slice-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'single-selection-llm-slice-task',
              clip.outputFileName ?? `single-selection-llm-slice-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'single-selection-llm-slice-task',
              `single-selection-llm-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `single-selection-llm-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `single-selection-llm-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567 + index,
              thumbnailByteSize: 12345 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    let singleSelectionLlmPrompt;
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        singleSelectionLlmBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        singleSelectionLlmPrompt = prompt;
        return {
          id: 'single-selection-llm-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds: prompt.candidates?.slice(0, 1).map((candidate) => candidate.id) ?? [],
            referencedUnitIds: prompt.contentUnits?.slice(0, 1).map((unit) => unit.id) ?? [],
            reviewNotes: ['Regression fixture intentionally omits the second strong content group.'],
          }),
          runtime,
        };
      },
    });
    const singleSelectionLlmResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-single-selection-llm-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/single-selection-llm-source.mp4',
          'single-selection-llm-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 20,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const singleSelectionLlmTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === singleSelectionLlmResult.taskId);
    const singleSelectionLlmSliceRequest = singleSelectionLlmCommands.find((entry) => entry.kind === 'slice')?.request;
    const singleSelectionLlmWorkflowNativeRequests = singleSelectionLlmCommands.filter((entry) =>
      entry.kind === 'transcribe' ||
      entry.kind === 'analyze-audio-boundaries' ||
      entry.kind === 'slice'
    );
    assertEqual(
      singleSelectionLlmBridgeRequests.length,
      1,
      'single-selection LLM regression exercises the configured LLM planner path',
    );
    assertEqual(
      singleSelectionLlmBridgeRequests[0]?.runtime?.defaultSegmentationAgentId,
      'dialogue-turn-agent',
      'single-selection LLM regression resolves Settings Center default Smart Slice segmentation agent at runtime',
    );
    assertEqual(
      singleSelectionLlmPrompt?.segmentationAgent?.id,
      'dialogue-turn-agent',
      'single-selection LLM regression sends the Settings Center default segmentation agent into the LLM review prompt when params omit segmentationAgentId',
    );
    assertEqual(
      singleSelectionLlmPrompt?.presetId,
      'interview-one-question-one-answer',
      'single-selection LLM regression lets the Settings Center default dialogue segmentation agent drive the Q/A slicer preset',
    );
    assertEqual(
      singleSelectionLlmPrompt?.candidates?.[0]?.dialogueTurnContinuity,
      'question-answer-complete',
      'single-selection LLM regression sends complete dialogue-turn continuity evidence into the LLM review prompt',
    );
    assertRule(
      singleSelectionLlmPrompt?.candidates?.[0]?.speakerIds?.includes('speaker-host') &&
        singleSelectionLlmPrompt?.candidates?.[0]?.speakerIds?.includes('speaker-guest') &&
        singleSelectionLlmPrompt?.candidates?.[0]?.speakerRoles?.includes('interviewer') &&
        singleSelectionLlmPrompt?.candidates?.[0]?.speakerRoles?.includes('guest'),
      'single-selection LLM regression sends candidate-level interviewer and guest evidence into the LLM review prompt',
    );
    assertRule(
      singleSelectionLlmPrompt?.contentUnits?.some((unit) =>
        unit.speakerTurnIds?.length > 0 &&
          typeof unit.speakerConfidence === 'number' &&
          Array.isArray(unit.overlapGroupIds)
      ),
      'single-selection LLM regression sends content-unit diarization evidence into the LLM review prompt',
    );
    assertEqual(
      singleSelectionLlmTask?.executionCheckpoint?.params?.segmentationAgentId,
      'dialogue-turn-agent',
      'single-selection LLM regression persists the resolved Settings Center default segmentation agent in the Smart Slice checkpoint',
    );
    assertEqual(
      singleSelectionLlmResult.success,
      true,
      'single-selection LLM regression reports success',
    );
    assertEqual(
      singleSelectionLlmSliceRequest?.clips?.length,
      2,
      'single-selection LLM regression renders every deterministic strong content group even when the model returns only one candidate',
    );
    assertRule(
      singleSelectionLlmSliceRequest?.clips?.some((clip) =>
        clip.transcriptText?.includes('native validation workflow stays safe')
      ) &&
        singleSelectionLlmSliceRequest?.clips?.some((clip) =>
          clip.transcriptText?.includes('artifact check rejects the bad artifact')
        ),
      'single-selection LLM regression preserves both independent transcript-derived stories in native render input',
    );
    assertEqual(
      singleSelectionLlmTask?.sliceResults?.length,
      2,
      'single-selection LLM regression persists both generated smart slices on the completed task',
    );
    assertRule(
      singleSelectionLlmWorkflowNativeRequests.length >= 3 &&
        singleSelectionLlmWorkflowNativeRequests.every((entry) => entry.request?.workflowTaskId === singleSelectionLlmResult.taskId),
      'single-selection LLM Smart Slice native requests carry the workflow task id for cancel progress and log correlation',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const failingNativeVideoSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/failing-native-source.mp4',
      'failing-native-source.mp4',
    );
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        localDirectorySelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionFileSelectCommandReady: false,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_slice_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async () => ({
        assetUuid: 'failing-native-slice-asset',
        sandboxPath: `${configuredOutputDirectory}/inputs/failing-native-slice-asset.mp4`,
        byteSize: 765000,
        name: 'failing-native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 765000,
        name: 'failing-native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      selectLocalVideoFile: async () => null,
      selectLocalDirectory: async () => null,
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for failing native video slice workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF is not configured for failing native video slice workflow contract');
      },
      sliceVideo: async () => {
        throw new Error('ffmpeg slice failed with status 1');
      },
      transcribeMedia: async (request) =>
        createStandardNativeVideoSliceTranscriptResult(
          request,
          'failing-native-slice-transcript-task',
          configuredOutputDirectory,
        ),
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'failing native video slice speech preflight contract',
      }),
      selectSpeechTranscriptionFile: async () => null,
      compressVideo: async () => {
        throw new Error('video compression is not configured for failing native video slice workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion is not configured for failing native video slice workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement is not configured for failing native video slice workflow contract');
      },
      sendLlmHttpRequest: async () => {
        throw new Error('LLM HTTP is not configured for failing native video slice workflow contract');
      },
      saveLlmSecret: async (request) => ({
        secretName: request.secretName,
        saved: false,
      }),
      getLlmSecret: async (request) => ({
        secretName: request.secretName,
        configured: false,
      }),
      deleteLlmSecret: async (request) => ({
        secretName: request.secretName,
        deleted: false,
      }),
      listNativeTasks: async () => [],
      cancelNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        status: 0,
        canceled: false,
        message: 'not configured',
      }),
      recoverNativeTasks: async () => ({
        inspected: 0,
        recovered: 0,
        interrupted: 0,
        canceled: 0,
        expiredLeases: 0,
        deferred: 0,
        taskUuids: [],
      }),
      retryNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        retryTaskUuid: '',
        status: 0,
        retried: false,
        message: 'not configured',
      }),
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-failing-native-video-slice',
            file: failingNativeVideoSliceSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'ffmpeg slice failed with status 1',
      'video slice native command failure',
    );
    const failingNativeVideoSliceTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(failingNativeVideoSliceTasks.length, 1, 'video slice native command failure persists one task');
    assertEqual(failingNativeVideoSliceTasks[0]?.status, types.AUTOCUT_TASK_STATUS.failed, 'video slice native command failure marks the task failed');
    assertIncludes(
      String(failingNativeVideoSliceTasks[0]?.errorMessage ?? ''),
      'ffmpeg slice failed with status 1',
      'video slice native command failure stores the native error message',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native command failure does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native command failure does not persist success messages');

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.addAsset({
      id: 'asset-reference-resumable-smart-slice',
      name: 'resumable-smart-slice-source.mp4',
      type: 'video',
      size: 765000,
      url: 'asset://localhost/resumable-smart-slice-reference.mp4',
      createdAt: '2026-05-05T00:00:01.000Z',
      updatedAt: '2026-05-05T00:00:01.000Z',
    });
    const resumableSmartSliceCommands = [];
    let resumableSmartSliceAudioAnalysisAttempts = 0;
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-resume-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: true,
        nativeTaskRecoveryCommandReady: true,
        nativeTaskRetryCommandReady: true,
        nativeTaskProgressEventsReady: true,
      }),
      importMediaFile: async (request) => {
        resumableSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'resumable-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/resumable-smart-slice-asset.mp4`,
          byteSize: 765000,
          name: 'resumable-smart-slice-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      probeSpeechTranscription: async (request) => {
        resumableSmartSliceCommands.push({ kind: 'speech-preflight', request });
        return {
          ready: true,
          executablePath: request.executablePath,
          modelPath: request.modelPath,
          sourceKind: request.sourceKind ?? 'execution-preflight',
          diagnostics: [],
          versionLine: 'resumable Smart Slice speech preflight contract',
        };
      },
      transcribeMedia: async (request) => {
        resumableSmartSliceCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'resumable-smart-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        resumableSmartSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        resumableSmartSliceAudioAnalysisAttempts += 1;
        if (resumableSmartSliceAudioAnalysisAttempts === 1) {
          throw new Error('simulated audio boundary outage');
        }
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        resumableSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'resumable-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/resumable-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'resumable-smart-slice-render-task',
              clip.outputFileName ?? `resumable-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'resumable-smart-slice-render-task',
              `resumable-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `resumable-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `resumable-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000 + index,
              thumbnailByteSize: 12000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const resumableSmartSliceFallbackResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-resumable-smart-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/resumable-smart-slice-source.mp4',
          'resumable-smart-slice-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        targetPlatform: 'generic',
        idealDuration: 25,
        continuityLevel: 'standard',
        minDuration: 10,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: false,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSmartDedup: true,
        videoDedupParams: {
          mode: 'slice-result-dedup',
          referenceAssetIds: ['asset-reference-resumable-smart-slice'],
          strategies: ['exact-file-hash', 'container-normalized'],
          sensitivity: 'balanced',
          minMatchDurationMs: 8_000,
          ignoreIntroOutro: true,
          introOutroMaxDurationMs: 12_000,
          actionMode: 'review-before-action',
        },
        enableSubtitles: false,
      }),
    );
    const resumableSmartSliceCompletedTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === resumableSmartSliceFallbackResult.taskId);
    assertEqual(
      resumableSmartSliceFallbackResult.success,
      true,
      'Smart Slice checkpoint regression reports success after audio boundary fallback render',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice checkpoint regression completes the original task after audio boundary fallback render',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.workflowId,
      'smart-slice',
      'Smart Slice checkpoint regression persists the workflow checkpoint envelope',
    );
    assertArrayIncludes(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.completedStepIds,
      'speech-to-text',
      'Smart Slice checkpoint regression records completed speech-to-text before fallback render',
    );
    assertArrayIncludes(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.completedStepIds,
      'plan-clips',
      'Smart Slice checkpoint regression records completed planning before fallback render',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.artifacts?.['speech-to-text']?.transcriptSegments?.length,
      4,
      'Smart Slice checkpoint regression stores transcript segments after fallback render without repeating STT',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.artifacts?.['speech-to-text']?.normalizedTranscriptEvidence?.relativePath,
      'evidence/transcript.normalized.json',
      'Smart Slice checkpoint regression stores normalized transcript evidence after fallback render',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.executionCheckpoint?.artifacts?.['plan-clips']?.plannedClips?.length,
      2,
      'Smart Slice checkpoint regression stores every planned clip so fallback render does not collapse to one slice',
    );
    assertEqual(
      resumableSmartSliceCommands.filter((entry) => entry.kind === 'import').length,
      1,
      'Smart Slice checkpoint regression imports the source only once before fallback render',
    );
    assertEqual(
      resumableSmartSliceCommands.filter((entry) => entry.kind === 'transcribe').length,
      1,
      'Smart Slice checkpoint regression transcribes only once before fallback render',
    );
    assertEqual(
      resumableSmartSliceCommands.filter((entry) => entry.kind === 'analyze-audio-boundaries').length,
      1,
      'Smart Slice checkpoint regression attempts audio boundary analysis once before fallback render',
    );
    assertRule(
      (resumableSmartSliceCompletedTask?.executionCheckpoint?.artifacts?.['analyze-duplicates']?.smartDedupReport?.matchCount ?? 0) >= 1,
      'Smart Slice checkpoint regression runs intelligent dedup after fallback render using the checkpointed runtime source asset',
    );
    assertRule(
      resumableSmartSliceCompletedTask?.sliceReviewSession?.duplicateGroups?.some((group) => group.reason === 'smart-dedup'),
      'Smart Slice checkpoint regression exposes fallback-render smart dedup matches in the rendered review session',
    );
    assertEqual(
      resumableSmartSliceCommands.filter((entry) => entry.kind === 'slice').length,
      1,
      'Smart Slice checkpoint regression renders once after audio boundary fallback',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.nativeTaskId,
      'resumable-smart-slice-render-task',
      'Smart Slice checkpoint regression records the resumed native render task id',
    );
    assertEqual(
      resumableSmartSliceCompletedTask?.sliceResults?.length,
      2,
      'Smart Slice checkpoint regression preserves multiple generated slices after fallback render',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').filter((asset) =>
        asset.sourceTaskId === resumableSmartSliceCompletedTask?.id
      ).length,
      2,
      'Smart Slice checkpoint regression persists generated assets once on fallback render completion',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').filter((asset) =>
        asset.id === 'resumable-smart-slice-asset'
      ).length,
      0,
      'Smart Slice checkpoint regression keeps the checkpointed runtime source out of the asset library after fallback dedup',
    );
    await assertRejects(
      () => services.resumeTaskFromStep(resumableSmartSliceCompletedTask.id, 'speech-to-text'),
      'requires a failed, canceled, or interrupted task',
      'Smart Slice checkpoint regression rejects resume from a completed fallback-render workflow task',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').filter((asset) =>
        asset.sourceTaskId === resumableSmartSliceCompletedTask?.id
      ).length,
      2,
      'Smart Slice checkpoint regression does not duplicate generated assets after rejected completed-task resume',
    );

    resetStorage();
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const checkpointSecondsTimestampCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-seconds-checkpoint-resume-regression',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        checkpointSecondsTimestampCommands.push({ kind: 'import', request });
        throw new Error('Smart Slice seconds checkpoint resume must reuse the prepared source checkpoint');
      },
      transcribeMedia: async (request) => {
        checkpointSecondsTimestampCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'seconds-checkpoint-smart-slice-refreshed-transcript-task',
          configuredOutputDirectory,
          checkpointSecondsTimestampSegments,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'seconds checkpoint resume speech preflight contract',
      }),
      analyzeVideoSliceAudioActivity: async (request) => {
        checkpointSecondsTimestampCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        checkpointSecondsTimestampCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'seconds-checkpoint-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/seconds-checkpoint-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'seconds-checkpoint-smart-slice-render-task',
              clip.outputFileName ?? `seconds-checkpoint-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'seconds-checkpoint-smart-slice-render-task',
              `seconds-checkpoint-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `seconds-checkpoint-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `seconds-checkpoint-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 250000 + index,
              thumbnailByteSize: 13000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }, { taskEvidenceCommands: checkpointSecondsTimestampCommands });
    const checkpointSecondsTimestampSegments = [
      {
        startMs: 0,
        endMs: 8.5,
        text:
          'Why native validation matters is simple. Because bad native validation can break review, the native validation problem must be caught before assets are saved.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 8.52,
        endMs: 19,
        text:
          'So native validation verifies every result and the native validation workflow stays safe.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 40,
        endMs: 48.5,
        text:
          'What makes artifact checks reliable is clear. The artifact check shows missing artifact thumbnails before publishing, creating an artifact conflict for reviewers.',
        speaker: 'Speaker 1',
      },
      {
        startMs: 48.52,
        endMs: 59,
        text:
          'So the artifact check rejects the bad artifact before persistence and keeps artifact storage clean.',
        speaker: 'Speaker 1',
      },
    ];
    await services.addTask({
      id: 'seconds-checkpoint-smart-slice-task',
      type: types.AUTOCUT_TASK_TYPE.videoSlice,
      name: 'seconds-checkpoint-smart-slice-source.mp4',
      status: types.AUTOCUT_TASK_STATUS.failed,
      progress: 65,
      progressMessage: 'Smart Slice failed during plan-clips.',
      currentStepId: 'plan-clips',
      createdAt: '2026-05-11T02:20:00.000Z',
      executionSteps: [
        {
          id: 'prepare-source',
          label: 'Prepare native source media',
          status: 'completed',
          progress: 45,
          attempts: 1,
          canResumeFromHere: true,
          checkpointKey: 'smart-slice:prepare-source',
          message: 'Prepared source.',
        },
        {
          id: 'speech-to-text',
          label: 'Transcribe source speech',
          status: 'completed',
          progress: 55,
          attempts: 1,
          canResumeFromHere: true,
          checkpointKey: 'smart-slice:speech-to-text',
          message: 'Transcribed source speech.',
        },
        {
          id: 'plan-clips',
          label: 'Plan publishable clips',
          status: 'failed',
          progress: 65,
          attempts: 1,
          canResumeFromHere: true,
          checkpointKey: 'smart-slice:plan-clips',
          message: 'AutoCut transcript speech has no renderable timestamped segment.',
        },
      ],
      executionCheckpoint: {
        workflowId: 'smart-slice',
        version: 3,
        source: {
          kind: 'trusted-local-file',
          sourcePath: 'D:/media/seconds-checkpoint-smart-slice-source.mp4',
          fileName: 'seconds-checkpoint-smart-slice-source.mp4',
          byteSize: 654000,
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          fileId: 'seconds-checkpoint-smart-slice-source-id',
        },
        params: {
          mode: 'contract-mode',
          fileId: 'seconds-checkpoint-smart-slice-source-id',
          llmModel: 'gemini-3-flash-preview',
          targetPlatform: 'generic',
          idealDuration: 25,
          continuityLevel: 'standard',
          minDuration: 10,
          maxDuration: 20,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: false,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        },
        completedStepIds: ['prepare-source', 'speech-to-text'],
        resumeFromStepIds: ['prepare-source', 'speech-to-text', 'plan-clips'],
        artifacts: {
          'prepare-source': {
            sourceMedia: {
              assetUuid: 'seconds-checkpoint-smart-slice-asset',
              durationMs: 90_000,
            },
            outputRootDir: configuredOutputDirectory,
            desktopSourcePath: 'D:/media/seconds-checkpoint-smart-slice-source.mp4',
          },
          'speech-to-text': {
            transcriptSegments: checkpointSecondsTimestampSegments,
            speechToTextEvidence: createNativeTaskEvidenceWriteResult(
              'seconds-checkpoint-smart-slice-task',
              'evidence/speech-to-text.json',
              configuredOutputDirectory,
            ),
          },
        },
        createdAt: '2026-05-11T02:20:00.000Z',
        updatedAt: '2026-05-11T02:20:00.000Z',
      },
    });
    const checkpointSecondsTimestampUpdates = captureEvents(services, 'taskUpdated');
    const checkpointSecondsTimestampResumeResult = await withImmediateTimers(() =>
      services.resumeTaskFromStep('seconds-checkpoint-smart-slice-task', 'plan-clips')
    );
    checkpointSecondsTimestampUpdates.stop();
    const checkpointSecondsTimestampCompletedTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === 'seconds-checkpoint-smart-slice-task');
    const checkpointSecondsTimestampRenderRequest = checkpointSecondsTimestampCommands
      .find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      checkpointSecondsTimestampResumeResult.success,
      true,
      'Smart Slice checkpoint resume normalizes seconds STT timestamps and reports success',
    );
    assertEqual(
      checkpointSecondsTimestampCommands.filter((entry) => entry.kind === 'import').length,
      0,
      'Smart Slice checkpoint resume reuses the prepared source instead of importing again',
    );
    assertEqual(
      checkpointSecondsTimestampCommands.filter((entry) => entry.kind === 'transcribe').length,
      1,
      'Smart Slice checkpoint resume refreshes incomplete speech-to-text checkpoints that lack normalized transcript evidence',
    );
    const checkpointSecondsNormalizedEvidenceWrite = checkpointSecondsTimestampCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/transcript.normalized.json',
    );
    assertEqual(
      checkpointSecondsNormalizedEvidenceWrite?.request?.workflowTaskId,
      'seconds-checkpoint-smart-slice-task',
      'Smart Slice checkpoint resume writes normalized transcript evidence when refreshing incomplete STT checkpoints',
    );
    assertEqual(
      checkpointSecondsTimestampCompletedTask?.executionCheckpoint?.artifacts?.['speech-to-text']?.normalizedTranscriptEvidence?.relativePath,
      'evidence/transcript.normalized.json',
      'Smart Slice checkpoint resume stores refreshed normalized transcript evidence in the checkpoint',
    );
    assertEqual(
      checkpointSecondsTimestampRenderRequest?.clips?.length,
      2,
      'Smart Slice checkpoint resume renders multiple clips from normalized seconds timestamp checkpoint transcript',
    );
    assertEqual(
      checkpointSecondsTimestampRenderRequest?.clips?.[1]?.speechStartMs,
      40_000,
      'Smart Slice checkpoint resume preserves the second story timestamp after checkpoint normalization',
    );
    assertRule(
      checkpointSecondsTimestampUpdates.details.some((task) =>
        task?.id === 'seconds-checkpoint-smart-slice-task' &&
          task.status === types.AUTOCUT_TASK_STATUS.processing &&
          task.currentStepId === 'plan-clips' &&
          task.progressMessage?.includes('Resuming smart-slice from plan-clips')
      ),
      'Task detail resume workflow exposes a processing update for the clicked plan-clips step',
    );
    assertRule(
      checkpointSecondsTimestampUpdates.details.some((task) =>
        task?.id === 'seconds-checkpoint-smart-slice-task' &&
          task.executionSteps?.some((step) =>
            step.id === 'plan-clips' &&
              step.status === 'running' &&
              step.attempts === 2 &&
              step.canResumeFromHere === false
          )
      ),
      'Task detail resume workflow marks the clicked plan-clips step running before replaying the checkpointed workflow',
    );
    assertRule(
      checkpointSecondsTimestampCompletedTask?.executionLogs?.some((log) =>
        log.eventType === 'task-resume-started' &&
          log.stepId === 'plan-clips' &&
          log.source === 'task-resume-service' &&
          String(log.message).includes('Resuming Smart Slice from step plan-clips')
      ),
      'Task detail resume workflow records a queryable resume-start log for the clicked plan-clips step',
    );
    assertRule(
      checkpointSecondsTimestampCompletedTask?.executionLogs?.some((log) =>
        log.eventType === 'checkpoint-reused' &&
          log.stepId === 'prepare-source'
      ),
      'Task detail resume workflow records prepare-source checkpoint reuse in the task execution log',
    );
    assertRule(
      checkpointSecondsTimestampCompletedTask?.executionLogs?.some((log) =>
        log.eventType === 'checkpoint-refreshed' &&
          log.stepId === 'speech-to-text'
      ),
      'Task detail resume workflow records speech-to-text checkpoint refresh when normalized transcript evidence is missing',
    );
    assertEqual(
      checkpointSecondsTimestampCompletedTask?.sliceResults?.length,
      2,
      'Smart Slice checkpoint resume persists both generated clips after seconds timestamp normalization',
    );
    assertDeepEqual(
      checkpointSecondsTimestampCompletedTask?.executionSteps?.map((step) => ({
        id: step.id,
        status: step.status,
      })),
      [
        { id: 'prepare-source', status: 'completed' },
        { id: 'speech-to-text', status: 'completed' },
        { id: 'plan-clips', status: 'completed' },
        { id: 'analyze-audio-boundaries', status: 'completed' },
        { id: 'analyze-duplicates', status: 'completed' },
        { id: 'human-review', status: 'completed' },
        { id: 'native-render', status: 'completed' },
        { id: 'verify-artifacts', status: 'completed' },
        { id: 'persist-results', status: 'completed' },
      ],
      'Task detail resume workflow leaves every Smart Slice execution step completed after successful resume',
    );

    resetStorage();
    await services.addTask({
      id: 'legacy-smart-slice-checkpoint-task',
      type: types.AUTOCUT_TASK_TYPE.videoSlice,
      name: 'legacy-smart-slice-checkpoint.mp4',
      status: types.AUTOCUT_TASK_STATUS.failed,
      progress: 65,
      progressMessage: 'Legacy Smart Slice task failed before native render.',
      currentStepId: 'analyze-audio-boundaries',
      createdAt: '2026-05-11T02:00:00.000Z',
      executionSteps: [
        {
          id: 'analyze-audio-boundaries',
          label: 'Analyze speech boundaries',
          status: 'failed',
          progress: 69,
          attempts: 1,
          canResumeFromHere: true,
          checkpointKey: 'smart-slice:analyze-audio-boundaries',
          message: 'Legacy audio boundary step failed.',
        },
      ],
      executionCheckpoint: {
        workflowId: 'smart-slice',
        version: 1,
        source: {
          kind: 'native-asset',
          fileId: 'legacy-smart-slice-asset',
        },
        params: {
          mode: 'contract-mode',
          fileId: 'legacy-smart-slice-asset',
          llmModel: 'gemini-3-flash-preview',
          minDuration: 10,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: false,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: false,
        },
        completedStepIds: ['prepare-source', 'speech-to-text', 'plan-clips'],
        resumeFromStepIds: ['analyze-audio-boundaries'],
        artifacts: {
          'plan-clips': {
            plannedClips: [
              {
                index: 0,
                startMs: 10_000,
                durationMs: 20_000,
                sourceStartMs: 10_000,
                sourceEndMs: 30_000,
                speechStartMs: 10_200,
                speechEndMs: 29_800,
                transcriptText: 'legacy single clip',
                transcriptSegmentCount: 1,
              },
            ],
          },
        },
        createdAt: '2026-05-11T02:00:00.000Z',
        updatedAt: '2026-05-11T02:00:00.000Z',
      },
    });
    await assertRejects(
      () => services.resumeTaskFromStep('legacy-smart-slice-checkpoint-task', 'analyze-audio-boundaries'),
      'older slicing algorithm',
      'Smart Slice checkpoint regression rejects old algorithm checkpoints instead of reusing stale single-clip planning',
    );
    const rejectedLegacyCheckpointTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === 'legacy-smart-slice-checkpoint-task');
    assertEqual(
      rejectedLegacyCheckpointTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'Smart Slice checkpoint regression leaves old checkpoint tasks failed after rejected resume',
    );
    assertRule(
      rejectedLegacyCheckpointTask?.errorMessage?.includes('older slicing algorithm'),
      'Smart Slice checkpoint regression stores an actionable old-checkpoint resume error',
    );

    resetStorage();
    const cancellableSmartSliceCommands = [];
    const cancellableSmartSliceCancellation = {
      requested: false,
      taskId: undefined,
      cancelResult: undefined,
    };
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'smart-slice-workflow-cancel-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: true,
        nativeTaskRecoveryCommandReady: true,
        nativeTaskRetryCommandReady: true,
        nativeTaskProgressEventsReady: true,
      }),
      importMediaFile: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'cancellable-smart-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/cancellable-smart-slice-asset.mp4`,
          byteSize: 765000,
          name: 'cancellable-smart-slice-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      probeSpeechTranscription: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'speech-preflight', request });
        return {
          ready: true,
          executablePath: request.executablePath,
          modelPath: request.modelPath,
          sourceKind: request.sourceKind ?? 'execution-preflight',
          diagnostics: [],
          versionLine: 'cancellable Smart Slice speech preflight contract',
        };
      },
      transcribeMedia: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'transcribe', request });
        const taskId = request.workflowTaskId;
        if (!taskId) {
          throw new Error('cancellable Smart Slice test requires a workflowTaskId');
        }
        cancellableSmartSliceCancellation.taskId = taskId;
        cancellableSmartSliceCancellation.cancelResult = await services.cancelTasks([taskId]);
        cancellableSmartSliceCancellation.requested = true;
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'cancellable-smart-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      analyzeVideoSliceAudioActivity: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'cancellable-smart-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/cancellable-smart-slice-render-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const sliceNumber = index + 1;
            const output = createNativeTaskOutputArtifact(
              'cancellable-smart-slice-render-task',
              clip.outputFileName ?? `cancellable-smart-slice-${sliceNumber}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'cancellable-smart-slice-render-task',
              `cancellable-smart-slice-thumb-${sliceNumber}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `cancellable-smart-slice-artifact-${sliceNumber}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `cancellable-smart-slice-thumb-artifact-${sliceNumber}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 240000 + index,
              thumbnailByteSize: 12000 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      cancelNativeTask: async (request) => {
        cancellableSmartSliceCommands.push({ kind: 'native-cancel', request });
        return {
          taskUuid: request.taskUuid,
          status: 4,
          canceled: true,
          message: 'cancel requested',
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    let cancellableSmartSliceRejectedTaskId;
    await assertRejects(
      async () => {
        try {
          await withImmediateTimers(async () =>
            processVideoSlice({
              fileId: 'asset-source-cancellable-smart-slice',
              file: createTrustedLocalMediaFile(
                commons,
                'D:/media/cancellable-smart-slice-source.mp4',
                'cancellable-smart-slice-source.mp4',
              ),
              mode: 'contract-mode',
              llmModel: 'gemini-3-flash-preview',
              targetPlatform: 'generic',
              idealDuration: 25,
              continuityLevel: 'standard',
              minDuration: 10,
              maxDuration: 60,
              baseAlgorithm: 'scene',
              highlightEngine: 'emotion',
              enableNoiseReduction: false,
              enableCoughFilter: true,
              enableRepeatFilter: true,
              enableSubtitles: false,
            }),
          );
        } catch (error) {
          cancellableSmartSliceRejectedTaskId = services.getAutoCutProcessingTaskErrorTaskId(error);
          throw error;
        }
      },
      'canceled',
      'Smart Slice workflow cancellation stops execution before later native render steps',
    );
    const cancellableSmartSliceCanceledTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === cancellableSmartSliceRejectedTaskId);
    assertRule(
      cancellableSmartSliceCancellation.requested,
      'Smart Slice workflow cancellation regression requests cancellation while speech-to-text is running',
    );
    assertDeepEqual(
      cancellableSmartSliceCancellation.cancelResult?.canceledTaskIds,
      [cancellableSmartSliceRejectedTaskId],
      'Smart Slice workflow cancellation regression returns the workflow task id as canceled before native render exists',
    );
    assertEqual(
      cancellableSmartSliceCanceledTask?.status,
      types.AUTOCUT_TASK_STATUS.canceled,
      'Smart Slice workflow cancellation regression persists canceled task status instead of failed',
    );
    assertRule(
      cancellableSmartSliceCanceledTask?.executionLogs?.some((log) =>
        log.eventType === 'task-cancel-requested' &&
          log.source === 'task-cancel-service'
      ),
      'Smart Slice workflow cancellation regression records a queryable cancel request log',
    );
    assertRule(
      cancellableSmartSliceCanceledTask?.executionSteps?.some((step) =>
        step.id === 'speech-to-text' &&
          step.status === 'canceled' &&
          step.canResumeFromHere === true
      ),
      'Smart Slice workflow cancellation regression marks the interrupted Smart Slice step as canceled and resumable',
    );
    assertEqual(
      cancellableSmartSliceCommands.filter((entry) => entry.kind === 'analyze-audio-boundaries').length,
      0,
      'Smart Slice workflow cancellation regression does not continue into audio boundary analysis after cancellation',
    );
    assertEqual(
      cancellableSmartSliceCommands.filter((entry) => entry.kind === 'slice').length,
      0,
      'Smart Slice workflow cancellation regression does not dispatch native render after cancellation',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length,
      0,
      'Smart Slice workflow cancellation regression does not persist generated assets after cancellation',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const partialNativeVideoSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        partialNativeVideoSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'partial-native-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/partial-native-slice-asset.mp4`,
          byteSize: 765000,
          name: 'partial-native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        partialNativeVideoSliceCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'partial-native-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'partial native video slice speech preflight contract',
      }),
      sliceVideo: async (request) => {
        partialNativeVideoSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'partial-native-slice-task',
          'partial-native-slice-artifact-1.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'partial-native-slice-task',
          'partial-native-slice-thumb-1.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'partial-native-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.slice(0, 1).map((clip, index) => ({
            artifactUuid: `partial-native-slice-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `partial-native-slice-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567,
            thumbnailByteSize: 12345,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const partialNativeVideoSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/partial-native-source.mp4',
      'partial-native-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-partial-native-video-slice',
            file: partialNativeVideoSliceSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 20,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'returned 1 slice artifacts for 2 planned clips',
      'video slice native partial result mismatch',
    );
    const partialNativeVideoSliceTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      partialNativeVideoSliceCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.length,
      2,
      'video slice native partial result test derives two planned clips from two complete transcript stories',
    );
    assertEqual(partialNativeVideoSliceTasks.length, 1, 'video slice native partial result mismatch persists one task');
    assertEqual(
      partialNativeVideoSliceTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native partial result mismatch marks the task failed',
    );
    assertEqual(
      partialNativeVideoSliceTasks[0]?.nativeTaskId,
      'partial-native-slice-task',
      'video slice native partial result mismatch records the completed native ops_task id for recovery suppression',
    );
    assertIncludes(
      String(partialNativeVideoSliceTasks[0]?.errorMessage ?? ''),
      'returned 1 slice artifacts for 2 planned clips',
      'video slice native partial result mismatch stores the count mismatch reason',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native partial result mismatch does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native partial result mismatch does not persist success messages');
    const visiblePartialNativeVideoSliceTasks = await withImmediateTimers(() => services.getTasks());
    assertEqual(
      visiblePartialNativeVideoSliceTasks.filter((task) =>
        task.id === partialNativeVideoSliceTasks[0]?.id || task.id === 'partial-native-slice-task'
      ).length,
      1,
      'video slice native partial result mismatch does not reappear as a completed recovered native task',
    );
    assertEqual(
      visiblePartialNativeVideoSliceTasks.find((task) => task.id === partialNativeVideoSliceTasks[0]?.id)?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native partial result mismatch keeps the local post-render validation failure visible after native task recovery',
    );

    resetStorage();
    const escapedNativeVideoSliceArtifactCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        escapedNativeVideoSliceArtifactCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'escaped-native-slice-artifact-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/escaped-native-slice-artifact-asset.mp4`,
          byteSize: 765000,
          name: 'escaped-native-artifact-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        escapedNativeVideoSliceArtifactCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'escaped-native-slice-artifact-transcript-task',
          configuredOutputDirectory,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'escaped native video slice speech preflight contract',
      }),
      sliceVideo: async (request) => {
        escapedNativeVideoSliceArtifactCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'escaped-native-slice-artifact-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/escaped-native-slice-artifact-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'escaped-native-slice-artifact-task',
              `escaped-native-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `escaped-native-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `escaped-native-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: `${configuredOutputDirectory}/tasks/escaped-native-slice-artifact-task/../thumb-${index + 1}.jpg`,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const escapedNativeVideoSliceArtifactSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/escaped-native-artifact-source.mp4',
      'escaped-native-artifact-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-escaped-native-video-slice-artifact',
            file: escapedNativeVideoSliceArtifactSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 20,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'slice artifact 1 thumbnailArtifactPath is outside its task output directory',
      'video slice native escaped artifact containment',
    );
    const escapedNativeVideoSliceArtifactTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      escapedNativeVideoSliceArtifactCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.length,
      2,
      'video slice native escaped artifact test derives two planned clips from two complete transcript stories',
    );
    assertEqual(
      escapedNativeVideoSliceArtifactTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native escaped artifact containment marks the task failed',
    );
    assertIncludes(
      String(escapedNativeVideoSliceArtifactTasks[0]?.errorMessage ?? ''),
      'thumbnailArtifactPath is outside its task output directory',
      'video slice native escaped artifact containment stores the failing artifact field',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native escaped artifact containment does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native escaped artifact containment does not persist success messages');

    resetStorage();
    const invalidNativeVideoSliceArtifactCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        invalidNativeVideoSliceArtifactCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'invalid-native-slice-artifact-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/invalid-native-slice-artifact-asset.mp4`,
          byteSize: 765000,
          name: 'invalid-native-artifact-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        invalidNativeVideoSliceArtifactCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'invalid-native-slice-artifact-transcript-task',
          configuredOutputDirectory,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'invalid native video slice artifact speech preflight contract',
      }),
      sliceVideo: async (request) => {
        invalidNativeVideoSliceArtifactCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'invalid-native-slice-artifact-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/invalid-native-slice-artifact-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'invalid-native-slice-artifact-task',
              `invalid-native-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'invalid-native-slice-artifact-task',
              `invalid-native-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `invalid-native-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `invalid-native-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: index === 0 ? thumbnail.artifactPath : '',
              taskOutputDir: output.taskOutputDir,
              byteSize: index === 0 ? 234567 : 0,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const invalidNativeVideoSliceArtifactSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/invalid-native-artifact-source.mp4',
      'invalid-native-artifact-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-invalid-native-video-slice-artifact',
            file: invalidNativeVideoSliceArtifactSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 20,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
          }),
        ),
      'slice artifact 2 is missing thumbnailArtifactPath',
      'video slice native invalid artifact metadata',
    );
    const invalidNativeVideoSliceArtifactTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      invalidNativeVideoSliceArtifactCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.length,
      2,
      'video slice native invalid artifact test derives two planned clips from two complete transcript stories',
    );
    assertEqual(
      invalidNativeVideoSliceArtifactTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native invalid artifact metadata marks the task failed',
    );
    assertIncludes(
      String(invalidNativeVideoSliceArtifactTasks[0]?.errorMessage ?? ''),
      'slice artifact 2 is missing thumbnailArtifactPath',
      'video slice native invalid artifact metadata stores the failing artifact field',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native invalid artifact metadata does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native invalid artifact metadata does not persist success messages');

    resetStorage();
    const mismatchedTimingNativeVideoSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        mismatchedTimingNativeVideoSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'mismatched-timing-native-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/mismatched-timing-native-slice-asset.mp4`,
          byteSize: 765000,
          name: 'mismatched-timing-native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        mismatchedTimingNativeVideoSliceCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'mismatched-timing-native-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'mismatched timing native video slice speech preflight contract',
      }),
      sliceVideo: async (request) => {
        mismatchedTimingNativeVideoSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'mismatched-timing-native-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/mismatched-timing-native-slice-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'mismatched-timing-native-slice-task',
              `mismatched-timing-native-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'mismatched-timing-native-slice-task',
              `mismatched-timing-native-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `mismatched-timing-native-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `mismatched-timing-native-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567 + index,
              thumbnailByteSize: 12345 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
              startMs: index === 1 ? clip.startMs + 1000 : clip.startMs,
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const mismatchedTimingNativeVideoSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/mismatched-timing-native-source.mp4',
      'mismatched-timing-native-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-mismatched-timing-native-video-slice',
            file: mismatchedTimingNativeVideoSliceSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 20,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'slice artifact 2 timing does not match planned clip 2',
      'video slice native timing mismatch',
    );
    const mismatchedTimingNativeVideoSliceTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      mismatchedTimingNativeVideoSliceCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.length,
      2,
      'video slice native timing mismatch test derives two planned clips from two complete transcript stories',
    );
    assertEqual(
      mismatchedTimingNativeVideoSliceTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native timing mismatch marks the task failed',
    );
    assertIncludes(
      String(mismatchedTimingNativeVideoSliceTasks[0]?.errorMessage ?? ''),
      'slice artifact 2 timing does not match planned clip 2',
      'video slice native timing mismatch stores the timing mismatch reason',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native timing mismatch does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native timing mismatch does not persist success messages');
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const mismatchedAudioActivityNativeVideoSliceCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        nativeTaskQueryCommandReady: true,
      }),
      importMediaFile: async (request) => {
        mismatchedAudioActivityNativeVideoSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'mismatched-audio-activity-native-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/mismatched-audio-activity-native-slice-asset.mp4`,
          byteSize: 765000,
          name: 'mismatched-audio-activity-native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        mismatchedAudioActivityNativeVideoSliceCommands.push({ kind: 'transcribe', request });
        return createContentDerivedTwoStoryNativeVideoSliceTranscriptResult(
          request,
          'mismatched-audio-activity-native-slice-transcript-task',
          configuredOutputDirectory,
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'mismatched audio activity native video slice speech preflight contract',
      }),
      sliceVideo: async (request) => {
        mismatchedAudioActivityNativeVideoSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'mismatched-audio-activity-native-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: `${configuredOutputDirectory}/tasks/mismatched-audio-activity-native-slice-task`,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'mismatched-audio-activity-native-slice-task',
              `mismatched-audio-activity-native-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'mismatched-audio-activity-native-slice-task',
              `mismatched-audio-activity-native-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `mismatched-audio-activity-native-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `mismatched-audio-activity-native-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567 + index,
              thumbnailByteSize: 12345 + index,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
              audioActivityAnalysisFilter: index === 0 ? 'not-a-supported-post-cut-filter' : clip.audioActivityAnalysisFilter,
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const mismatchedAudioActivityNativeVideoSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/mismatched-audio-activity-native-source.mp4',
      'mismatched-audio-activity-native-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-mismatched-audio-activity-native-video-slice',
            file: mismatchedAudioActivityNativeVideoSliceSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 20,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'slice artifact 1 audioActivityAnalysisFilter must match the post-cut cleanup noise reduction decision',
      'video slice native audio activity metadata mismatch rejects invalid post-cut filter evidence',
    );
    const mismatchedAudioActivityNativeVideoSliceTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      mismatchedAudioActivityNativeVideoSliceCommands.find((entry) => entry.kind === 'slice')?.request?.clips?.length,
      2,
      'video slice native audio activity metadata mismatch test derives two planned clips from two complete transcript stories',
    );
    assertEqual(
      mismatchedAudioActivityNativeVideoSliceTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice native audio activity metadata mismatch marks the task failed when native returns invalid post-cut evidence',
    );
    assertIncludes(
      String(mismatchedAudioActivityNativeVideoSliceTasks[0]?.errorMessage ?? ''),
      'audioActivityAnalysisFilter must match the post-cut cleanup noise reduction decision',
      'video slice native audio activity metadata mismatch stores the invalid post-cut evidence reason',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video slice native audio activity metadata mismatch does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video slice native audio activity metadata mismatch does not persist success messages');
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const invalidLlmVideoSliceWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        localDirectorySelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoSliceCommandReady: true,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionFileSelectCommandReady: true,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: [
          'autocut_import_media_file',
          'autocut_transcribe_media',
          'autocut_analyze_video_slice_audio_activity',
          'autocut_slice_video',
        ],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        invalidLlmVideoSliceWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'invalid-llm-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/invalid-llm-slice-asset.mp4`,
          byteSize: 654000,
          name: 'invalid-llm-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 654000,
        name: 'invalid-llm-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the invalid LLM video slice workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF is not configured for the invalid LLM video slice workflow contract');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'invalid LLM video slice workflow contract',
      }),
      selectSpeechTranscriptionFile: async () => null,
      transcribeMedia: async (request) => {
        invalidLlmVideoSliceWorkflowCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'invalid-llm-transcript-task',
          'invalid-llm-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'invalid-llm-transcript-artifact',
          taskUuid: 'invalid-llm-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text:
            'Why invalid plans still need transcript fallback. Because the LLM can fail, viewers still need a complete problem and result. So the local speech plan keeps the solution and the payoff works. What makes fallback safe is clear. The case shows planning risk before rendering, so the answer is to use verified speech windows.',
          segments: [
            {
              startMs: 31000,
              endMs: 46000,
              text:
                'Why invalid plans still need transcript fallback. Because the LLM can fail, viewers still need a complete problem and result. So the local speech plan keeps the solution and the payoff works.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 60000,
              endMs: 76000,
              text:
                'What makes fallback safe is clear. The case shows planning risk before rendering, so the answer is to use verified speech windows.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      sliceVideo: async (request) => {
        invalidLlmVideoSliceWorkflowCommands.push({ kind: 'slice', request });
        const taskOutputDir = createNativeTaskOutputArtifact(
          'invalid-llm-slice-task',
          'invalid-llm-slice-artifact-1.mp4',
          configuredOutputDirectory,
        ).taskOutputDir;
        return {
          taskUuid: 'invalid-llm-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'invalid-llm-slice-task',
              `invalid-llm-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'invalid-llm-slice-task',
              `invalid-llm-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `invalid-llm-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `invalid-llm-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      compressVideo: async () => {
        throw new Error('video compression is not configured for the invalid LLM video slice workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion is not configured for the invalid LLM video slice workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement is not configured for the invalid LLM video slice workflow contract');
      },
      sendLlmHttpRequest: async () => {
        throw new Error('native LLM HTTP is not used because the test installs an approved AI SDK bridge');
      },
      saveLlmSecret: async (request) => ({
        secretName: request.secretName,
        saved: false,
      }),
      getLlmSecret: async (request) => ({
        secretName: request.secretName,
        configured: false,
      }),
      deleteLlmSecret: async (request) => ({
        secretName: request.secretName,
        deleted: false,
      }),
      listNativeTasks: async () => [],
      cancelNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        status: 0,
        canceled: false,
        message: 'not configured',
      }),
      recoverNativeTasks: async () => ({
        inspected: 0,
        recovered: 0,
        interrupted: 0,
        canceled: 0,
        expiredLeases: 0,
        deferred: 0,
        taskUuids: [],
      }),
      retryNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        retryTaskUuid: '',
        status: 0,
        retried: false,
        message: 'not configured',
      }),
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const invalidLlmBridgeRequests = [];
    services.configureAutoCutApprovedAiSdkBridge({
      createChatCompletion: async (request, runtime) => {
        invalidLlmBridgeRequests.push({ request, runtime });
        return {
        id: 'invalid-llm-plan',
        model: request.model,
        content: 'not-json',
        runtime,
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-invalid-llm-plan',
      temperature: 0.7,
      maxTokens: 4096,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      model: 'gemini-3-flash-preview',
      temperature: 0.7,
      maxTokens: 4096,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      maxTokens: 4096,
    });
    const invalidLlmVideoSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/invalid-llm-source.mp4',
      'invalid-llm-source.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-invalid-llm-video-slice',
        file: invalidLlmVideoSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    assertEqual(
      invalidLlmBridgeRequests[0]?.request?.model,
      'gemini-3-flash-preview',
      'video slice workflow passes the page-selected LLM model to the approved AI SDK bridge',
    );
    assertEqual(
      invalidLlmBridgeRequests[0]?.request?.temperature,
      0.7,
      'video slice workflow uses the Settings Center temperature for LLM planning',
    );
    assertEqual(
      invalidLlmBridgeRequests[0]?.request?.maxTokens,
      4096,
      'video slice workflow uses the Settings Center maxTokens for LLM planning',
    );
    const invalidLlmSliceCommand = invalidLlmVideoSliceWorkflowCommands.find((entry) => entry.kind === 'slice');
    assertEqual(
      invalidLlmSliceCommand?.request?.clips?.[0]?.startMs,
      30800,
      'video slice workflow keeps transcript-assisted render padding when the configured LLM returns an invalid plan',
    );
    assertEqual(
      invalidLlmSliceCommand?.request?.clips?.[0]?.label?.startsWith(
        'Why invalid plans still need transcript fallback',
      ),
      true,
      'video slice workflow keeps Smart Cut Engine transcript-derived labels when the configured LLM returns an invalid ID review',
    );
    assertArrayIncludes(
      invalidLlmSliceCommand?.request?.clips?.[0]?.risks,
      'smart-cut-engine',
      'video slice workflow marks native clip requests with Smart Cut Engine evidence when the configured LLM returns an invalid ID review',
    );
    assertArrayNotIncludes(
      invalidLlmSliceCommand?.request?.clips?.[0]?.risks,
      'fallback-plan',
      'video slice workflow does not restore legacy fallback-plan risk when the configured LLM returns an invalid ID review',
    );
    const invalidLlmTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'invalid-llm-slice-task');
    assertArrayIncludes(
      invalidLlmTask?.sliceResults?.[0]?.risks,
      'smart-cut-engine',
      'video slice workflow records Smart Cut Engine evidence on slice results when the configured LLM returns an invalid ID review',
    );
    assertArrayNotIncludes(
      invalidLlmTask?.sliceResults?.[0]?.risks,
      'fallback-plan',
      'video slice workflow does not persist legacy fallback-plan risk when the configured LLM returns an invalid ID review',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const invalidDurationSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/invalid-duration-source.mp4',
      'invalid-duration-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-invalid-duration-video-slice',
            file: invalidDurationSliceSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 90,
            maxDuration: 15,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
            enableSubtitles: false,
          }),
        ),
      'minimum slice duration',
      'video slice workflow rejects inverted duration range before native work',
    );
    assertEqual(
      readScopedStoredArray(services, 'tasks').length,
      0,
      'video slice workflow does not persist a task for invalid duration range',
    );

    resetStorage();
    const shortSourceSliceCommands = [];
    const shortSourceBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        shortSourceSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'short-source-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/short-source-slice-asset.mp4',
          byteSize: 123000,
          name: 'short-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 4000,
        };
      },
      transcribeMedia: async (request) => {
        shortSourceSliceCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'short-source-slice-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 3500,
              text: 'Short source speech.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'sparse transcript speech preflight contract',
      }),
      sliceVideo: async (request) => {
        shortSourceSliceCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'short-source-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: 'D:/autocut-configured-output/tasks/short-source-slice-task',
          ffmpegExecutable: 'ffmpeg',
          slices: [],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-short-source-plan',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        shortSourceBridgeRequests.push({ request, runtime });
        return {
          id: 'short-source-plan',
          model: request.model,
          content: JSON.stringify([{ startMs: 0, durationMs: 15000, label: 'Impossible short clip' }]),
          runtime,
        };
      },
    });
    const shortSourceSliceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/short-source.mp4',
      'short-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-short-video-slice',
            file: shortSourceSliceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'source video is too short',
      'video slice workflow rejects source media that is shorter than the minimum renderable slice',
    );
    assertEqual(
      shortSourceSliceCommands.some((entry) => entry.kind === 'slice'),
      false,
      'video slice workflow does not call native slicing when source media cannot produce any valid clip',
    );
    assertEqual(
      shortSourceBridgeRequests.length,
      0,
      'video slice workflow does not call the LLM planner when the source media is too short for any valid clip',
    );
    const shortSourceSliceTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      shortSourceSliceTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video slice workflow persists a failed task for source media that is too short to slice',
    );
    assertRule(
      String(shortSourceSliceTask?.errorMessage ?? '').includes('source video is too short'),
      'video slice workflow explains the short-source failure on the persisted task',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const unknownDurationShortTranscriptCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        unknownDurationShortTranscriptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'unknown-duration-short-transcript-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/unknown-duration-short-transcript.mp4',
          byteSize: 456000,
          name: 'unknown-duration-short-transcript.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      transcribeMedia: async (request) => {
        unknownDurationShortTranscriptCommands.push({ kind: 'transcribe', request });
        return {
          artifactUuid: 'unknown-duration-short-transcript-artifact',
          taskUuid: 'unknown-duration-short-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: 'D:/autocut-configured-output/tasks/unknown-duration-short-transcript-task/transcript.json',
          taskOutputDir: 'D:/autocut-configured-output/tasks/unknown-duration-short-transcript-task',
          language: 'auto',
          text: 'Short speech only',
          segments: [
            {
              startMs: 0,
              endMs: 4000,
              text: 'Short speech only',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'unknown duration short transcript speech preflight contract',
      }),
      sliceVideo: async (request) => {
        unknownDurationShortTranscriptCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'unknown-duration-short-transcript-slice-task',
          'unknown-duration-short-transcript-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'unknown-duration-short-transcript-slice-task',
          'unknown-duration-short-transcript-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'unknown-duration-short-transcript-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'unknown-duration-short-transcript-slice-artifact',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'unknown-duration-short-transcript-thumb-artifact',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 123456,
              thumbnailByteSize: 1234,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(request.clips[0], request.noiseReduction),
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const unknownDurationShortTranscriptFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/unknown-duration-short-transcript.mp4',
      'unknown-duration-short-transcript.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-unknown-duration-short-transcript',
        file: unknownDurationShortTranscriptFile,
        mode: 'contract-mode',
        llmModel: 'gemini-3-flash-preview',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const unknownDurationShortTranscriptSliceCommand = unknownDurationShortTranscriptCommands.find((entry) => entry.kind === 'slice');
    const unknownDurationShortTranscriptTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'unknown-duration-short-transcript-slice-task');
    assertRule(
      Boolean(unknownDurationShortTranscriptSliceCommand),
      'video slice workflow renders a short speech-aligned clip when imported media duration is unknown but STT has verified speech evidence',
    );
    assertRule(
      unknownDurationShortTranscriptSliceCommand?.request?.clips?.every((clip) => clip.durationMs >= 1000 && clip.durationMs < 5000),
      'video slice workflow keeps unknown-duration short transcript clips above the speech-aligned minimum without padding them to the requested duration',
    );
    assertRule(
      unknownDurationShortTranscriptSliceCommand?.request?.clips?.every((clip) => clip.transcriptText === 'Short speech only'),
      'video slice workflow sends native slicing transcript text for unknown-duration short transcript clips',
    );
    assertRule(
      unknownDurationShortTranscriptSliceCommand?.request?.clips?.every((clip) => clip.transcriptSegmentCount === 1),
      'video slice workflow sends native slicing structured transcript counts for unknown-duration short transcript clips',
    );
    assertEqual(
      unknownDurationShortTranscriptTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice workflow completes unknown-duration short transcript smart slicing with verified STT evidence',
    );
    assertRule(
      unknownDurationShortTranscriptTask?.sliceResults?.every((slice) => slice.transcriptText === 'Short speech only' && slice.transcriptCoverageScore >= 0.8),
      'video slice workflow records transcript evidence on unknown-duration short transcript slice results',
    );

    resetStorage();
    const isolatedMicroSpeechCommands = [];
    const isolatedMicroSpeechBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        isolatedMicroSpeechCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'isolated-micro-speech-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/isolated-micro-speech.mp4',
          byteSize: 789000,
          name: 'isolated-micro-speech.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        isolatedMicroSpeechCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'isolated-micro-speech-transcript-task',
          'isolated-micro-speech-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'isolated-micro-speech-transcript-artifact',
          taskUuid: 'isolated-micro-speech-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: 'Tiny isolated speech. Another tiny isolated speech.',
          segments: [
            {
              startMs: 10000,
              endMs: 12000,
              text: 'Tiny isolated speech.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40000,
              endMs: 42000,
              text: 'Another tiny isolated speech.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'transcript candidate speech preflight contract',
      }),
      sliceVideo: async (request) => {
        isolatedMicroSpeechCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'isolated-micro-speech-slice-task',
          'isolated-micro-speech-slice-artifact-1.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'isolated-micro-speech-slice-task',
          'isolated-micro-speech-slice-thumb-1.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'isolated-micro-speech-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `isolated-micro-speech-slice-artifact-${index + 1}`,
            artifactPath: index === 0
              ? output.artifactPath
              : createNativeTaskOutputArtifact(
                  'isolated-micro-speech-slice-task',
                  `isolated-micro-speech-slice-artifact-${index + 1}.mp4`,
                  configuredOutputDirectory,
                ).artifactPath,
            thumbnailArtifactUuid: `isolated-micro-speech-slice-thumb-${index + 1}`,
            thumbnailArtifactPath: index === 0
              ? thumbnail.artifactPath
              : createNativeTaskCoverArtifact(
                  'isolated-micro-speech-slice-task',
                  `isolated-micro-speech-slice-thumb-${index + 1}.jpg`,
                  configuredOutputDirectory,
                ).artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 223344,
            thumbnailByteSize: 1234,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-isolated-micro-speech-plan',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        isolatedMicroSpeechBridgeRequests.push({ request, runtime });
        return {
          id: 'isolated-micro-speech-plan',
          model: request.model,
          content: JSON.stringify([{ startMs: 0, durationMs: 15000, label: 'Bad silent filler' }]),
          runtime,
        };
      },
    });
    const isolatedMicroSpeechFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/isolated-micro-speech.mp4',
      'isolated-micro-speech.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-isolated-micro-speech',
        file: isolatedMicroSpeechFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'cover',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
      }),
    );
    assertEqual(
      isolatedMicroSpeechBridgeRequests.length,
      1,
      'video slice workflow exposes sparse transcript fallback candidates to the LLM planner instead of asking it to invent silent timings',
    );
    const isolatedMicroSpeechPrompt = JSON.parse(
      isolatedMicroSpeechBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    const isolatedMicroSpeechSliceRequest = isolatedMicroSpeechCommands.find((entry) => entry.kind === 'slice')?.request;
    const isolatedMicroSpeechTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'isolated-micro-speech-slice-task');
    assertRule(
      isolatedMicroSpeechPrompt?.candidates?.every((candidate) => candidate.risks?.includes('sparse-transcript-speech')),
      'video slice workflow labels sparse transcript Smart Cut Engine candidates for ID-only LLM review',
    );
    assertRule(
      isolatedMicroSpeechSliceRequest?.clips?.length >= 1,
      'video slice workflow calls native slicing for isolated micro speech when STT evidence is verified',
    );
    assertRule(
      isolatedMicroSpeechSliceRequest?.clips?.length >= 2,
      'video slice workflow keeps far-apart isolated micro speech windows separate instead of merging them into one long discontinuous clip',
    );
    assertRule(
      isolatedMicroSpeechSliceRequest?.clips?.every((clip) => clip.durationMs >= 1000),
      'video slice workflow renders isolated micro speech as short speech-aligned windows instead of silent padded clips',
    );
    assertRule(
      isolatedMicroSpeechSliceRequest?.clips?.every((clip) => clip.transcriptText?.trim() && clip.transcriptCoverageScore >= 0.8),
      'video slice workflow sends transcript evidence with isolated micro speech native clip requests',
    );
    assertEqual(
      isolatedMicroSpeechTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice workflow completes isolated micro speech smart slicing when short STT-backed clips are valid',
    );
    assertRule(
      isolatedMicroSpeechTask?.sliceResults?.every((slice) => slice.risks?.includes('sparse-transcript-speech')),
      'video slice workflow records sparse transcript review risk on isolated micro speech Smart Cut Engine slice results',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const wenan5DesktopUiSliceCommands = [];
    let wenan5DesktopUiTranscriptSegments = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        wenan5DesktopUiSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'wenan5-desktop-ui-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/wenan5-desktop-ui.mp4',
          byteSize: 789000,
          name: 'wenan5-desktop-ui.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 182360,
        };
      },
      transcribeMedia: async (request) => {
        wenan5DesktopUiSliceCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'wenan5-desktop-ui-transcript-task',
          'wenan5-desktop-ui-transcript.json',
          configuredOutputDirectory,
        );
        const segments = [
          [0, 22600, '\\u806a\\u660e\\u7684\\u7f8e\\u56fd\\u7559\\u5b66\\u6d77\\u5f52\\u518d\\u53bb\\u7f8e\\u56fd\\u751f\\u6d3b\\uff0c\\u65e9\\u5c31\\u4e0d\\u6324\\u7559\\u5b66\\u7b7e\\uff0c\\u65e9\\u5c31\\u4e0d\\u4f1a\\u518d\\u53bb\\u6324\\u82e6\\u903c\\u7684\\u7559\\u5b66\\u7b7e\\uff0c\\u4e5f\\u522b\\u53bb\\u5e7b\\u60f3\\u8fdb\\u4e2d\\u8d8a\\u7684\\u5de5\\u94b1\\u4e86\\u3002'],
          [26600, 43740, '\\u806a\\u660e\\u7684\\u7f8e\\u56fd\\u7559\\u5b66\\u6d77\\u5f52\\u60f3\\u518d\\u53bb\\u7f8e\\u56fd\\u751f\\u6d3b\\uff0c\\u4e0d\\u4f1a\\u518d\\u53bb\\u6324\\u82e6\\u903c\\u7684\\u7559\\u5b66\\u7b7e\\uff0c\\u66f4\\u52a0\\u4e0d\\u4f1a\\u53bb\\uff0c\\u66f4\\u52a0\\u4e0d\\u4f1a\\u6302\\u7740\\u811a\\u53bb\\u8df3\\u8fdb\\u4e2d\\u8d8a\\u7684\\u5de5\\u94b1\\u3002'],
          [53200, 66260, '\\u4ed6\\u4eec\\u4f1a\\u7528\\u8fd9\\u4e07\\u8bbf\\u5b66\\u7684\\u8def\\u5f84\\u7ed9\\u81ea\\u5df1\\u548c\\u5bb6\\u4eba\\u94fa\\u5c31\\u4e00\\u6761\\u901a\\u5411\\u7f8e\\u56fd\\u7684\\u5934\\u7b49\\u8231\\u673a\\u7968\\u3002'],
          [73420, 86260, '\\u6211\\u505a\\u7f8e\\u56fd\\u8bbf\\u5b6616\\u5e74\\uff0c\\u5e2e\\u52a9\\u51e0\\u767e\\u4f4d\\u7f8e\\u56fd\\u7559\\u5b66\\u6d77\\u5f52\\uff0c\\u5e2e\\u52a9\\u51e0\\u767e\\u4f4d\\u6d77\\u5f52\\u91cd\\u65b0\\u6740\\u56de\\u7f8e\\u56fd\\u3002'],
          [90760, 109240, '\\u8fd9\\u4e07\\u8bbf\\u5b66\\u4e0d\\u780d\\u4f60\\u7684\\u6bd5\\u4e1a\\u8fde\\u7ebf\\uff0c\\u53ea\\u8981\\u4f60\\u6709\\u4e00\\u5b9a\\u7684\\u884c\\u4e1a\\u7ecf\\u9a8c\\uff0c\\u53ea\\u8981\\u4f60\\u6709\\u4e00\\u5b9a\\u7684\\u884c\\u4e1a\\u7ecf\\u9a8c\\u5c31\\u80fd\\u7533\\u8bf7\\uff0c\\u6700\\u5feb\\u4e09\\u4e2a\\u6708\\u5e26\\u7740\\u5bb6\\u4eba\\u4e00\\u8d77\\u53bb\\u7f8e\\u56fd\\uff0c\\u957f\\u671f\\u7684\\u5408\\u6cd5\\u3002'],
          [110240, 115060, '\\u5e26\\u7740\\u5bb6\\u4eba\\u4e00\\u8d77\\u53bb\\u7f8e\\u56fd\\uff0c\\u957f\\u671f\\u7684\\u8f7b\\u677e\\u81ea\\u5728\\u7684\\u751f\\u6d3b\\u3002'],
          [115060, 128880, '\\u6700\\u5feb\\u4e09\\u4e2a\\u6708\\u5c31\\u80fd\\u5e26\\u7740\\u5bb6\\u4eba\\u5408\\u6cd5\\u5165\\u5883\\uff0c\\u91cd\\u56de\\u4f60\\u719f\\u6089\\u7684\\u7f8e\\u56fd\\u751f\\u6d3b\\u3002'],
          [128880, 136460, '\\u540c\\u65f6\\u7ed9\\u81ea\\u5df1\\u7559\\u8db3\\uff0c\\u540c\\u65f6\\u7ed9\\u81ea\\u5df1\\u7559\\u8db3\\uff0c\\u957f\\u671f\\u7684\\u53d1\\u5c55\\u56f0\\u4ef6\\u3002'],
          [136460, 148740, '\\u5f88\\u591a\\u4eba\\u89c9\\u5f97\\u56de\\u7f8e\\u56fd\\uff0c\\u5c31\\u8981\\u5148\\u653e\\u5f03\\u56fd\\u5185\\u7684\\u4e00\\u5207\\uff0c\\u8d4c\\u4e00\\u628a\\u3002'],
          [151740, 164700, '\\u6211\\u544a\\u8bc9\\u4f60\\uff0c\\u9ad8\\u8ba4\\u77e5\\u7684\\u6d77\\u5f52\\uff0c\\u5c31\\u662f\\u5148\\u7ed9\\u81ea\\u5df1\\u548c\\u5bb6\\u4eba\\u62ff\\u4e00\\u5f20\\uff0c\\u5c31\\u662f\\u5148\\u7ed9\\u81ea\\u5df1\\u548c\\u5bb6\\u4eba\\u62ff\\u4e00\\u5f20\\u8fdb\\u53ef\\u653b\\uff0c\\u9000\\u53ef\\u626f\\u7684\\u5165\\u573a\\u5238\\u3002'],
          [164700, 170200, '\\u8fd9\\u5176\\u5b9e\\u633a\\u5570\\u55e6\\u7684\\u3002'],
          [170200, 175640, '\\u4f60\\u60f3\\u957f\\u671f\\u5728\\u7f8e\\u56fd\\u751f\\u6d3b\\uff0c\\u8fd9\\u5176\\u5b9e\\u633a\\u5570\\u55e6\\u7684\\u3002'],
          [178060, 182380, "I'll show you the same thing. I'll show you the same thing. I'll show you the same thing."],
        ].map(([startMs, endMs, text]) => ({
          startMs,
          endMs,
          text: text.includes('\\u') ? decodeEscapedText(text) : text,
          speaker: 'Speaker 1',
        }));
        wenan5DesktopUiTranscriptSegments = segments;
        return {
          artifactUuid: 'wenan5-desktop-ui-transcript-artifact',
          taskUuid: 'wenan5-desktop-ui-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: segments.map((segment) => segment.text).join(' '),
          segments,
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'wenan5 desktop UI speech preflight contract',
      }),
      sliceVideo: async (request) => {
        wenan5DesktopUiSliceCommands.push({ kind: 'slice', request });
        const taskOutputDir = createNativeTaskOutputArtifact(
          'wenan5-desktop-ui-slice-task',
          'wenan5-desktop-ui-slice-artifact-1.mp4',
          configuredOutputDirectory,
        ).taskOutputDir;
        const subtitlesRequested = Array.isArray(request.subtitleSegments) && request.subtitleSegments.length > 0;
        return {
          taskUuid: 'wenan5-desktop-ui-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'wenan5-desktop-ui-slice-task',
              `wenan5-desktop-ui-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'wenan5-desktop-ui-slice-task',
              `wenan5-desktop-ui-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            const subtitle = createNativeTaskOutputArtifact(
              'wenan5-desktop-ui-slice-task',
              `wenan5-desktop-ui-slice-subtitle-${index + 1}.srt`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `wenan5-desktop-ui-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `wenan5-desktop-ui-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567 + index,
              thumbnailByteSize: 12345 + index,
              ...(subtitlesRequested
                ? {
                    subtitleArtifactUuid: `wenan5-desktop-ui-slice-subtitle-${index + 1}`,
                    subtitleArtifactPath: subtitle.artifactPath,
                    subtitleByteSize: 1234 + index,
                    subtitleFormat: 'srt',
                  }
                : {}),
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, { language: 'en' });
    const wenan5DesktopUiSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/wenan5-desktop-ui.mp4',
      'wenan5-desktop-ui.mp4',
    );
    const wenan5DesktopUiResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-wenan5-desktop-ui',
        file: wenan5DesktopUiSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'contain',
        idealDuration: 45,
        continuityLevel: 'standard',
        minDuration: 30,
        maxDuration: 70,
        baseAlgorithm: 'nlp',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    const wenan5DesktopUiTranscribeRequest = wenan5DesktopUiSliceCommands.find((entry) => entry.kind === 'transcribe')?.request;
    const wenan5DesktopUiSliceRequest = wenan5DesktopUiSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const wenan5DesktopUiTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === wenan5DesktopUiResult.taskId);
    assertEqual(
      wenan5DesktopUiTranscribeRequest?.language,
      'auto',
      'Smart Slice desktop upload uses automatic STT language detection even when the saved default speech language is English',
    );
    assertRule(
      (wenan5DesktopUiSliceRequest?.clips?.length ?? 0) >= 2,
      `Smart Slice desktop upload renders multiple wenan5 transcript clips instead of failing with no renderable timestamped segment (${wenan5DesktopUiSliceRequest?.clips?.length ?? 0})`,
    );
    assertRule(
      wenan5DesktopUiSliceRequest?.clips?.every((clip) =>
        clip.durationMs >= 1_000 &&
          clip.durationMs <= 70_000 &&
          (clip.transcriptSegmentCount ?? 0) > 0 &&
          Boolean(clip.transcriptText?.trim())
      ),
      'Smart Slice desktop upload sends renderable transcript-backed wenan5 clips to native slicing',
    );
    assertSmartSliceNativeRenderRequestClipsReady(
      wenan5DesktopUiSliceRequest?.clips,
      182_360,
      'Smart Slice desktop upload sends native-ready non-overlapping wenan5 clip timing',
    );
    assertRule(
      wenan5DesktopUiSliceRequest?.clips?.some((clip) =>
        clip.transcriptText?.includes(decodeEscapedText('\\u53ea\\u8981\\u4f60\\u6709\\u4e00\\u5b9a\\u7684\\u884c\\u4e1a\\u7ecf\\u9a8c')) &&
          clip.transcriptText?.includes(decodeEscapedText('\\u6700\\u5feb\\u4e09\\u4e2a\\u6708\\u5c31\\u80fd\\u5e26\\u7740\\u5bb6\\u4eba\\u5408\\u6cd5\\u5165\\u5883'))
      ),
      'Smart Slice desktop upload keeps the application condition and legal-entry payoff together in the wenan5 native clip request',
    );
    assertRule(
      wenan5DesktopUiSliceRequest?.clips?.every((clip) =>
        !clip.transcriptText?.includes(decodeEscapedText('\\u5570\\u55e6')) &&
          !clip.transcriptText?.includes('show you the same thing')
      ),
      'Smart Slice desktop upload excludes NG and re-recording tail transcript from native slice requests',
    );
    assertRule(
      getUncoveredSmartSliceTranscriptSegments(
        wenan5DesktopUiSliceRequest?.clips,
        wenan5DesktopUiTranscriptSegments,
      ).length === 0,
      'Smart Slice desktop upload native clip request covers every eligible source speech-to-text segment',
    );
    assertEqual(
      wenan5DesktopUiTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice desktop upload completes the coarse wenan5 transcript workflow',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const wenan5RealTranscriptDesktopUiSliceCommands = [];
    let wenan5RealTranscriptDesktopUiSegments = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        wenan5RealTranscriptDesktopUiSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'wenan5-real-transcript-desktop-ui-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/wenan5-real-transcript-desktop-ui.mp4',
          byteSize: 1312141320,
          name: 'wenan5-real-transcript-desktop-ui.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 182360,
        };
      },
      transcribeMedia: async (request) => {
        wenan5RealTranscriptDesktopUiSliceCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'wenan5-real-transcript-desktop-ui-transcript-task',
          'wenan5-real-transcript-desktop-ui-transcript.json',
          configuredOutputDirectory,
        );
        const segments = readWenan5RealSpeechTranscriptSegments();
        wenan5RealTranscriptDesktopUiSegments = segments;
        return {
          artifactUuid: 'wenan5-real-transcript-desktop-ui-transcript-artifact',
          taskUuid: 'wenan5-real-transcript-desktop-ui-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: segments.map((segment) => segment.text).join(' '),
          segments,
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'wenan5 real transcript desktop UI speech preflight contract',
      }),
      sliceVideo: async (request) => {
        wenan5RealTranscriptDesktopUiSliceCommands.push({ kind: 'slice', request });
        const taskOutputDir = createNativeTaskOutputArtifact(
          'wenan5-real-transcript-desktop-ui-slice-task',
          'wenan5-real-transcript-desktop-ui-slice-artifact-1.mp4',
          configuredOutputDirectory,
        ).taskOutputDir;
        const subtitlesRequested = Array.isArray(request.subtitleSegments) && request.subtitleSegments.length > 0;
        return {
          taskUuid: 'wenan5-real-transcript-desktop-ui-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'wenan5-real-transcript-desktop-ui-slice-task',
              `wenan5-real-transcript-desktop-ui-slice-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'wenan5-real-transcript-desktop-ui-slice-task',
              `wenan5-real-transcript-desktop-ui-slice-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            const subtitle = createNativeTaskOutputArtifact(
              'wenan5-real-transcript-desktop-ui-slice-task',
              `wenan5-real-transcript-desktop-ui-slice-subtitle-${index + 1}.srt`,
              configuredOutputDirectory,
            );
            return {
              artifactUuid: `wenan5-real-transcript-desktop-ui-slice-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `wenan5-real-transcript-desktop-ui-slice-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 345678 + index,
              thumbnailByteSize: 23456 + index,
              ...(subtitlesRequested
                ? {
                    subtitleArtifactUuid: `wenan5-real-transcript-desktop-ui-slice-subtitle-${index + 1}`,
                    subtitleArtifactPath: subtitle.artifactPath,
                    subtitleByteSize: 2345 + index,
                    subtitleFormat: 'srt',
                  }
                : {}),
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, { language: 'en' });
    const wenan5RealTranscriptDesktopUiSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/wenan5-real-transcript-desktop-ui.mp4',
      'wenan5-real-transcript-desktop-ui.mp4',
      { byteSize: 1312141320 },
    );
    const wenan5RealTranscriptDesktopUiResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-wenan5-real-transcript-desktop-ui',
        file: wenan5RealTranscriptDesktopUiSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'contain',
        idealDuration: 45,
        continuityLevel: 'standard',
        minDuration: 30,
        maxDuration: 70,
        baseAlgorithm: 'nlp',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    const wenan5RealTranscriptDesktopUiTranscribeRequest =
      wenan5RealTranscriptDesktopUiSliceCommands.find((entry) => entry.kind === 'transcribe')?.request;
    const wenan5RealTranscriptDesktopUiSliceRequest =
      wenan5RealTranscriptDesktopUiSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const wenan5RealTranscriptDesktopUiTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === wenan5RealTranscriptDesktopUiResult.taskId);
    assertEqual(
      wenan5RealTranscriptDesktopUiTranscribeRequest?.language,
      'auto',
      'Smart Slice real wenan5 transcript desktop upload still uses automatic STT language detection',
    );
    assertEqual(
      wenan5RealTranscriptDesktopUiSegments.length,
      35,
      'Smart Slice real wenan5 transcript desktop upload consumes the full real whisper transcript fixture',
    );
    assertRule(
      (wenan5RealTranscriptDesktopUiSliceRequest?.clips?.length ?? 0) >= 4,
      `Smart Slice real wenan5 transcript desktop upload renders multiple clips from real STT instead of ${wenan5RealTranscriptDesktopUiSliceRequest?.clips?.length ?? 0}`,
    );
    assertSmartSliceNativeRenderRequestClipsReady(
      wenan5RealTranscriptDesktopUiSliceRequest?.clips,
      182_360,
      'Smart Slice real wenan5 transcript desktop upload sends native-ready non-overlapping clip timing',
    );
    assertRule(
      getUncoveredSmartSliceTranscriptSegments(
        wenan5RealTranscriptDesktopUiSliceRequest?.clips,
        wenan5RealTranscriptDesktopUiSegments,
      ).length === 0,
      'Smart Slice real wenan5 transcript desktop upload native clip request covers every eligible real STT segment',
    );
    assertRule(
      wenan5RealTranscriptDesktopUiSliceRequest?.clips?.every((clip) =>
        !clip.transcriptText?.includes(decodeEscapedText('\\u5570\\u55e6')) &&
          !clip.transcriptText?.includes(decodeEscapedText('\\u91cd\\u65b0\\u5f55')) &&
          !clip.transcriptText?.includes(decodeEscapedText('\\u81ea\\u4e8e\\u53d1\\u6325'))
      ),
      'Smart Slice real wenan5 transcript desktop upload excludes NG, re-recording, and tail ad-lib transcript text',
    );
    assertRule(
      wenan5RealTranscriptDesktopUiSliceRequest?.clips?.some((clip) =>
        clip.transcriptText?.includes(decodeEscapedText('\\u53ea\\u8981\\u4f60\\u6709\\u4e00\\u5b9a\\u7684\\u884c\\u4e1a\\u7ecf\\u9a8c')) &&
          clip.transcriptText?.includes(decodeEscapedText('\\u6700\\u5feb\\u4e09\\u4e2a\\u6708\\u5c31\\u80fd\\u5e26\\u7740\\u5bb6\\u4eba\\u5408\\u6cd5\\u5165\\u5883'))
      ),
      'Smart Slice real wenan5 transcript desktop upload keeps application condition and legal-entry payoff together',
    );
    assertEqual(
      wenan5RealTranscriptDesktopUiTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'Smart Slice real wenan5 transcript desktop upload completes through the service workflow',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nonRenderableTranscriptDiagnosticsCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        nonRenderableTranscriptDiagnosticsCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'non-renderable-transcript-diagnostics-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/non-renderable-transcript-diagnostics.mp4',
          byteSize: 789000,
          name: 'non-renderable-transcript-diagnostics.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        nonRenderableTranscriptDiagnosticsCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'non-renderable-transcript-diagnostics-task',
          'non-renderable-transcript-diagnostics.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'non-renderable-transcript-diagnostics-artifact',
          taskUuid: 'non-renderable-transcript-diagnostics-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: 'Renderable text that is too brief. Another renderable text that is too brief.',
          segments: [
            {
              startMs: 10000,
              endMs: 10400,
              text: 'Renderable text that is too brief.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40000,
              endMs: 40400,
              text: 'Another renderable text that is too brief.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'non-renderable transcript diagnostics speech preflight contract',
      }),
      analyzeVideoSliceAudioActivity: async (request) => {
        nonRenderableTranscriptDiagnosticsCommands.push({ kind: 'analyze-audio-boundaries', request });
        throw new Error('audio activity analysis must not run when planning produced no renderable transcript-backed clip');
      },
      sliceVideo: async (request) => {
        nonRenderableTranscriptDiagnosticsCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'non-renderable-transcript-diagnostics-slice-task',
          request.clips[0]?.outputFileName ?? 'non-renderable-transcript-diagnostics.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'non-renderable-transcript-diagnostics-slice-task',
          'non-renderable-transcript-diagnostics-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'non-renderable-transcript-diagnostics-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `non-renderable-transcript-diagnostics-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `non-renderable-transcript-diagnostics-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 345678 + index,
            thumbnailByteSize: 23456 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const nonRenderableTranscriptDiagnosticsFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/non-renderable-transcript-diagnostics.mp4',
      'non-renderable-transcript-diagnostics.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-non-renderable-transcript-diagnostics',
        file: nonRenderableTranscriptDiagnosticsFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'cover',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const nonRenderableTranscriptDiagnosticsTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      nonRenderableTranscriptDiagnosticsTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice non-renderable transcript workflow completes with deterministic fallback slices',
    );
    assertRule(
      nonRenderableTranscriptDiagnosticsCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders deterministic fallback slices when transcript-backed planning has no renderable segment',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const sparseTranscriptSliceCommands = [];
    const sparseTranscriptBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        sparseTranscriptSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'sparse-transcript-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/sparse-transcript-source.mp4',
          byteSize: 654000,
          name: 'sparse-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
        };
      },
      transcribeMedia: async (request) => {
        sparseTranscriptSliceCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'sparse-transcript-task',
          'sparse-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'sparse-transcript-artifact',
          taskUuid: 'sparse-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: 'How to remove silent intros from short video clips. How to remove silent intros from short video clips. Then keep only the complete spoken payoff.',
          segments: [
            {
              startMs: 10000,
              endMs: 15600,
              text: 'How to remove silent intros from short video clips.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 31000,
              endMs: 36600,
              text: 'How to remove silent intros from short video clips.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 55000,
              endMs: 60700,
              text: 'Then keep only the complete spoken payoff.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'sparse transcript speech preflight contract',
      }),
      sliceVideo: async (request) => {
        sparseTranscriptSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'sparse-transcript-slice-task',
          'sparse-transcript-slice-artifact-1.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'sparse-transcript-slice-task',
          'sparse-transcript-slice-thumb-1.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'sparse-transcript-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `sparse-transcript-slice-artifact-${index + 1}`,
            artifactPath: index === 0
              ? output.artifactPath
              : createNativeTaskOutputArtifact(
                  'sparse-transcript-slice-task',
                  `sparse-transcript-slice-artifact-${index + 1}.mp4`,
                  configuredOutputDirectory,
                ).artifactPath,
            thumbnailArtifactUuid: `sparse-transcript-slice-thumb-${index + 1}`,
            thumbnailArtifactPath: index === 0
              ? thumbnail.artifactPath
              : createNativeTaskCoverArtifact(
                  'sparse-transcript-slice-task',
                  `sparse-transcript-slice-thumb-${index + 1}.jpg`,
                  configuredOutputDirectory,
                ).artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567,
            thumbnailByteSize: 12345,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-sparse-transcript-plan',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        sparseTranscriptBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        const rankedCandidates = [
          ...(prompt.candidates?.filter((candidate) => candidate.risks?.includes('transcript-repeat-filtered')) ?? []),
          ...(prompt.candidates?.filter((candidate) => !candidate.risks?.includes('transcript-repeat-filtered')) ?? []),
        ];
        return {
          id: 'sparse-transcript-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds: rankedCandidates.map((candidate) => candidate.id),
            referencedUnitIds: prompt.contentUnits?.map((unit) => unit.id) ?? [],
            reviewNotes: ['Trimmed speech candidate'],
          }),
          runtime,
        };
      },
    });
    const sparseTranscriptSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/sparse-transcript-source.mp4',
      'sparse-transcript-source.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-sparse-transcript-video-slice',
        file: sparseTranscriptSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'cover',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const sparseTranscriptPrompt = JSON.parse(
      sparseTranscriptBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    const sparseTranscriptSliceRequest = sparseTranscriptSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const sparseTranscriptTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'sparse-transcript-slice-task');
    assertEqual(
      sparseTranscriptPrompt?.candidates?.length,
      2,
      'video slice workflow removes repeated speech-to-text candidates before asking the Smart Cut Engine LLM reviewer',
    );
    assertRule(
      sparseTranscriptPrompt?.candidates?.some((candidate) => candidate.risks?.includes('transcript-repeat-filtered')),
      'video slice workflow tells the Smart Cut Engine LLM reviewer when repeated speech-to-text content was filtered',
    );
    assertRule(
      sparseTranscriptSliceRequest?.clips?.every((clip) => clip.durationMs < 7000),
      'video slice workflow sends native slicing short speech-aligned windows instead of padding sparse speech to the requested minimum duration',
    );
    assertRule(
      sparseTranscriptSliceRequest?.clips?.every((clip) => clip.startMs >= 9800),
      'video slice workflow trims leading silence before native rendering sparse transcript windows',
    );
    assertRule(
      sparseTranscriptTask?.sliceResults?.every((slice) => (slice.boundaryPaddingAfterMs ?? 0) <= 500),
      'video slice workflow records short trailing speech boundary padding for sparse transcript slices',
    );
    assertRule(
      sparseTranscriptTask?.sliceResults?.some((slice) => slice.risks?.includes('transcript-repeat-filtered')),
      'video slice workflow records repeated transcript filtering on task slice results',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const transcriptCandidateSliceCommands = [];
    const transcriptCandidateBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        transcriptCandidateSliceCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'transcript-candidate-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/transcript-candidate-slice-asset.mp4',
          byteSize: 654000,
          name: 'transcript-candidate-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 62000,
        };
      },
      transcribeMedia: async (request) => {
        transcriptCandidateSliceCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'transcript-candidate-task',
          'transcript-candidate-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'transcript-candidate-transcript-artifact',
          taskUuid: 'transcript-candidate-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: 'Watch this case background. Then the real spike comes from concentrated user pain. So this is the complete short-video payoff.',
          segments: [
            {
              startMs: 0,
              endMs: 12000,
              text: 'Watch this case background.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 12000,
              endMs: 26000,
              text: 'Then the real spike comes from concentrated user pain.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 26000,
              endMs: 41000,
              text: 'So this is the complete short-video payoff.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'transcript candidate speech preflight contract',
      }),
      sliceVideo: async (request) => {
        transcriptCandidateSliceCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'transcript-candidate-slice-task',
          'transcript-candidate-slice-artifact-1.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'transcript-candidate-slice-task',
          'transcript-candidate-slice-thumb-1.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'transcript-candidate-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'transcript-candidate-slice-artifact-1',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'transcript-candidate-slice-thumb-1',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(request.clips[0], request.noiseReduction),
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-transcript-candidate-plan',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        transcriptCandidateBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        const selectedCandidate =
          prompt.candidates?.find((candidate) => String(candidate.title ?? '').includes('complete short-video payoff')) ??
          prompt.candidates?.[0];
        const rankedCandidateIds = [
          selectedCandidate?.id,
          ...(prompt.candidates?.map((candidate) => candidate.id) ?? []),
        ].filter((candidateId, index, ids) =>
          typeof candidateId === 'string' && candidateId && ids.indexOf(candidateId) === index
        );
        return {
          id: 'transcript-candidate-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds,
            referencedUnitIds: prompt.contentUnits?.map((unit) => unit.id) ?? [],
            reviewNotes: ['Selected complete transcript-derived candidate by stable ID.'],
          }),
          runtime,
        };
      },
    });
    const transcriptCandidateSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/transcript-candidate-source.mp4',
      'transcript-candidate-source.mp4',
    );
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-transcript-candidate-video-slice',
        file: transcriptCandidateSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'cover',
        idealDuration: 45,
        continuityLevel: 'standard',
        customKeywords: ['retention'],
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const transcriptCandidatePrompt = JSON.parse(
      transcriptCandidateBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    const transcriptCandidateReviewCandidate =
      transcriptCandidatePrompt?.candidates?.find((candidate) =>
        String(candidate.title ?? '').includes('complete short-video payoff')
      ) ??
      transcriptCandidatePrompt?.candidates?.[0];
    const transcriptCandidateReviewUnit =
      transcriptCandidatePrompt?.contentUnits?.find((unit) =>
        String(unit.text ?? '').includes('complete short-video payoff')
      ) ??
      transcriptCandidatePrompt?.contentUnits?.[0];
    const transcriptCandidateSliceRequest = transcriptCandidateSliceCommands.find((entry) => entry.kind === 'slice')?.request;
    const transcriptCandidateTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'transcript-candidate-slice-task');
    assertRule(
      Boolean(transcriptCandidateReviewCandidate),
      'video slice workflow sends Smart Cut Engine candidate ids to the ID-only LLM reviewer',
    );
    assertRule(
      Array.isArray(transcriptCandidateReviewCandidate?.unitIds) &&
        transcriptCandidateReviewCandidate.unitIds.length > 0,
      'video slice workflow sends candidate-to-content-unit links to the ID-only LLM reviewer',
    );
    assertRule(
      Boolean(transcriptCandidateReviewUnit?.id) &&
        String(transcriptCandidateReviewUnit?.text ?? '').includes('complete short-video payoff'),
      'video slice workflow sends transcript-derived content units to the ID-only LLM reviewer',
    );
    assertNumberBetween(
      transcriptCandidateReviewUnit?.completenessScore,
      0.65,
      1,
      'video slice workflow sends content-unit completeness evidence to the ID-only LLM reviewer',
    );
    assertNumberBetween(
      transcriptCandidateReviewUnit?.continuityScore,
      0.65,
      1,
      'video slice workflow sends content-unit continuity evidence to the ID-only LLM reviewer',
    );
    assertNumberBetween(
      transcriptCandidateReviewUnit?.publishabilityScore,
      0.65,
      1,
      'video slice workflow sends content-unit publishability evidence to the ID-only LLM reviewer',
    );
    assertNotOwnProperty(
      transcriptCandidatePrompt,
      'requestedClipCount',
      'video slice workflow does not send requested clip counts to the ID-only LLM reviewer',
    );
    assertNotOwnProperty(
      transcriptCandidatePrompt,
      'candidateWindows',
      'video slice workflow does not send legacy timestamp candidate windows to the ID-only LLM reviewer',
    );
    assertNotOwnProperty(
      transcriptCandidatePrompt,
      'transcriptTimeline',
      'video slice workflow does not send legacy raw transcript timeline prompts to the ID-only LLM reviewer',
    );
    assertRule(
      transcriptCandidatePrompt?.rules?.some((rule) =>
        rule.includes('Rank every executable candidate id once')
      ),
      'video slice workflow tells the ID-only LLM reviewer to rank stable candidate ids',
    );
    assertRule(
      transcriptCandidatePrompt?.rules?.some((rule) =>
        rule.includes('Do not output timestamps')
      ),
      'video slice workflow tells the ID-only LLM reviewer not to output timestamps',
    );
    assertEqual(
      transcriptCandidatePrompt?.customKeywords?.[0],
      'retention',
      'video slice workflow sends custom keywords to the ID-only LLM reviewer',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.renderProfile?.targetAspectRatio,
      '9:16',
      'video slice workflow sends target aspect ratio to native slice rendering',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.renderProfile?.objectFit,
      'cover',
      'video slice workflow sends target object-fit to native slice rendering',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.clips?.[0]?.qualityScore,
      undefined,
      'video slice workflow keeps AI planning metadata out of native slice clip requests',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.clips?.[0]?.startMs,
      0,
      'video slice workflow expands connector-led transcript candidates backward for context continuity and clamps leading padding at source start',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.clips?.[0]?.durationMs,
      41250,
      'video slice workflow keeps the repaired connector-led candidate through the complete payoff segment with trailing boundary padding',
    );
    assertEqual(
      transcriptCandidateSliceRequest?.clips?.[0]?.outputFileName,
      '01-watch-this-case-background.mp4',
      'video slice workflow preserves Smart Cut Engine transcript-derived titles in native output filenames',
    );
    assertRule(
      transcriptCandidateSliceRequest?.clips?.every((clip, index, clips) =>
        index === 0 || clip.startMs >= clips[index - 1].startMs + clips[index - 1].durationMs,
      ),
      'video slice workflow keeps repaired transcript candidate clips non-overlapping',
    );
    assertEqual(
      typeof transcriptCandidateTask?.sliceResults?.[0]?.qualityScore,
      'number',
      'video slice workflow preserves deterministic Smart Cut Engine quality evidence on task slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.name,
      '01-watch-this-case-background.mp4',
      'video slice workflow names persisted slice results from Smart Cut Engine transcript-derived titles',
    );
    assertEqual(
      typeof transcriptCandidateTask?.sliceResults?.[0]?.continuityScore,
      'number',
      'video slice workflow preserves deterministic Smart Cut Engine continuity evidence on task slice results',
    );
    assertEqual(
      typeof transcriptCandidateTask?.sliceResults?.[0]?.storyShape,
      'string',
      'video slice workflow preserves planner story-shape metadata on task slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.summary,
      'Speech-to-text window: Watch this case background. Then the real spike comes from concentrated user pain. So this is the complete short-video payoff.',
      'video slice workflow preserves deterministic Smart Cut Engine summary on task slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.reason,
      'Planner selected a continuous speech-to-text window and aligned slice boundaries to transcript segment edges for complete voice content.',
      'video slice workflow preserves deterministic Smart Cut Engine selection reason on task slice results',
    );
    assertArrayIncludes(
      transcriptCandidateTask?.sliceResults?.[0]?.risks,
      'semantic-story-merged',
      'video slice workflow records deterministic semantic-story merge risks on task slice results',
    );
    assertArrayIncludes(
      transcriptCandidateTask?.sliceResults?.[0]?.risks,
      'connector-repaired',
      'video slice workflow preserves deterministic transcript boundary repair risks on task slice results',
    );
    assertRule(
      !transcriptCandidateTask?.sliceResults?.[0]?.risks?.includes('needs-cover-title'),
      'video slice workflow ignores LLM-invented candidate-id risks on task slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.sourceStartMs,
      0,
      'video slice workflow records repaired source start metadata on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.sourceEndMs,
      41250,
      'video slice workflow records padded source end metadata on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.speechStartMs,
      0,
      'video slice workflow records repaired speech start metadata on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.speechEndMs,
      41000,
      'video slice workflow records repaired speech end metadata on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.boundaryPaddingAfterMs,
      250,
      'video slice workflow records speech boundary padding metadata on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.transcriptText,
      'Watch this case background. Then the real spike comes from concentrated user pain. So this is the complete short-video payoff.',
      'video slice workflow records repaired transcript text on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.transcriptSegmentCount,
      3,
      'video slice workflow records structured speech-to-text segment counts on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.transcriptSegments?.[2]?.text,
      'So this is the complete short-video payoff.',
      'video slice workflow records structured speech-to-text transcript segments on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.transcriptCoverageScore,
      1,
      'video slice workflow records repaired transcript coverage scores on slice results',
    );
    assertEqual(
      transcriptCandidateTask?.sliceResults?.[0]?.speechContinuityGrade,
      'repaired',
      'video slice workflow records repaired speech continuity grades on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.publishabilityScore,
      0.7,
      1,
      'video slice workflow records composite publishability scores on slice results',
    );
    assertRule(
      ['excellent', 'good'].includes(transcriptCandidateTask?.sliceResults?.[0]?.publishabilityGrade),
      `video slice workflow records publishability grades on slice results (got ${JSON.stringify(transcriptCandidateTask?.sliceResults?.[0]?.publishabilityGrade)})`,
    );
    assertRule(
      Array.isArray(transcriptCandidateTask?.sliceResults?.[0]?.publishabilityIssues),
      'video slice workflow records normalized publishability issue tags on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.boundaryQualityScore,
      0.65,
      1,
      'video slice workflow records boundary quality scores on slice results',
    );
    assertRule(
      ['strong', 'contextual', 'weak'].includes(transcriptCandidateTask?.sliceResults?.[0]?.hookStrength),
      'video slice workflow records hook strength grades on slice results',
    );
    assertRule(
      ['complete', 'soft', 'open'].includes(transcriptCandidateTask?.sliceResults?.[0]?.endingCompleteness),
      'video slice workflow records ending completeness grades on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.contentArcScore,
      0.65,
      1,
      'video slice workflow records content-arc scores on slice results',
    );
    assertRule(
      ['complete', 'partial', 'thin'].includes(transcriptCandidateTask?.sliceResults?.[0]?.contentArcGrade),
      'video slice workflow records content-arc grades on slice results',
    );
    assertArrayIncludes(
      transcriptCandidateTask?.sliceResults?.[0]?.contentArcStages,
      'payoff',
      'video slice workflow records detected content-arc stages on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.topicCoherenceScore,
      0.65,
      1,
      'video slice workflow records topic coherence scores on slice results',
    );
    assertRule(
      ['strong', 'mixed', 'weak'].includes(transcriptCandidateTask?.sliceResults?.[0]?.topicCoherenceGrade),
      'video slice workflow records topic coherence grades on slice results',
    );
    assertRule(
      typeof transcriptCandidateTask?.sliceResults?.[0]?.topicShiftCount === 'number',
      'video slice workflow records topic shift counts on slice results',
    );
    assertRule(
      Array.isArray(transcriptCandidateTask?.sliceResults?.[0]?.topicKeywords),
      'video slice workflow records topic keywords on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.platformReadinessScore,
      0.65,
      1,
      'video slice workflow records platform readiness scores on slice results',
    );
    assertRule(
      ['ready', 'review', 'reject'].includes(transcriptCandidateTask?.sliceResults?.[0]?.platformReadinessGrade),
      'video slice workflow records platform readiness grades on slice results',
    );
    assertRule(
      Array.isArray(transcriptCandidateTask?.sliceResults?.[0]?.platformReadinessIssues),
      'video slice workflow records platform readiness issue tags on slice results',
    );
    assertNumberBetween(
      transcriptCandidateTask?.sliceResults?.[0]?.sentenceBoundaryIntegrityScore,
      0,
      1,
      'video slice workflow records sentence boundary integrity scores on slice results',
    );
    assertRule(
      ['clean', 'repaired', 'broken'].includes(transcriptCandidateTask?.sliceResults?.[0]?.sentenceBoundaryIntegrityGrade),
      `video slice workflow records sentence boundary integrity grades on slice results (got ${JSON.stringify(transcriptCandidateTask?.sliceResults?.[0]?.sentenceBoundaryIntegrityGrade)})`,
    );
    assertRule(
      Array.isArray(transcriptCandidateTask?.sliceResults?.[0]?.sentenceBoundaryIssues),
      'video slice workflow records sentence boundary issue tags on slice results',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const longTranscriptPromptCommands = [];
    const longTranscriptPromptBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        longTranscriptPromptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'long-transcript-prompt-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/long-transcript-source.mp4',
          byteSize: 654000,
          name: 'long-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 2_100_000,
        };
      },
      transcribeMedia: async (request) => {
        longTranscriptPromptCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'long-transcript-prompt-task',
          'long-transcript-prompt.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'long-transcript-prompt-artifact',
          taskUuid: 'long-transcript-prompt-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: 'long transcript prompt fixture',
          segments: Array.from({ length: 260 }, (_, index) => {
            const startMs = index * 8_000;
            const keyWindowTextByIndex = {
              12: 'Watch the onboarding funnel setup, signup pain, pricing conflict, and complete activation payoff.',
              130: 'Watch the refund workflow setup, support queue pain, escalation conflict, and complete retention payoff.',
              238: 'Watch the creator analytics setup, audience dropoff pain, packaging conflict, and complete publishing payoff.',
            };
            return {
              startMs,
              endMs: startMs + 6_000,
              text: keyWindowTextByIndex[index] ?? `Routine long transcript context ${index}.`,
              speaker: 'Speaker 1',
            };
          }),
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'long transcript prompt speech preflight contract',
      }),
      sliceVideo: async (request) => {
        longTranscriptPromptCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'long-transcript-prompt-slice-task',
          request.clips[0]?.outputFileName ?? 'long-transcript-prompt-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'long-transcript-prompt-slice-task',
          'long-transcript-prompt-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'long-transcript-prompt-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'long-transcript-prompt-slice-artifact',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'long-transcript-prompt-thumb',
              thumbnailArtifactPath: thumbnail.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(request.clips[0], request.noiseReduction),
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-long-transcript-prompt',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        longTranscriptPromptBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        const selectedCandidate =
          prompt.candidates?.find((candidate) => String(candidate.title ?? '').includes('creator analytics')) ??
          prompt.candidates?.[0];
        const rankedCandidateIds = [
          selectedCandidate?.id,
          ...(prompt.candidates?.map((candidate) => candidate.id) ?? []),
        ].filter((candidateId, index, ids) =>
          typeof candidateId === 'string' && candidateId && ids.indexOf(candidateId) === index
        );
        return {
          id: 'long-transcript-prompt-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds,
            referencedUnitIds: prompt.contentUnits?.map((unit) => unit.id) ?? [],
            reviewNotes: ['Ranked late creator analytics candidate by stable ID.'],
          }),
          runtime,
        };
      },
    });
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-long-transcript-prompt-video-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/long-transcript-source.mp4',
          'long-transcript-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        targetPlatform: 'douyin',
        targetAspectRatio: '9:16',
        videoObjectFit: 'cover',
        minDuration: 5,
        maxDuration: 15,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
    );
    const longTranscriptPrompt = JSON.parse(
      longTranscriptPromptBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    assertRule(
      longTranscriptPrompt?.candidates?.some((candidate) => String(candidate.title ?? '').includes('creator analytics')),
      'video slice workflow sends late high-value Smart Cut Engine candidates to the ID-only LLM reviewer',
    );
    assertRule(
      longTranscriptPrompt?.contentUnits?.some((unit) => String(unit.text ?? '').includes('creator analytics')),
      'video slice workflow includes late transcript-derived content units instead of only the first transcript segments',
    );
    assertRule(
      (longTranscriptPrompt?.contentUnits?.length ?? 0) <= 80,
      'video slice workflow keeps ID-only LLM review content units bounded for long transcripts',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const nativeExtractorTextWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        localDirectorySelectCommandReady: true,
        audioExtractionCommandReady: true,
        audioExtractionFromAssetReady: true,
        videoGifCommandReady: false,
        videoSliceCommandReady: false,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionFileSelectCommandReady: true,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_transcribe_media'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeExtractorTextWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-extractor-text-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/native-extractor-text-asset.mp4',
          byteSize: 765000,
          name: 'native-extractor-text.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 765000,
        name: 'native-extractor-text.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      selectLocalVideoFile: async () => null,
      selectLocalDirectory: async () => null,
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for native extractor text workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF is not configured for native extractor text workflow contract');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'native extractor text workflow contract',
      }),
      selectSpeechTranscriptionFile: async () => null,
      transcribeMedia: async (request) => {
        nativeExtractorTextWorkflowCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'native-extractor-text-task',
          'native-extractor-text-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'native-extractor-text-transcript-artifact',
          taskUuid: 'native-extractor-text-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language,
          text: 'First real local transcript sentence. Second real local transcript sentence.',
          segments: [
            {
              startMs: 12000,
              endMs: 22000,
              text: 'First real local transcript sentence.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 26000,
              endMs: 36000,
              text: 'Second real local transcript sentence.',
              speaker: 'Speaker 2',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      sliceVideo: async () => {
        throw new Error('video slicing is not configured for native extractor text workflow contract');
      },
      compressVideo: async () => {
        throw new Error('video compression is not configured for native extractor text workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion is not configured for native extractor text workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement is not configured for native extractor text workflow contract');
      },
      sendLlmHttpRequest: async () => {
        throw new Error('LLM HTTP is not configured for native extractor text workflow contract');
      },
      saveLlmSecret: async (request) => ({
        secretName: request.secretName,
        saved: false,
      }),
      getLlmSecret: async (request) => ({
        secretName: request.secretName,
        configured: false,
      }),
      deleteLlmSecret: async (request) => ({
        secretName: request.secretName,
        deleted: false,
      }),
      listNativeTasks: async () => [],
      cancelNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        status: 0,
        canceled: false,
        message: 'not configured',
      }),
      recoverNativeTasks: async () => ({
        inspected: 0,
        recovered: 0,
        interrupted: 0,
        canceled: 0,
        expiredLeases: 0,
        deferred: 0,
        taskUuids: [],
      }),
      retryNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        retryTaskUuid: '',
        status: 0,
        retried: false,
        message: 'not configured',
      }),
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services);
    const nativeExtractorTextSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/native-extractor-text.mp4',
      'native-extractor-text.mp4',
    );
    const nativeExtractorTextWorkflowResult = await withImmediateTimers(async () =>
      processExtractorText({
        fileId: 'asset-source-native-extractor-text',
        file: nativeExtractorTextSourceFile,
        language: 'zh',
        format: 'raw',
        separateSpeakers: true,
      }),
    );
    const nativeExtractorTextTasks = readScopedStoredArray(services, 'tasks');
    const nativeExtractorTextTask = nativeExtractorTextTasks.find(
      (task) => task.id === nativeExtractorTextWorkflowResult.taskId,
    );
    const nativeExtractorTextAssets = readScopedStoredArray(services, 'assets');
    const nativeExtractorTextAsset = nativeExtractorTextAssets.find(
      (asset) => asset.sourceTaskId === nativeExtractorTextWorkflowResult.taskId,
    );
    assertEqual(nativeExtractorTextWorkflowResult.success, true, 'extractor text native workflow reports success');
    assertEqual(nativeExtractorTextWorkflowCommands[0]?.kind, 'import', 'extractor text native workflow imports local media before transcription');
    assertEqual(
      nativeExtractorTextWorkflowCommands[0]?.request?.outputRootDir,
      configuredOutputDirectory,
      'extractor text native workflow passes the configured output directory to media import',
    );
    assertEqual(nativeExtractorTextWorkflowCommands[1]?.kind, 'transcribe', 'extractor text native workflow calls local speech transcription after import');
    assertEqual(
      nativeExtractorTextWorkflowCommands[1]?.request?.assetUuid,
      'native-extractor-text-asset',
      'extractor text native workflow transcribes by imported assetUuid',
    );
    assertEqual(
      nativeExtractorTextWorkflowCommands[1]?.request?.language,
      'zh',
      'extractor text native workflow forwards the selected transcription language',
    );
    assertEqual(
      nativeExtractorTextWorkflowCommands[1]?.request?.executablePath,
      'D:/tools/whisper-cli.exe',
      'extractor text native workflow passes the configured local speech executable to transcription',
    );
    assertEqual(
      nativeExtractorTextWorkflowCommands[1]?.request?.modelPath,
      'D:/models/ggml-large-v3-turbo.bin',
      'extractor text native workflow passes the configured local speech model to transcription',
    );
    assertEqual(
      nativeExtractorTextWorkflowCommands[1]?.request?.outputRootDir,
      configuredOutputDirectory,
      'extractor text native workflow passes the configured output directory to local speech transcription',
    );
    assertEqual(
      nativeExtractorTextTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'extractor text native workflow completes the persisted task',
    );
    assertEqual(
      nativeExtractorTextTask?.extractedText?.[0]?.time,
      '00:00:12',
      'extractor text native workflow converts speech segment timing to extracted text timestamps',
    );
    assertEqual(
      nativeExtractorTextTask?.extractedText?.[0]?.text,
      'First real local transcript sentence.',
      'extractor text native workflow stores the real local speech transcription text',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptText,
      'First real local transcript sentence. Second real local transcript sentence.',
      'extractor text native workflow stores the complete speech-to-text transcript text on the task',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptSegments?.length,
      2,
      'extractor text native workflow stores reusable structured speech-to-text transcript segments on the task',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptSegments?.[1]?.speaker,
      'Speaker 2',
      'extractor text native workflow preserves speaker labels on structured transcript segments',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptSegmentCount,
      2,
      'extractor text native workflow records the structured transcript segment count',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptProviderId,
      'local-whisper-cli',
      'extractor text native workflow records the selected speech-to-text provider on the completed task',
    );
    assertEqual(
      nativeExtractorTextTask?.transcriptSourceAssetId,
      'native-extractor-text-asset',
      'extractor text native workflow links transcript evidence to the imported source asset',
    );
    assertEqual(
      nativeExtractorTextAsset?.type,
      'doc',
      'extractor text native workflow stores the transcript as a document asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const smartSliceMissingNativeRenderCommands = [];
    services.configureAutoCutNativeHostClient({
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        localDirectorySelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoSliceCommandReady: false,
        videoSliceAudioActivityAnalysisCommandReady: false,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionFileSelectCommandReady: true,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_transcribe_media'],
      }),
      importMediaFile: async (request) => {
        smartSliceMissingNativeRenderCommands.push({ kind: 'import', request });
        throw new Error('Smart Slice must not import media when native video slicing is unavailable');
      },
      transcribeMedia: async (request) => {
        smartSliceMissingNativeRenderCommands.push({ kind: 'transcribe', request });
        throw new Error('Smart Slice must not transcribe when native video slicing is unavailable');
      },
      sliceVideo: async (request) => {
        smartSliceMissingNativeRenderCommands.push({ kind: 'slice', request });
        throw new Error('Smart Slice must not render when native video slicing is unavailable');
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const smartSliceMissingNativeRenderSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/smart-slice-missing-native-render.mp4',
      'smart-slice-missing-native-render.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoSlice({
            fileId: 'asset-source-smart-slice-missing-native-render',
            file: smartSliceMissingNativeRenderSourceFile,
            mode: 'contract-mode',
            llmModel: 'deepseek-chat',
            minDuration: 15,
            maxDuration: 60,
            baseAlgorithm: 'scene',
            highlightEngine: 'emotion',
            enableNoiseReduction: true,
            enableCoughFilter: true,
            enableRepeatFilter: true,
          }),
        ),
      'the native video slicing command is unavailable',
      'Smart Slice native render preflight fails with the missing render command reason',
    );
    const smartSliceMissingNativeRenderTask = readScopedStoredArray(services, 'tasks')[0];
    const smartSliceMissingNativeRenderMessage = String(smartSliceMissingNativeRenderTask?.errorMessage ?? '');
    assertEqual(
      smartSliceMissingNativeRenderTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'Smart Slice native render preflight stores a failed task',
    );
    assertIncludes(
      smartSliceMissingNativeRenderMessage,
      'the native video slicing command is unavailable',
      'Smart Slice native render preflight stores the missing render command reason',
    );
    assertRule(
      !smartSliceMissingNativeRenderMessage.includes('requires a trusted local desktop media file'),
      'Smart Slice native render preflight does not blame trusted local source when a trusted source was provided',
    );
    assertRule(
      !smartSliceMissingNativeRenderMessage.includes('speech-to-text'),
      'Smart Slice native render preflight does not blame speech-to-text when STT is ready',
    );
    assertEqual(
      smartSliceMissingNativeRenderCommands.length,
      0,
      'Smart Slice native render preflight does not import, transcribe, or render after a hard native capability is missing',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const extractorTextAudioVideoCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        extractorTextAudioVideoCommands.push({ kind: 'import', request });
        const isAudio = request.sourcePath.endsWith('.mp3');
        return {
          assetUuid: isAudio ? 'extractor-text-audio-asset' : 'extractor-text-video-asset',
          sandboxPath: isAudio
            ? 'D:/autocut-configured-output/inputs/extractor-text-audio-asset.mp3'
            : 'D:/autocut-configured-output/inputs/extractor-text-video-asset.mp4',
          byteSize: isAudio ? 123000 : 456000,
          name: isAudio ? 'extractor-text-audio.mp3' : 'extractor-text-video.mp4',
          mediaType: isAudio ? 'audio' : 'video',
          hasAudioStream: true,
          hasVideoStream: !isAudio,
          mimeType: isAudio ? 'audio/mpeg' : 'video/mp4',
        };
      },
      transcribeMedia: async (request) => {
        extractorTextAudioVideoCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          request.assetUuid === 'extractor-text-audio-asset'
            ? 'extractor-text-audio-task'
            : 'extractor-text-video-task',
          'transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: `${request.assetUuid}-transcript`,
          taskUuid: request.assetUuid === 'extractor-text-audio-asset'
            ? 'extractor-text-audio-task'
            : 'extractor-text-video-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language,
          text: request.assetUuid === 'extractor-text-audio-asset'
            ? 'uh Bonjour audio transcript.'
            : 'um Video transcript, you know.',
          segments: [
            {
              startMs: 0,
              endMs: 2400,
              text: request.assetUuid === 'extractor-text-audio-asset'
                ? 'uh Bonjour audio transcript.'
                : 'um Video transcript, you know.',
              speaker: 'Speaker 1',
            },
            ...(request.assetUuid === 'extractor-text-video-asset'
              ? [
                  {
                    startMs: 2500,
                    endMs: 3000,
                    text: 'um',
                    speaker: 'Speaker 1',
                  },
                ]
              : []),
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'extractor text audio video speech preflight contract',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const extractorTextAudioSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/extractor-text-audio.mp3',
      'extractor-text-audio.mp3',
      { mimeType: 'audio/mpeg', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
    );
    const extractorTextVideoSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/extractor-text-video.mp4',
      'extractor-text-video.mp4',
      { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: true, hasVideoStream: true },
    );
    const extractorTextAudioResult = await withImmediateTimers(async () =>
      processExtractorText({
        fileId: 'asset-source-extractor-text-audio',
        file: extractorTextAudioSourceFile,
        language: 'fr',
        format: 'raw',
        separateSpeakers: false,
      }),
    );
    const extractorTextVideoResult = await withImmediateTimers(async () =>
      processExtractorText({
        fileId: 'asset-source-extractor-text-video',
        file: extractorTextVideoSourceFile,
        language: 'ja-JP',
        format: 'filtered',
        separateSpeakers: true,
      }),
    );
    const audioTranscribeCommand = extractorTextAudioVideoCommands.find(
      (entry) => entry.kind === 'transcribe' && entry.request.assetUuid === 'extractor-text-audio-asset',
    );
    const videoTranscribeCommand = extractorTextAudioVideoCommands.find(
      (entry) => entry.kind === 'transcribe' && entry.request.assetUuid === 'extractor-text-video-asset',
    );
    const extractorTextAudioTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === extractorTextAudioResult.taskId);
    const extractorTextVideoTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.id === extractorTextVideoResult.taskId);
    assertEqual(extractorTextAudioResult.success, true, 'extractor text accepts trusted local audio sources');
    assertEqual(extractorTextVideoResult.success, true, 'extractor text accepts trusted local video sources');
    assertEqual(audioTranscribeCommand?.request?.language, 'fr', 'extractor text forwards non-default audio transcription languages');
    assertEqual(videoTranscribeCommand?.request?.language, 'ja-JP', 'extractor text forwards BCP-47 style video transcription languages');
    assertEqual(audioTranscribeCommand?.request?.modelPath, 'D:/models/ggml-large-v3-turbo.gguf', 'extractor text audio workflow uses the configured local model path');
    assertEqual(videoTranscribeCommand?.request?.executablePath, 'D:/tools/whisper-cli.exe', 'extractor text video workflow uses the configured local executable path');
    assertEqual(extractorTextAudioTask?.extractedText?.[0]?.speaker, 'Speaker 1', 'extractor text audio workflow can disable speaker separation without losing text');
    assertEqual(extractorTextAudioTask?.extractedText?.[0]?.text, 'uh Bonjour audio transcript.', 'extractor text raw mode preserves native speech-to-text filler words');
    assertEqual(extractorTextVideoTask?.extractedText?.[0]?.text, 'Video transcript.', 'extractor text filtered mode removes redundant filler words from native speech-to-text');
    assertEqual(extractorTextVideoTask?.extractedText?.length, 1, 'extractor text filtered mode removes pure filler speech segments');
    assertEqual(extractorTextAudioTask?.transcriptText, 'uh Bonjour audio transcript.', 'extractor text audio workflow stores complete raw transcript text');
    assertEqual(extractorTextAudioTask?.transcriptSegments?.[0]?.text, 'uh Bonjour audio transcript.', 'extractor text audio workflow stores structured raw transcript segments');
    assertEqual(extractorTextVideoTask?.transcriptText, 'Video transcript.', 'extractor text video filtered workflow stores the filtered visible transcript text');
    assertEqual(extractorTextVideoTask?.transcriptSegments?.length, 1, 'extractor text video filtered workflow drops pure filler structured transcript segments');
    assertEqual(extractorTextVideoTask?.transcriptSegments?.[0]?.startMs, 0, 'extractor text video workflow keeps structured transcript segment timing');
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const subtitleTranslateAudioCommands = [];
    const subtitleTranslateBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        subtitleTranslateAudioCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-translate-audio-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/subtitle-translate-audio-asset.flac',
          byteSize: 223000,
          name: 'subtitle-translate-audio.flac',
          mediaType: 'audio',
          hasAudioStream: true,
          hasVideoStream: false,
          mimeType: 'audio/flac',
          durationMs: 7200,
        };
      },
      transcribeMedia: async (request) => {
        subtitleTranslateAudioCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'subtitle-translate-audio-task',
          'subtitle-translate-audio-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'subtitle-translate-audio-transcript',
          taskUuid: 'subtitle-translate-audio-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language,
          text: 'Hello from audio. Subtitle output works.',
          segments: [
            {
              startMs: 0,
              endMs: 2400,
              text: 'Hello from audio.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 2600,
              endMs: 5200,
              text: 'Subtitle output works.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'subtitle translate audio speech preflight contract',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-subtitle-translate',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        subtitleTranslateBridgeRequests.push({ request, runtime });
        return {
          id: 'subtitle-translation-bridge-result',
          model: request.model,
          runtime,
          content: JSON.stringify({
            segments: [
              { index: 1, text: 'Translated hello from audio.' },
              { index: 2, text: 'Translated subtitle output works.' },
            ],
          }),
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const subtitleTranslateAudioSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/subtitle-translate-audio.flac',
      'subtitle-translate-audio.flac',
      { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
    );
    const subtitleTranslateAudioResult = await withImmediateTimers(async () =>
      processSubtitleTranslate({
        fileId: 'asset-source-subtitle-translate-audio',
        file: subtitleTranslateAudioSourceFile,
        sourceLang: 'auto',
        targetLang: 'en',
        keepOriginal: true,
        hardcode: false,
      }),
    );
    const subtitleTranslateAudioTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === subtitleTranslateAudioResult.taskId,
    );
    const subtitleTranslateAudioAsset = readScopedStoredArray(services, 'assets').find(
      (asset) => asset.sourceTaskId === subtitleTranslateAudioResult.taskId,
    );
    assertEqual(subtitleTranslateAudioResult.success, true, 'subtitle translate accepts trusted local audio sources');
    assertEqual(subtitleTranslateAudioCommands[0]?.kind, 'import', 'subtitle translate imports local audio before transcription');
    assertEqual(
      subtitleTranslateAudioCommands[0]?.request?.sourcePath,
      'D:/media/subtitle-translate-audio.flac',
      'subtitle translate passes trusted audio source paths to media import',
    );
    assertEqual(subtitleTranslateAudioCommands[1]?.kind, 'transcribe', 'subtitle translate transcribes imported audio media');
    assertEqual(
      subtitleTranslateAudioCommands[1]?.request?.assetUuid,
      'subtitle-translate-audio-asset',
      'subtitle translate transcribes by imported audio assetUuid',
    );
    assertEqual(
      subtitleTranslateAudioCommands[1]?.request?.language,
      'auto',
      'subtitle translate forwards the selected source language to speech transcription',
    );
    assertEqual(
      subtitleTranslateAudioTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'subtitle translate audio workflow completes the persisted task',
    );
    assertEqual(
      subtitleTranslateAudioTask?.transcriptText,
      'Hello from audio. Subtitle output works.',
      'subtitle translate stores transcript text from trusted audio media',
    );
    assertEqual(
      subtitleTranslateAudioTask?.translationText,
      'Translated hello from audio. Translated subtitle output works.',
      'subtitle translate stores target-language translation text from the approved AI SDK bridge',
    );
    assertEqual(
      subtitleTranslateAudioTask?.translationSegments?.[0]?.text,
      'Translated hello from audio.',
      'subtitle translate stores structured target-language subtitle segments',
    );
    assertEqual(
      subtitleTranslateBridgeRequests.length,
      1,
      'subtitle translate dispatches one target-language translation request through the approved AI SDK bridge',
    );
    assertIncludes(
      subtitleTranslateBridgeRequests[0]?.request?.messages?.[1]?.content ?? '',
      '"targetLang":"en"',
      'subtitle translate includes the selected target language in the translation prompt payload',
    );
    assertEqual(
      subtitleTranslateAudioTask?.transcriptSegments?.length,
      2,
      'subtitle translate stores structured transcript segments from trusted audio media',
    );
    assertEqual(
      subtitleTranslateAudioTask?.subtitleFormat,
      'srt',
      'subtitle translate stores the generated subtitle format on the completed task',
    );
    assertRule(
      Boolean(subtitleTranslateAudioTask?.subtitleUrl),
      'subtitle translate stores a downloadable subtitle URL on the completed task',
    );
    const subtitleTranslateAudioSubtitleText = await readObjectUrlText(subtitleTranslateAudioTask?.subtitleUrl);
    assertIncludes(
      subtitleTranslateAudioSubtitleText,
      'Hello from audio.',
      'subtitle translate keepOriginal=true writes source-language text into the generated SRT',
    );
    assertIncludes(
      subtitleTranslateAudioSubtitleText,
      'Translated hello from audio.',
      'subtitle translate writes target-language text into the generated SRT',
    );
    assertEqual(
      subtitleTranslateAudioAsset?.type,
      'doc',
      'subtitle translate stores the SRT subtitle as a document asset',
    );
    assertRule(
      subtitleTranslateAudioAsset?.name?.endsWith('.srt'),
      'subtitle translate generated document asset uses an SRT filename',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const subtitleTranslateHardcodeCommands = [];
    const subtitleTranslateHardcodeBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        subtitleTranslateHardcodeCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-translate-hardcode-video-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/subtitle-translate-hardcode-video-asset.mp4',
          byteSize: 923000,
          name: 'subtitle-translate-hardcode-video.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 7200,
        };
      },
      transcribeMedia: async (request) => {
        subtitleTranslateHardcodeCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'subtitle-translate-hardcode-transcript-task',
          'subtitle-translate-hardcode-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'subtitle-translate-hardcode-transcript',
          taskUuid: 'subtitle-translate-hardcode-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language,
          text: 'Hello from video. Hard subtitle output works.',
          segments: [
            {
              startMs: 0,
              endMs: 2400,
              text: 'Hello from video.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 2600,
              endMs: 5200,
              text: 'Hard subtitle output works.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'subtitle translate hardcode speech preflight contract',
      }),
      sliceVideo: async (request) => {
        subtitleTranslateHardcodeCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'subtitle-translate-hardcode-render-task',
          'subtitle-translate-hardcode-video.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskOutputArtifact(
          'subtitle-translate-hardcode-render-task',
          'cover/subtitle-translate-hardcode-video.jpg',
          configuredOutputDirectory,
        );
        const subtitle = createNativeTaskOutputArtifact(
          'subtitle-translate-hardcode-render-task',
          'subtitle-translate-hardcode-video.srt',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'subtitle-translate-hardcode-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: [
            {
              artifactUuid: 'subtitle-translate-hardcode-video-artifact',
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: 'subtitle-translate-hardcode-cover',
              thumbnailArtifactPath: thumbnail.artifactPath,
              subtitleArtifactUuid: 'subtitle-translate-hardcode-subtitle',
              subtitleArtifactPath: subtitle.artifactPath,
              taskOutputDir: output.taskOutputDir,
              byteSize: 823000,
              thumbnailByteSize: 12000,
              subtitleByteSize: 900,
              subtitleFormat: 'srt',
              format: 'mp4',
              startMs: request.clips[0]?.startMs ?? 0,
              durationMs: request.clips[0]?.durationMs ?? 7200,
              label: request.clips[0]?.label ?? 'Translated subtitle video',
              transcriptText: request.clips[0]?.transcriptText,
              transcriptSegments: request.clips[0]?.transcriptSegments,
              transcriptSegmentCount: request.clips[0]?.transcriptSegmentCount,
              transcriptCoverageScore: request.clips[0]?.transcriptCoverageScore,
              speechContinuityGrade: request.clips[0]?.speechContinuityGrade,
              sourceStartMs: request.clips[0]?.sourceStartMs,
              sourceEndMs: request.clips[0]?.sourceEndMs,
              speechStartMs: request.clips[0]?.speechStartMs,
              speechEndMs: request.clips[0]?.speechEndMs,
            },
          ],
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-subtitle-hardcode',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        subtitleTranslateHardcodeBridgeRequests.push({ request, runtime });
        return {
          id: 'subtitle-hardcode-translation-bridge-result',
          model: request.model,
          runtime,
          content: JSON.stringify({
            segments: [
              { index: 1, text: 'Translated hello from video.' },
              { index: 2, text: 'Translated hard subtitle output works.' },
            ],
          }),
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const subtitleTranslateHardcodeSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/subtitle-translate-hardcode-video.mp4',
      'subtitle-translate-hardcode-video.mp4',
      { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: true, hasVideoStream: true },
    );
    const subtitleTranslateHardcodeResult = await withImmediateTimers(async () =>
      processSubtitleTranslate({
        fileId: 'asset-source-subtitle-translate-hardcode-video',
        file: subtitleTranslateHardcodeSourceFile,
        sourceLang: 'auto',
        targetLang: 'en',
        keepOriginal: true,
        hardcode: true,
      }),
    );
    const subtitleTranslateHardcodeTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === subtitleTranslateHardcodeResult.taskId,
    );
    const subtitleTranslateHardcodeAssets = readScopedStoredArray(services, 'assets').filter(
      (asset) => asset.sourceTaskId === subtitleTranslateHardcodeResult.taskId,
    );
    const subtitleTranslateHardcodeSliceCommand = subtitleTranslateHardcodeCommands.find((entry) => entry.kind === 'slice');
    assertEqual(subtitleTranslateHardcodeResult.success, true, 'subtitle translate hardcode video workflow reports success');
    assertEqual(subtitleTranslateHardcodeSliceCommand?.request?.subtitleMode, 'both', 'subtitle translate hardcode video requests burned video plus SRT sidecar');
    assertEqual(subtitleTranslateHardcodeSliceCommand?.request?.subtitleFormat, 'srt', 'subtitle translate hardcode video requests SRT subtitle data');
    assertIncludes(
      joinSubtitleCueCoverageText(subtitleTranslateHardcodeSliceCommand?.request?.subtitleSegments),
      'Hello from video.',
      'subtitle translate hardcode video forwards source-language subtitle text when keepOriginal is enabled',
    );
    assertIncludes(
      joinSubtitleCueCoverageText(subtitleTranslateHardcodeSliceCommand?.request?.subtitleSegments),
      'Translated hello from video.',
      'subtitle translate hardcode video forwards target-language subtitle text for burned rendering',
    );
    assertEqual(
      subtitleTranslateHardcodeSliceCommand?.request?.clips?.[0]?.durationMs,
      7200,
      'subtitle translate hardcode video renders the whole imported source duration',
    );
    assertEqual(
      subtitleTranslateHardcodeTask?.videoUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fsubtitle-translate-hardcode-render-task%2Fsubtitle-translate-hardcode-video.mp4',
      'subtitle translate hardcode video stores the rendered video asset URL on the completed task',
    );
    assertEqual(
      subtitleTranslateHardcodeTask?.nativeTaskId,
      'subtitle-translate-hardcode-render-task',
      'subtitle translate hardcode video records the native render task id',
    );
    assertEqual(
      subtitleTranslateHardcodeTask?.subtitleUrl,
      'asset://localhost/D%3A%2Fautocut-configured-output%2Ftasks%2Fsubtitle-translate-hardcode-render-task%2Fsubtitle-translate-hardcode-video.srt',
      'subtitle translate hardcode video prefers the native task-scoped SRT sidecar URL',
    );
    assertRule(
      subtitleTranslateHardcodeAssets.some((asset) => asset.type === 'video' && asset.url === subtitleTranslateHardcodeTask?.videoUrl),
      'subtitle translate hardcode video persists the rendered video as a generated video asset',
    );
    assertRule(
      subtitleTranslateHardcodeAssets.some((asset) => asset.type === 'doc' && asset.url === subtitleTranslateHardcodeTask?.subtitleUrl),
      'subtitle translate hardcode video persists the native SRT sidecar as a generated document asset',
    );
    assertEqual(
      subtitleTranslateHardcodeBridgeRequests.length,
      1,
      'subtitle translate hardcode video still dispatches exactly one approved AI SDK translation request',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const subtitleTranslateHardcodeNoVideoCommands = [];
    const subtitleTranslateHardcodeNoVideoBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        subtitleTranslateHardcodeNoVideoCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-translate-hardcode-no-video-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/subtitle-translate-hardcode-no-video.flac',
          byteSize: 123000,
          name: 'subtitle-translate-hardcode-no-video.flac',
          mediaType: 'audio',
          hasAudioStream: true,
          hasVideoStream: false,
          mimeType: 'audio/flac',
          durationMs: 7200,
        };
      },
      transcribeMedia: async (request) => {
        subtitleTranslateHardcodeNoVideoCommands.push({ kind: 'transcribe', request });
        throw new Error('subtitle hardcode must not transcribe audio-only media before rejecting the missing video stream');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'subtitle translate hardcode no-video speech preflight contract',
      }),
      sliceVideo: async (request) => {
        subtitleTranslateHardcodeNoVideoCommands.push({ kind: 'slice', request });
        throw new Error('subtitle hardcode must not render audio-only media without a video stream');
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-subtitle-hardcode-no-video',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        subtitleTranslateHardcodeNoVideoBridgeRequests.push({ request, runtime });
        throw new Error('subtitle hardcode must not translate audio-only media before rejecting the missing video stream');
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    await assertRejects(
      () => withImmediateTimers(async () =>
        processSubtitleTranslate({
          fileId: 'asset-source-subtitle-translate-hardcode-no-video',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/subtitle-translate-hardcode-no-video.flac',
            'subtitle-translate-hardcode-no-video.flac',
            { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
          ),
          sourceLang: 'auto',
          targetLang: 'en',
          keepOriginal: false,
          hardcode: true,
        }),
      ),
      'requires source media with a video stream',
      'subtitle translate hardcode rejects trusted audio-only media before transcription, translation, or rendering',
    );
    assertEqual(
      subtitleTranslateHardcodeNoVideoCommands.map((entry) => entry.kind).join(','),
      'import',
      'subtitle translate hardcode imports once for stream evidence and never runs transcription or rendering without video',
    );
    assertEqual(
      subtitleTranslateHardcodeNoVideoBridgeRequests.length,
      0,
      'subtitle translate hardcode does not dispatch AI translation before rejecting audio-only media',
    );
    const subtitleTranslateHardcodeNoVideoTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      subtitleTranslateHardcodeNoVideoTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'subtitle translate hardcode stores a failed task for audio-only source media',
    );
    assertIncludes(
      subtitleTranslateHardcodeNoVideoTask?.errorMessage ?? '',
      'requires source media with a video stream',
      'subtitle translate hardcode stores the no-video stream reason on the failed task',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    resetStorage();
    const voiceTranslateAudioCommands = [];
    const voiceTranslateBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        voiceTranslateAudioCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'voice-translate-audio-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/voice-translate-audio-asset.m4a',
          byteSize: 323000,
          name: 'voice-translate-audio.m4a',
          mediaType: 'audio',
          hasAudioStream: true,
          hasVideoStream: false,
          mimeType: 'audio/mp4',
          durationMs: 6400,
        };
      },
      transcribeMedia: async (request) => {
        voiceTranslateAudioCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'voice-translate-audio-task',
          'voice-translate-audio-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'voice-translate-audio-transcript',
          taskUuid: 'voice-translate-audio-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language,
          text: 'Voice workflow recognizes audio.',
          segments: [
            {
              startMs: 500,
              endMs: 4200,
              text: 'Voice workflow recognizes audio.',
              speaker: 'Narrator',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'voice translate audio speech preflight contract',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      baseUrl: 'https://generativelanguage.googleapis.com/v1beta/openai',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-voice-translate',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        voiceTranslateBridgeRequests.push({ request, runtime });
        return {
          id: 'voice-translation-bridge-result',
          model: request.model,
          runtime,
          content: JSON.stringify({
            segments: [
              { index: 1, text: 'Voice workflow translated audio.' },
            ],
          }),
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: 'en',
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const voiceTranslateAudioSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/voice-translate-audio.m4a',
      'voice-translate-audio.m4a',
      { mimeType: 'audio/mp4', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
    );
    const voiceTranslateAudioResult = await withImmediateTimers(async () =>
      processVoiceTranslate({
        fileId: 'asset-source-voice-translate-audio',
        file: voiceTranslateAudioSourceFile,
        sourceLang: 'en',
        targetLang: 'zh',
      }),
    );
    const voiceTranslateAudioTask = readScopedStoredArray(services, 'tasks').find(
      (task) => task.id === voiceTranslateAudioResult.taskId,
    );
    const voiceTranslateAudioAsset = readScopedStoredArray(services, 'assets').find(
      (asset) => asset.sourceTaskId === voiceTranslateAudioResult.taskId,
    );
    assertEqual(voiceTranslateAudioResult.success, true, 'voice translate accepts trusted local audio sources');
    assertEqual(voiceTranslateAudioCommands[0]?.kind, 'import', 'voice translate imports local audio before transcription');
    assertEqual(
      voiceTranslateAudioCommands[0]?.request?.sourcePath,
      'D:/media/voice-translate-audio.m4a',
      'voice translate passes trusted audio source paths to media import',
    );
    assertEqual(voiceTranslateAudioCommands[1]?.kind, 'transcribe', 'voice translate transcribes imported audio media');
    assertEqual(
      voiceTranslateAudioCommands[1]?.request?.language,
      'en',
      'voice translate forwards the selected source language to speech transcription',
    );
    assertEqual(
      voiceTranslateAudioTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'voice translate audio workflow completes the persisted task',
    );
    assertEqual(
      voiceTranslateAudioTask?.transcriptText,
      'Voice workflow recognizes audio.',
      'voice translate stores transcript text from trusted audio media',
    );
    assertEqual(
      voiceTranslateAudioTask?.translationText,
      'Voice workflow translated audio.',
      'voice translate stores target-language translation text from the approved AI SDK bridge',
    );
    assertEqual(
      voiceTranslateAudioTask?.translationSegments?.[0]?.text,
      'Voice workflow translated audio.',
      'voice translate stores structured target-language translation subtitle segments',
    );
    assertEqual(
      voiceTranslateBridgeRequests.length,
      1,
      'voice translate dispatches one target-language translation request through the approved AI SDK bridge',
    );
    assertIncludes(
      voiceTranslateBridgeRequests[0]?.request?.messages?.[1]?.content ?? '',
      '"targetLang":"zh"',
      'voice translate includes the selected target language in the translation prompt payload',
    );
    assertEqual(
      voiceTranslateAudioTask?.transcriptSegments?.[0]?.speaker,
      'Narrator',
      'voice translate preserves speaker labels from trusted audio media',
    );
    assertEqual(
      voiceTranslateAudioTask?.subtitleFormat,
      'srt',
      'voice translate stores the generated voice-translation transcript subtitle format',
    );
    assertRule(
      Boolean(voiceTranslateAudioTask?.subtitleUrl),
      'voice translate stores a downloadable transcript subtitle URL on the completed task',
    );
    assertRule(
      !voiceTranslateAudioTask?.videoUrl,
      'voice translate does not claim a dubbed video output when no native TTS or video dubbing engine exists',
    );
    assertEqual(
      voiceTranslateAudioAsset?.type,
      'doc',
      'voice translate stores its transcript/subtitle output as a document asset',
    );
    services.resetAutoCutNativeHostClient();
    services.configureAutoCutApprovedAiSdkBridge(null);

    for (const audioStreamGuardWorkflow of [
      {
        name: 'extractor audio',
        operation: 'audio extraction',
        process: () => processAudioExtraction({
          fileId: 'asset-source-audio-extraction-no-audio',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/audio-extraction-no-audio.mp4',
            'audio-extraction-no-audio.mp4',
            { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: false, hasVideoStream: true },
          ),
          format: 'wav',
          quality: '192',
          channel: 'stereo',
        }),
      },
      {
        name: 'extractor text',
        operation: 'speech transcription',
        process: () => processExtractorText({
          fileId: 'asset-source-extractor-text-no-audio',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/extractor-text-no-audio.mp4',
            'extractor-text-no-audio.mp4',
            { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: false, hasVideoStream: true },
          ),
          language: 'auto',
          format: 'raw',
          separateSpeakers: false,
        }),
      },
      {
        name: 'subtitle translate',
        operation: 'subtitle translation',
        process: () => processSubtitleTranslate({
          fileId: 'asset-source-subtitle-translate-no-audio',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/subtitle-translate-no-audio.mp4',
            'subtitle-translate-no-audio.mp4',
            { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: false, hasVideoStream: true },
          ),
          sourceLang: 'auto',
          targetLang: 'en',
          keepOriginal: false,
          hardcode: false,
        }),
      },
      {
        name: 'voice translate',
        operation: 'voice translation',
        process: () => processVoiceTranslate({
          fileId: 'asset-source-voice-translate-no-audio',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/voice-translate-no-audio.mp4',
            'voice-translate-no-audio.mp4',
            { mimeType: 'video/mp4', mediaType: 'video', hasAudioStream: false, hasVideoStream: true },
          ),
          sourceLang: 'auto',
          targetLang: 'en',
        }),
      },
    ]) {
      resetStorage();
      const audioStreamProbeCommands = [];
      configureAutoCutNativeHostClient(
        services,
        createAudioStreamProbeNativeHostClient(
          audioStreamProbeCommands,
          audioStreamGuardWorkflow.operation,
        ),
      );
      await assertRejects(
        () => withImmediateTimers(audioStreamGuardWorkflow.process),
        'requires source media with an audio stream',
        `${audioStreamGuardWorkflow.name} rejects trusted video sources without an audio stream before native audio work`,
      );
      assertEqual(
        audioStreamProbeCommands.map((entry) => entry.kind).join(','),
        'import',
        `${audioStreamGuardWorkflow.name} imports once for stream evidence and never runs transcription or extraction without audio`,
      );
      const failedAudioStreamTask = readScopedStoredArray(services, 'tasks')[0];
      assertEqual(
        failedAudioStreamTask?.status,
        types.AUTOCUT_TASK_STATUS.failed,
        `${audioStreamGuardWorkflow.name} stores a failed task for no-audio source media`,
      );
      assertIncludes(
        failedAudioStreamTask?.errorMessage ?? '',
        'requires source media with an audio stream',
        `${audioStreamGuardWorkflow.name} stores the no-audio stream reason on the failed task`,
      );
      services.resetAutoCutNativeHostClient();
    }

    for (const videoStreamGuardWorkflow of [
      {
        name: 'video gif',
        operation: 'video GIF generation',
        expectedCommandName: 'generateGif',
        process: () => processVideoGif({
          fileId: 'asset-source-video-gif-no-video',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/video-gif-no-video.flac',
            'video-gif-no-video.flac',
            { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
          ),
          fps: '12',
          resolution: '720p',
          dither: true,
        }),
      },
      {
        name: 'video compress',
        operation: 'video compression',
        expectedCommandName: 'compressVideo',
        process: () => processVideoCompress({
          fileId: 'asset-source-video-compress-no-video',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/video-compress-no-video.flac',
            'video-compress-no-video.flac',
            { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
          ),
          compressionMode: 'balanced',
        }),
      },
      {
        name: 'video convert',
        operation: 'video conversion',
        expectedCommandName: 'convertVideo',
        process: () => processVideoConvert({
          fileId: 'asset-source-video-convert-no-video',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/video-convert-no-video.flac',
            'video-convert-no-video.flac',
            { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
          ),
          targetFormat: 'mp4',
          videoCodec: 'h264',
          audioCodec: 'aac',
          resolution: '1080p',
        }),
      },
      {
        name: 'video enhance',
        operation: 'video enhancement',
        expectedCommandName: 'enhanceVideo',
        process: () => processVideoEnhance({
          fileId: 'asset-source-video-enhance-no-video',
          file: createTrustedLocalMediaFile(
            commons,
            'D:/media/video-enhance-no-video.flac',
            'video-enhance-no-video.flac',
            { mimeType: 'audio/flac', mediaType: 'audio', hasAudioStream: true, hasVideoStream: false },
          ),
          targetResolution: '1080p',
          enhanceMode: 'real',
          frameRate: '60',
        }),
      },
    ]) {
      resetStorage();
      const videoStreamProbeCommands = [];
      configureAutoCutNativeHostClient(
        services,
        createVideoStreamProbeNativeHostClient(
          videoStreamProbeCommands,
          videoStreamGuardWorkflow.operation,
        ),
      );
      await assertRejects(
        () => withImmediateTimers(videoStreamGuardWorkflow.process),
        'requires source media with a video stream',
        `${videoStreamGuardWorkflow.name} rejects trusted audio sources without a video stream before native video work`,
      );
      assertEqual(
        videoStreamProbeCommands.map((entry) => entry.kind).join(','),
        'import',
        `${videoStreamGuardWorkflow.name} imports once for stream evidence and never runs ${videoStreamGuardWorkflow.expectedCommandName} without video`,
      );
      const failedVideoStreamTask = readScopedStoredArray(services, 'tasks')[0];
      assertEqual(
        failedVideoStreamTask?.status,
        types.AUTOCUT_TASK_STATUS.failed,
        `${videoStreamGuardWorkflow.name} stores a failed task for no-video source media`,
      );
      assertIncludes(
        failedVideoStreamTask?.errorMessage ?? '',
        'requires source media with a video stream',
        `${videoStreamGuardWorkflow.name} stores the no-video stream reason on the failed task`,
      );
      services.resetAutoCutNativeHostClient();
    }

    resetStorage();
    const nativeExtractorTextEmptyTranscriptCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        nativeExtractorTextEmptyTranscriptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'empty-transcript-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/empty-transcript-source.mp4',
          byteSize: 654000,
          name: 'empty-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 32000,
        };
      },
      transcribeMedia: async (request) => {
        nativeExtractorTextEmptyTranscriptCommands.push({ kind: 'transcribe', request });
        const output = createNativeTaskOutputArtifact(
          'empty-transcript-task',
          'empty-transcript.json',
          configuredOutputDirectory,
        );
        return {
          artifactUuid: 'empty-transcript-artifact',
          taskUuid: 'empty-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          language: request.language ?? 'auto',
          text: '',
          segments: [],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: 'whisper-cli',
        };
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'empty transcript speech preflight contract',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const emptyTranscriptSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/empty-transcript-source.mp4',
      'empty-transcript-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processExtractorText({
            fileId: 'asset-source-empty-transcript',
            file: emptyTranscriptSourceFile,
            language: 'zh',
            format: 'filtered',
            separateSpeakers: true,
          }),
        ),
      'valid timestamped speech segments',
      'extractor text workflow fails closed when the STT provider returns no transcript segments',
    );
    const emptyTranscriptTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      emptyTranscriptTask?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'extractor text empty transcript workflow marks the task failed',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length,
      0,
      'extractor text empty transcript workflow does not persist an empty transcript asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeSliceSubtitleFailureCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        nativeSliceSubtitleFailureCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-required-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/subtitle-required-source.mp4',
          byteSize: 654000,
          name: 'subtitle-required-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      transcribeMedia: async (request) => {
        nativeSliceSubtitleFailureCommands.push({ kind: 'transcribe', request });
        throw new Error('whisper stderr: model file missing');
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'subtitle required speech preflight contract',
      }),
      sliceVideo: async (request) => {
        nativeSliceSubtitleFailureCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'subtitle-required-fallback-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
      language: 'zh',
    });
    const subtitleRequiredSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/subtitle-required-source.mp4',
      'subtitle-required-source.mp4',
    );
    const subtitleFailureResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-subtitle-required-slice',
        file: subtitleRequiredSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    const subtitleFailureTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      subtitleFailureResult.success,
      true,
      'video slice workflow reports success when required speech-to-text fails and deterministic planning is used',
    );
    assertEqual(
      subtitleFailureTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice subtitle transcription failure falls back to a completed task',
    );
    assertRule(
      nativeSliceSubtitleFailureCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders a no-subtitle video after required subtitle transcription fails',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length > 0,
      true,
      'video slice subtitle transcription fallback persists generated slice assets',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeSliceNoContentTranscriptCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        nativeSliceNoContentTranscriptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'no-content-transcript-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/no-content-transcript-source.mp4',
          byteSize: 654000,
          name: 'no-content-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      transcribeMedia: async (request) => {
        nativeSliceNoContentTranscriptCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'no-content-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 12_000,
              text: '[BLANK_AUDIO]',
              speaker: 'Speaker 1',
            },
            {
              startMs: 12_500,
              endMs: 24_000,
              text: 'um uh hmm',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'no content transcript speech preflight contract',
      }),
      sliceVideo: async (request) => {
        nativeSliceNoContentTranscriptCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'no-content-transcript-fallback-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
      language: 'zh',
    });
    const noContentTranscriptSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/no-content-transcript-source.mp4',
      'no-content-transcript-source.mp4',
    );
    const noContentTranscriptResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-no-content-transcript-slice',
        file: noContentTranscriptSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    const noContentTranscriptTask = readScopedStoredArray(services, 'tasks')[0];
    assertEqual(
      noContentTranscriptResult.success,
      true,
      'video slice workflow reports success when speech-to-text returns only silence or filler segments',
    );
    assertEqual(
      noContentTranscriptTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice no-content transcript workflow falls back to a completed task',
    );
    assertRule(
      nativeSliceNoContentTranscriptCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders when speech-to-text has no real transcript content evidence',
    );
    assertEqual(
      readScopedStoredArray(services, 'assets').length > 0,
      true,
      'video slice no-content transcript workflow persists generated slice assets',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const tailClampedTranscriptCommands = [];
    const tailClampedTranscriptBridgeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        tailClampedTranscriptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'tail-clamped-transcript-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/tail-clamped-transcript-source.mp4',
          byteSize: 654000,
          name: 'tail-clamped-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 60_000,
        };
      },
      transcribeMedia: async (request) => {
        tailClampedTranscriptCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'tail-clamped-transcript-task',
          configuredOutputDirectory,
          [
            ...Array.from({ length: 11 }, (_, index) => ({
              startMs: index * 1_000,
              endMs: index * 1_000 + 500,
              text: 'um',
              speaker: 'Speaker 1',
            })),
            {
              startMs: 30_000,
              endMs: 43_000,
              text: 'Why tail timestamp repair matters is that speech-to-text can round the final boundary beyond the container duration.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 43_200,
              endMs: 60_900,
              text: 'Because the tail example shows the real payoff, so the solution clamps only the final segment and preserves the complete story.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 59_980,
              endMs: 61_100,
              text: 'um',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'tail-clamped transcript speech preflight contract',
      }),
      analyzeVideoSliceAudioActivity: async (request) => {
        tailClampedTranscriptCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        tailClampedTranscriptCommands.push({ kind: 'slice', request });
        const output = createNativeTaskOutputArtifact(
          'tail-clamped-transcript-slice-task',
          request.clips[0]?.outputFileName ?? 'tail-clamped-transcript-slice.mp4',
          configuredOutputDirectory,
        );
        const thumbnail = createNativeTaskCoverArtifact(
          'tail-clamped-transcript-slice-task',
          'tail-clamped-transcript-thumb.jpg',
          configuredOutputDirectory,
        );
        return {
          taskUuid: 'tail-clamped-transcript-slice-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: output.taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => ({
            artifactUuid: `tail-clamped-transcript-slice-artifact-${index + 1}`,
            artifactPath: index === 0
              ? output.artifactPath
              : createNativeTaskOutputArtifact(
                  'tail-clamped-transcript-slice-task',
                  `tail-clamped-transcript-slice-artifact-${index + 1}.mp4`,
                  configuredOutputDirectory,
                ).artifactPath,
            thumbnailArtifactUuid: `tail-clamped-transcript-thumb-${index + 1}`,
            thumbnailArtifactPath: index === 0
              ? thumbnail.artifactPath
              : createNativeTaskCoverArtifact(
                  'tail-clamped-transcript-slice-task',
                  `tail-clamped-transcript-thumb-${index + 1}.jpg`,
                  configuredOutputDirectory,
                ).artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567 + index,
            thumbnailByteSize: 12345 + index,
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          })),
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        tailClampedTranscriptBridgeRequests.push({ request, runtime });
        const prompt = JSON.parse(request.messages?.[1]?.content ?? '{}');
        const selectedCandidate =
          prompt.candidates?.find((candidate) => String(candidate.title ?? '').includes('final real source')) ??
          prompt.candidates?.[0];
        const rankedCandidateIds = [
          selectedCandidate?.id,
          ...(prompt.candidates?.map((candidate) => candidate.id) ?? []),
        ].filter((candidateId, index, ids) =>
          typeof candidateId === 'string' && candidateId && ids.indexOf(candidateId) === index
        );
        return {
          id: 'tail-clamped-transcript-plan',
          model: request.model,
          content: JSON.stringify({
            rankedCandidateIds,
            referencedUnitIds: prompt.contentUnits?.map((unit) => unit.id) ?? [],
            reviewNotes: ['Selected source-bounded tail candidate by stable ID.'],
          }),
          runtime,
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-tail-clamped-transcript-secret',
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
      language: 'zh',
    });
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-tail-clamped-transcript-slice',
        file: createTrustedLocalMediaFile(
          commons,
          'D:/media/tail-clamped-transcript-source.mp4',
          'tail-clamped-transcript-source.mp4',
        ),
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 10,
        maxDuration: 40,
        baseAlgorithm: 'scene',
        highlightEngine: 'keyword',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: false,
      }),
    );
    const tailClampedTranscriptPrompt = JSON.parse(
      tailClampedTranscriptBridgeRequests[0]?.request?.messages?.[1]?.content ?? '{}',
    );
    const tailClampedTranscriptSliceRequest = tailClampedTranscriptCommands
      .find((entry) => entry.kind === 'slice')?.request;
    const tailClampedTranscriptTask = readScopedStoredArray(services, 'tasks')
      .find((task) => task.nativeTaskId === 'tail-clamped-transcript-slice-task');
    assertRule(
      tailClampedTranscriptPrompt?.contentUnits?.some((unit) => String(unit.text ?? '').includes('tail example shows the real payoff')),
      'video slice workflow exposes the source-bounded final STT segment as a content unit before ID-only LLM review',
    );
    assertRule(
      tailClampedTranscriptPrompt?.candidates?.every((candidate) =>
        Array.isArray(candidate.unitIds) &&
          candidate.unitIds.every((unitId) => tailClampedTranscriptPrompt.contentUnits?.some((unit) => unit.id === unitId))
      ),
      'video slice workflow prompts the ID-only LLM reviewer only with candidates backed by source-bounded content units',
    );
    assertRule(
      Array.isArray(tailClampedTranscriptPrompt?.contentUnits) &&
        tailClampedTranscriptPrompt.contentUnits.every((unit) => !String(unit.text ?? '').includes('um')),
      'video slice workflow drops tail filler that crosses the imported media end before ID-only LLM review',
    );
    assertRule(
      tailClampedTranscriptSliceRequest?.clips?.every((clip) =>
        (clip.sourceEndMs ?? clip.startMs + clip.durationMs) <= 60_000 &&
          clip.transcriptSegments?.every((segment) => segment.endMs <= 60_000)
      ),
      'video slice workflow sends native rendering only source-bounded transcript segments after tail timestamp repair',
    );
    assertRule(
      tailClampedTranscriptSliceRequest?.clips?.every((clip) =>
        clip.transcriptSegments?.every((segment) => !(segment.text === 'um' && segment.startMs >= 59_000))
      ),
      'video slice workflow keeps source-bounded native clip transcript evidence free of tail filler noise',
    );
    assertEqual(
      tailClampedTranscriptTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video slice workflow completes when the final real-content STT segment lightly exceeds source duration before a tail filler segment',
    );
    assertRule(
      tailClampedTranscriptTask?.sliceResults?.every((slice) =>
        (slice.sourceEndMs ?? 0) <= 60_000 &&
          slice.transcriptSegments?.every((segment) => segment.endMs <= 60_000)
      ),
      'video slice workflow persists source-bounded structured transcript segments after tail timestamp repair',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeSliceOutOfSourceTranscriptCommands = [];
    let outOfSourceTranscriptLlmCallCount = 0;
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        nativeSliceOutOfSourceTranscriptCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'out-of-source-transcript-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/out-of-source-transcript-source.mp4',
          byteSize: 654000,
          name: 'out-of-source-transcript-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 60_000,
        };
      },
      transcribeMedia: async (request) => {
        nativeSliceOutOfSourceTranscriptCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'out-of-source-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 2_000,
              endMs: 18_000,
              text: 'The valid story starts and ends inside the imported source duration.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 58_000,
              endMs: 65_000,
              text: 'This speech-to-text timestamp extends beyond the imported source duration.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: true,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'out-of-source transcript speech preflight contract',
      }),
      sliceVideo: async (request) => {
        nativeSliceOutOfSourceTranscriptCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'out-of-source-transcript-fallback-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        outOfSourceTranscriptLlmCallCount += 1;
        return {
          id: 'out-of-source-transcript-llm-plan',
          model: request.model,
          content: JSON.stringify([{ candidateId: 'transcript-1', title: 'Valid internal clip' }]),
          runtime,
        };
      },
    });
    await services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredOutputDirectory,
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-out-of-source-transcript-secret',
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
      language: 'zh',
    });
    const outOfSourceTranscriptSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/out-of-source-transcript-source.mp4',
      'out-of-source-transcript-source.mp4',
    );
    const outOfSourceTranscriptResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-out-of-source-transcript-slice',
        file: outOfSourceTranscriptSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    assertEqual(
      outOfSourceTranscriptResult.success,
      true,
      'video slice workflow reports success when STT timestamps exceed source duration and deterministic planning is used',
    );
    assertEqual(
      outOfSourceTranscriptLlmCallCount,
      0,
      'video slice workflow does not prompt the LLM with out-of-source STT timestamp evidence',
    );
    assertRule(
      nativeSliceOutOfSourceTranscriptCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders when STT timestamps exceed source duration',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeSliceSubtitleUnavailableCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        speechTranscriptionCommandReady: false,
        speechTranscriptionToolchainReady: true,
      }),
      importMediaFile: async (request) => {
        nativeSliceSubtitleUnavailableCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-unavailable-slice-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/subtitle-unavailable-source.mp4',
          byteSize: 654000,
          name: 'subtitle-unavailable-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 42000,
        };
      },
      sliceVideo: async (request) => {
        nativeSliceSubtitleUnavailableCommands.push({ kind: 'slice', request });
        return createSuccessfulNativeVideoSliceResult(request, 'subtitle-unavailable-fallback-slice-task', configuredOutputDirectory);
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const subtitleUnavailableSliceSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/subtitle-unavailable-source.mp4',
      'subtitle-unavailable-source.mp4',
    );
    const subtitleUnavailableResult = await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-subtitle-unavailable-slice',
        file: subtitleUnavailableSliceSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
        enableSubtitles: true,
        subtitleMode: 'both',
      }),
    );
    assertEqual(
      subtitleUnavailableResult.success,
      true,
      'video slice workflow reports success when local speech-to-text is unavailable',
    );
    assertRule(
      nativeSliceSubtitleUnavailableCommands.some((entry) => entry.kind === 'slice'),
      'video slice workflow renders a no-subtitle video when required speech-to-text is unavailable',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoSliceLlmPlanCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoSliceCommandReady: true,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        llmHttpCommandReady: false,
        llmSecretStoreReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        nativeWorkerLeaseReady: true,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_slice_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'path',
        manifestReady: false,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoSliceLlmPlanCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-slice-llm-asset',
          sandboxPath: 'D:/autocut-configured-output/inputs/native-slice-llm-asset.mp4',
          byteSize: 654000,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 32000,
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 654000,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the LLM slice plan contract');
      },
      generateGif: async () => {
        throw new Error('video GIF is not configured for the LLM slice plan contract');
      },
      transcribeMedia: async (request) => {
        nativeVideoSliceLlmPlanCommands.push({ kind: 'transcribe', request });
        return {
          artifactUuid: 'native-slice-llm-transcript-artifact',
          taskUuid: 'native-slice-llm-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: 'D:/autocut-configured-output/tasks/native-slice-llm-transcript-task/transcript.json',
          taskOutputDir: 'D:/autocut-configured-output/tasks/native-slice-llm-transcript-task',
          language: request.language ?? 'auto',
          text:
            'Why LLM plans still need speech checks is simple. Because a model can choose bad timing, the problem appears before rendering. So use the verified transcript result and the final answer works.',
          segments: [
            {
              startMs: 0,
              endMs: 9_200,
              text:
                'Why LLM plans still need speech checks is simple. Because a model can choose bad timing, the problem appears before rendering.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 9_250,
              endMs: 20_200,
              text:
                'So use the verified transcript result and the final answer works.',
              speaker: 'Speaker 1',
            },
          ],
          ffmpegExecutable: 'ffmpeg',
          speechExecutable: request.executablePath,
        };
      },
      probeSpeechTranscription: async (request) => {
        nativeVideoSliceLlmPlanCommands.push({ kind: 'speech-preflight', request });
        return {
          ready: true,
          executablePath: request.executablePath,
          modelPath: request.modelPath,
          sourceKind: request.sourceKind ?? 'execution-preflight',
          diagnostics: [],
          versionLine: 'LLM slice plan speech preflight contract',
        };
      },
      sliceVideo: async (request) => {
        nativeVideoSliceLlmPlanCommands.push({ kind: 'slice', request });
        return {
          taskUuid: 'native-slice-llm-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: createNativeTaskOutputArtifact(
            'native-slice-llm-task',
            'native-slice-llm-artifact-1.mp4',
            configuredOutputDirectory,
          ).taskOutputDir,
          ffmpegExecutable: 'ffmpeg',
          slices: request.clips.map((clip, index) => {
            const output = createNativeTaskOutputArtifact(
              'native-slice-llm-task',
              `native-slice-llm-artifact-${index + 1}.mp4`,
              configuredOutputDirectory,
            );
            const thumbnail = createNativeTaskCoverArtifact(
              'native-slice-llm-task',
              `native-slice-llm-thumb-${index + 1}.jpg`,
              configuredOutputDirectory,
            );
            const subtitle = createNativeTaskOutputArtifact(
              'native-slice-llm-task',
              `native-slice-llm-subtitle-${index + 1}.srt`,
              configuredOutputDirectory,
            );
            const subtitleRequested = request.subtitleFormat === 'srt' && request.subtitleMode !== 'none';
            return {
              artifactUuid: `native-slice-llm-artifact-${index + 1}`,
              artifactPath: output.artifactPath,
              thumbnailArtifactUuid: `native-slice-llm-thumb-${index + 1}`,
              thumbnailArtifactPath: thumbnail.artifactPath,
              ...(subtitleRequested
                ? {
                    subtitleArtifactUuid: `native-slice-llm-subtitle-${index + 1}`,
                    subtitleArtifactPath: subtitle.artifactPath,
                    subtitleByteSize: 234 + index,
                    subtitleFormat: 'srt',
                  }
                : {}),
              taskOutputDir: output.taskOutputDir,
              byteSize: 234567,
              thumbnailByteSize: 12345,
              format: 'mp4',
              ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
            };
          }),
        };
      },
      compressVideo: async () => {
        throw new Error('video compression is not configured for the LLM slice plan contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion is not configured for the LLM slice plan contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement is not configured for the LLM slice plan contract');
      },
      sendLlmHttpRequest: async () => {
        throw new Error('LLM HTTP is not configured for the LLM slice plan contract');
      },
      saveLlmSecret: async (request) => ({
        secretName: request.secretName,
        saved: false,
      }),
      getLlmSecret: async (request) => ({
        secretName: request.secretName,
        configured: false,
      }),
      deleteLlmSecret: async (request) => ({
        secretName: request.secretName,
        deleted: false,
      }),
      listNativeTasks: async () => [],
      cancelNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        status: 0,
        canceled: false,
        message: 'not configured',
      }),
      recoverNativeTasks: async () => ({
        inspected: 0,
        recovered: 0,
        interrupted: 0,
        canceled: 0,
        expiredLeases: 0,
        deferred: 0,
        taskUuids: [],
      }),
      retryNativeTask: async (request) => ({
        taskUuid: request.taskUuid,
        retryTaskUuid: '',
        status: 0,
        retried: false,
        message: 'not configured',
      }),
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }, { taskEvidenceCommands: nativeVideoSliceLlmPlanCommands });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      apiKey: 'sk-llm-plan-secret',
    });
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        return {
          id: 'llm-plan-contract',
          model: request.model,
          content: JSON.stringify([
            { startMs: 40000, durationMs: 999999, label: 'Late' },
            { startMs: 0, durationMs: 1000, label: 'Short' },
          ]),
          runtime,
        };
      },
    });
    const nativeVideoSliceLlmPlanSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    await withImmediateTimers(async () =>
      processVideoSlice({
        fileId: 'asset-source-native-video-slice-llm-plan',
        file: nativeVideoSliceLlmPlanSourceFile,
        mode: 'contract-mode',
        llmModel: 'deepseek-v4-flash',
        minDuration: 15,
        maxDuration: 60,
        baseAlgorithm: 'scene',
        highlightEngine: 'emotion',
        enableNoiseReduction: true,
        enableCoughFilter: true,
        enableRepeatFilter: true,
      }),
    );
    const llmPlanSliceRequest = nativeVideoSliceLlmPlanCommands.find((entry) => entry.kind === 'slice')?.request;
    assertEqual(
      nativeVideoSliceLlmPlanCommands.find((entry) => entry.kind === 'transcribe')?.kind,
      'transcribe',
      'video slice LLM workflow transcribes before planning and slicing because smart slicing requires speech-to-text',
    );
    assertEqual(
      llmPlanSliceRequest?.subtitleMode,
      undefined,
      'video slice LLM workflow does not request subtitle rendering unless subtitles are enabled',
    );
    assertEqual(
      llmPlanSliceRequest?.subtitleSegments,
      undefined,
      'video slice LLM workflow keeps transcript segments out of native subtitle rendering when subtitles are disabled',
    );
    assertEqual(
      llmPlanSliceRequest?.clips?.length,
      1,
      'video slice LLM workflow prefers speech-aligned transcript candidates instead of adding silent filler clips',
    );
    assertEqual(
      llmPlanSliceRequest?.clips?.[0]?.startMs,
      0,
      'video slice LLM workflow sorts normalized clips by start time',
    );
    assertNumberBetween(
      llmPlanSliceRequest?.clips?.[0]?.durationMs,
      20000,
      21000,
      'video slice LLM workflow keeps speech-aligned clips tight instead of padding them to the configured minimum duration',
    );
    assertRule(
      llmPlanSliceRequest?.clips?.every((clip) => clip.durationMs < 22000),
      'video slice LLM workflow avoids minimum-duration silent padding when transcript speech produces a valid tight candidate',
    );
    assertRule(
      !llmPlanSliceRequest?.clips?.some((clip) => clip.startMs >= 32000),
      'video slice LLM workflow drops model clips that start after imported media duration',
    );
    assertRule(
      llmPlanSliceRequest?.clips?.every((clip) => clip.startMs + clip.durationMs <= 32000),
      'video slice LLM workflow clamps all normalized clips inside imported media duration',
    );
    assertRule(
      llmPlanSliceRequest?.clips?.every((clip, index, clips) =>
        index === 0 || clip.startMs >= clips[index - 1].startMs + clips[index - 1].durationMs,
      ),
      'video slice LLM workflow returns non-overlapping normalized clips',
    );
    const llmPlanSpeechToTextEvidenceWrite = nativeVideoSliceLlmPlanCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/speech-to-text.json',
    );
    const llmPlanNormalizedTranscriptEvidenceWrite = nativeVideoSliceLlmPlanCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/transcript.normalized.json',
    );
    const llmPlanSemanticSegmentationEvidenceWrite = nativeVideoSliceLlmPlanCommands.find(
      (entry) => entry.kind === 'task-evidence' && entry.request?.relativePath === 'evidence/semantic-segmentation.json',
    );
    const llmPlanTask = readScopedStoredArray(services, 'tasks').find((task) => task.type === types.AUTOCUT_TASK_TYPE.videoSlice);
    assertEqual(
      llmPlanSpeechToTextEvidenceWrite?.request?.workflowTaskId,
      llmPlanTask?.id,
      'video slice LLM workflow writes speech-to-text evidence under the Smart Slice workflow task id',
    );
    assertEqual(
      llmPlanSpeechToTextEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.speech-to-text.v1',
      'video slice LLM workflow writes versioned speech-to-text JSON evidence',
    );
    assertEqual(
      llmPlanSpeechToTextEvidenceWrite?.request?.contentJson?.segments?.length,
      2,
      'video slice LLM workflow speech-to-text evidence stores the normalized segment timeline',
    );
    assertEqual(
      llmPlanNormalizedTranscriptEvidenceWrite?.request?.workflowTaskId,
      llmPlanTask?.id,
      'video slice LLM workflow writes normalized transcript evidence under the Smart Slice workflow task id',
    );
    assertEqual(
      llmPlanNormalizedTranscriptEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.transcript-artifact.v1',
      'video slice LLM workflow writes versioned normalized transcript artifact JSON evidence',
    );
    assertEqual(
      llmPlanNormalizedTranscriptEvidenceWrite?.request?.contentJson?.transcript?.schema,
      'smart-slice.transcript.v1',
      'video slice LLM workflow normalized transcript evidence stores the canonical transcript schema',
    );
    assertEqual(
      llmPlanNormalizedTranscriptEvidenceWrite?.request?.contentJson?.transcript?.speakers?.[0]?.id,
      'speaker-1',
      'video slice LLM workflow normalized transcript evidence records stable speaker identifiers',
    );
    assertEqual(
      llmPlanNormalizedTranscriptEvidenceWrite?.request?.contentJson?.transcript?.segments?.[0]?.speakerId,
      'speaker-1',
      'video slice LLM workflow normalized transcript evidence maps segments to speaker identifiers',
    );
    assertEqual(
      llmPlanSemanticSegmentationEvidenceWrite?.request?.workflowTaskId,
      llmPlanTask?.id,
      'video slice LLM workflow writes semantic segmentation evidence under the Smart Slice workflow task id',
    );
    assertEqual(
      llmPlanSemanticSegmentationEvidenceWrite?.request?.contentJson?.schema,
      'smart-slice.semantic-segmentation.v1',
      'video slice LLM workflow writes versioned semantic segmentation JSON evidence',
    );
    assertOwnProperty(
      llmPlanSemanticSegmentationEvidenceWrite?.request?.contentJson ?? {},
      'llmReviewAudit',
      'video slice LLM workflow semantic evidence records the model review audit payload',
    );
    assertEqual(
      llmPlanTask?.executionCheckpoint?.artifacts?.['speech-to-text']?.speechToTextEvidence?.relativePath,
      'evidence/speech-to-text.json',
      'video slice LLM workflow checkpoint references the speech-to-text evidence artifact',
    );
    assertEqual(
      llmPlanTask?.executionCheckpoint?.artifacts?.['speech-to-text']?.normalizedTranscriptEvidence?.relativePath,
      'evidence/transcript.normalized.json',
      'video slice LLM workflow checkpoint references the normalized transcript evidence artifact',
    );
    assertEqual(
      llmPlanTask?.executionCheckpoint?.artifacts?.['plan-clips']?.semanticSegmentationEvidence?.relativePath,
      'evidence/semantic-segmentation.json',
      'video slice LLM workflow checkpoint references the semantic segmentation evidence artifact',
    );
    services.configureAutoCutApprovedAiSdkBridge(null);
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoCompressWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoCompressCommandReady: true,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_compress_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoCompressWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-compress-asset',
          sandboxPath: 'D:/autocut/media/inputs/native-compress-asset.mp4',
          byteSize: 987654,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 987654,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the native video compression workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for the native video compression workflow contract');
      },
      compressVideo: async (request) => {
        nativeVideoCompressWorkflowCommands.push({ kind: 'compress', request });
        const output = createNativeTaskOutputArtifact('native-compress-task', 'native-compress-artifact.mp4');
        return {
          artifactUuid: 'native-compress-artifact',
          taskUuid: 'native-compress-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 345678,
          originalByteSize: 987654,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for the native video compression workflow contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for the native video compression workflow contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const nativeVideoCompressSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeVideoCompressWorkflowResult = await withImmediateTimers(async () =>
      processVideoCompress({
        fileId: 'asset-source-native-video-compress',
        file: nativeVideoCompressSourceFile,
        compressionMode: 'balanced',
      }),
    );
    const nativeVideoCompressWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeVideoCompressWorkflowTask = nativeVideoCompressWorkflowTasks.find(
      (task) => task.id === nativeVideoCompressWorkflowResult.taskId,
    );
    const nativeVideoCompressWorkflowAssets = readScopedStoredArray(services, 'assets');
    assertEqual(nativeVideoCompressWorkflowResult.success, true, 'video compress native workflow reports success');
    assertEqual(
      nativeVideoCompressWorkflowCommands[0]?.kind,
      'import',
      'video compress native workflow imports local media before compression',
    );
    assertEqual(
      nativeVideoCompressWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'video compress native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeVideoCompressWorkflowCommands[1]?.kind,
      'compress',
      'video compress native workflow compresses after import',
    );
    assertEqual(
      nativeVideoCompressWorkflowCommands[1]?.request?.assetUuid,
      'native-compress-asset',
      'video compress native workflow compresses by imported assetUuid',
    );
    assertEqual(
      nativeVideoCompressWorkflowCommands[1]?.request?.compressionMode,
      'balanced',
      'video compress native workflow forwards the selected compression mode',
    );
    assertEqual(
      nativeVideoCompressWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video compress native workflow completes the persisted task',
    );
    assertEqual(
      nativeVideoCompressWorkflowTask?.videoUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fnative-compress-task%2Fnative-compress-artifact.mp4',
      'video compress native workflow converts native artifact paths to safe asset URLs',
    );
    assertEqual(
      nativeVideoCompressWorkflowTask?.fileSizeStats?.originalSize,
      987654,
      'video compress native workflow stores native original byte size on the task',
    );
    assertEqual(
      nativeVideoCompressWorkflowTask?.fileSizeStats?.newSize,
      345678,
      'video compress native workflow stores native compressed byte size on the task',
    );
    assertEqual(
      nativeVideoCompressWorkflowTask?.fileSizeStats?.compressionRatio,
      0.65,
      'video compress native workflow stores normalized compression ratio on the task',
    );
    assertEqual(
      nativeVideoCompressWorkflowAssets[0]?.type,
      'video',
      'video compress native workflow stores the compressed output as a video asset',
    );
    assertEqual(
      nativeVideoCompressWorkflowAssets[0]?.size,
      345678,
      'video compress native workflow stores the native artifact byte size on the generated asset',
    );
    assertEqual(
      nativeVideoCompressWorkflowAssets[0]?.url,
      nativeVideoCompressWorkflowTask?.videoUrl,
      'video compress native workflow stores the safe asset URL on the generated asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const escapedNativeVideoCompressWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoCompressCommandReady: true,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_compress_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        escapedNativeVideoCompressWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'escaped-native-compress-asset',
          sandboxPath: 'D:/autocut/media/inputs/escaped-native-compress-asset.mp4',
          byteSize: 987654,
          name: 'escaped-native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 987654,
        name: 'escaped-native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for escaped native video compression contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for escaped native video compression contract');
      },
      compressVideo: async (request) => {
        escapedNativeVideoCompressWorkflowCommands.push({ kind: 'compress', request });
        return {
          artifactUuid: 'escaped-native-compress-artifact',
          taskUuid: 'escaped-native-compress-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: 'D:/autocut/media/tasks/escaped-native-compress-task/../compressed.mp4',
          taskOutputDir: 'D:/autocut/media/tasks/escaped-native-compress-task',
          byteSize: 345678,
          originalByteSize: 987654,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for escaped native video compression contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for escaped native video compression contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const escapedNativeVideoCompressSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/escaped-native-source.mp4',
      'escaped-native-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoCompress({
            fileId: 'asset-source-escaped-native-video-compress',
            file: escapedNativeVideoCompressSourceFile,
            compressionMode: 'balanced',
          }),
        ),
      'artifactPath is outside its task output directory',
      'video compress native escaped artifact containment',
    );
    const escapedNativeVideoCompressWorkflowTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      escapedNativeVideoCompressWorkflowCommands[1]?.kind,
      'compress',
      'video compress native escaped artifact containment reaches the native compression boundary',
    );
    assertEqual(
      escapedNativeVideoCompressWorkflowTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video compress native escaped artifact containment marks the task failed',
    );
    assertIncludes(
      String(escapedNativeVideoCompressWorkflowTasks[0]?.errorMessage ?? ''),
      'artifactPath is outside its task output directory',
      'video compress native escaped artifact containment stores the path containment reason',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video compress native escaped artifact containment does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video compress native escaped artifact containment does not persist success messages');
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const dotSegmentEscapedNativeVideoCompressWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
        localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoCompressCommandReady: true,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_compress_video'],
      }),
      importMediaFile: async (request) => {
        dotSegmentEscapedNativeVideoCompressWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'dot-segment-escaped-native-compress-asset',
          sandboxPath: 'D:/autocut/media/inputs/dot-segment-escaped-native-compress-asset.mp4',
          byteSize: 987654,
          name: 'dot-segment-escaped-native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 987654,
        name: 'dot-segment-escaped-native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for dot-segment escaped native video compression contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for dot-segment escaped native video compression contract');
      },
      compressVideo: async (request) => {
        dotSegmentEscapedNativeVideoCompressWorkflowCommands.push({ kind: 'compress', request });
        return {
          artifactUuid: 'dot-segment-escaped-native-compress-artifact',
          taskUuid: 'dot-segment-escaped-native-compress-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: 'D:/autocut/media/tasks/dot-segment-escaped-native-compress-task/../compressed.mp4',
          taskOutputDir: 'D:/autocut/media/tasks/dot-segment-escaped-native-compress-task',
          byteSize: 345678,
          originalByteSize: 987654,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for dot-segment escaped native video compression contract');
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for dot-segment escaped native video compression contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const dotSegmentEscapedNativeVideoCompressSourceFile = createTrustedLocalMediaFile(
      commons,
      'D:/media/dot-segment-escaped-native-source.mp4',
      'dot-segment-escaped-native-source.mp4',
    );
    await assertRejects(
      () =>
        withImmediateTimers(async () =>
          processVideoCompress({
            fileId: 'asset-source-dot-segment-escaped-native-video-compress',
            file: dotSegmentEscapedNativeVideoCompressSourceFile,
            compressionMode: 'balanced',
          }),
        ),
      'artifactPath is outside its task output directory',
      'video compress native dot-segment escaped artifact containment',
    );
    const dotSegmentEscapedNativeVideoCompressWorkflowTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(
      dotSegmentEscapedNativeVideoCompressWorkflowCommands[1]?.kind,
      'compress',
      'video compress native dot-segment escaped artifact containment reaches the native compression boundary',
    );
    assertEqual(
      dotSegmentEscapedNativeVideoCompressWorkflowTasks[0]?.status,
      types.AUTOCUT_TASK_STATUS.failed,
      'video compress native dot-segment escaped artifact containment marks the task failed',
    );
    assertIncludes(
      String(dotSegmentEscapedNativeVideoCompressWorkflowTasks[0]?.errorMessage ?? ''),
      'artifactPath is outside its task output directory',
      'video compress native dot-segment escaped artifact containment stores the path containment reason',
    );
    assertEqual(readScopedStoredArray(services, 'assets').length, 0, 'video compress native dot-segment escaped artifact containment does not persist generated assets');
    assertEqual(readScopedStoredArray(services, 'messages').length, 0, 'video compress native dot-segment escaped artifact containment does not persist success messages');
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoConvertWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoCompressCommandReady: false,
        videoConvertCommandReady: true,
        videoEnhanceCommandReady: false,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_convert_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoConvertWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-convert-asset',
          sandboxPath: 'D:/autocut/media/inputs/native-convert-asset.mp4',
          byteSize: 987654,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 987654,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the native video conversion workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for the native video conversion workflow contract');
      },
      compressVideo: async () => {
        throw new Error('video compression command is not configured for the native video conversion workflow contract');
      },
      convertVideo: async (request) => {
        nativeVideoConvertWorkflowCommands.push({ kind: 'convert', request });
        const output = createNativeTaskOutputArtifact('native-convert-task', 'native-convert-artifact.webm');
        return {
          artifactUuid: 'native-convert-artifact',
          taskUuid: 'native-convert-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 456789,
          format: 'webm',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      enhanceVideo: async () => {
        throw new Error('video enhancement command is not configured for the native video conversion workflow contract');
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const nativeVideoConvertSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeVideoConvertWorkflowResult = await withImmediateTimers(async () =>
      processVideoConvert({
        fileId: 'asset-source-native-video-convert',
        file: nativeVideoConvertSourceFile,
        targetFormat: 'webm',
        videoCodec: 'vp9',
        audioCodec: 'opus',
        resolution: '720p',
      }),
    );
    const nativeVideoConvertWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeVideoConvertWorkflowTask = nativeVideoConvertWorkflowTasks.find(
      (task) => task.id === nativeVideoConvertWorkflowResult.taskId,
    );
    const nativeVideoConvertWorkflowAssets = readScopedStoredArray(services, 'assets');
    assertEqual(nativeVideoConvertWorkflowResult.success, true, 'video convert native workflow reports success');
    assertEqual(
      nativeVideoConvertWorkflowCommands[0]?.kind,
      'import',
      'video convert native workflow imports local media before conversion',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'video convert native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.kind,
      'convert',
      'video convert native workflow converts after import',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.request?.assetUuid,
      'native-convert-asset',
      'video convert native workflow converts by imported assetUuid',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.request?.targetFormat,
      'webm',
      'video convert native workflow forwards the selected target format',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.request?.videoCodec,
      'vp9',
      'video convert native workflow forwards the selected video codec',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.request?.audioCodec,
      'opus',
      'video convert native workflow forwards the selected audio codec',
    );
    assertEqual(
      nativeVideoConvertWorkflowCommands[1]?.request?.resolution,
      '720p',
      'video convert native workflow forwards the selected resolution',
    );
    assertEqual(
      nativeVideoConvertWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video convert native workflow completes the persisted task',
    );
    assertEqual(
      nativeVideoConvertWorkflowTask?.videoUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fnative-convert-task%2Fnative-convert-artifact.webm',
      'video convert native workflow converts native artifact paths to safe asset URLs',
    );
    assertEqual(
      nativeVideoConvertWorkflowAssets[0]?.type,
      'video',
      'video convert native workflow stores the converted output as a video asset',
    );
    assertEqual(
      nativeVideoConvertWorkflowAssets[0]?.size,
      456789,
      'video convert native workflow stores the native artifact byte size on the generated asset',
    );
    assertEqual(
      nativeVideoConvertWorkflowAssets[0]?.url,
      nativeVideoConvertWorkflowTask?.videoUrl,
      'video convert native workflow stores the safe asset URL on the generated asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoEnhanceWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        contractVersion: 'contract-test',
        hostKind: 'native-host',
        databaseContractReady: true,
        sqliteMigrationReady: true,
        databaseHealthCommandReady: true,
        ffmpegProbeCommandReady: true,
        mediaImportCommandReady: true,
        mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
        audioExtractionCommandReady: false,
        audioExtractionFromAssetReady: false,
        videoGifCommandReady: false,
        videoCompressCommandReady: false,
        videoConvertCommandReady: false,
        videoEnhanceCommandReady: true,
        nativeTaskQueryCommandReady: true,
        nativeTaskCancelCommandReady: false,
        nativeTaskRecoveryCommandReady: false,
        nativeTaskRetryCommandReady: false,
        nativeTaskProgressEventsReady: false,
        ffmpegToolchainManifestReady: true,
        ffmpegToolchainResolverReady: true,
        ffmpegBundledReady: false,
        ffmpegExecutionReady: false,
        supportedCommands: ['autocut_import_media_file', 'autocut_enhance_video'],
      }),
      getDatabaseHealth: async () => ({
        ready: true,
        databasePath: 'memory',
        appliedMigrations: ['baseline'],
        verifiedTables: ['media_asset'],
        missingTables: [],
        diagnostics: [],
      }),
      probeFfmpeg: async () => ({
        available: true,
        executable: 'ffmpeg',
        sourceKind: 'system-path',
        manifestReady: true,
        bundledReady: false,
        versionLine: 'ffmpeg contract',
        diagnostics: [],
      }),
      importMediaFile: async (request) => {
        nativeVideoEnhanceWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'native-enhance-asset',
          sandboxPath: 'D:/autocut/media/inputs/native-enhance-asset.mp4',
          byteSize: 987654,
          name: 'native-source.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
        };
      },
      describeLocalMediaFile: async (request) => ({
        sourcePath: request.sourcePath,
        byteSize: 987654,
        name: 'native-source.mp4',
        mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
        mimeType: 'video/mp4',
      }),
      extractAudio: async () => {
        throw new Error('audio extraction is not configured for the native video enhancement workflow contract');
      },
      generateGif: async () => {
        throw new Error('video GIF command is not configured for the native video enhancement workflow contract');
      },
      compressVideo: async () => {
        throw new Error('video compression command is not configured for the native video enhancement workflow contract');
      },
      convertVideo: async () => {
        throw new Error('video conversion command is not configured for the native video enhancement workflow contract');
      },
      enhanceVideo: async (request) => {
        nativeVideoEnhanceWorkflowCommands.push({ kind: 'enhance', request });
        const output = createNativeTaskOutputArtifact('native-enhance-task', 'native-enhance-artifact.mp4');
        return {
          artifactUuid: 'native-enhance-artifact',
          taskUuid: 'native-enhance-task',
          sourceAssetUuid: request.assetUuid,
          artifactPath: output.artifactPath,
          taskOutputDir: output.taskOutputDir,
          byteSize: 567890,
          format: 'mp4',
          ffmpegExecutable: 'ffmpeg',
        };
      },
      runAudioSmoke: async () => ({
        artifactUuid: 'native-audio-smoke',
        taskUuid: 'native-audio-smoke-task',
        sourceAssetUuid: '',
        artifactPath: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').artifactPath,
        taskOutputDir: createNativeTaskOutputArtifact('native-audio-smoke-task', 'native-audio-smoke.wav').taskOutputDir,
        byteSize: 1,
        format: 'wav',
        ffmpegExecutable: 'ffmpeg',
      }),
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    const nativeVideoEnhanceSourceFile = createTrustedLocalMediaFile(commons, 'D:/media/native-source.mp4');
    const nativeVideoEnhanceWorkflowResult = await withImmediateTimers(async () =>
      processVideoEnhance({
        fileId: 'asset-source-native-video-enhance',
        file: nativeVideoEnhanceSourceFile,
        targetResolution: '1080p',
        enhanceMode: 'real',
        frameRate: '60',
      }),
    );
    const nativeVideoEnhanceWorkflowTasks = readScopedStoredArray(services, 'tasks');
    const nativeVideoEnhanceWorkflowTask = nativeVideoEnhanceWorkflowTasks.find(
      (task) => task.id === nativeVideoEnhanceWorkflowResult.taskId,
    );
    const nativeVideoEnhanceWorkflowAssets = readScopedStoredArray(services, 'assets');
    assertEqual(nativeVideoEnhanceWorkflowResult.success, true, 'video enhance native workflow reports success');
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[0]?.kind,
      'import',
      'video enhance native workflow imports local media before enhancement',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[0]?.request?.sourcePath,
      'D:/media/native-source.mp4',
      'video enhance native workflow passes the trusted desktop source path to media import',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[1]?.kind,
      'enhance',
      'video enhance native workflow enhances after import',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[1]?.request?.assetUuid,
      'native-enhance-asset',
      'video enhance native workflow enhances by imported assetUuid',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[1]?.request?.targetResolution,
      '1080p',
      'video enhance native workflow forwards the selected target resolution',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[1]?.request?.enhanceMode,
      'real',
      'video enhance native workflow forwards the selected enhance mode',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowCommands[1]?.request?.frameRate,
      '60',
      'video enhance native workflow forwards the selected frame rate',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowTask?.status,
      types.AUTOCUT_TASK_STATUS.completed,
      'video enhance native workflow completes the persisted task',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowTask?.videoUrl,
      'asset://localhost/D%3A%2Fautocut%2Fmedia%2Ftasks%2Fnative-enhance-task%2Fnative-enhance-artifact.mp4',
      'video enhance native workflow converts native artifact paths to safe asset URLs',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowAssets[0]?.type,
      'video',
      'video enhance native workflow stores the enhanced output as a video asset',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowAssets[0]?.size,
      567890,
      'video enhance native workflow stores the native artifact byte size on the generated asset',
    );
    assertEqual(
      nativeVideoEnhanceWorkflowAssets[0]?.url,
      nativeVideoEnhanceWorkflowTask?.videoUrl,
      'video enhance native workflow stores the safe asset URL on the generated asset',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const taskEvents = captureEvents(services, 'taskAdded');
    const taskUpdateEvents = captureEvents(services, 'taskUpdated');
    const taskDeleteEvents = captureEvents(services, 'taskDeleted');
    const task = {
      id: 'contract-task',
      type: types.AUTOCUT_TASK_TYPE.videoSlice,
      name: 'contract.mp4',
      status: types.AUTOCUT_TASK_STATUS.pending,
      progress: 0,
      createdAt: '2026-05-05T00:00:00.000Z',
    };
    await withImmediateTimers(async () => {
      await services.addTask(task);
      await services.updateTask(task.id, {
        status: types.AUTOCUT_TASK_STATUS.completed,
        progress: 100,
        videoUrl: 'blob:contract-video',
      });
      await services.deleteTask(task.id);
    });
    taskEvents.stop();
    taskUpdateEvents.stop();
    taskDeleteEvents.stop();
    const storedTasks = readScopedStoredArray(services, 'tasks');
    assertEqual(taskEvents.details[0]?.id, task.id, 'addTask dispatches taskAdded with the created task');
    assertEqual(taskUpdateEvents.details[0]?.progress, 100, 'updateTask dispatches taskUpdated with merged progress');
    assertEqual(taskDeleteEvents.details[0]?.id, task.id, 'deleteTask dispatches taskDeleted with the deleted task id');
    assertRule(storedTasks.every((storedTask) => storedTask.id !== task.id), 'deleteTask removes the task from persisted storage');

    resetStorage();
    services.resetAutoCutNativeHostClient();
    const editableTranscriptTask = {
      id: 'editable-transcript-local-task',
      type: types.AUTOCUT_TASK_TYPE.videoSlice,
      name: 'editable-transcript.mp4',
      status: types.AUTOCUT_TASK_STATUS.completed,
      progress: 100,
      createdAt: '2026-05-05T00:00:00.000Z',
      resultCount: 1,
      sourceFileId: 'editable-transcript-source-asset',
      generatedAssetIds: ['editable-transcript-slice'],
      sliceResults: [
        {
          id: 'editable-transcript-slice',
          name: 'slice-001.mp4',
          title: 'Editable transcript slice',
          duration: 12,
          size: 123456,
          resolution: '1080p',
          thumbnailUrl: 'blob:editable-thumb',
          url: 'blob:editable-video',
          sourceStartMs: 1000,
          sourceEndMs: 13000,
          speechStartMs: 1200,
          speechEndMs: 12200,
          transcriptText: 'Original first line. Original second line.',
          transcriptSegments: [
            { startMs: 1200, endMs: 6200, text: 'Original first line.', speaker: 'Speaker 1' },
            { startMs: 6400, endMs: 12200, text: 'Original second line.', speaker: 'Speaker 1' },
          ],
          transcriptSegmentCount: 2,
          transcriptCoverageScore: 0.96,
          speechContinuityGrade: 'strong',
        },
      ],
    };
    const transcriptUpdateEvents = captureEvents(services, 'taskUpdated');
    await withImmediateTimers(async () => {
      await services.addTask(editableTranscriptTask);
      await services.updateTaskSliceTranscript(editableTranscriptTask.id, 'editable-transcript-slice', [
        { startMs: 1200, endMs: 6200, text: 'Corrected first line.', speaker: 'Speaker 1' },
        { startMs: 6400, endMs: 12200, text: 'Corrected second line with   normalized spacing.', speaker: 'Speaker 1' },
      ]);
    });
    transcriptUpdateEvents.stop();
    const editableStoredTask = readScopedStoredArray(services, 'tasks').find((storedTask) => storedTask.id === editableTranscriptTask.id);
    assertEqual(
      editableStoredTask?.sliceResults?.[0]?.transcriptText,
      'Corrected first line. Corrected second line with normalized spacing.',
      'updateTaskSliceTranscript stores corrected transcriptText from edited segments',
    );
    assertEqual(
      editableStoredTask?.sliceResults?.[0]?.transcriptSegmentCount,
      2,
      'updateTaskSliceTranscript keeps transcriptSegmentCount in sync with edited segments',
    );
    assertEqual(
      transcriptUpdateEvents.details.at(-1)?.sliceResults?.[0]?.transcriptSegments?.[1]?.text,
      'Corrected second line with normalized spacing.',
      'updateTaskSliceTranscript dispatches taskUpdated with normalized edited transcript segments',
    );
    assertEqual(
      editableStoredTask?.sliceResults?.[0]?.transcriptCorrection?.source,
      'task-detail',
      'updateTaskSliceTranscript records task-detail as the transcript correction source',
    );
    assertEqual(
      editableStoredTask?.sliceResults?.[0]?.transcriptCorrection?.originalTranscriptText,
      'Original first line. Original second line.',
      'updateTaskSliceTranscript preserves the original transcript text for correction audit',
    );
    assertEqual(
      editableStoredTask?.sliceResults?.[0]?.transcriptCorrection?.correctionCount,
      2,
      'updateTaskSliceTranscript records how many transcript segments were corrected',
    );
    assertRule(
      typeof editableStoredTask?.sliceResults?.[0]?.transcriptCorrection?.correctedAt === 'string' &&
        !Number.isNaN(Date.parse(editableStoredTask.sliceResults[0].transcriptCorrection.correctedAt)),
      'updateTaskSliceTranscript records a machine-readable transcript correction timestamp',
    );

    resetStorage();
    const partiallyEditedTranscriptTask = {
      ...editableTranscriptTask,
      id: 'partially-edited-transcript-local-task',
      sliceResults: [
        {
          ...editableTranscriptTask.sliceResults[0],
          id: 'partially-edited-transcript-slice',
        },
      ],
    };
    await withImmediateTimers(async () => {
      await services.addTask(partiallyEditedTranscriptTask);
      await services.updateTaskSliceTranscript(partiallyEditedTranscriptTask.id, 'partially-edited-transcript-slice', [
        { startMs: 1200, endMs: 6200, text: 'Original first line.', speaker: 'Speaker 1' },
        { startMs: 6400, endMs: 12200, text: 'Corrected only the second line.', speaker: 'Speaker 1' },
      ]);
    });
    const partiallyEditedStoredTask = readScopedStoredArray(services, 'tasks')
      .find((storedTask) => storedTask.id === partiallyEditedTranscriptTask.id);
    assertEqual(
      partiallyEditedStoredTask?.sliceResults?.[0]?.transcriptCorrection?.correctionCount,
      1,
      'updateTaskSliceTranscript counts only transcript segments whose normalized text or speaker changed',
    );

    resetStorage();
    const nativeEditableTranscriptEvents = captureEvents(services, 'taskUpdated');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'editable-native-transcript-test',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localMediaFileSelectCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          openArtifactInFolderCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          speechTranscriptionModelDownloadCommandReady: true,
          speechTranscriptionExecutableDownloadCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: true,
          ffmpegExecutionReady: true,
          supportedCommands: ['autocut_host_capabilities', 'autocut_list_native_tasks'],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        return [
          {
            uuid: 'editable-native-transcript-task',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'editable-native-source-asset',
            inputJson: JSON.stringify({
              operation: 'videoSlice',
              assetUuid: 'editable-native-source-asset',
              sourceName: 'editable-native.mp4',
              outputFormat: 'mp4',
            }),
            outputJson: JSON.stringify({
              name: 'slice-001.mp4',
              assetUuid: 'editable-native-source-asset',
              taskOutputDir: 'D:/autocut/media/tasks/editable-native-transcript-task',
              sliceCount: 1,
              sliceResults: [
                {
                  artifactUuid: 'editable-native-slice',
                  artifactPath: 'D:/autocut/media/tasks/editable-native-transcript-task/slice-001.mp4',
                  thumbnailArtifactUuid: 'editable-native-thumb',
                  thumbnailArtifactPath: 'D:/autocut/media/tasks/editable-native-transcript-task/cover/slice-001.jpg',
                  subtitleArtifactUuid: 'editable-native-subtitle',
                  subtitleArtifactPath: 'D:/autocut/media/tasks/editable-native-transcript-task/slice-001.srt',
                  taskOutputDir: 'D:/autocut/media/tasks/editable-native-transcript-task',
                  byteSize: 123456,
                  thumbnailByteSize: 1234,
                  subtitleByteSize: 456,
                  subtitleFormat: 'srt',
                  format: 'mp4',
                  startMs: 1000,
                  durationMs: 12000,
                  label: 'Native editable',
                  sourceStartMs: 1000,
                  sourceEndMs: 13000,
                  speechStartMs: 1200,
                  speechEndMs: 12800,
                  boundaryPaddingBeforeMs: 200,
                  boundaryPaddingAfterMs: 200,
                  transcriptText: 'Original native transcript text.',
                  transcriptSegments: [
                    {
                      startMs: 1200,
                      endMs: 12800,
                      text: 'Original native transcript text.',
                      speaker: 'Speaker 1',
                    },
                  ],
                  transcriptSegmentCount: 1,
                  transcriptCoverageScore: 0.96,
                  speechContinuityGrade: 'strong',
                  ...createRecoveredSmartSliceAudioCleanupEvidence({
                    audioActivityEndMs: 12800,
                    trailingSilenceMs: 200,
                  }),
                },
              ],
            }),
            createdAt: '2026-05-05T00:00:00.000Z',
            updatedAt: '2026-05-05T00:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
      }
      throw new Error(`Unexpected editable native transcript command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));
    await withImmediateTimers(async () => {
      await services.updateTaskSliceTranscript('editable-native-transcript-task', 'editable-native-slice', [
        { startMs: 1200, endMs: 12800, text: 'Corrected native transcript text.', speaker: 'Speaker 1' },
      ]);
    });
    nativeEditableTranscriptEvents.stop();
    const nativeEditableTasks = await withImmediateTimers(() => services.getTasks());
    const nativeEditableTask = nativeEditableTasks.find((candidate) => candidate.id === 'editable-native-transcript-task');
    assertEqual(
      nativeEditableTask?.sliceResults?.[0]?.transcriptText,
      'Corrected native transcript text.',
      'updateTaskSliceTranscript creates a local transcript override for native-only slice tasks',
    );
    assertEqual(
      nativeEditableTranscriptEvents.details.at(-1)?.sliceResults?.[0]?.transcriptText,
      'Corrected native transcript text.',
      'updateTaskSliceTranscript dispatches a merged taskUpdated event for native-only transcript edits',
    );
    assertEqual(
      nativeEditableTask?.sliceResults?.[0]?.transcriptCorrection?.source,
      'task-detail',
      'updateTaskSliceTranscript preserves transcript correction audit metadata after native task recovery merge',
    );
    assertEqual(
      nativeEditableTask?.sliceResults?.[0]?.transcriptCorrection?.originalTranscriptText,
      'Original native transcript text.',
      'updateTaskSliceTranscript preserves recovered native transcript text as the correction audit baseline',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const bulkTaskDeleteEvents = captureEvents(services, 'taskDeleted');
    const bulkTasks = [
      {
        id: 'bulk-task-a',
        type: types.AUTOCUT_TASK_TYPE.videoSlice,
        name: 'bulk-a.mp4',
        status: types.AUTOCUT_TASK_STATUS.completed,
        progress: 100,
        createdAt: '2026-05-05T00:00:00.000Z',
      },
      {
        id: 'bulk-task-b',
        type: types.AUTOCUT_TASK_TYPE.audioExtraction,
        name: 'bulk-b.wav',
        status: types.AUTOCUT_TASK_STATUS.failed,
        progress: 0,
        createdAt: '2026-05-05T00:01:00.000Z',
      },
      {
        id: 'bulk-task-c',
        type: types.AUTOCUT_TASK_TYPE.videoCompress,
        name: 'bulk-c.mp4',
        status: types.AUTOCUT_TASK_STATUS.processing,
        progress: 50,
        createdAt: '2026-05-05T00:02:00.000Z',
      },
    ];
    await withImmediateTimers(async () => {
      await Promise.all(bulkTasks.map((bulkTask) => services.addTask(bulkTask)));
    });
    const bulkDeleteResult = await services.deleteTasks(['bulk-task-a', 'missing-task', 'bulk-task-b', 'bulk-task-a']);
    bulkTaskDeleteEvents.stop();
    const storedTasksAfterBulkDelete = readScopedStoredArray(services, 'tasks');
    assertDeepEqual(
      bulkDeleteResult,
      {
        requested: 4,
        succeeded: 2,
        skipped: 1,
        taskIds: ['bulk-task-a', 'bulk-task-b'],
        skippedTaskIds: ['missing-task'],
        deleted: 2,
        deletedTaskIds: ['bulk-task-a', 'bulk-task-b'],
      },
      'deleteTasks returns a canonical bulk operation result with deleted and skipped task ids',
    );
    assertDeepEqual(
      bulkTaskDeleteEvents.details.map((detail) => detail.id),
      ['bulk-task-a', 'bulk-task-b'],
      'deleteTasks dispatches one taskDeleted event for each deleted task in request order',
    );
    assertRule(
      storedTasksAfterBulkDelete.some((storedTask) => storedTask.id === 'bulk-task-c') &&
        storedTasksAfterBulkDelete.every((storedTask) => storedTask.id !== 'bulk-task-a' && storedTask.id !== 'bulk-task-b'),
      'deleteTasks removes selected existing tasks without deleting unselected tasks',
    );

    resetStorage();
    const bulkNativeTaskCommands = [];
    const bulkNativeTaskDeleteEvents = captureEvents(services, 'taskDeleted');
    const bulkNativeTaskUpdateEvents = captureEvents(services, 'taskUpdated');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      bulkNativeTaskCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'bulk-task-operations-test',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localMediaFileSelectCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          localMediaPreviewDirectoryScopeCommandReady: true,
          openArtifactInFolderCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoSliceCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          speechTranscriptionModelDownloadCommandReady: true,
          speechTranscriptionExecutableDownloadCommandReady: false,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: true,
          ffmpegExecutionReady: true,
          supportedCommands: [
            'autocut_host_capabilities',
            'autocut_list_native_tasks',
            'autocut_cancel_native_task',
            'autocut_retry_native_task',
          ],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        const snapshots = [
          {
            uuid: 'native-bulk-delete',
            taskType: 3,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-native-bulk-delete',
            inputJson: '{"name":"native-delete.mp4"}',
            outputJson: '{"artifactUuid":"artifact-native-bulk-delete","artifactPath":"D:/autocut/media/tasks/native-bulk-delete/native-delete.mp4","byteSize":1234}',
            createdAt: '2026-05-05T01:00:00.000Z',
            updatedAt: '2026-05-05T01:01:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'native-bulk-cancel',
            taskType: 3,
            status: 1,
            progress: 45,
            sourceAssetUuid: 'asset-native-bulk-cancel',
            inputJson: '{"name":"native-cancel.mp4"}',
            outputJson: '{}',
            createdAt: '2026-05-05T01:02:00.000Z',
            updatedAt: '2026-05-05T01:03:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'native-bulk-retry',
            taskType: 3,
            status: 3,
            progress: 15,
            sourceAssetUuid: 'asset-native-bulk-retry',
            inputJson: '{"name":"native-retry.mp4"}',
            outputJson: '{}',
            errorMessage: 'previous failure',
            createdAt: '2026-05-05T01:04:00.000Z',
            updatedAt: '2026-05-05T01:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
        if (args?.request?.taskUuid) {
          return snapshots.filter((snapshot) => snapshot.uuid === args.request.taskUuid);
        }
        return snapshots;
      }
      if (command === 'autocut_cancel_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          status: 4,
          canceled: true,
          message: 'cancel requested',
        };
      }
      if (command === 'autocut_retry_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          retryTaskUuid: `${args.request.taskUuid}-retry`,
          status: 1,
          retried: true,
          message: 'retry queued',
        };
      }
      throw new Error(`Unexpected bulk native task command: ${command}`);
    }, {
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    }));

    const nativeBulkDeleteResult = await services.deleteTasks(['native-bulk-delete', 'missing-native-task']);
    const nativeBulkCancelResult = await services.cancelTasks(['native-bulk-cancel', 'native-bulk-delete', 'native-bulk-cancel']);
    const nativeBulkRetryResult = await services.retryTasks(['native-bulk-retry', 'native-bulk-cancel', 'native-bulk-retry']);
    bulkNativeTaskDeleteEvents.stop();
    bulkNativeTaskUpdateEvents.stop();
    const dismissedNativeTaskIds = readScopedStoredArray(services, 'dismissedNativeTasks');
    assertDeepEqual(
      nativeBulkDeleteResult,
      {
        requested: 2,
        succeeded: 1,
        skipped: 1,
        taskIds: ['native-bulk-delete'],
        skippedTaskIds: ['missing-native-task'],
        deleted: 1,
        deletedTaskIds: ['native-bulk-delete'],
      },
      'deleteTasks tombstones recovered native tasks and reports missing native ids as skipped',
    );
    assertIncludes(
      dismissedNativeTaskIds,
      'native-bulk-delete',
      'deleteTasks records recovered native task ids in the dismissed native task tombstone list',
    );
    assertDeepEqual(
      nativeBulkCancelResult,
      {
        requested: 3,
        succeeded: 1,
        skipped: 1,
        taskIds: ['native-bulk-cancel'],
        skippedTaskIds: ['native-bulk-delete'],
        canceled: 1,
        canceledTaskIds: ['native-bulk-cancel'],
      },
      'cancelTasks only cancels active selected native tasks and deduplicates repeated ids',
    );
    assertDeepEqual(
      nativeBulkRetryResult,
      {
        requested: 3,
        succeeded: 1,
        skipped: 1,
        taskIds: ['native-bulk-retry'],
        skippedTaskIds: ['native-bulk-cancel'],
        retried: 1,
        retriedTaskIds: ['native-bulk-retry'],
        retryTaskIds: ['native-bulk-retry-retry'],
      },
      'retryTasks only retries failed selected native tasks and returns queued retry task ids',
    );
    assertDeepEqual(
      bulkNativeTaskCommands
        .filter((entry) => entry.command === 'autocut_cancel_native_task')
        .map((entry) => entry.args.request.taskUuid),
      ['native-bulk-cancel'],
      'cancelTasks forwards one native cancel command for each eligible task in request order',
    );
    assertDeepEqual(
      bulkNativeTaskCommands
        .filter((entry) => entry.command === 'autocut_retry_native_task')
        .map((entry) => entry.args.request.taskUuid),
      ['native-bulk-retry'],
      'retryTasks forwards one native retry command for each eligible failed task in request order',
    );
    assertDeepEqual(
      bulkNativeTaskDeleteEvents.details.map((detail) => detail.id),
      ['native-bulk-delete'],
      'deleteTasks dispatches taskDeleted for recovered native task tombstones',
    );
    assertRule(
      bulkNativeTaskUpdateEvents.details.some((detail) => detail.id === 'native-bulk-cancel') &&
        bulkNativeTaskUpdateEvents.details.some((detail) => detail.id === 'native-bulk-retry-retry'),
      'cancelTasks and retryTasks dispatch taskUpdated events for successful native operations',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const localMappedNativeTaskCommands = [];
    const localMappedTaskUpdateEvents = captureEvents(services, 'taskUpdated');
    const localMappedTaskDeleteEvents = captureEvents(services, 'taskDeleted');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      localMappedNativeTaskCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'local-mapped-native-task-operations-test',
          hostKind: 'native-host',
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRetryCommandReady: true,
          supportedCommands: [
            'autocut_host_capabilities',
            'autocut_list_native_tasks',
            'autocut_cancel_native_task',
            'autocut_retry_native_task',
          ],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        const snapshots = [
          {
            uuid: 'native-local-mapped-delete',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-local-mapped-delete',
            inputJson: '{"name":"local-mapped-delete.mp4"}',
            outputJson: '{}',
            createdAt: '2026-05-05T02:00:00.000Z',
            updatedAt: '2026-05-05T02:01:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'native-local-mapped-cancel',
            taskType: 6,
            status: 1,
            progress: 45,
            sourceAssetUuid: 'asset-local-mapped-cancel',
            inputJson: '{"name":"local-mapped-cancel.mp4"}',
            outputJson: '{}',
            createdAt: '2026-05-05T02:02:00.000Z',
            updatedAt: '2026-05-05T02:03:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
          {
            uuid: 'native-local-mapped-retry',
            taskType: 6,
            status: 3,
            progress: 15,
            sourceAssetUuid: 'asset-local-mapped-retry',
            inputJson: '{"name":"local-mapped-retry.mp4"}',
            outputJson: '{}',
            errorMessage: 'post-render validation failed',
            createdAt: '2026-05-05T02:04:00.000Z',
            updatedAt: '2026-05-05T02:05:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
        if (args?.request?.taskUuid) {
          return snapshots.filter((snapshot) => snapshot.uuid === args.request.taskUuid);
        }
        return snapshots;
      }
      if (command === 'autocut_cancel_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          status: 4,
          canceled: true,
          message: 'local mapped cancel requested',
        };
      }
      if (command === 'autocut_retry_native_task') {
        return {
          taskUuid: args.request.taskUuid,
          retryTaskUuid: `${args.request.taskUuid}-retry`,
          status: 1,
          retried: true,
          message: 'local mapped retry queued',
        };
      }
      throw new Error(`Unexpected local mapped native task command: ${command}`);
    }));
    await withImmediateTimers(async () => {
      await services.addTask({
        id: 'local-mapped-delete',
        nativeTaskId: 'native-local-mapped-delete',
        type: types.AUTOCUT_TASK_TYPE.videoSlice,
        name: 'local mapped delete.mp4',
        status: types.AUTOCUT_TASK_STATUS.completed,
        progress: 100,
        createdAt: '2026-05-05T02:00:00.000Z',
      });
      await services.addTask({
        id: 'local-mapped-cancel',
        nativeTaskId: 'native-local-mapped-cancel',
        type: types.AUTOCUT_TASK_TYPE.videoSlice,
        name: 'local mapped cancel.mp4',
        status: types.AUTOCUT_TASK_STATUS.processing,
        progress: 45,
        createdAt: '2026-05-05T02:02:00.000Z',
      });
      await services.addTask({
        id: 'local-mapped-retry',
        nativeTaskId: 'native-local-mapped-retry',
        type: types.AUTOCUT_TASK_TYPE.videoSlice,
        name: 'local mapped retry.mp4',
        status: types.AUTOCUT_TASK_STATUS.failed,
        progress: 15,
        errorMessage: 'post-render validation failed',
        createdAt: '2026-05-05T02:04:00.000Z',
      });
    });
    const localMappedDeleteResult = await services.deleteTasks(['local-mapped-delete']);
    const localMappedCancelResult = await services.cancelTasks(['local-mapped-cancel']);
    const localMappedRetryResult = await services.retryTasks(['local-mapped-retry']);
    localMappedTaskUpdateEvents.stop();
    localMappedTaskDeleteEvents.stop();
    const localMappedDismissedNativeTaskIds = readScopedStoredArray(services, 'dismissedNativeTasks');
    const localMappedVisibleTasksAfterDelete = await withImmediateTimers(() => services.getTasks());
    assertDeepEqual(
      localMappedDeleteResult.deletedTaskIds,
      ['local-mapped-delete'],
      'deleteTasks returns local task ids when deleting a task mapped to a native ops_task',
    );
    assertIncludes(
      localMappedDismissedNativeTaskIds,
      'native-local-mapped-delete',
      'deleteTasks tombstones the native ops_task id when deleting a local task mapped by nativeTaskId',
    );
    assertRule(
      localMappedVisibleTasksAfterDelete.every((task) =>
        task.id !== 'local-mapped-delete' && task.id !== 'native-local-mapped-delete'
      ),
      'deleteTasks hides the recovered native ops_task after deleting its mapped local task',
    );
    assertDeepEqual(
      localMappedCancelResult.canceledTaskIds,
      ['local-mapped-cancel'],
      'cancelTasks returns local task ids when canceling a task mapped to a native ops_task',
    );
    assertDeepEqual(
      localMappedRetryResult.retriedTaskIds,
      ['local-mapped-retry'],
      'retryTasks returns local task ids when retrying a task mapped to a native ops_task',
    );
    assertDeepEqual(
      localMappedNativeTaskCommands
        .filter((entry) => entry.command === 'autocut_cancel_native_task')
        .map((entry) => entry.args.request.taskUuid),
      ['native-local-mapped-cancel'],
      'cancelTasks forwards nativeTaskId, not the local task id, for mapped local tasks',
    );
    assertDeepEqual(
      localMappedNativeTaskCommands
        .filter((entry) => entry.command === 'autocut_retry_native_task')
        .map((entry) => entry.args.request.taskUuid),
      ['native-local-mapped-retry'],
      'retryTasks forwards nativeTaskId, not the local task id, for mapped local tasks',
    );
    assertRule(
      localMappedTaskUpdateEvents.details.some((detail) =>
        detail.id === 'local-mapped-cancel' &&
        detail.nativeTaskId === 'native-local-mapped-cancel'
      ) &&
        localMappedTaskUpdateEvents.details.some((detail) =>
          detail.id === 'local-mapped-retry' &&
          detail.nativeTaskId === 'native-local-mapped-retry-retry'
        ),
      'cancelTasks and retryTasks dispatch local task updates while preserving the native task mapping',
    );
    assertDeepEqual(
      localMappedTaskDeleteEvents.details.map((detail) => detail.id),
      ['local-mapped-delete'],
      'deleteTasks dispatches local taskDeleted ids for mapped local tasks',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const singleLocalMappedDeleteEvents = captureEvents(services, 'taskDeleted');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'single-local-mapped-native-delete-test',
          hostKind: 'native-host',
          nativeTaskQueryCommandReady: true,
          supportedCommands: [
            'autocut_host_capabilities',
            'autocut_list_native_tasks',
          ],
        };
      }
      if (command === 'autocut_list_native_tasks') {
        const snapshots = [
          {
            uuid: 'native-single-local-mapped-delete',
            taskType: 6,
            status: 2,
            progress: 100,
            sourceAssetUuid: 'asset-single-local-mapped-delete',
            inputJson: '{"name":"single-local-mapped-delete.mp4"}',
            outputJson: '{}',
            createdAt: '2026-05-05T02:10:00.000Z',
            updatedAt: '2026-05-05T02:11:00.000Z',
            stages: [],
            events: [],
            workerLeases: [],
          },
        ];
        if (args?.request?.taskUuid) {
          return snapshots.filter((snapshot) => snapshot.uuid === args.request.taskUuid);
        }
        return snapshots;
      }
      throw new Error(`Unexpected single local mapped native delete command: ${command}`);
    }));
    await services.addTask({
      id: 'single-local-mapped-delete',
      nativeTaskId: 'native-single-local-mapped-delete',
      type: types.AUTOCUT_TASK_TYPE.videoSlice,
      name: 'single local mapped delete.mp4',
      status: types.AUTOCUT_TASK_STATUS.completed,
      progress: 100,
      createdAt: '2026-05-05T02:10:00.000Z',
    });
    await services.deleteTask('single-local-mapped-delete');
    singleLocalMappedDeleteEvents.stop();
    const singleLocalMappedDismissedNativeTaskIds = readScopedStoredArray(services, 'dismissedNativeTasks');
    const singleLocalMappedVisibleTasksAfterDelete = await withImmediateTimers(() => services.getTasks());
    assertIncludes(
      singleLocalMappedDismissedNativeTaskIds,
      'native-single-local-mapped-delete',
      'deleteTask tombstones the native ops_task id when deleting a local task mapped by nativeTaskId',
    );
    assertRule(
      singleLocalMappedVisibleTasksAfterDelete.every((task) =>
        task.id !== 'single-local-mapped-delete' && task.id !== 'native-single-local-mapped-delete'
      ),
      'deleteTask hides the recovered native ops_task after deleting its mapped local task',
    );
    assertDeepEqual(
      singleLocalMappedDeleteEvents.details.map((detail) => detail.id),
      ['single-local-mapped-delete'],
      'deleteTask dispatches the local taskDeleted id for mapped local tasks',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const emptyAssets = await services.getAssets();
    const emptyStorageInfo = await services.getStorageInfo();
    assertEqual(emptyAssets.length, 0, 'getAssets does not seed asset center data from mock/default assets');
    assertEqual(emptyStorageInfo.used, 0, 'getStorageInfo reports zero used bytes for a clean workspace');

    resetStorage();
    const nativeVideoDedupFingerprintRequests = [];
    const nativeVideoDedupIdentityRequests = [];
    const nativeVideoDedupFileRevisionByPath = new Map([
      ['D:/autocut/media/tasks/source/camera-a.mp4', 1000],
      ['D:/autocut/media/tasks/target/renamed-upload.mov', 2000],
    ]);
    const nativeVideoDedupShaByPath = new Map([
      ['D:/autocut/media/tasks/source/camera-a.mp4', 'a'.repeat(64)],
      ['D:/autocut/media/tasks/target/renamed-upload.mov', 'a'.repeat(64)],
    ]);
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        videoDedupFingerprintCommandReady: true,
        videoDedupFileIdentityCommandReady: true,
        supportedCommands: ['autocut_fingerprint_video_file', 'autocut_probe_video_file_identity'],
      }),
      probeVideoFileIdentity: async (request) => {
        nativeVideoDedupIdentityRequests.push(request);
        return {
          sourcePath: request.sourcePath.replace('/autocut/media/', '/autocut-canonical/media/'),
          byteSize: 4096,
          modifiedAtMs: nativeVideoDedupFileRevisionByPath.get(request.sourcePath) ?? 0,
          fileIdentityVersion: '2026-05-15.video-file-identity.v1',
        };
      },
      fingerprintVideoFile: async (request) => {
        nativeVideoDedupFingerprintRequests.push(request);
        return {
          sourcePath: request.sourcePath.replace('/autocut/media/', '/autocut-canonical/media/'),
          byteSize: 4096,
          modifiedAtMs: nativeVideoDedupFileRevisionByPath.get(request.sourcePath) ?? 0,
          sha256: nativeVideoDedupShaByPath.get(request.sourcePath) ?? 'f'.repeat(64),
          algorithm: 'sha256',
          fingerprintVersion: '2026-05-15.video-file-fingerprint.v1',
          fileIdentityVersion: '2026-05-15.video-file-identity.v1',
        };
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-dedup-source',
        name: 'camera-a.mp4',
        type: 'video',
        size: 4096,
        artifactPath: 'D:/autocut/media/tasks/source/camera-a.mp4',
        createdAt: '2026-05-05T00:00:00.000Z',
        updatedAt: '2026-05-05T00:00:00.000Z',
      });
      await services.addAsset({
        id: 'native-dedup-target',
        name: 'renamed-upload.mov',
        type: 'video',
        size: 8192,
        artifactPath: 'D:/autocut/media/tasks/target/renamed-upload.mov',
        createdAt: '2026-05-05T00:01:00.000Z',
        updatedAt: '2026-05-05T00:01:00.000Z',
      });
    });
    const nativeVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'standard',
        sourceAssetIds: ['native-dedup-source'],
        referenceAssetIds: ['native-dedup-target'],
        strategies: ['exact-file-hash'],
        sensitivity: 'forensic',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: true,
        introOutroMaxDurationMs: 12000,
        actionMode: 'review-before-action',
      })
    );
    assertEqual(
      nativeVideoDedupFingerprintRequests.length,
      2,
      'video dedup exact-file-hash uses native SHA-256 fingerprints for source and reference artifacts',
    );
    assertDeepEqual(
      nativeVideoDedupFingerprintRequests.map((request) => request.sourcePath),
      [
        'D:/autocut/media/tasks/source/camera-a.mp4',
        'D:/autocut/media/tasks/target/renamed-upload.mov',
      ],
      'video dedup exact-file-hash fingerprints each local artifact path through the native host',
    );
    assertEqual(
      nativeVideoDedupReport.matches[0]?.matchKind,
      'exact',
      'video dedup exact-file-hash uses native SHA-256 fingerprints to match renamed files without relying on name metadata',
    );
    assertRule(
      nativeVideoDedupReport.matches[0]?.evidence?.[0]?.detail?.includes('Native SHA-256'),
      'video dedup exact-file-hash reports native SHA-256 evidence details for human review',
    );

    const cachedNativeVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'standard',
        sourceAssetIds: ['native-dedup-source'],
        referenceAssetIds: ['native-dedup-target'],
        strategies: ['exact-file-hash'],
        sensitivity: 'forensic',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: true,
        introOutroMaxDurationMs: 12000,
        actionMode: 'review-before-action',
      })
    );
    assertEqual(
      nativeVideoDedupFingerprintRequests.length,
      2,
      'video dedup exact-file-hash reuses cached native SHA-256 fingerprints on repeated analysis of unchanged artifacts',
    );
    assertEqual(
      nativeVideoDedupIdentityRequests.length,
      2,
      'video dedup exact-file-hash validates cached SHA-256 fingerprints with lightweight native file identity probes',
    );
    assertDeepEqual(
      nativeVideoDedupIdentityRequests.map((request) => request.sourcePath),
      [
        'D:/autocut/media/tasks/source/camera-a.mp4',
        'D:/autocut/media/tasks/target/renamed-upload.mov',
      ],
      'video dedup exact-file-hash probes each cached local artifact identity without rescanning file bytes',
    );
    assertEqual(
      cachedNativeVideoDedupReport.matches[0]?.matchKind,
      'exact',
      'video dedup exact-file-hash keeps exact duplicate results when native fingerprints are served from cache',
    );

    nativeVideoDedupFileRevisionByPath.set('D:/autocut/media/tasks/target/renamed-upload.mov', 3000);
    nativeVideoDedupShaByPath.set('D:/autocut/media/tasks/target/renamed-upload.mov', 'b'.repeat(64));
    await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'standard',
        sourceAssetIds: ['native-dedup-source'],
        referenceAssetIds: ['native-dedup-target'],
        strategies: ['exact-file-hash'],
        sensitivity: 'forensic',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: true,
        introOutroMaxDurationMs: 12000,
        actionMode: 'review-before-action',
      })
    );
    assertEqual(
      nativeVideoDedupFingerprintRequests.length,
      3,
      'video dedup exact-file-hash invalidates only the changed artifact fingerprint cache entry after native identity changes',
    );
    assertDeepEqual(
      nativeVideoDedupFingerprintRequests.map((request) => request.sourcePath),
      [
        'D:/autocut/media/tasks/source/camera-a.mp4',
        'D:/autocut/media/tasks/target/renamed-upload.mov',
        'D:/autocut/media/tasks/target/renamed-upload.mov',
      ],
      'video dedup exact-file-hash does not rescan unchanged local artifacts after native identity cache invalidation',
    );
    assertEqual(
      nativeVideoDedupIdentityRequests.length,
      4,
      'video dedup exact-file-hash keeps cache validation lightweight even when one cached artifact identity changes',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoDedupVisualEvidenceRequests = [];
    const nativeVideoDedupVisualIdentityRequests = [];
    const nativeVideoDedupVisualRevisionByPath = new Map([
      ['D:/autocut/media/imports/conference-keynote-original.mp4', 1000],
      ['D:/autocut/media/imports/social-publish-remux.mov', 2000],
    ]);
    const createNativeVideoDedupVisualEvidence = (assetUuid) => ({
      taskUuid: `visual-evidence-${assetUuid}`,
      sourceAssetUuid: assetUuid,
      provider: 'ffmpeg-scene',
      profile: 'scene-index-v1',
      ready: true,
      shots: [
        { id: `${assetUuid}-shot-001`, startMs: 0, endMs: 6200, confidence: 0.96 },
        { id: `${assetUuid}-shot-002`, startMs: 6200, endMs: 15200, confidence: 0.94 },
        { id: `${assetUuid}-shot-003`, startMs: 15200, endMs: 24100, confidence: 0.95 },
      ],
      sceneBoundaries: [
        { startMs: 0, endMs: 6200 },
        { startMs: 6200, endMs: 15200 },
        { startMs: 15200, endMs: 24100 },
      ],
      frameQuality: [
        { atMs: 0, blurScore: 0.18, exposureScore: 0.82, stabilityScore: 0.91 },
        { atMs: 6200, blurScore: 0.2, exposureScore: 0.81, stabilityScore: 0.9 },
        { atMs: 15200, blurScore: 0.19, exposureScore: 0.83, stabilityScore: 0.89 },
      ],
      frameFingerprints: [
        {
          atMs: 0,
          algorithm: 'ahash-8x8-luma-v1',
          hash: '3c3c3c3c3c3c3c3c',
          meanLuma: 0.42,
          histogram: [0.06, 0.12, 0.2, 0.24, 0.18, 0.1, 0.06, 0.04],
        },
        {
          atMs: 6200,
          algorithm: 'ahash-8x8-luma-v1',
          hash: '3c3c7e7e7e7e3c3c',
          meanLuma: 0.5,
          histogram: [0.04, 0.08, 0.16, 0.26, 0.22, 0.14, 0.07, 0.03],
        },
        {
          atMs: 15200,
          algorithm: 'ahash-8x8-luma-v1',
          hash: '18183c3c3c3c1818',
          meanLuma: 0.46,
          histogram: [0.05, 0.1, 0.18, 0.24, 0.21, 0.12, 0.07, 0.03],
        },
      ],
      diagnostics: ['ffmpeg-scene provider threshold=0.32 minShotDurationMs=1600'],
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        visualEvidenceExtractionContractReady: true,
        visualEvidenceExtractionCommandReady: true,
        visualEvidenceExtractionAdapterReady: true,
        videoDedupFileIdentityCommandReady: true,
        supportedCommands: ['autocut_extract_visual_evidence', 'autocut_probe_video_file_identity'],
      }),
      probeVideoFileIdentity: async (request) => {
        nativeVideoDedupVisualIdentityRequests.push(request);
        return {
          sourcePath: request.sourcePath.replace('/autocut/media/', '/autocut-canonical/media/'),
          byteSize: request.sourcePath.endsWith('conference-keynote-original.mp4') ? 98_000_000 : 54_000_000,
          modifiedAtMs: nativeVideoDedupVisualRevisionByPath.get(request.sourcePath) ?? 0,
          fileIdentityVersion: '2026-05-15.video-file-identity.v1',
        };
      },
      extractVisualEvidence: async (request) => {
        nativeVideoDedupVisualEvidenceRequests.push(request);
        return createNativeVideoDedupVisualEvidence(request.assetUuid);
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-visual-dedup-source',
        name: 'conference-keynote-original.mp4',
        type: 'video',
        size: 98_000_000,
        artifactPath: 'D:/autocut/media/imports/conference-keynote-original.mp4',
        createdAt: '2026-05-05T00:10:00.000Z',
        updatedAt: '2026-05-05T00:10:00.000Z',
      });
      await services.addAsset({
        id: 'native-visual-dedup-target',
        name: '社媒发布版-完全改名.mov',
        type: 'video',
        size: 54_000_000,
        artifactPath: 'D:/autocut/media/imports/social-publish-remux.mov',
        createdAt: '2026-05-05T00:11:00.000Z',
        updatedAt: '2026-05-05T00:11:00.000Z',
      });
    });
    const nativeVisualVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-visual-dedup-source'],
        referenceAssetIds: ['native-visual-dedup-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeVideoDedupVisualEvidenceRequests.map((request) => ({
        assetUuid: request.assetUuid,
        visualEvidenceProfile: request.visualEvidenceProfile,
        includeFrameQuality: request.includeFrameQuality,
      })),
      [
        {
          assetUuid: 'native-visual-dedup-source',
          visualEvidenceProfile: 'scene-index-v1',
          includeFrameQuality: true,
        },
        {
          assetUuid: 'native-visual-dedup-target',
          visualEvidenceProfile: 'scene-index-v1',
          includeFrameQuality: true,
        },
      ],
      'video dedup visual-fingerprint extracts native visual evidence for each selected asset',
    );
    assertEqual(
      nativeVisualVideoDedupReport.matches[0]?.matchKind,
      'near-duplicate',
      'video dedup visual-fingerprint uses native scene evidence to match renamed and resized videos without metadata-token overlap',
    );
    assertRule(
      nativeVisualVideoDedupReport.matches[0]?.evidence?.[0]?.detail?.includes('Native visual evidence'),
      'video dedup visual-fingerprint reports native visual evidence details for human review',
    );
    assertEqual(
      nativeVideoDedupVisualIdentityRequests.length,
      2,
      'video dedup visual-fingerprint records lightweight native file identity when caching visual evidence',
    );

    const cachedNativeVisualVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-visual-dedup-source'],
        referenceAssetIds: ['native-visual-dedup-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertEqual(
      nativeVideoDedupVisualEvidenceRequests.length,
      2,
      'video dedup visual-fingerprint reuses cached native visual evidence on repeated analysis of unchanged artifacts',
    );
    assertEqual(
      nativeVideoDedupVisualIdentityRequests.length,
      4,
      'video dedup visual-fingerprint validates cached visual evidence with lightweight native file identity probes',
    );
    assertDeepEqual(
      nativeVideoDedupVisualIdentityRequests.slice(-2).map((request) => request.sourcePath),
      [
        'D:/autocut/media/imports/conference-keynote-original.mp4',
        'D:/autocut/media/imports/social-publish-remux.mov',
      ],
      'video dedup visual-fingerprint probes cached local artifact identity without re-extracting visual evidence',
    );
    assertEqual(
      cachedNativeVisualVideoDedupReport.matches[0]?.matchKind,
      'near-duplicate',
      'video dedup visual-fingerprint keeps visual duplicate results when native evidence is served from cache',
    );

    nativeVideoDedupVisualRevisionByPath.set('D:/autocut/media/imports/social-publish-remux.mov', 3000);
    await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-visual-dedup-source'],
        referenceAssetIds: ['native-visual-dedup-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertEqual(
      nativeVideoDedupVisualEvidenceRequests.length,
      3,
      'video dedup visual-fingerprint invalidates only changed visual evidence cache entry after native identity changes',
    );
    assertEqual(
      nativeVideoDedupVisualIdentityRequests.length,
      7,
      'video dedup visual-fingerprint keeps cache validation lightweight even when one cached visual evidence identity changes',
    );
    assertDeepEqual(
      nativeVideoDedupVisualEvidenceRequests.map((request) => request.sourcePath),
      [
        'D:/autocut/media/imports/conference-keynote-original.mp4',
        'D:/autocut/media/imports/social-publish-remux.mov',
        'D:/autocut/media/imports/social-publish-remux.mov',
      ],
      'video dedup visual-fingerprint does not re-extract unchanged local artifact visual evidence after identity cache invalidation',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoDedupStructureOnlyVisualEvidenceRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        visualEvidenceExtractionContractReady: true,
        visualEvidenceExtractionCommandReady: true,
        visualEvidenceExtractionAdapterReady: true,
        supportedCommands: ['autocut_extract_visual_evidence'],
      }),
      extractVisualEvidence: async (request) => {
        nativeVideoDedupStructureOnlyVisualEvidenceRequests.push(request);
        const { frameFingerprints, ...structureOnlyEvidence } = createNativeVideoDedupVisualEvidence(request.assetUuid);
        return structureOnlyEvidence;
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-visual-structure-source',
        name: 'talking-head-lesson.mp4',
        type: 'video',
        size: 72_000_000,
        artifactPath: 'D:/autocut/media/imports/talking-head-lesson.mp4',
        createdAt: '2026-05-05T00:10:20.000Z',
        updatedAt: '2026-05-05T00:10:20.000Z',
      });
      await services.addAsset({
        id: 'native-visual-structure-target',
        name: 'city-broll-same-cuts.mov',
        type: 'video',
        size: 71_000_000,
        artifactPath: 'D:/autocut/media/imports/city-broll-same-cuts.mov',
        createdAt: '2026-05-05T00:10:30.000Z',
        updatedAt: '2026-05-05T00:10:30.000Z',
      });
    });
    const structureOnlyVisualDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-visual-structure-source'],
        referenceAssetIds: ['native-visual-structure-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeVideoDedupStructureOnlyVisualEvidenceRequests.map((request) => ({
        assetUuid: request.assetUuid,
        includeFrameFingerprint: request.includeFrameFingerprint,
      })),
      [
        {
          assetUuid: 'native-visual-structure-source',
          includeFrameFingerprint: true,
        },
        {
          assetUuid: 'native-visual-structure-target',
          includeFrameFingerprint: true,
        },
      ],
      'video dedup visual-fingerprint requests perceptual frame fingerprints even when native provider returns structure-only evidence',
    );
    assertEqual(
      structureOnlyVisualDedupReport.matches.length,
      0,
      'video dedup visual-fingerprint does not auto-match structure-only native visual evidence without perceptual frame content',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoDedupDifferentContentRequests = [];
    const createNativeVideoDedupVisualEvidenceWithContent = (assetUuid, frameHashes) => ({
      ...createNativeVideoDedupVisualEvidence(assetUuid),
      frameFingerprints: frameHashes.map((hash, index) => ({
        atMs: [0, 6200, 15200][index] ?? index * 6200,
        algorithm: 'ahash-8x8-luma-v1',
        hash,
        meanLuma: index % 2 === 0 ? 0.24 : 0.76,
        histogram: index % 2 === 0
          ? [0.78, 0.12, 0.04, 0.02, 0.02, 0.01, 0.01, 0]
          : [0, 0.01, 0.01, 0.02, 0.02, 0.04, 0.12, 0.78],
      })),
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        visualEvidenceExtractionContractReady: true,
        visualEvidenceExtractionCommandReady: true,
        visualEvidenceExtractionAdapterReady: true,
        supportedCommands: ['autocut_extract_visual_evidence'],
      }),
      extractVisualEvidence: async (request) => {
        nativeVideoDedupDifferentContentRequests.push(request);
        return createNativeVideoDedupVisualEvidenceWithContent(
          request.assetUuid,
          request.assetUuid === 'native-visual-content-source'
            ? ['0000000000000000', '00ff00ff00ff00ff', '0f0f0f0f0f0f0f0f']
            : ['ffffffffffffffff', 'ff00ff00ff00ff00', 'f0f0f0f0f0f0f0f0'],
        );
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-visual-content-source',
        name: 'training-room-wide.mp4',
        type: 'video',
        size: 88_000_000,
        artifactPath: 'D:/autocut/media/imports/training-room-wide.mp4',
        createdAt: '2026-05-05T00:11:10.000Z',
        updatedAt: '2026-05-05T00:11:10.000Z',
      });
      await services.addAsset({
        id: 'native-visual-content-target',
        name: 'product-broll-same-tempo.mov',
        type: 'video',
        size: 87_000_000,
        artifactPath: 'D:/autocut/media/imports/product-broll-same-tempo.mov',
        createdAt: '2026-05-05T00:11:20.000Z',
        updatedAt: '2026-05-05T00:11:20.000Z',
      });
    });
    const differentContentVisualDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-visual-content-source'],
        referenceAssetIds: ['native-visual-content-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeVideoDedupDifferentContentRequests.map((request) => ({
        assetUuid: request.assetUuid,
        includeFrameFingerprint: request.includeFrameFingerprint,
      })),
      [
        {
          assetUuid: 'native-visual-content-source',
          includeFrameFingerprint: true,
        },
        {
          assetUuid: 'native-visual-content-target',
          includeFrameFingerprint: true,
        },
      ],
      'video dedup visual-fingerprint requests native perceptual frame fingerprints for picture-content evidence',
    );
    assertEqual(
      differentContentVisualDedupReport.matches.length,
      0,
      'video dedup visual-fingerprint does not mark different picture content as duplicate just because scene timing matches',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoDedupAudioFingerprintRequests = [];
    const createNativeVideoDedupAudioFingerprint = (assetUuid, hash = '5f'.repeat(32), energyBuckets = [8, 12, 26, 64, 128, 154, 92, 40]) => ({
      taskUuid: `audio-fingerprint-${assetUuid}`,
      sourceAssetUuid: assetUuid,
      provider: 'ffmpeg-audio',
      profile: 'audio-energy-v1',
      ready: true,
      durationMs: 24_000,
      sampleRateHz: 16_000,
      windowDurationMs: 1_000,
      fingerprint: {
        algorithm: 'audio-energy-v1',
        hash,
        energyBuckets,
        silenceRatio: 0.08,
        spectralCentroidBuckets: [12, 18, 28, 46, 55, 50, 34, 20],
      },
      diagnostics: ['ffmpeg-audio provider sampleRateHz=16000 windowDurationMs=1000'],
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        audioFingerprintCommandReady: true,
        audioFingerprintAdapterReady: true,
        supportedCommands: ['autocut_extract_audio_fingerprint'],
      }),
      fingerprintAudio: async (request) => {
        nativeVideoDedupAudioFingerprintRequests.push(request);
        return createNativeVideoDedupAudioFingerprint(request.assetUuid);
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-audio-dedup-source',
        name: 'speaker-session-alpha.mp4',
        type: 'video',
        size: 41_000_000,
        artifactPath: 'D:/autocut/media/imports/speaker-session-alpha.mp4',
        createdAt: '2026-05-05T00:11:30.000Z',
        updatedAt: '2026-05-05T00:11:30.000Z',
      });
      await services.addAsset({
        id: 'native-audio-dedup-target',
        name: 'random-camera-roll.mov',
        type: 'video',
        size: 64_000_000,
        artifactPath: 'D:/autocut/media/imports/random-camera-roll.mov',
        createdAt: '2026-05-05T00:11:40.000Z',
        updatedAt: '2026-05-05T00:11:40.000Z',
      });
    });
    const nativeAudioVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-audio-dedup-source'],
        referenceAssetIds: ['native-audio-dedup-target'],
        strategies: ['audio-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeVideoDedupAudioFingerprintRequests.map((request) => ({
        assetUuid: request.assetUuid,
        sourcePath: request.sourcePath,
        fingerprintProfile: request.fingerprintProfile,
        sampleRateHz: request.sampleRateHz,
        windowDurationMs: request.windowDurationMs,
      })),
      [
        {
          assetUuid: 'native-audio-dedup-source',
          sourcePath: 'D:/autocut/media/imports/speaker-session-alpha.mp4',
          fingerprintProfile: 'audio-energy-v1',
          sampleRateHz: 16_000,
          windowDurationMs: 1_000,
        },
        {
          assetUuid: 'native-audio-dedup-target',
          sourcePath: 'D:/autocut/media/imports/random-camera-roll.mov',
          fingerprintProfile: 'audio-energy-v1',
          sampleRateHz: 16_000,
          windowDurationMs: 1_000,
        },
      ],
      'video dedup audio-fingerprint extracts native audio fingerprints for each selected asset',
    );
    assertEqual(
      nativeAudioVideoDedupReport.matches[0]?.matchKind,
      'same-audio',
      'video dedup audio-fingerprint uses native audio evidence to match renamed videos without metadata-token overlap',
    );
    assertRule(
      nativeAudioVideoDedupReport.matches[0]?.evidence?.[0]?.detail?.includes('Native audio fingerprint'),
      'video dedup audio-fingerprint reports native audio evidence details for human review',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeVideoDedupDifferentAudioRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        audioFingerprintCommandReady: true,
        audioFingerprintAdapterReady: true,
        supportedCommands: ['autocut_extract_audio_fingerprint'],
      }),
      fingerprintAudio: async (request) => {
        nativeVideoDedupDifferentAudioRequests.push(request);
        return request.assetUuid === 'native-audio-content-source'
          ? createNativeVideoDedupAudioFingerprint(request.assetUuid, '00'.repeat(32), [4, 8, 12, 16, 20, 24, 28, 32])
          : createNativeVideoDedupAudioFingerprint(request.assetUuid, 'ff'.repeat(32), [240, 220, 200, 180, 160, 140, 120, 100]);
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'native-audio-content-source',
        name: 'weekly-lesson-final.mp4',
        type: 'video',
        size: 51_000_000,
        artifactPath: 'D:/autocut/media/imports/weekly-lesson-final.mp4',
        createdAt: '2026-05-05T00:11:50.000Z',
        updatedAt: '2026-05-05T00:11:50.000Z',
      });
      await services.addAsset({
        id: 'native-audio-content-target',
        name: 'weekly-lesson-copy.mov',
        type: 'video',
        size: 50_000_000,
        artifactPath: 'D:/autocut/media/imports/weekly-lesson-copy.mov',
        createdAt: '2026-05-05T00:12:00.000Z',
        updatedAt: '2026-05-05T00:12:00.000Z',
      });
    });
    const differentAudioVideoDedupReport = await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['native-audio-content-source'],
        referenceAssetIds: ['native-audio-content-target'],
        strategies: ['audio-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeVideoDedupDifferentAudioRequests.map((request) => ({
        assetUuid: request.assetUuid,
        sourcePath: request.sourcePath,
      })),
      [
        {
          assetUuid: 'native-audio-content-source',
          sourcePath: 'D:/autocut/media/imports/weekly-lesson-final.mp4',
        },
        {
          assetUuid: 'native-audio-content-target',
          sourcePath: 'D:/autocut/media/imports/weekly-lesson-copy.mov',
        },
      ],
      'video dedup audio-fingerprint passes local artifactPath to native audio fingerprint extraction for asset-library files',
    );
    assertEqual(
      differentAudioVideoDedupReport.matches.length,
      0,
      'video dedup audio-fingerprint does not fall back to metadata-token proxy when native audio fingerprints prove different audio',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const nativeArtifactPathVideoDedupVisualEvidenceRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        visualEvidenceExtractionContractReady: true,
        visualEvidenceExtractionCommandReady: true,
        visualEvidenceExtractionAdapterReady: true,
        supportedCommands: ['autocut_extract_visual_evidence'],
      }),
      extractVisualEvidence: async (request) => {
        nativeArtifactPathVideoDedupVisualEvidenceRequests.push(request);
        return createNativeVideoDedupVisualEvidence(request.assetUuid);
      },
    });
    await withImmediateTimers(async () => {
      await services.addAsset({
        id: 'artifact-path-visual-dedup-source',
        name: 'camera-export-01.mp4',
        type: 'video',
        size: 123_000_000,
        artifactPath: 'D:/autocut/library/camera-export-01.mp4',
        createdAt: '2026-05-05T00:12:00.000Z',
        updatedAt: '2026-05-05T00:12:00.000Z',
      });
      await services.addAsset({
        id: 'artifact-path-visual-dedup-target',
        name: 'publish-copy-random-title.mov',
        type: 'video',
        size: 66_000_000,
        artifactPath: 'D:/autocut/library/publish-copy-random-title.mov',
        createdAt: '2026-05-05T00:13:00.000Z',
        updatedAt: '2026-05-05T00:13:00.000Z',
      });
    });
    await withImmediateTimers(() =>
      services.analyzeAutoCutVideoDedup({
        mode: 'deep-audit',
        sourceAssetIds: ['artifact-path-visual-dedup-source'],
        referenceAssetIds: ['artifact-path-visual-dedup-target'],
        strategies: ['visual-fingerprint'],
        sensitivity: 'balanced',
        minMatchDurationMs: 8000,
        ignoreIntroOutro: false,
        introOutroMaxDurationMs: 0,
        actionMode: 'review-before-action',
      })
    );
    assertDeepEqual(
      nativeArtifactPathVideoDedupVisualEvidenceRequests.map((request) => ({
        assetUuid: request.assetUuid,
        sourcePath: request.sourcePath,
      })),
      [
        {
          assetUuid: 'artifact-path-visual-dedup-source',
          sourcePath: 'D:/autocut/library/camera-export-01.mp4',
        },
        {
          assetUuid: 'artifact-path-visual-dedup-target',
          sourcePath: 'D:/autocut/library/publish-copy-random-title.mov',
        },
      ],
      'video dedup visual-fingerprint passes local artifactPath to native visual evidence extraction for asset-library files',
    );
    services.resetAutoCutNativeHostClient();

    resetStorage();
    const assetAddedEvents = captureEvents(services, 'assetAdded');
    const assetDeletedEvents = captureEvents(services, 'assetDeleted');
    const storageBeforeAssetMutation = await services.getStorageInfo();
    const { importedAsset, folder } = await withImmediateTimers(async () => {
      const createdAsset = await services.importAssetFile(new File(['video'], 'clip.mp4', { type: 'video/mp4' }));
      const createdFolder = await services.createAssetFolder('Contract Folder');
      await services.deleteAsset(createdAsset.id);
      return {
        importedAsset: createdAsset,
        folder: createdFolder,
      };
    });
    const storageAfterAssetMutation = await services.getStorageInfo();
    assetAddedEvents.stop();
    assetDeletedEvents.stop();
    const storedAssets = readScopedStoredArray(services, 'assets');
    const expectedInitialAssetUsage = (await services.getAssets()).reduce((total, asset) => total + asset.size, 0);
    const expectedStoredAssetUsage = storedAssets.reduce((total, asset) => total + asset.size, 0);
    assertEqual(storageBeforeAssetMutation.used, expectedInitialAssetUsage, 'getStorageInfo derives initial used bytes from persisted assets');
    assertEqual(storageAfterAssetMutation.used, expectedStoredAssetUsage, 'getStorageInfo reflects asset import, folder creation, and deletion');
    assertEqual(importedAsset.type, 'video', 'importAssetFile infers video assets from the File MIME type');
    assertRule(importedAsset.url?.startsWith('blob:autocut-contract-'), 'importAssetFile creates a preview object URL');
    assertEqual(folder.type, 'folder', 'createAssetFolder persists a folder asset type');
    assertIncludes(
      assetAddedEvents.details.map((asset) => asset.id),
      importedAsset.id,
      'importAssetFile dispatches assetAdded',
    );
    assertIncludes(
      assetAddedEvents.details.map((asset) => asset.id),
      folder.id,
      'createAssetFolder dispatches assetAdded',
    );
    assertEqual(assetDeletedEvents.details[0]?.id, importedAsset.id, 'deleteAsset dispatches assetDeleted');
    assertIncludes(revokedObjectUrls, importedAsset.url, 'deleteAsset revokes imported asset object URLs');
    assertRule(
      storedAssets.some((asset) => asset.id === folder.id) && storedAssets.every((asset) => asset.id !== importedAsset.id),
      'asset mutations are persisted after import, folder creation, and delete',
    );

    resetStorage();
    const emptyMessages = await services.getMessages();
    assertEqual(emptyMessages.length, 0, 'getMessages does not seed message center data from mock/default messages');

    resetStorage();
    const messageAddedEvents = captureEvents(services, 'messageAdded');
    const messagesUpdatedEvents = captureEvents(services, 'messagesUpdated');
    const message = {
      id: 'contract-message',
      type: 'info',
      title: 'Contract Message',
      description: 'Service behavior contract',
      createdAt: '2026-05-05T00:00:00.000Z',
      read: false,
    };
    await withImmediateTimers(async () => {
      await services.addMessage(message);
      await services.updateMessageRead(message.id, true);
      await services.markAllMessagesRead();
      await services.clearReadMessages();
    });
    messageAddedEvents.stop();
    messagesUpdatedEvents.stop();
    const storedMessages = readScopedStoredArray(services, 'messages');
    assertEqual(messageAddedEvents.details[0]?.id, message.id, 'addMessage dispatches messageAdded');
    assertRule(messagesUpdatedEvents.details.length >= 3, 'message read workflows dispatch messagesUpdated');
    assertRule(storedMessages.every((storedMessage) => storedMessage.id !== message.id), 'clearReadMessages removes read messages from storage');

    resetStorage();
    let settingsNativeLlmSecretValue;
    const settingsNativeSecretCommands = [];
    const createSettingsNativeHostClient = () => services.createAutoCutNativeHostClient(async (command, args) => {
      settingsNativeSecretCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'settings-secret-contract-test',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          localDirectorySelectCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionFileSelectCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: [
            'autocut_save_llm_secret',
            'autocut_get_llm_secret',
            'autocut_delete_llm_secret',
            'autocut_probe_speech_transcription',
          ],
        };
      }
      if (command === 'autocut_save_llm_secret') {
        settingsNativeLlmSecretValue = args.request.secretValue;
        return {
          secretName: args.request.secretName,
          saved: true,
        };
      }
      if (command === 'autocut_get_llm_secret') {
        return {
          secretName: args.request.secretName,
          configured: Boolean(settingsNativeLlmSecretValue),
          ...(settingsNativeLlmSecretValue ? { secretValue: settingsNativeLlmSecretValue } : {}),
        };
      }
      if (command === 'autocut_delete_llm_secret') {
        const deleted = Boolean(settingsNativeLlmSecretValue);
        settingsNativeLlmSecretValue = undefined;
        return {
          secretName: args.request.secretName,
          deleted,
        };
      }
      if (command === 'autocut_probe_speech_transcription') {
        return {
          ready: Boolean(args.request.executablePath && args.request.modelPath),
          executablePath: args.request.executablePath,
          modelPath: args.request.modelPath,
          sourceKind: 'settings',
          diagnostics: [],
          versionLine: 'whisper.cpp contract',
        };
      }

      throw new Error(`Unexpected settings native host command: ${command}`);
    });
    configureAutoCutNativeHostClient(services, createSettingsNativeHostClient());
    const settingsEvents = captureEvents(services, 'settingsUpdated');
    const defaultSettings = await services.getAutoCutSettings();
    assertEqual(defaultSettings.llm.modelVendor, 'deepseek', 'AutoCut LLM settings default to DeepSeek');
    assertEqual(defaultSettings.llm.baseUrl, 'https://api.deepseek.com', 'AutoCut LLM settings default to DeepSeek OpenAI-compatible base URL');
    assertEqual(defaultSettings.llm.model, 'deepseek-v4-flash', 'AutoCut LLM settings default to the DeepSeek chat model');
    assertEqual(defaultSettings.llm.maxTokens, 8192, 'AutoCut LLM settings default to the DeepSeek V4 Flash recommended output budget');
    assertEqual(defaultSettings.workspace.language, 'zh-CN', 'AutoCut workspace language defaults to the canonical zh-CN application locale');
    assertEqual(services.normalizeAutoCutLocale('zh'), 'zh-CN', 'AutoCut i18n normalizes legacy zh locale aliases to zh-CN');
    assertEqual(services.normalizeAutoCutLocale('en'), 'en-US', 'AutoCut i18n normalizes legacy en locale aliases to en-US');
    assertEqual(defaultSettings.speechTranscription.executablePath, '', 'AutoCut local speech-to-text executable path defaults to empty');
    assertEqual(defaultSettings.speechTranscription.modelPath, '', 'AutoCut local speech-to-text model path defaults to empty');
    assertEqual(defaultSettings.speechTranscription.language, 'auto', 'AutoCut local speech-to-text language defaults to auto');
    assertEqual(defaultSettings.speechTranscription.configured, false, 'AutoCut local speech-to-text defaults to not configured');
    assertEqual(
      defaultSettings.speechTranscription.providerId,
      'openai-transcription',
      'AutoCut speech-to-text provider defaults to the commercial cloud STT provider id',
    );
    assertRule(
      Array.isArray(types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS),
      'AutoCut speech-to-text provider definitions are exposed as a canonical registry',
    );
    assertRule(
      types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS?.some((provider) => provider.id === 'local-whisper-cli' && provider.kind === 'local'),
      'AutoCut speech-to-text provider registry includes local Whisper CLI as a local plugin provider',
    );
    const localSpeechProviderIds = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS
      ?.filter((provider) => provider.kind === 'local')
      .map((provider) => provider.id) ?? [];
    assertDeepEqual(
      localSpeechProviderIds,
      ['local-whisper-cli'],
      'AutoCut exposes only local speech-to-text providers that the native runtime can execute end-to-end',
    );
    assertRule(
      types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS?.some((provider) => provider.id === 'openai-transcription' && provider.kind === 'api'),
      'AutoCut speech-to-text provider registry includes OpenAI transcription as an API plugin provider',
    );
    assertRule(
      Array.isArray(types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_MODEL_PRESETS),
      'AutoCut exposes a canonical local speech-to-text model acquisition preset registry',
    );
    assertRule(
      !('AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_EXECUTABLE_PRESETS' in types) &&
        !('AUTOCUT_DEFAULT_LOCAL_SPEECH_TRANSCRIPTION_EXECUTABLE_PRESET_ID' in types),
      'AutoCut does not expose a runtime whisper-cli executable download preset registry because whisper-cli is packaged as an installer sidecar',
    );
    assertRule(
      Array.isArray(types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_EXECUTABLE_PLATFORMS) &&
        ['windows-x86_64', 'linux-x86_64', 'macos-x86_64', 'macos-aarch64'].every((platform) =>
          types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_EXECUTABLE_PLATFORMS.includes(platform),
        ),
      'AutoCut keeps the canonical packaged whisper-cli sidecar platform keys for Windows, Ubuntu/Linux, and both macOS architectures',
    );
    const whisperCppLargeTurboPreset = types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_MODEL_PRESETS?.find(
      (preset) => preset.id === 'whisper-cpp-large-v3-turbo-q5',
    );
    assertEqual(
      types.AUTOCUT_DEFAULT_LOCAL_SPEECH_TRANSCRIPTION_MODEL_PRESET_ID,
      'whisper-cpp-large-v3-turbo-q5',
      'AutoCut speech-to-text defaults to the recommended offline Whisper large-v3-turbo Q5 model preset',
    );
    assertEqual(
      whisperCppLargeTurboPreset?.providerId,
      'local-whisper-cli',
      'local speech model preset registry targets the default local Whisper CLI provider',
    );
    assertEqual(
      whisperCppLargeTurboPreset?.recommended,
      true,
      'local speech model preset registry marks one balanced local Whisper model as recommended',
    );
    assertRule(
      whisperCppLargeTurboPreset?.url?.startsWith('https://huggingface.co/'),
      'local speech model preset registry uses official HTTPS model acquisition URLs',
    );
    assertRule(
      whisperCppLargeTurboPreset?.mirrorUrls?.some((url) => url.startsWith('https://hf-mirror.com/')),
      'local speech model preset registry includes a vetted Hugging Face mirror URL for resilient model downloads',
    );
    assertRule(
      /^[a-f0-9]{64}$/u.test(whisperCppLargeTurboPreset?.sha256 ?? ''),
      'local speech model preset registry pins a SHA-256 model digest before native download is allowed',
    );
    assertEqual(
      whisperCppLargeTurboPreset?.sha256,
      '394221709cd5ad1f40c46e6031ca61bce88931e6e088c188294c6d5a55ffa7e2',
      'local speech model preset registry pins the verified Hugging Face LFS digest for ggml-large-v3-turbo-q5_0.bin',
    );
    assertEqual(
      whisperCppLargeTurboPreset?.fileName.endsWith('.bin') || whisperCppLargeTurboPreset?.fileName.endsWith('.gguf'),
      true,
      'local speech model preset registry only advertises model files compatible with the accepted local extensions',
    );
    assertEqual(
      whisperCppLargeTurboPreset?.sizeLabel,
      '547 MiB',
      'local speech model preset registry shows the official whisper.cpp disk size for the default large-v3-turbo Q5 model',
    );
    const localProviderPresetCounts = Object.fromEntries(localSpeechProviderIds.map((providerId) => [
      providerId,
      types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_MODEL_PRESETS?.filter((preset) => preset.providerId === providerId).length ?? 0,
    ]));
    assertRule(
      Object.values(localProviderPresetCounts).every((count) => count > 0),
      `local speech model preset registry covers every executable local provider (got ${JSON.stringify(localProviderPresetCounts)})`,
    );
    const localSpeechModelPresets = services.getAutoCutLocalSpeechTranscriptionModelPresets('local-whisper-cli');
    assertDeepEqual(
      localSpeechModelPresets.map((preset) => preset.id),
      types.AUTOCUT_LOCAL_SPEECH_TRANSCRIPTION_MODEL_PRESETS
        ?.filter((preset) => preset.providerId === 'local-whisper-cli')
        .map((preset) => preset.id),
      'speech-transcription.service.ts exposes validated local STT model presets through the service boundary',
    );
    assertEqual(
      localSpeechModelPresets[0]?.recommended,
      true,
      'getAutoCutLocalSpeechTranscriptionModelPresets keeps the recommended local STT model first for guided setup',
    );
    assertEqual(
      localSpeechModelPresets.every((preset) => preset.providerId === 'local-whisper-cli'),
      true,
      'getAutoCutLocalSpeechTranscriptionModelPresets only returns presets for the requested local provider',
    );
    const resolvedLocalSpeechModelPreset = services.resolveAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    assertEqual(
      resolvedLocalSpeechModelPreset.fileName,
      'ggml-large-v3-turbo-q5_0.bin',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset returns the vetted download file name',
    );
    assertEqual(
      resolvedLocalSpeechModelPreset.url.endsWith('/ggml-large-v3-turbo-q5_0.bin'),
      true,
      'resolveAutoCutLocalSpeechTranscriptionModelPreset keeps the vetted URL aligned with fileName',
    );
    const resolvedLocalSpeechModelMirrorUrl = resolvedLocalSpeechModelPreset.mirrorUrls?.[0];
    assertRule(
      resolvedLocalSpeechModelMirrorUrl?.endsWith('/ggml-large-v3-turbo-q5_0.bin'),
      'resolveAutoCutLocalSpeechTranscriptionModelPreset keeps vetted mirror URLs aligned with fileName',
    );
    assertEqual(
      services.resolveAutoCutRecommendedLocalSpeechTranscriptionModelPreset('local-whisper-cli')?.id,
      'whisper-cpp-large-v3-turbo-q5',
      'resolveAutoCutRecommendedLocalSpeechTranscriptionModelPreset returns the guided offline Whisper default for local STT',
    );
    assertRule(
      Array.isArray(types.AUTOCUT_SPEECH_TRANSCRIPTION_WORKFLOW_PRESETS) &&
        types.AUTOCUT_SPEECH_TRANSCRIPTION_WORKFLOW_PRESETS.some((preset) =>
          preset.id === 'smart-slice-fast-preview' &&
          preset.executionProfile === 'fast-preview' &&
          preset.localWhisper?.chunkParallelism === 4 &&
          preset.localWhisper?.chunkSourceStrategy === 'source-direct' &&
          !Object.prototype.hasOwnProperty.call(preset.localWhisper?.decode ?? {}, 'audioContext') &&
          preset.localWhisper?.decode?.beamSize === 1 &&
          preset.localWhisper?.decode?.bestOf === 1 &&
          preset.localWhisper?.decode?.noFallback === true
        ) &&
        types.AUTOCUT_SPEECH_TRANSCRIPTION_WORKFLOW_PRESETS.some((preset) =>
          preset.id === 'smart-slice-quality-local' &&
          preset.executionProfile === 'quality' &&
          preset.localWhisper?.chunkSourceStrategy === 'audio-first' &&
          !preset.localWhisper?.decode
        ),
      'STT workflow preset registry defines product-facing Smart Slice fast-preview stable performance decode and quality-local strategies',
    );
    const balancedWorkflowPreset = services.resolveAutoCutSpeechTranscriptionWorkflowPreset('smart-slice-balanced-local');
    assertEqual(
      balancedWorkflowPreset.localWhisper?.chunkSourceStrategy,
      'source-direct',
      'resolveAutoCutSpeechTranscriptionWorkflowPreset uses source-direct chunking for balanced long-video local STT',
    );
    assertDeepEqual(
      balancedWorkflowPreset.localWhisper?.decode,
      {
        beamSize: 1,
        bestOf: 1,
        noFallback: true,
      },
      'resolveAutoCutSpeechTranscriptionWorkflowPreset exposes balanced local Whisper stable decode settings from measured CPU performance',
    );
    const fastPreviewWorkflowPreset = services.resolveAutoCutSpeechTranscriptionWorkflowPreset('smart-slice-fast-preview');
    assertEqual(
      fastPreviewWorkflowPreset.providerId,
      'local-whisper-cli',
      'resolveAutoCutSpeechTranscriptionWorkflowPreset resolves the fast Smart Slice preset to the local provider until API/GPU providers are explicitly configured',
    );
    assertEqual(
      fastPreviewWorkflowPreset.localWhisper?.chunkParallelism,
      4,
      'resolveAutoCutSpeechTranscriptionWorkflowPreset exposes native long-video Whisper chunk parallelism for the fast Smart Slice preset',
    );
    assertEqual(
      fastPreviewWorkflowPreset.localWhisper?.chunkSourceStrategy,
      'source-direct',
      'resolveAutoCutSpeechTranscriptionWorkflowPreset uses source-direct chunking for fast-preview long-video local STT',
    );
    assertDeepEqual(
      fastPreviewWorkflowPreset.localWhisper?.decode,
      {
        beamSize: 1,
        bestOf: 1,
        noFallback: true,
      },
      'resolveAutoCutSpeechTranscriptionWorkflowPreset exposes fast-preview stable local Whisper decode settings from measured CPU performance',
    );
    browserDownloadRequests.length = 0;
    services.downloadAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    assertDeepEqual(
      browserDownloadRequests.at(-1),
      {
        href: resolvedLocalSpeechModelPreset.url,
        download: resolvedLocalSpeechModelPreset.fileName,
      },
      'downloadAutoCutLocalSpeechTranscriptionModelPreset downloads only the vetted preset URL with the vetted fileName',
    );
    clipboardWrites.length = 0;
    await services.copyAutoCutLocalSpeechTranscriptionModelPresetUrl('whisper-cpp-large-v3-turbo-q5');
    assertEqual(
      clipboardWrites.at(-1),
      [resolvedLocalSpeechModelPreset.url, ...(resolvedLocalSpeechModelPreset.mirrorUrls ?? [])].join('\n'),
      'copyAutoCutLocalSpeechTranscriptionModelPresetUrl copies only vetted model acquisition URLs and never writes modelPath',
    );
    const invalidLocalSpeechModelProgressEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    await assertRejects(
      () => services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
        providerId: 'local-whisper-cli',
        presetId: 'whisper-cpp-large-v3-turbo-q5',
        fileName: 'ggml-large-v3-turbo-q5_0.bin',
        phase: 'corrupted-native-phase',
        downloadedBytes: 1,
      }),
      'unsupported local speech-to-text model download phase',
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress rejects unsupported native download phases before publishing events',
    );
    await assertRejects(
      () => services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
        providerId: 'local-whisper-cli',
        presetId: 'unregistered-local-speech-model',
        fileName: 'ggml-large-v3-turbo-q5_0.bin',
        phase: 'started',
        downloadedBytes: 0,
        sourceUrl: resolvedLocalSpeechModelPreset.url,
      }),
      'local speech-to-text model preset is not registered',
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress rejects unregistered native model preset ids before publishing events',
    );
    await assertRejects(
      () => services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
        providerId: 'openai-transcription',
        presetId: 'whisper-cpp-large-v3-turbo-q5',
        fileName: 'ggml-large-v3-turbo-q5_0.bin',
        phase: 'downloading',
        downloadedBytes: 1024,
        totalBytes: 4096,
        sourceUrl: resolvedLocalSpeechModelPreset.url,
      }),
      'model download progress did not match the registered local speech-to-text model preset',
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress rejects native provider ids that do not match the registered local preset before publishing events',
    );
    await assertRejects(
      () => services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
        providerId: 'local-whisper-cli',
        presetId: 'whisper-cpp-large-v3-turbo-q5',
        fileName: 'ggml-medium.bin',
        phase: 'completed',
        downloadedBytes: 573571072,
        totalBytes: 573571072,
        modelPath: 'D:/autocut/media/models/speech/ggml-medium.bin',
        sourceUrl: resolvedLocalSpeechModelPreset.url,
      }),
      'model download progress did not match the registered local speech-to-text model preset',
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress rejects native file names that do not match the registered local preset before publishing events',
    );
    await assertRejects(
      () => services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
        providerId: 'local-whisper-cli',
        presetId: 'whisper-cpp-large-v3-turbo-q5',
        fileName: 'ggml-large-v3-turbo-q5_0.bin',
        phase: 'started',
        downloadedBytes: 0,
        sourceUrl: 'https://example.com/ggml-large-v3-turbo-q5_0.bin',
      }),
      'model download progress did not match the registered local speech-to-text model preset',
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress rejects native source URLs that do not match the registered local preset before publishing events',
    );
    services.dispatchAutoCutSpeechTranscriptionModelDownloadProgress({
      providerId: 'local-whisper-cli',
      presetId: 'whisper-cpp-large-v3-turbo-q5',
      fileName: 'ggml-large-v3-turbo-q5_0.bin',
      phase: 'downloading',
      downloadedBytes: 1024,
      totalBytes: 4096,
      sourceUrl: resolvedLocalSpeechModelMirrorUrl,
    });
    assertEqual(
      invalidLocalSpeechModelProgressEvents.details.at(-1)?.sourceUrl,
      resolvedLocalSpeechModelMirrorUrl,
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress accepts vetted mirror source URLs and preserves the actual download source',
    );
    invalidLocalSpeechModelProgressEvents.stop();
    assertEqual(
      invalidLocalSpeechModelProgressEvents.details.length,
      1,
      'dispatchAutoCutSpeechTranscriptionModelDownloadProgress publishes only valid primary or vetted mirror download progress events',
    );
    await services.saveAutoCutWorkspaceSettings({
      ...defaultSettings.workspace,
      outputDirectory: 'D:/autocut/media',
    });
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...defaultSettings.speechTranscription,
      executablePath: 'D:/tools/whisper-cli.exe',
      modelPath: 'D:/models/old-local-whisper.bin',
      lastTestedAt: '2026-05-08T00:00:00.000Z',
      lastProbeReady: true,
      lastProbeDiagnostics: ['stale probe must be cleared after model auto setup'],
    });
    const nativeSpeechModelDownloadRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => {
        nativeSpeechModelDownloadRequests.push(request);
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    browserDownloadRequests.length = 0;
    const configuredLocalSpeechModel = await services.setupAutoCutLocalSpeechTranscriptionModelPreset();
    assertDeepEqual(
      nativeSpeechModelDownloadRequests[0],
      {
        providerId: 'local-whisper-cli',
        presetId: 'whisper-cpp-large-v3-turbo-q5',
        fileName: 'ggml-large-v3-turbo-q5_0.bin',
        url: resolvedLocalSpeechModelPreset.url,
        mirrorUrls: resolvedLocalSpeechModelPreset.mirrorUrls,
        sha256: resolvedLocalSpeechModelPreset.sha256,
        outputRootDir: 'D:/autocut/media',
      },
      'setupAutoCutLocalSpeechTranscriptionModelPreset asks native host to download only the vetted recommended local STT preset and mirror sources into the configured output root',
    );
    assertEqual(
      browserDownloadRequests.length,
      0,
      'setupAutoCutLocalSpeechTranscriptionModelPreset does not use browser download when native model download is available',
    );
    assertEqual(
      configuredLocalSpeechModel.preset.id,
      'whisper-cpp-large-v3-turbo-q5',
      'setupAutoCutLocalSpeechTranscriptionModelPreset returns the configured local STT model preset',
    );
    assertEqual(
      configuredLocalSpeechModel.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'setupAutoCutLocalSpeechTranscriptionModelPreset returns the native installed model path',
    );
    assertEqual(
      configuredLocalSpeechModel.settings.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'setupAutoCutLocalSpeechTranscriptionModelPreset persists the native installed model path for local STT',
    );
    assertEqual(
      configuredLocalSpeechModel.settings.speechTranscription.executablePath,
      'D:/tools/whisper-cli.exe',
      'setupAutoCutLocalSpeechTranscriptionModelPreset preserves the selected local STT executable path while replacing only the model',
    );
    assertEqual(
      configuredLocalSpeechModel.settings.speechTranscription.lastProbeReady,
      undefined,
      'setupAutoCutLocalSpeechTranscriptionModelPreset clears stale local STT probe readiness after changing modelPath',
    );
    assertEqual(
      configuredLocalSpeechModel.settings.speechTranscription.lastProbeDiagnostics,
      undefined,
      'setupAutoCutLocalSpeechTranscriptionModelPreset clears stale local STT probe diagnostics after changing modelPath',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...configuredLocalSpeechModel.settings.speechTranscription,
      modelPath: '',
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => ({
        providerId: request.providerId,
        presetId: request.presetId,
        fileName: request.fileName,
        modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
        byteSize: 573571072,
        downloaded: true,
        sourceUrl: request.url,
        sha256: ` ${request.sha256.toUpperCase()} `,
      }),
    });
    const normalizedChecksumLocalSpeechModel = await services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    assertEqual(
      normalizedChecksumLocalSpeechModel.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'setupAutoCutLocalSpeechTranscriptionModelPreset accepts native SHA-256 digests with harmless case and whitespace differences',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...normalizedChecksumLocalSpeechModel.settings.speechTranscription,
      modelPath: '',
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => ({
        providerId: request.providerId,
        presetId: request.presetId,
        fileName: request.fileName,
        modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
        byteSize: 573571072,
        downloaded: true,
        sourceUrl: request.url,
        sha256: request.sha256,
      }),
    });
    const existingLocalSpeechModel = await services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    assertEqual(
      existingLocalSpeechModel.downloaded,
      true,
      'setupAutoCutLocalSpeechTranscriptionModelPreset reports a verified existing native model as already downloaded',
    );
    assertEqual(
      existingLocalSpeechModel.settings.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'setupAutoCutLocalSpeechTranscriptionModelPreset saves the verified existing native modelPath',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...existingLocalSpeechModel.settings.speechTranscription,
      modelPath: '',
      lastProbeReady: true,
      lastProbeDiagnostics: ['stale probe must be cleared after UI-yielded setup'],
    });
    let nativeDownloadSawUiFrameYield = false;
    const originalRequestAnimationFrame = globalThis.requestAnimationFrame;
    const originalWindowRequestAnimationFrame = globalThis.window?.requestAnimationFrame;
    let restoreUiFrameYieldProbe = () => undefined;
    try {
      globalThis.requestAnimationFrame = (callback) => {
        nativeDownloadSawUiFrameYield = true;
        callback(0);
        return 1;
      };
      if (globalThis.window) {
        globalThis.window.requestAnimationFrame = globalThis.requestAnimationFrame;
      }
      restoreUiFrameYieldProbe = () => {
        if (originalRequestAnimationFrame === undefined) {
          delete globalThis.requestAnimationFrame;
        } else {
          globalThis.requestAnimationFrame = originalRequestAnimationFrame;
        }
        if (globalThis.window) {
          if (originalWindowRequestAnimationFrame === undefined) {
            delete globalThis.window.requestAnimationFrame;
          } else {
            globalThis.window.requestAnimationFrame = originalWindowRequestAnimationFrame;
          }
        }
      };
      configureAutoCutNativeHostClient(services, {
        getCapabilities: async () => ({
          speechTranscriptionModelDownloadCommandReady: true,
        }),
        downloadSpeechTranscriptionModel: async (request) => {
          assertRule(
            nativeDownloadSawUiFrameYield,
            'setupAutoCutLocalSpeechTranscriptionModelPreset yields one browser frame before starting the heavy native STT model download',
          );
          return {
            providerId: request.providerId,
            presetId: request.presetId,
            fileName: request.fileName,
            modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
            byteSize: 573571072,
            downloaded: true,
            sourceUrl: request.url,
            sha256: request.sha256,
          };
        },
      });
      await services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    } finally {
      restoreUiFrameYieldProbe();
    }
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...configuredLocalSpeechModel.settings.speechTranscription,
      modelPath: '',
      lastProbeReady: true,
      lastProbeDiagnostics: ['stale probe must be cleared after coalesced model setup'],
    });
    const concurrentNativeSpeechModelDownloadRequests = [];
    let releaseConcurrentNativeSpeechModelDownload = () => undefined;
    const concurrentNativeSpeechModelDownloadGate = new Promise((resolve) => {
      releaseConcurrentNativeSpeechModelDownload = resolve;
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => {
        concurrentNativeSpeechModelDownloadRequests.push(request);
        await concurrentNativeSpeechModelDownloadGate;
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const concurrentNativeSpeechModelSetupEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    const firstConcurrentNativeSpeechModelSetup = services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    await flushMicrotasks();
    const secondConcurrentNativeSpeechModelSetup = services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    await flushMicrotasks();
    releaseConcurrentNativeSpeechModelDownload();
    const [firstConcurrentNativeSpeechModelResult, secondConcurrentNativeSpeechModelResult] = await Promise.all([
      firstConcurrentNativeSpeechModelSetup,
      secondConcurrentNativeSpeechModelSetup,
    ]);
    concurrentNativeSpeechModelSetupEvents.stop();
    assertEqual(
      concurrentNativeSpeechModelDownloadRequests.length,
      1,
      'setupAutoCutLocalSpeechTranscriptionModelPreset coalesces concurrent setup requests for the same local STT model into one native download',
    );
    assertEqual(
      firstConcurrentNativeSpeechModelResult.modelPath,
      secondConcurrentNativeSpeechModelResult.modelPath,
      'coalesced local STT model setup callers receive the same verified native model path',
    );
    assertEqual(
      concurrentNativeSpeechModelSetupEvents.details.filter((event) => event.phase === 'started').length,
      1,
      'coalesced local STT model setup emits one visible native download start event',
    );
    assertEqual(
      concurrentNativeSpeechModelSetupEvents.details.filter((event) => event.phase === 'completed').length,
      1,
      'coalesced local STT model setup emits one visible native download completion event',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...firstConcurrentNativeSpeechModelResult.settings.speechTranscription,
      executablePath: 'D:/tools/old-whisper-cli.exe',
      modelPath: '',
      language: 'auto',
      lastProbeReady: true,
      lastProbeDiagnostics: ['stale probe must be cleared after preserving in-flight edits'],
    });
    const inFlightEditedSpeechModelDownloadRequests = [];
    let releaseInFlightEditedSpeechModelDownload = () => undefined;
    const inFlightEditedSpeechModelDownloadGate = new Promise((resolve) => {
      releaseInFlightEditedSpeechModelDownload = resolve;
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => {
        inFlightEditedSpeechModelDownloadRequests.push(request);
        await services.saveAutoCutSpeechTranscriptionSettings({
          ...firstConcurrentNativeSpeechModelResult.settings.speechTranscription,
          executablePath: 'D:/tools/new-whisper-cli.exe',
          modelPath: '',
          language: 'zh',
          lastProbeReady: true,
          lastProbeDiagnostics: ['edited while model download was still in flight'],
        });
        await inFlightEditedSpeechModelDownloadGate;
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const inFlightEditedSpeechModelSetup = services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    await flushMicrotasks();
    releaseInFlightEditedSpeechModelDownload();
    const inFlightEditedSpeechModelResult = await inFlightEditedSpeechModelSetup;
    assertEqual(
      inFlightEditedSpeechModelDownloadRequests.length,
      1,
      'setupAutoCutLocalSpeechTranscriptionModelPreset downloads once while preserving in-flight user edits',
    );
    assertEqual(
      inFlightEditedSpeechModelResult.settings.speechTranscription.executablePath,
      'D:/tools/new-whisper-cli.exe',
      'setupAutoCutLocalSpeechTranscriptionModelPreset preserves executablePath changes made while the native model download is in flight',
    );
    assertEqual(
      inFlightEditedSpeechModelResult.settings.speechTranscription.language,
      'zh',
      'setupAutoCutLocalSpeechTranscriptionModelPreset preserves language changes made while the native model download is in flight',
    );
    assertEqual(
      inFlightEditedSpeechModelResult.settings.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'setupAutoCutLocalSpeechTranscriptionModelPreset still persists the verified modelPath after preserving in-flight settings edits',
    );
    assertEqual(
      inFlightEditedSpeechModelResult.settings.speechTranscription.lastProbeReady,
      undefined,
      'setupAutoCutLocalSpeechTranscriptionModelPreset clears stale probe readiness after preserving in-flight settings edits',
    );
    assertEqual(
      inFlightEditedSpeechModelResult.settings.speechTranscription.lastProbeDiagnostics,
      undefined,
      'setupAutoCutLocalSpeechTranscriptionModelPreset clears stale probe diagnostics after preserving in-flight settings edits',
    );
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => ({
        providerId: request.providerId,
        presetId: request.presetId,
        fileName: request.fileName,
        modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
        byteSize: 1024,
        downloaded: true,
        sourceUrl: request.url,
        sha256: request.sha256,
      }),
    });
    await assertRejects(
      () => services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5'),
      'speech recognition model download did not finish',
      'setupAutoCutLocalSpeechTranscriptionModelPreset rejects incomplete native model downloads before saving modelPath',
    );
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionModelDownloadCommandReady: false,
      }),
    });
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...configuredLocalSpeechModel.settings.speechTranscription,
      modelPath: '',
    });
    browserDownloadRequests.length = 0;
    const fallbackLocalSpeechModel = await services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    assertDeepEqual(
      browserDownloadRequests.at(-1),
      {
        href: resolvedLocalSpeechModelPreset.url,
        download: resolvedLocalSpeechModelPreset.fileName,
      },
      'setupAutoCutLocalSpeechTranscriptionModelPreset falls back to the vetted browser download when native model download is unavailable',
    );
    assertEqual(
      fallbackLocalSpeechModel.settings.speechTranscription.modelPath,
      '',
      'setupAutoCutLocalSpeechTranscriptionModelPreset fallback never writes a remote URL or unverified browser download location into modelPath',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...fallbackLocalSpeechModel.settings.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const localSpeechSetupCommands = [];
    let localSpeechSetupModelInstalled = false;
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: localSpeechSetupModelInstalled,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      probeSpeechTranscription: async (request) => {
        localSpeechSetupCommands.push({ kind: 'probe', request });
        const executablePath = request.executablePath ?? 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe';
        return {
          ready: Boolean(executablePath && request.modelPath),
          executableReady: Boolean(executablePath),
          modelReady: Boolean(request.modelPath),
          executablePath,
          modelPath: request.modelPath ?? '',
          sourceKind: request.executablePath ? 'execution-preflight' : 'bundled-sidecar',
          diagnostics: request.modelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/common local whisper-cli',
          ...(request.modelPath ? { versionLine: 'setup status bundled whisper sidecar' } : {}),
        };
      },
      downloadSpeechTranscriptionModel: async (request) => {
        localSpeechSetupCommands.push({ kind: 'download-model', request });
        localSpeechSetupModelInstalled = true;
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const inspectedLocalSpeechSetup = await services.inspectAutoCutLocalSpeechTranscriptionSetup();
    assertEqual(
      inspectedLocalSpeechSetup.providerId,
      'local-whisper-cli',
      'inspectAutoCutLocalSpeechTranscriptionSetup reports the selected local STT provider',
    );
    assertDeepEqual(
      inspectedLocalSpeechSetup.localProviderIds,
      ['local-whisper-cli'],
      'inspectAutoCutLocalSpeechTranscriptionSetup checks every executable local STT provider registered in the canonical provider registry',
    );
    assertEqual(
      inspectedLocalSpeechSetup.executable.ready,
      true,
      'inspectAutoCutLocalSpeechTranscriptionSetup accepts a native-discovered packaged Whisper executable',
    );
    assertEqual(
      inspectedLocalSpeechSetup.executable.sourceKind,
      'bundled-sidecar',
      'inspectAutoCutLocalSpeechTranscriptionSetup exposes the local STT executable source for product UI',
    );
    assertEqual(
      inspectedLocalSpeechSetup.model.ready,
      false,
      'inspectAutoCutLocalSpeechTranscriptionSetup reports missing local STT model readiness separately from executable readiness',
    );
    assertEqual(
      inspectedLocalSpeechSetup.readiness,
      'needs-model',
      'inspectAutoCutLocalSpeechTranscriptionSetup recommends model initialization when the local executable exists but modelPath is missing',
    );
    assertEqual(
      inspectedLocalSpeechSetup.nextAction,
      'initialize',
      'inspectAutoCutLocalSpeechTranscriptionSetup exposes a primary initialize action for product UI',
    );
    assertEqual(
      inspectedLocalSpeechSetup.defaults.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'inspectAutoCutLocalSpeechTranscriptionSetup surfaces the native-resolved default STT model path for product UI',
    );
    assertEqual(
      inspectedLocalSpeechSetup.defaults.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'inspectAutoCutLocalSpeechTranscriptionSetup surfaces the native-resolved default STT executable install target for product UI',
    );
    assertRule(
      inspectedLocalSpeechSetup.defaults.executableStrategy.includes('PATH/common local whisper-cli'),
      'inspectAutoCutLocalSpeechTranscriptionSetup explains automatic local whisper-cli discovery through PATH and common install directories',
    );
    const localSpeechSetupProgressEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    const initializedLocalSpeechSetup = await services.initializeAutoCutLocalSpeechTranscriptionSetup();
    localSpeechSetupProgressEvents.stop();
    assertEqual(
      localSpeechSetupCommands[0]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup probes local STT executable discovery before model download',
    );
    assertEqual(
      localSpeechSetupCommands[1]?.kind,
      'download-model',
      'initializeAutoCutLocalSpeechTranscriptionSetup downloads the recommended local STT model after executable discovery succeeds',
    );
    assertEqual(
      localSpeechSetupCommands[2]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup probes the final executable plus model before reporting ready',
    );
    assertRule(
      localSpeechSetupProgressEvents.details.some((event) => event.phase === 'started'),
      'initializeAutoCutLocalSpeechTranscriptionSetup dispatches a visible local STT model download started progress event',
    );
    assertRule(
      localSpeechSetupProgressEvents.details.some((event) => event.phase === 'completed' && event.progress === 100),
      'initializeAutoCutLocalSpeechTranscriptionSetup dispatches a visible local STT model download completion progress event',
    );
    assertEqual(
      initializedLocalSpeechSetup.status.readiness,
      'ready',
      'initializeAutoCutLocalSpeechTranscriptionSetup returns ready status after local executable, model, and probe all verify',
    );
    assertEqual(
      initializedLocalSpeechSetup.status.capabilities.toolchainReady,
      true,
      'initializeAutoCutLocalSpeechTranscriptionSetup returns final refreshed local STT capability status after automatic model setup',
    );
    assertEqual(
      initializedLocalSpeechSetup.settings.speechTranscription.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'initializeAutoCutLocalSpeechTranscriptionSetup persists native-discovered packaged Whisper executablePath',
    );
    assertEqual(
      initializedLocalSpeechSetup.settings.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'initializeAutoCutLocalSpeechTranscriptionSetup persists the native-installed recommended modelPath',
    );
    assertEqual(
      initializedLocalSpeechSetup.settings.speechTranscription.lastProbeReady,
      true,
      'initializeAutoCutLocalSpeechTranscriptionSetup marks the local STT provider tested only after a fresh successful probe',
    );
    const refreshedSamePathModelCommandCount = localSpeechSetupCommands.length;
    await services.setupAutoCutLocalSpeechTranscriptionModelPreset('whisper-cpp-large-v3-turbo-q5');
    const initializedAfterSamePathModelRefresh = await services.initializeAutoCutLocalSpeechTranscriptionSetup();
    assertEqual(
      localSpeechSetupCommands[refreshedSamePathModelCommandCount]?.kind,
      'download-model',
      'setupAutoCutLocalSpeechTranscriptionModelPreset can refresh the recommended local STT model at the same path',
    );
    assertEqual(
      localSpeechSetupCommands[refreshedSamePathModelCommandCount + 1]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup re-probes after a same-path local STT model refresh instead of reusing stale ready cache',
    );
    assertEqual(
      initializedAfterSamePathModelRefresh.status.readiness,
      'ready',
      'initializeAutoCutLocalSpeechTranscriptionSetup reports ready only after revalidating the same-path refreshed local STT model',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...initializedAfterSamePathModelRefresh.settings.speechTranscription,
      modelPath: '',
    });
    const staleReadyCacheCommandCount = localSpeechSetupCommands.length;
    const repairedAfterClearedModelPath = await services.initializeAutoCutLocalSpeechTranscriptionSetup();
    assertEqual(
      localSpeechSetupCommands[staleReadyCacheCommandCount]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup does not reuse stale ready setup cache after modelPath is cleared',
    );
    assertEqual(
      localSpeechSetupCommands[staleReadyCacheCommandCount + 1]?.kind,
      'download-model',
      'initializeAutoCutLocalSpeechTranscriptionSetup reinitializes the recommended local STT model after modelPath is cleared',
    );
    assertEqual(
      repairedAfterClearedModelPath.settings.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'initializeAutoCutLocalSpeechTranscriptionSetup restores modelPath instead of reporting stale ready status',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...repairedAfterClearedModelPath.settings.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const blockedExecutableSetupCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
        speechTranscriptionExecutableDownloadCommandReady: true,
      }),
      probeSpeechTranscription: async (request) => {
        blockedExecutableSetupCommands.push({ kind: 'probe', request });
        const installedExecutablePath = request.executablePath ?? '';
        const installedModelPath = request.modelPath ?? '';
        return {
          ready: Boolean(installedExecutablePath && installedModelPath),
          executableReady: Boolean(installedExecutablePath),
          modelReady: Boolean(installedModelPath),
          executablePath: installedExecutablePath,
          modelPath: installedModelPath,
          sourceKind: installedExecutablePath ? 'bundled-sidecar' : 'missing',
          diagnostics: installedExecutablePath
            ? (installedModelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'])
            : ['AutoCut local speech transcription executablePath is not configured; AutoCut checked Settings, SDKWORK_AUTOCUT_WHISPER_EXECUTABLE, verified bundled sidecar, PATH, and common local installation directories.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
          ...(installedExecutablePath && installedModelPath ? { versionLine: 'whisper.cpp v1.8.4' } : {}),
        };
      },
      downloadSpeechTranscriptionExecutable: async (request) => {
        blockedExecutableSetupCommands.push({ kind: 'download-executable', request });
        throw new Error('AutoCut must not download whisper-cli at runtime.');
      },
      downloadSpeechTranscriptionModel: async (request) => {
        blockedExecutableSetupCommands.push({ kind: 'download-model', request });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const blockedExecutableModelEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    await assertRejects(
      () => services.initializeAutoCutLocalSpeechTranscriptionSetup(),
      'runtime download is disabled',
      'initializeAutoCutLocalSpeechTranscriptionSetup refuses to download whisper-cli even when a legacy native host advertises executable download support',
    );
    blockedExecutableModelEvents.stop();
    assertDeepEqual(
      blockedExecutableSetupCommands.map((command) => command.kind),
      ['probe', 'download-model', 'probe', 'probe'],
      'initializeAutoCutLocalSpeechTranscriptionSetup downloads only the verified model and never downloads whisper-cli at runtime',
    );
    assertRule(
      !('speechTranscriptionExecutableDownloadProgress' in services.AUTOCUT_EVENTS),
      'initializeAutoCutLocalSpeechTranscriptionSetup has no executable download progress event because whisper-cli must be packaged as a sidecar',
    );
    assertRule(
      blockedExecutableModelEvents.details.some((event) => event.phase === 'completed' && event.progress === 100),
      'initializeAutoCutLocalSpeechTranscriptionSetup keeps model download progress visible while executable packaging is still blocking readiness',
    );
    assertEqual(
      blockedExecutableSetupCommands.some((command) => command.kind === 'download-executable'),
      false,
      'initializeAutoCutLocalSpeechTranscriptionSetup never calls the native executable download command',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...(await services.getAutoCutSettings()).speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const staleMissingExecutableSetupCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
        speechTranscriptionExecutableDownloadCommandReady: false,
      }),
      probeSpeechTranscription: async (request) => {
        staleMissingExecutableSetupCommands.push({ kind: 'stale-probe', request });
        return {
          ready: false,
          executableReady: false,
          modelReady: Boolean(request.modelPath),
          executablePath: '',
          modelPath: request.modelPath ?? '',
          sourceKind: 'missing',
          diagnostics: ['AutoCut local speech transcription executablePath is not configured; AutoCut checked Settings, SDKWORK_AUTOCUT_WHISPER_EXECUTABLE, verified bundled sidecar, PATH, and common local installation directories.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
        };
      },
    });
    const staleMissingExecutableStatus = await services.inspectAutoCutLocalSpeechTranscriptionSetup();
    assertEqual(
      staleMissingExecutableStatus.capabilities.executableDownloadReady,
      false,
      'inspectAutoCutLocalSpeechTranscriptionSetup reports executable runtime download as disabled even when whisper-cli is missing',
    );
    const discoveredSidecarSetupCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
        speechTranscriptionExecutableDownloadCommandReady: false,
      }),
      probeSpeechTranscription: async (request) => {
        discoveredSidecarSetupCommands.push({ kind: 'probe', request });
        const installedExecutablePath = request.executablePath || 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe';
        const installedModelPath = request.modelPath ?? '';
        return {
          ready: Boolean(installedExecutablePath && installedModelPath),
          executableReady: Boolean(installedExecutablePath),
          modelReady: Boolean(installedModelPath),
          executablePath: installedExecutablePath,
          modelPath: installedModelPath,
          sourceKind: 'bundled-sidecar',
          diagnostics: installedExecutablePath
            ? (installedModelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'])
            : ['AutoCut local speech transcription executablePath is not configured; AutoCut checked Settings, SDKWORK_AUTOCUT_WHISPER_EXECUTABLE, verified bundled sidecar, PATH, and common local installation directories.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
          ...(installedExecutablePath && installedModelPath ? { versionLine: 'whisper.cpp v1.8.4' } : {}),
        };
      },
      downloadSpeechTranscriptionModel: async (request) => {
        discoveredSidecarSetupCommands.push({ kind: 'download-model', request });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const recoveredAfterStaleExecutableSetupStatus = await services.initializeAutoCutLocalSpeechTranscriptionSetup();
    assertDeepEqual(
      discoveredSidecarSetupCommands.map((command) => command.kind),
      ['probe', 'download-model', 'probe'],
      'initializeAutoCutLocalSpeechTranscriptionSetup re-inspects native capabilities instead of reusing a stale missing-executable setup cache before resolving the packaged whisper-cli sidecar',
    );
    assertEqual(
      recoveredAfterStaleExecutableSetupStatus.settings.speechTranscription.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'initializeAutoCutLocalSpeechTranscriptionSetup persists the verified bundled sidecar executablePath after stale-cache recovery',
    );
    assertEqual(
      recoveredAfterStaleExecutableSetupStatus.status.readiness,
      'ready',
      'initializeAutoCutLocalSpeechTranscriptionSetup reports ready after recovering from a stale missing-executable setup cache with the packaged sidecar',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...recoveredAfterStaleExecutableSetupStatus.settings.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const missingExecutableSetupCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
        speechTranscriptionExecutableDownloadCommandReady: false,
      }),
      probeSpeechTranscription: async (request) => {
        missingExecutableSetupCommands.push({ kind: 'probe', request });
        return {
          ready: false,
          executableReady: false,
          modelReady: Boolean(request.modelPath),
          executablePath: '',
          modelPath: request.modelPath ?? '',
          sourceKind: 'missing',
          diagnostics: ['AutoCut local speech transcription executablePath is not configured; AutoCut checked Settings, SDKWORK_AUTOCUT_WHISPER_EXECUTABLE, verified bundled sidecar, PATH, and common local installation directories.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
        };
      },
      downloadSpeechTranscriptionModel: async (request) => {
        missingExecutableSetupCommands.push({ kind: 'download-model', request });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const missingExecutableSetupEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    await assertRejects(
      () => services.initializeAutoCutLocalSpeechTranscriptionSetup(),
      'final availability check, not a model download failure',
      'initializeAutoCutLocalSpeechTranscriptionSetup auto-installs the default local STT model before blocking on a missing executable',
    );
    missingExecutableSetupEvents.stop();
    assertEqual(
      missingExecutableSetupCommands[0]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup probes default local STT paths before repairing a missing model',
    );
    assertEqual(
      missingExecutableSetupCommands[1]?.kind,
      'download-model',
      'initializeAutoCutLocalSpeechTranscriptionSetup downloads the recommended local STT model even when the executable must still be installed',
    );
    assertEqual(
      missingExecutableSetupCommands[2]?.kind,
      'probe',
      'initializeAutoCutLocalSpeechTranscriptionSetup re-probes after default model installation before reporting the missing executable',
    );
    assertRule(
      missingExecutableSetupEvents.details.some((event) => event.phase === 'started') &&
        missingExecutableSetupEvents.details.some((event) => event.phase === 'completed' && event.progress === 100),
      'initializeAutoCutLocalSpeechTranscriptionSetup keeps the model download visible when the executable is still missing',
    );
    assertRule(
      !missingExecutableSetupEvents.details.some((event) => event.phase === 'failed'),
      'initializeAutoCutLocalSpeechTranscriptionSetup does not report the model download as failed after the download completed and only executable readiness is blocked',
    );
    const settingsAfterMissingExecutableSetup = await services.getAutoCutSettings();
    assertEqual(
      settingsAfterMissingExecutableSetup.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'initializeAutoCutLocalSpeechTranscriptionSetup persists the verified default model path even when executable initialization is still blocked',
    );
    assertEqual(
      settingsAfterMissingExecutableSetup.speechTranscription.executablePath,
      '',
      'initializeAutoCutLocalSpeechTranscriptionSetup never persists a fake executablePath for a missing whisper-cli target',
    );
    const inspectedMissingExecutableSetup = await services.inspectAutoCutLocalSpeechTranscriptionSetup();
    assertEqual(
      inspectedMissingExecutableSetup.defaults.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'inspectAutoCutLocalSpeechTranscriptionSetup keeps showing the default bundled executable target after model auto-installation',
    );
    assertEqual(
      inspectedMissingExecutableSetup.model.ready,
      true,
      'inspectAutoCutLocalSpeechTranscriptionSetup reports the default local STT model as ready after auto-installation',
    );
    assertEqual(
      inspectedMissingExecutableSetup.executable.ready,
      false,
      'inspectAutoCutLocalSpeechTranscriptionSetup continues to fail closed until whisper-cli itself is verified',
    );

    await services.saveAutoCutSpeechTranscriptionSettings({
      ...settingsAfterMissingExecutableSetup.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const concurrentSpeechSetupCommands = [];
    let releaseConcurrentSpeechSetupDownload = () => undefined;
    const concurrentSpeechSetupDownloadGate = new Promise((resolve) => {
      releaseConcurrentSpeechSetupDownload = resolve;
    });
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
        speechTranscriptionExecutableDownloadCommandReady: false,
      }),
      probeSpeechTranscription: async (request) => {
        concurrentSpeechSetupCommands.push({ kind: 'probe', request });
        const executablePath = request.executablePath || 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe';
        const modelPath = request.modelPath ?? '';
        return {
          ready: Boolean(executablePath && modelPath),
          executableReady: Boolean(executablePath),
          modelReady: Boolean(modelPath),
          executablePath,
          modelPath,
          sourceKind: 'bundled-sidecar',
          diagnostics: modelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'],
          defaultExecutableDirectory: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64',
          defaultExecutablePath: 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
          defaultModelDirectory: 'D:/autocut/media/models/speech',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > SDKWORK_AUTOCUT_WHISPER_EXECUTABLE > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
          ...(modelPath ? { versionLine: 'concurrent setup whisper sidecar' } : {}),
        };
      },
      downloadSpeechTranscriptionModel: async (request) => {
        concurrentSpeechSetupCommands.push({ kind: 'download-model', request });
        await concurrentSpeechSetupDownloadGate;
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
    });
    const concurrentSpeechSetupEvents = captureEvents(services, 'speechTranscriptionModelDownloadProgress');
    const firstConcurrentSetup = services.initializeAutoCutLocalSpeechTranscriptionSetup();
    await flushMicrotasks();
    const secondConcurrentSetup = services.initializeAutoCutLocalSpeechTranscriptionSetup();
    await flushMicrotasks();
    releaseConcurrentSpeechSetupDownload();
    const [firstConcurrentSetupResult, secondConcurrentSetupResult] = await Promise.all([
      firstConcurrentSetup,
      secondConcurrentSetup,
    ]);
    concurrentSpeechSetupEvents.stop();
    assertEqual(
      firstConcurrentSetupResult.status.readiness,
      'ready',
      'first concurrent local STT initialization receives the shared ready result',
    );
    assertEqual(
      secondConcurrentSetupResult.status.readiness,
      'ready',
      'second concurrent local STT initialization receives the shared ready result',
    );
    assertDeepEqual(
      concurrentSpeechSetupCommands.map((command) => command.kind),
      ['probe', 'download-model', 'probe'],
      'concurrent local STT initialization performs one probe-download-probe sequence instead of duplicating heavy native work',
    );
    assertEqual(
      concurrentSpeechSetupCommands.filter((command) => command.kind === 'download-model').length,
      1,
      'initializeAutoCutLocalSpeechTranscriptionSetup coalesces concurrent model initialization into a single native download',
    );
    assertEqual(
      concurrentSpeechSetupEvents.details.filter((event) => event.phase === 'started').length,
      1,
      'concurrent local STT initialization emits one visible model download start event',
    );
    assertEqual(
      concurrentSpeechSetupEvents.details.filter((event) => event.phase === 'completed').length,
      1,
      'concurrent local STT initialization emits one visible model download completion event',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...firstConcurrentSetupResult.settings.speechTranscription,
      executablePath: 'D:/tools/whisper-cli.exe',
      modelPath: '',
      language: 'auto',
      lastProbeReady: true,
      lastProbeDiagnostics: ['stale probe must be cleared after UI-yielded initialization'],
    });
    let initializationDownloadSawUiFrameYield = false;
    const originalInitializationRequestAnimationFrame = globalThis.requestAnimationFrame;
    const originalInitializationWindowRequestAnimationFrame = globalThis.window?.requestAnimationFrame;
    let restoreInitializationUiFrameYieldProbe = () => undefined;
    try {
      globalThis.requestAnimationFrame = (callback) => {
        initializationDownloadSawUiFrameYield = true;
        callback(0);
        return 1;
      };
      if (globalThis.window) {
        globalThis.window.requestAnimationFrame = globalThis.requestAnimationFrame;
      }
      restoreInitializationUiFrameYieldProbe = () => {
        if (originalInitializationRequestAnimationFrame === undefined) {
          delete globalThis.requestAnimationFrame;
        } else {
          globalThis.requestAnimationFrame = originalInitializationRequestAnimationFrame;
        }
        if (globalThis.window) {
          if (originalInitializationWindowRequestAnimationFrame === undefined) {
            delete globalThis.window.requestAnimationFrame;
          } else {
            globalThis.window.requestAnimationFrame = originalInitializationWindowRequestAnimationFrame;
          }
        }
      };
      let initializationProbeModelPath = '';
      configureAutoCutNativeHostClient(services, {
        getCapabilities: async () => ({
          speechTranscriptionModelDownloadCommandReady: true,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: true,
        }),
        probeSpeechTranscription: async (request) => ({
          ready: Boolean(request.executablePath && initializationProbeModelPath),
          executableReady: Boolean(request.executablePath),
          modelReady: Boolean(initializationProbeModelPath),
          executablePath: request.executablePath,
          modelPath: initializationProbeModelPath,
          sourceKind: 'settings',
          diagnostics: initializationProbeModelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'],
          defaultExecutablePath: 'D:/tools/whisper-cli.exe',
          defaultModelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          executableStrategy: 'Settings executablePath > verified bundled sidecar > PATH/Homebrew/apt/common local whisper-cli',
        }),
        downloadSpeechTranscriptionModel: async (request) => {
          assertRule(
            initializationDownloadSawUiFrameYield,
            'initializeAutoCutLocalSpeechTranscriptionSetup yields one browser frame before starting the heavy native STT model download',
          );
          initializationProbeModelPath = 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin';
          return {
            providerId: request.providerId,
            presetId: request.presetId,
            fileName: request.fileName,
            modelPath: initializationProbeModelPath,
            byteSize: 573571072,
            downloaded: true,
            sourceUrl: request.url,
            sha256: request.sha256,
          };
        },
      });
      const uiYieldedInitialization = await services.initializeAutoCutLocalSpeechTranscriptionSetup();
      assertEqual(
        uiYieldedInitialization.status.readiness,
        'ready',
        'UI-yielded local STT initialization still returns ready after the final probe verifies the downloaded model',
      );
    } finally {
      restoreInitializationUiFrameYieldProbe();
    }
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...settingsAfterMissingExecutableSetup.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    await services.saveAutoCutWorkspaceSettings({
      ...defaultSettings.workspace,
      outputDirectory: '',
    });
    configureAutoCutNativeHostClient(services, createSettingsNativeHostClient());
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        providerId: 'openai-transcription',
      }),
      'target a local speech-to-text provider',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects presets for API providers',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        engine: 'faster-whisper',
      }),
      'implemented local speech-to-text engine',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects presets for unimplemented local engines',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        url: 'http://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3-turbo-q5_0.bin',
      }),
      'trusted Hugging Face source',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects non-HTTPS model URLs',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        url: 'https://huggingface.co/ggerganov/whisper.cpp/resolve/main/other-model.bin',
      }),
      'match fileName',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects mismatched URL file names',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        mirrorUrls: ['https://example.com/ggerganov/whisper.cpp/resolve/main/ggml-large-v3-turbo-q5_0.bin'],
      }),
      'trusted Hugging Face source',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects untrusted mirror model URLs',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        mirrorUrls: ['https://hf-mirror.com/ggerganov/whisper.cpp/resolve/main/other-model.bin'],
      }),
      'match fileName',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects mirror URLs whose file names do not match fileName',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        fileName: 'ggml-large-v3-turbo-q5_0.txt',
        url: 'https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3-turbo-q5_0.txt',
      }),
      'supported model file extension',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects unsupported model file extensions',
    );
    await assertRejects(
      () => services.resolveAutoCutLocalSpeechTranscriptionModelPreset({
        ...resolvedLocalSpeechModelPreset,
        sha256: 'not-a-real-digest',
      }),
      'pinned SHA-256 model digest',
      'resolveAutoCutLocalSpeechTranscriptionModelPreset rejects model presets without a pinned SHA-256 digest',
    );
    assertEqual(defaultSettings.workspace.outputDirectory, '', 'AutoCut workspace output directory defaults to empty so the desktop host uses its per-user app-data media root');
    assertEqual(await services.resolveAutoCutOutputRootDir(), undefined, 'resolveAutoCutOutputRootDir does not synthesize a hard-coded OS-specific path before the user configures one');
    const defaultSpeechRuntimeConfig = await services.resolveAutoCutSpeechTranscriptionRuntimeConfig();
    assertEqual(defaultSpeechRuntimeConfig.configured, false, 'resolveAutoCutSpeechTranscriptionRuntimeConfig fails closed before the user configures local speech-to-text');
    assertEqual(
      defaultSpeechRuntimeConfig.providerId,
      'local-whisper-cli',
      'resolveAutoCutSpeechTranscriptionRuntimeConfig resolves the default stable STT provider id',
    );
    await services.clearAutoCutLlmApiKey();
    settingsNativeLlmSecretValue = 'sk-env-default-secret';
    const envDefaultLlmSettings = await withImmediateTimers(() => services.initializeAutoCutDefaultLlmSettingsFromEnvironment());
    services.clearTransientAutoCutLlmApiKeyForTest();
    const envDefaultLlmRuntimeConfig = await withImmediateTimers(() => services.resolveAutoCutLlmRuntimeConfig());
    const envDefaultStoredSettings = readScopedStoredObject(services, 'settings');
    assertEqual(envDefaultLlmSettings?.llm.modelVendor, 'deepseek', 'environment default LLM initialization keeps DeepSeek as the default vendor');
    assertEqual(envDefaultLlmSettings?.llm.apiKeyConfigured, true, 'environment default LLM initialization marks the API key as configured');
    assertEqual(envDefaultLlmSettings?.llm.maskedApiKey, 'sk-en*************cret', 'environment default LLM initialization stores only a masked DeepSeek API key');
    assertEqual(envDefaultLlmRuntimeConfig.sessionApiKey, 'sk-env-default-secret', 'environment default LLM initialization makes the native secret available to runtime config');
    assertEqual(envDefaultStoredSettings.llm.apiKey, undefined, 'environment default LLM initialization never persists the raw API key in settings storage');
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_get_llm_secret' && entry.args?.request?.secretName === 'release-default'),
      'environment default LLM initialization reads the runtime-scoped native default secret',
    );
    settingsNativeLlmSecretValue = undefined;
    const legacyZhWorkspaceSettings = await withImmediateTimers(() => services.saveAutoCutWorkspaceSettings({
      ...defaultSettings.workspace,
      language: 'zh',
    }));
    const legacyEnWorkspaceSettings = await withImmediateTimers(() => services.saveAutoCutWorkspaceSettings({
      ...legacyZhWorkspaceSettings.workspace,
      language: 'en',
    }));
    assertEqual(legacyZhWorkspaceSettings.workspace.language, 'zh-CN', 'saveAutoCutWorkspaceSettings normalizes legacy zh to the canonical zh-CN locale');
    assertEqual(legacyEnWorkspaceSettings.workspace.language, 'en-US', 'saveAutoCutWorkspaceSettings normalizes legacy en to the canonical en-US locale');
    assertEqual(
      services.getAutoCutI18nText('settings.page.title'),
      'Settings Center',
      'saveAutoCutWorkspaceSettings switches the active i18next language when the application locale changes',
    );
    const {
      accountSettings,
      workspaceSettings,
      notificationSettings,
      llmVendorSettings,
      llmCustomModelSettings,
      llmNativeSecretSettings,
      llmNativeSecretRuntimeConfig,
      llmDeepSeekMaxSettings,
      llmDeepSeekAliasMaxSettings,
      llmOpenAiMaxSettings,
      llmGeminiMaxSettings,
      llmOpenAiMiniMaxSettings,
      llmGeminiFlashRuntimeSettings,
      llmAnthropicVendorSettings,
      llmXaiVendorSettings,
      llmQwenVendorSettings,
      llmMoonshotVendorSettings,
      llmBaiduVendorSettings,
      llmZhipuVendorSettings,
      llmMinimaxVendorSettings,
      llmHunyuanVendorSettings,
      llmDoubaoVendorSettings,
      llmAnthropicMaxSettings,
      llmXaiMaxSettings,
      llmQwenMaxSettings,
      llmMoonshotMaxSettings,
      llmBaiduMaxSettings,
      llmZhipuMaxSettings,
      llmMinimaxMaxSettings,
      llmHunyuanMaxSettings,
      llmDoubaoMaxSettings,
      llmRuntimeConfig,
      speechTranscriptionSettings,
      speechTranscriptionRuntimeConfig,
      speechTranscriptionProbe,
      speechTranscriptionSettingsAfterProbe,
      changedSpeechTranscriptionSettingsAfterProbe,
      apiKeySettings,
      revokedSettings,
      cacheClearedSettings,
      invoiceSettings,
      avatarSettings,
      subscriptionManagementSettings,
      passwordSettings,
      twoFactorSettings,
      sessionsRevokedSettings,
      subscriptionSettings,
      deletedAccountSettings,
    } = await withImmediateTimers(async () => {
      const initialSettings = await services.getAutoCutSettings();
      const savedAccountSettings = await services.saveAutoCutAccountSettings({
        ...initialSettings.account,
        displayName: 'Contract User',
      });
      const savedWorkspaceSettings = await services.saveAutoCutWorkspaceSettings({
        ...savedAccountSettings.workspace,
        hardwareAcceleration: false,
      });
      const savedNotificationSettings = await services.saveAutoCutNotificationSettings({
        ...savedWorkspaceSettings.notifications,
        usageReports: false,
      });
      const savedLlmVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedNotificationSettings.llm,
        modelVendor: 'openai',
      });
      const savedLlmCustomModelSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmVendorSettings.llm,
        apiKey: 'sk-contract-secret',
        model: 'gpt-4.1-mini',
      });
      const savedLlmNativeSecretSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmCustomModelSettings.llm,
        apiKey: 'sk-native-stored-secret',
      });
      services.clearTransientAutoCutLlmApiKeyForTest();
      const resolvedNativeSecretRuntimeConfig = await services.resolveAutoCutLlmRuntimeConfig();
      const savedLlmDeepSeekMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmCustomModelSettings.llm,
        modelVendor: 'deepseek',
        model: 'deepseek-v4-flash',
        maxTokens: 999999,
      });
      const savedLlmDeepSeekAliasMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmDeepSeekMaxSettings.llm,
        modelVendor: 'deepseek',
        model: 'deepseek-chat',
        maxTokens: 999999,
      });
      const savedLlmOpenAiMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmDeepSeekAliasMaxSettings.llm,
        modelVendor: 'openai',
        model: 'gpt-5.2',
        maxTokens: 999999,
      });
      const savedLlmOpenAiMiniMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmOpenAiMaxSettings.llm,
        modelVendor: 'openai',
        model: 'gpt-5.2-chat-latest',
        maxTokens: 999999,
      });
      const savedLlmGeminiMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmOpenAiMiniMaxSettings.llm,
        modelVendor: 'gemini',
        model: 'gemini-3.1-pro-preview',
        maxTokens: 999999,
      });
      const savedLlmGeminiFlashRuntimeSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmGeminiMaxSettings.llm,
        modelVendor: 'gemini',
        model: 'gemini-3-flash-preview',
        maxTokens: 999999,
      });
      const savedLlmAnthropicVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmGeminiFlashRuntimeSettings.llm,
        modelVendor: 'anthropic',
      });
      const savedLlmXaiVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmAnthropicVendorSettings.llm,
        modelVendor: 'xai',
      });
      const savedLlmQwenVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmXaiVendorSettings.llm,
        modelVendor: 'qwen',
      });
      const savedLlmMoonshotVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmQwenVendorSettings.llm,
        modelVendor: 'moonshot',
      });
      const savedLlmBaiduVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmMoonshotVendorSettings.llm,
        modelVendor: 'baidu',
      });
      const savedLlmZhipuVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmBaiduVendorSettings.llm,
        modelVendor: 'zhipu',
      });
      const savedLlmMinimaxVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmZhipuVendorSettings.llm,
        modelVendor: 'minimax',
      });
      const savedLlmHunyuanVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmMinimaxVendorSettings.llm,
        modelVendor: 'hunyuan',
      });
      const savedLlmDoubaoVendorSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmHunyuanVendorSettings.llm,
        modelVendor: 'doubao',
      });
      const savedLlmAnthropicMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmDoubaoVendorSettings.llm,
        modelVendor: 'anthropic',
        model: 'claude-sonnet-4-5-20250929',
        maxTokens: 999999,
      });
      const savedLlmXaiMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmAnthropicMaxSettings.llm,
        modelVendor: 'xai',
        model: 'grok-4.1',
        maxTokens: 999999,
      });
      const savedLlmQwenMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmXaiMaxSettings.llm,
        modelVendor: 'qwen',
        model: 'qwen3.6-plus',
        maxTokens: 999999,
      });
      const savedLlmMoonshotMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmQwenMaxSettings.llm,
        modelVendor: 'moonshot',
        model: 'kimi-k2-0905-preview',
        maxTokens: 999999,
      });
      const savedLlmBaiduMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmMoonshotMaxSettings.llm,
        modelVendor: 'baidu',
        model: 'ernie-5.0-preview',
        maxTokens: 999999,
      });
      const savedLlmZhipuMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmBaiduMaxSettings.llm,
        modelVendor: 'zhipu',
        model: 'glm-4.6',
        maxTokens: 999999,
      });
      const savedLlmMinimaxMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmZhipuMaxSettings.llm,
        modelVendor: 'minimax',
        model: 'MiniMax-M2.7',
        maxTokens: 999999,
      });
      const savedLlmHunyuanMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmMinimaxMaxSettings.llm,
        modelVendor: 'hunyuan',
        model: 'hunyuan-turbos-latest',
        maxTokens: 999999,
      });
      const savedLlmDoubaoMaxSettings = await services.saveAutoCutLlmSettings({
        ...savedLlmHunyuanMaxSettings.llm,
        modelVendor: 'doubao',
        model: 'doubao-seed-2-0-pro-250828',
        maxTokens: 999999,
      });
      const resolvedLlmRuntimeConfig = await services.resolveAutoCutLlmRuntimeConfig();
      const savedSpeechTranscriptionSettings = await services.saveAutoCutSpeechTranscriptionSettings({
        ...initialSettings.speechTranscription,
        providerId: 'local-whisper-cli',
        executablePath: ' D:/tools/whisper-cli.exe ',
        modelPath: ' D:/models/ggml-large-v3-turbo.bin ',
        language: ' zh ',
      });
      const resolvedSpeechTranscriptionRuntimeConfig = await services.resolveAutoCutSpeechTranscriptionRuntimeConfig();
      const testedSpeechTranscriptionProbe = await services.testAutoCutSpeechTranscriptionProvider();
      const speechTranscriptionSettingsAfterProbe = await services.getAutoCutSettings();
      const changedSpeechTranscriptionSettingsAfterProbe = await services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettingsAfterProbe.speechTranscription,
        modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
      });
      const createdApiKeySettings = await services.createAutoCutApiKey('Contract Key');
      const revokedApiKeySettings = await services.revokeAutoCutApiKey(createdApiKeySettings.apiKeys[0].id);
      const clearedCacheSettings = await services.clearAutoCutStorageCache();
      const loadedInvoiceSettings = await services.loadMoreAutoCutInvoices();
      const avatarRequestedSettings = await services.requestAutoCutAvatarChange();
      const subscriptionManagementSettings = await services.openAutoCutSubscriptionManagement();
      const passwordRequestedSettings = await services.requestAutoCutPasswordChange();
      const enabledTwoFactorSettings = await services.setAutoCutTwoFactorEnabled(true);
      const revokedSessionsSettings = await services.revokeAutoCutSessions();
      const canceledSubscriptionSettings = await services.cancelAutoCutSubscription();
      const deletedSettings = await services.deleteAutoCutAccount();
      return {
        accountSettings: savedAccountSettings,
        workspaceSettings: savedWorkspaceSettings,
        notificationSettings: savedNotificationSettings,
        llmVendorSettings: savedLlmVendorSettings,
        llmCustomModelSettings: savedLlmCustomModelSettings,
        llmNativeSecretSettings: savedLlmNativeSecretSettings,
        llmNativeSecretRuntimeConfig: resolvedNativeSecretRuntimeConfig,
        llmDeepSeekMaxSettings: savedLlmDeepSeekMaxSettings,
        llmDeepSeekAliasMaxSettings: savedLlmDeepSeekAliasMaxSettings,
        llmOpenAiMaxSettings: savedLlmOpenAiMaxSettings,
        llmOpenAiMiniMaxSettings: savedLlmOpenAiMiniMaxSettings,
        llmGeminiMaxSettings: savedLlmGeminiMaxSettings,
        llmGeminiFlashRuntimeSettings: savedLlmGeminiFlashRuntimeSettings,
        llmAnthropicVendorSettings: savedLlmAnthropicVendorSettings,
        llmXaiVendorSettings: savedLlmXaiVendorSettings,
        llmQwenVendorSettings: savedLlmQwenVendorSettings,
        llmMoonshotVendorSettings: savedLlmMoonshotVendorSettings,
        llmBaiduVendorSettings: savedLlmBaiduVendorSettings,
        llmZhipuVendorSettings: savedLlmZhipuVendorSettings,
        llmMinimaxVendorSettings: savedLlmMinimaxVendorSettings,
        llmHunyuanVendorSettings: savedLlmHunyuanVendorSettings,
        llmDoubaoVendorSettings: savedLlmDoubaoVendorSettings,
        llmAnthropicMaxSettings: savedLlmAnthropicMaxSettings,
        llmXaiMaxSettings: savedLlmXaiMaxSettings,
        llmQwenMaxSettings: savedLlmQwenMaxSettings,
        llmMoonshotMaxSettings: savedLlmMoonshotMaxSettings,
        llmBaiduMaxSettings: savedLlmBaiduMaxSettings,
        llmZhipuMaxSettings: savedLlmZhipuMaxSettings,
        llmMinimaxMaxSettings: savedLlmMinimaxMaxSettings,
        llmHunyuanMaxSettings: savedLlmHunyuanMaxSettings,
        llmDoubaoMaxSettings: savedLlmDoubaoMaxSettings,
        llmRuntimeConfig: resolvedLlmRuntimeConfig,
        speechTranscriptionSettings: savedSpeechTranscriptionSettings,
        speechTranscriptionRuntimeConfig: resolvedSpeechTranscriptionRuntimeConfig,
        speechTranscriptionProbe: testedSpeechTranscriptionProbe,
        speechTranscriptionSettingsAfterProbe,
        changedSpeechTranscriptionSettingsAfterProbe,
        apiKeySettings: createdApiKeySettings,
        revokedSettings: revokedApiKeySettings,
        cacheClearedSettings: clearedCacheSettings,
        invoiceSettings: loadedInvoiceSettings,
        avatarSettings: avatarRequestedSettings,
        subscriptionManagementSettings,
        passwordSettings: passwordRequestedSettings,
        twoFactorSettings: enabledTwoFactorSettings,
        sessionsRevokedSettings: revokedSessionsSettings,
        subscriptionSettings: canceledSubscriptionSettings,
        deletedAccountSettings: deletedSettings,
      };
    });
    settingsEvents.stop();
    const storedSettings = readScopedStoredObject(services, 'settings');
    assertEqual(accountSettings.account.displayName, 'Contract User', 'saveAutoCutAccountSettings persists account edits');
    assertEqual(workspaceSettings.workspace.hardwareAcceleration, false, 'saveAutoCutWorkspaceSettings persists workspace edits');
    assertEqual(workspaceSettings.workspace.language, 'en-US', 'saveAutoCutWorkspaceSettings preserves canonical application locale values');
    assertEqual(notificationSettings.notifications.usageReports, false, 'saveAutoCutNotificationSettings persists notification edits');
    assertEqual(llmVendorSettings.llm.modelVendor, 'openai', 'saveAutoCutLlmSettings persists the selected ModelVendor');
    assertEqual(llmVendorSettings.llm.baseUrl, 'https://api.openai.com/v1', 'saveAutoCutLlmSettings switches base URL from the selected ModelVendor preset');
    assertEqual(llmVendorSettings.llm.model, 'gpt-5.2', 'saveAutoCutLlmSettings switches default model from the selected ModelVendor preset');
    assertEqual(llmVendorSettings.llm.maxTokens, 8192, 'saveAutoCutLlmSettings switches to the selected vendor default output budget');
    assertEqual(llmCustomModelSettings.llm.model, 'gpt-4.1-mini', 'saveAutoCutLlmSettings preserves an explicit model override');
    assertEqual(llmCustomModelSettings.llm.maxTokens, 4096, 'saveAutoCutLlmSettings keeps a valid output budget when the model override supports it');
    assertEqual(llmNativeSecretSettings.llm.maskedApiKey, 'sk-na*************cret', 'saveAutoCutLlmSettings stores native-secret-backed masked LLM API keys in AppSettings');
    assertEqual(llmNativeSecretRuntimeConfig.sessionApiKey, 'sk-native-stored-secret', 'resolveAutoCutLlmRuntimeConfig restores the LLM API key from native secret storage after session memory is cleared');
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_save_llm_secret' && entry.args?.request?.secretValue === 'sk-native-stored-secret'),
      'saveAutoCutLlmSettings writes raw LLM API keys to the native secret store instead of browser storage',
    );
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_get_llm_secret'),
      'resolveAutoCutLlmRuntimeConfig reads LLM API keys from the native secret store when session memory is empty',
    );
    assertEqual(llmDeepSeekMaxSettings.llm.maxTokens, 393216, 'saveAutoCutLlmSettings allows DeepSeek V4 384K maximum output tokens');
    assertEqual(llmDeepSeekAliasMaxSettings.llm.maxTokens, 393216, 'saveAutoCutLlmSettings treats DeepSeek legacy chat alias as the V4 Flash capability class');
    assertEqual(llmOpenAiMaxSettings.llm.maxTokens, 128000, 'saveAutoCutLlmSettings clamps GPT-5.2 output tokens to its model-specific ceiling');
    assertEqual(llmOpenAiMiniMaxSettings.llm.maxTokens, 128000, 'saveAutoCutLlmSettings clamps GPT-5.2 Chat Latest output tokens to its model-specific ceiling');
    assertEqual(llmGeminiMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Gemini 3.1 Pro Preview output tokens to its model-specific ceiling');
    assertEqual(llmGeminiFlashRuntimeSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Gemini 3 Flash Preview output tokens to its model-specific ceiling');
    assertDeepEqual(
      [
        [llmAnthropicVendorSettings.llm.modelVendor, llmAnthropicVendorSettings.llm.baseUrl, llmAnthropicVendorSettings.llm.model],
        [llmXaiVendorSettings.llm.modelVendor, llmXaiVendorSettings.llm.baseUrl, llmXaiVendorSettings.llm.model],
        [llmQwenVendorSettings.llm.modelVendor, llmQwenVendorSettings.llm.baseUrl, llmQwenVendorSettings.llm.model],
        [llmMoonshotVendorSettings.llm.modelVendor, llmMoonshotVendorSettings.llm.baseUrl, llmMoonshotVendorSettings.llm.model],
        [llmBaiduVendorSettings.llm.modelVendor, llmBaiduVendorSettings.llm.baseUrl, llmBaiduVendorSettings.llm.model],
        [llmZhipuVendorSettings.llm.modelVendor, llmZhipuVendorSettings.llm.baseUrl, llmZhipuVendorSettings.llm.model],
        [llmMinimaxVendorSettings.llm.modelVendor, llmMinimaxVendorSettings.llm.baseUrl, llmMinimaxVendorSettings.llm.model],
        [llmHunyuanVendorSettings.llm.modelVendor, llmHunyuanVendorSettings.llm.baseUrl, llmHunyuanVendorSettings.llm.model],
        [llmDoubaoVendorSettings.llm.modelVendor, llmDoubaoVendorSettings.llm.baseUrl, llmDoubaoVendorSettings.llm.model],
      ],
      [
        ['anthropic', 'https://api.anthropic.com/v1', 'claude-sonnet-4-5-20250929'],
        ['xai', 'https://api.x.ai/v1', 'grok-4.1'],
        ['qwen', 'https://dashscope.aliyuncs.com/compatible-mode/v1', 'qwen3.6-plus'],
        ['moonshot', 'https://api.moonshot.cn/v1', 'kimi-k2-0905-preview'],
        ['baidu', 'https://qianfan.baidubce.com/v2', 'ernie-5.0-preview'],
        ['zhipu', 'https://open.bigmodel.cn/api/paas/v4', 'glm-4.6'],
        ['minimax', 'https://api.minimax.io/v1', 'MiniMax-M2.7'],
        ['hunyuan', 'https://api.hunyuan.cloud.tencent.com/v1', 'hunyuan-t1-latest'],
        ['doubao', 'https://ark.cn-beijing.volces.com/api/v3', 'doubao-seed-2-0-pro-250828'],
      ],
      'saveAutoCutLlmSettings switches mainstream China/US ModelVendors to canonical base URLs and default models',
    );
    assertEqual(llmAnthropicMaxSettings.llm.maxTokens, 64000, 'saveAutoCutLlmSettings clamps Claude Sonnet output tokens to its model-specific ceiling');
    assertEqual(llmXaiMaxSettings.llm.maxTokens, 32768, 'saveAutoCutLlmSettings clamps Grok output tokens to its model-specific ceiling');
    assertEqual(llmQwenMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Qwen3 Max output tokens to its model-specific ceiling');
    assertEqual(llmMoonshotMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Kimi output tokens to its model-specific ceiling');
    assertEqual(llmBaiduMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps ERNIE output tokens to its model-specific ceiling');
    assertEqual(llmZhipuMaxSettings.llm.maxTokens, 128000, 'saveAutoCutLlmSettings clamps GLM output tokens to its model-specific ceiling');
    assertEqual(llmMinimaxMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps MiniMax output tokens to its model-specific ceiling');
    assertEqual(llmHunyuanMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Hunyuan output tokens to its model-specific ceiling');
    assertEqual(llmDoubaoMaxSettings.llm.maxTokens, 65536, 'saveAutoCutLlmSettings clamps Doubao output tokens to its model-specific ceiling');
    assertEqual(llmCustomModelSettings.llm.maskedApiKey, 'sk-co*************cret', 'saveAutoCutLlmSettings stores only a masked LLM API key in AppSettings');
    assertEqual(storedSettings.llm.apiKey, undefined, 'settings storage never persists the raw LLM API key');
    assertEqual(storedSettings.llm.sessionApiKey, undefined, 'settings storage never persists transient LLM session API keys');
    assertEqual(llmRuntimeConfig.modelVendor, 'doubao', 'resolveAutoCutLlmRuntimeConfig returns the selected ModelVendor');
    assertEqual(llmRuntimeConfig.baseUrl, 'https://ark.cn-beijing.volces.com/api/v3', 'resolveAutoCutLlmRuntimeConfig returns the OpenAI-compatible base URL');
    assertEqual(llmRuntimeConfig.model, 'doubao-seed-2-0-pro-250828', 'resolveAutoCutLlmRuntimeConfig returns the active model');
    assertEqual(llmRuntimeConfig.maxTokens, 65536, 'resolveAutoCutLlmRuntimeConfig returns the model-specific output budget');
    assertEqual(llmRuntimeConfig.apiKeyConfigured, true, 'resolveAutoCutLlmRuntimeConfig reports configured API key status without exposing the key');
    assertEqual('apiKey' in llmRuntimeConfig, false, 'resolveAutoCutLlmRuntimeConfig does not expose raw API keys to the renderer');
    assertEqual(speechTranscriptionSettings.speechTranscription.executablePath, 'D:/tools/whisper-cli.exe', 'saveAutoCutSpeechTranscriptionSettings trims and persists executablePath');
    assertEqual(speechTranscriptionSettings.speechTranscription.modelPath, 'D:/models/ggml-large-v3-turbo.bin', 'saveAutoCutSpeechTranscriptionSettings trims and persists modelPath');
    assertEqual(speechTranscriptionSettings.speechTranscription.language, 'zh', 'saveAutoCutSpeechTranscriptionSettings trims and persists language');
    assertEqual(speechTranscriptionSettings.speechTranscription.providerId, 'local-whisper-cli', 'saveAutoCutSpeechTranscriptionSettings persists the selected stable STT provider id');
    assertEqual(speechTranscriptionSettings.speechTranscription.configured, true, 'saveAutoCutSpeechTranscriptionSettings marks the local speech-to-text toolchain as configured');
    const invalidProviderSettings = await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechTranscriptionSettings.speechTranscription,
      providerId: '閺傚洦顢嶉幓鎰絿',
    });
    assertEqual(
      invalidProviderSettings.speechTranscription.providerId,
      'local-whisper-cli',
      'saveAutoCutSpeechTranscriptionSettings normalizes invalid or localized provider ids back to the default stable enum id',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        executablePath: 'tools/whisper-cli.exe',
      }),
      'absolute local executable file path',
      'saveAutoCutSpeechTranscriptionSettings rejects relative local speech-to-text executable paths',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        executablePath: 'https://tools.example.com/whisper-cli.exe',
      }),
      'absolute local executable file path',
      'saveAutoCutSpeechTranscriptionSettings rejects remote speech-to-text executable URLs',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        executablePath: 'D:/tools/',
      }),
      'absolute local executable file path',
      'saveAutoCutSpeechTranscriptionSettings rejects directory-like local speech-to-text executable paths',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        modelPath: 'models/ggml-large-v3-turbo.bin',
      }),
      'absolute local model file path',
      'saveAutoCutSpeechTranscriptionSettings rejects relative local speech-to-text model paths',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        modelPath: 'https://models.example.com/ggml-large-v3-turbo.bin',
      }),
      'absolute local model file path',
      'saveAutoCutSpeechTranscriptionSettings rejects remote speech-to-text model URLs',
    );
    await assertRejects(
      () => services.saveAutoCutSpeechTranscriptionSettings({
        ...speechTranscriptionSettings.speechTranscription,
        modelPath: 'D:/models/ggml-large-v3-turbo.txt',
      }),
      'supported model file extension',
      'saveAutoCutSpeechTranscriptionSettings rejects unsupported local speech-to-text model extensions',
    );
    assertEqual(storedSettings.speechTranscription.executablePath, 'D:/tools/whisper-cli.exe', 'settings storage persists local speech-to-text executablePath');
    assertEqual(speechTranscriptionSettings.speechTranscription.modelPath, 'D:/models/ggml-large-v3-turbo.bin', 'settings save result persists local speech-to-text modelPath');
    assertEqual(storedSettings.speechTranscription.modelPath, 'D:/models/ggml-large-v3-turbo.gguf', 'settings storage persists the latest local speech-to-text modelPath after user edits');
    assertEqual(storedSettings.speechTranscription.providerId, 'local-whisper-cli', 'settings storage persists the selected stable speech-to-text provider id');
    assertEqual(speechTranscriptionRuntimeConfig.configured, true, 'resolveAutoCutSpeechTranscriptionRuntimeConfig reports saved local speech-to-text settings');
    assertEqual(speechTranscriptionRuntimeConfig.providerId, 'local-whisper-cli', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the saved STT provider id');
    assertEqual(speechTranscriptionRuntimeConfig.executablePath, 'D:/tools/whisper-cli.exe', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the saved speech executable path');
    assertEqual(speechTranscriptionRuntimeConfig.modelPath, 'D:/models/ggml-large-v3-turbo.bin', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the saved speech model path');
    assertEqual(speechTranscriptionRuntimeConfig.language, 'zh', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the saved default speech language');
    assertEqual(speechTranscriptionProbe.ready, true, 'testAutoCutSpeechTranscriptionProvider reports native probe readiness');
    assertEqual(speechTranscriptionSettingsAfterProbe.speechTranscription.lastProbeReady, true, 'testAutoCutSpeechTranscriptionProvider stores the latest probe readiness on settings');
    const subtitlePacedWorkflowCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        videoSliceCommandReady: true,
        videoSliceAudioActivityAnalysisCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      importMediaFile: async (request) => {
        subtitlePacedWorkflowCommands.push({ kind: 'import', request });
        return {
          assetUuid: 'subtitle-paced-native-slice-asset',
          sandboxPath: `${configuredOutputDirectory}/inputs/subtitle-paced-native-slice-asset.mp4`,
          byteSize: 3000000,
          name: 'subtitle-paced.mp4',
          mediaType: 'video',
          hasAudioStream: true,
          hasVideoStream: true,
          mimeType: 'video/mp4',
          durationMs: 90000,
          width: 1080,
          height: 1920,
        };
      },
      transcribeMedia: async (request) => {
        subtitlePacedWorkflowCommands.push({ kind: 'transcribe', request });
        return createNativeVideoSliceTranscriptResult(
          request,
          'subtitle-paced-transcript-task',
          configuredOutputDirectory,
          [
            {
              startMs: 0,
              endMs: 3700,
              text:
                'Why native validation matters is simple.',
              speaker: 'Speaker 1',
              words: [
                { startMs: 0, endMs: 650, text: 'Why' },
                { startMs: 650, endMs: 1200, text: 'native' },
                { startMs: 1200, endMs: 1900, text: 'validation' },
                { startMs: 1900, endMs: 2500, text: 'matters' },
                { startMs: 2500, endMs: 3100, text: 'is' },
                { startMs: 3100, endMs: 3700, text: 'simple.' },
              ],
            },
            {
              startMs: 3720,
              endMs: 8500,
              text:
                'Because bad native validation can break review, the native validation problem must be caught before assets are saved.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8520,
              endMs: 19000,
              text:
                'So native validation verifies every result and the native validation workflow stays safe.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 40000,
              endMs: 48500,
              text:
                'What makes artifact checks reliable is clear. The artifact check shows missing artifact thumbnails before publishing, creating an artifact conflict for reviewers.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 48520,
              endMs: 59000,
              text:
                '\u771f\u6b63\u597d\u7684\u5b57\u5e55\u5e94\u8be5\u8ddf\u7740\u8bb2\u8bdd\u4e00\u70b9\u4e00\u70b9\u51fa\u73b0\u3002\u5b83\u4e0d\u80fd\u628a\u4e00\u6574\u53e5\u8bdd\u957f\u65f6\u95f4\u538b\u5728\u753b\u9762\u5e95\u90e8\u3002\u8fd9\u6837\u624d\u80fd\u4fdd\u6301\u89c2\u770b\u8282\u594f\u6e05\u695a\u3002',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: Boolean(request.executablePath && request.modelPath),
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'settings',
        diagnostics: [],
      }),
      analyzeVideoSliceAudioActivity: async (request) => {
        subtitlePacedWorkflowCommands.push({ kind: 'analyze-audio-boundaries', request });
        return createNativeVideoSliceAudioActivityAnalysisResult(request);
      },
      sliceVideo: async (request) => {
        subtitlePacedWorkflowCommands.push({ kind: 'slice', request });
        const slices = request.clips.map((clip, index) => {
          const output = createNativeTaskOutputArtifact(
            'subtitle-paced-native-slice-render-task',
            clip.outputFileName ?? `subtitle-paced-slice-${index + 1}.mp4`,
            configuredOutputDirectory,
          );
          const thumbnail = createNativeTaskCoverArtifact(
            'subtitle-paced-native-slice-render-task',
            `subtitle-paced-thumb-${index + 1}.jpg`,
            configuredOutputDirectory,
          );
          const subtitle = createNativeTaskOutputArtifact(
            'subtitle-paced-native-slice-render-task',
            `subtitle-paced-subtitle-${index + 1}.srt`,
            configuredOutputDirectory,
          );
          return {
            artifactUuid: `subtitle-paced-native-slice-artifact-${index + 1}`,
            artifactPath: output.artifactPath,
            thumbnailArtifactUuid: `subtitle-paced-native-slice-thumb-${index + 1}`,
            thumbnailArtifactPath: thumbnail.artifactPath,
            taskOutputDir: output.taskOutputDir,
            byteSize: 234567 + index,
            thumbnailByteSize: 12345 + index,
            subtitleArtifactUuid: `subtitle-paced-native-slice-subtitle-${index + 1}`,
            subtitleArtifactPath: subtitle.artifactPath,
            subtitleByteSize: 1234 + index,
            subtitleFormat: 'srt',
            format: 'mp4',
            ...copyNativeVideoSliceClipEvidence(clip, request.noiseReduction),
          };
        });
        return {
          taskUuid: 'subtitle-paced-native-slice-render-task',
          sourceAssetUuid: request.assetUuid,
          taskOutputDir: slices[0]?.taskOutputDir ?? configuredOutputDirectory,
          ffmpegExecutable: 'ffmpeg',
          slices,
        };
      },
      createAssetUrl: (artifactPath) => `asset://localhost/${encodeURIComponent(artifactPath)}`,
    });
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    await withImmediateTimers(() => processVideoSlice({
      fileId: 'asset-source-subtitle-paced',
      file: createTrustedLocalMediaFile(commons, 'D:/media/subtitle-paced.mp4', 'subtitle-paced.mp4'),
      mode: 'contract-mode',
      llmModel: 'gemini-3-flash-preview',
      minDuration: 15,
      idealDuration: 22,
      maxDuration: 30,
      baseAlgorithm: 'scene',
      highlightEngine: 'emotion',
      targetAspectRatio: '9:16',
      videoObjectFit: 'contain',
      enableCoughFilter: true,
      enableRepeatFilter: true,
      enableSubtitles: true,
      subtitleMode: 'both',
      subtitleStyleId: 'tiktok',
      outputRootDir: configuredOutputDirectory,
    }));
    const subtitlePacedSliceRequest = subtitlePacedWorkflowCommands.find((command) => command.kind === 'slice')?.request;
    assertEqual(
      subtitlePacedSliceRequest?.subtitleFormat,
      'srt',
      'video slice native workflow requests SRT subtitles when Smart Slice subtitles are enabled',
    );
    assertEqual(
      subtitlePacedSliceRequest?.subtitleMode,
      'both',
      'video slice native workflow preserves the requested subtitle publish mode',
    );
    assertRule(
      (subtitlePacedSliceRequest?.subtitleSegments?.length ?? 0) >= 5,
      'video slice native workflow splits long STT transcript segments into paced subtitle cues before native rendering',
    );
    assertRule(
      subtitlePacedSliceRequest?.subtitleSegments?.every((segment) =>
        typeof segment.text === 'string' &&
        normalizeSubtitleCueText(segment.text).length <= 52 &&
        segment.endMs > segment.startMs &&
        segment.endMs - segment.startMs <= 4500
      ),
      'video slice native workflow keeps subtitle cues short enough to follow speech progress, complete short sentences, and fit vertical video',
    );
    assertRule(
      subtitlePacedSliceRequest?.subtitleSegments?.some((segment) =>
        segment.text.startsWith('Why native validation matters') &&
        !segment.text.includes('before assets are saved')
      ),
      'video slice native workflow emits the first paced subtitle chunk in transcript order',
    );
    assertRule(
      subtitlePacedSliceRequest?.subtitleSegments?.some((segment) =>
        normalizeSubtitleCueText(segment.text) === 'Why native validation matters is simple.' &&
        segment.startMs === 0 &&
        segment.endMs === 3700
      ),
      'video slice native workflow uses STT word timestamps to pace subtitles at spoken-word boundaries',
    );
    const cjkSubtitleSegments = subtitlePacedSliceRequest?.subtitleSegments?.filter((segment) =>
      /\p{Script=Han}/u.test(segment.text),
    ) ?? [];
    assertRule(
      cjkSubtitleSegments.length >= 3,
      'video slice native workflow splits long CJK STT transcript segments into multiple paced subtitle cues',
    );
    assertRule(
      cjkSubtitleSegments.every((segment) =>
        countSubtitleCueDisplayUnits(segment.text) <= 40 &&
        segment.endMs - segment.startMs <= 4500
      ),
      'video slice native workflow keeps CJK subtitle cues compact enough for vertical video, complete short sentences, and speech-progress pacing',
    );
    assertRule(
      !cjkSubtitleSegments.some((segment) => segment.text.includes('閼板奔绗夐弰顖欑閺佹潙褰炵拠婵嬫毐閺冨爼妫块崢瀣躬閻㈠娼版惔鏇㈠劥瑜板崬鎼风憴鍌滄箙')),
      'video slice native workflow never forwards a full long CJK sentence as one static subtitle overlay',
    );
    services.resetAutoCutNativeHostClient();
    assertEqual(
      changedSpeechTranscriptionSettingsAfterProbe.speechTranscription.lastProbeReady,
      undefined,
      'saveAutoCutSpeechTranscriptionSettings clears stale probe readiness when local speech-to-text modelPath changes',
    );
    assertEqual(
      changedSpeechTranscriptionSettingsAfterProbe.speechTranscription.lastProbeDiagnostics,
      undefined,
      'saveAutoCutSpeechTranscriptionSettings clears stale probe diagnostics when local speech-to-text modelPath changes',
    );
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_probe_speech_transcription' && entry.args?.request?.sourceKind === 'settings'),
      'testAutoCutSpeechTranscriptionProvider probes the persisted settings-backed local speech-to-text provider',
    );
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_probe_speech_transcription' && entry.args?.request?.providerId === 'local-whisper-cli'),
      'testAutoCutSpeechTranscriptionProvider passes the selected providerId to the native probe',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechTranscriptionSettings.speechTranscription,
      modelPath: 'D:/models/ggml-large-v3-turbo.gguf',
    });
    const unverifiedLocalProviderTranscriptionRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => {
        unverifiedLocalProviderTranscriptionRequests.push({ kind: 'download-model', request });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: request.modelPath ?? 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
      probeSpeechTranscription: async (request) => {
        unverifiedLocalProviderTranscriptionRequests.push({ kind: 'probe', request });
        return {
          ready: Boolean(request.executablePath && request.modelPath),
          executablePath: request.executablePath,
          modelPath: request.modelPath,
          sourceKind: request.sourceKind ?? 'execution-preflight',
          diagnostics: [],
          versionLine: 'auto repaired unverified local STT',
        };
      },
      transcribeMedia: async (request) => {
        unverifiedLocalProviderTranscriptionRequests.push({ kind: 'transcribe', request });
        return createStandardNativeVideoSliceTranscriptResult(request, 'unverified-local-transcript-task');
      },
    });
    const autoRepairedUnverifiedLocalProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-unverified-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertEqual(
      autoRepairedUnverifiedLocalProviderTranscription.segments.length,
      2,
      'transcribeAutoCutMediaWithConfiguredProvider auto-initializes and verifies repairable untested local STT settings',
    );
    assertEqual(
      unverifiedLocalProviderTranscriptionRequests.some((entry) => entry.kind === 'transcribe'),
      true,
      'transcribeAutoCutMediaWithConfiguredProvider dispatches native transcription after repairable local STT settings pass automatic setup',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechTranscriptionSettings.speechTranscription,
      executablePath: 'D:/tools/whisper-cli.exe',
      modelPath: '',
      language: 'auto',
    });
    const autoInitializedLocalProviderCommands = [];
    const autoInitializedSpeechDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      configureAutoCutNativeHostClient(services, {
        getCapabilities: async () => ({
          mediaImportCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: false,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionModelDownloadCommandReady: true,
        }),
        downloadSpeechTranscriptionModel: async (request) => {
          autoInitializedLocalProviderCommands.push({ kind: 'download-model', request });
          return {
            providerId: request.providerId,
            presetId: request.presetId,
            fileName: request.fileName,
            modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
            byteSize: 573571072,
            downloaded: true,
            sourceUrl: request.url,
            sha256: request.sha256,
          };
        },
        probeSpeechTranscription: async (request) => {
          autoInitializedLocalProviderCommands.push({ kind: 'probe', request });
          return {
            ready: Boolean(request.executablePath && request.modelPath),
            executablePath: request.executablePath,
            modelPath: request.modelPath,
            sourceKind: request.sourceKind ?? 'execution-preflight',
            diagnostics: [],
            versionLine: 'auto initialized local STT',
          };
        },
        transcribeMedia: async (request) => {
          autoInitializedLocalProviderCommands.push({ kind: 'transcribe', request });
          return createStandardNativeVideoSliceTranscriptResult(request, 'auto-initialized-local-transcript-task');
        },
      });
      const autoInitializedLocalProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'provider-local-auto-initialized-model-asset',
        language: 'en',
        outputRootDir: 'D:/autocut/media',
      });
      assertEqual(
        autoInitializedLocalProviderTranscription.segments.length,
        2,
        'transcribeAutoCutMediaWithConfiguredProvider returns transcript segments after automatic local STT model initialization',
      );
    });
    assertEqual(
      autoInitializedLocalProviderCommands[0]?.kind,
      'download-model',
      'transcribeAutoCutMediaWithConfiguredProvider initializes the recommended local STT model before probing when only modelPath is missing',
    );
    assertEqual(
      autoInitializedLocalProviderCommands[1]?.kind,
      'probe',
      'transcribeAutoCutMediaWithConfiguredProvider probes local STT again after automatic model initialization',
    );
    assertEqual(
      autoInitializedLocalProviderCommands[1]?.request?.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'transcribeAutoCutMediaWithConfiguredProvider probes the native-installed local STT model path',
    );
    assertEqual(
      autoInitializedLocalProviderCommands[2]?.kind,
      'transcribe',
      'transcribeAutoCutMediaWithConfiguredProvider dispatches native transcription only after local STT initialization succeeds',
    );
    assertEqual(
      autoInitializedLocalProviderCommands[2]?.request?.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'transcribeAutoCutMediaWithConfiguredProvider sends the initialized local STT model path to native transcription',
    );
    assertRule(
      autoInitializedSpeechDiagnostics.some((call) =>
        call.level === 'warning' &&
        String(call.args?.[0] ?? '').includes('[AutoCut:speech-transcription] Local speech-to-text model initialization started')
      ),
      'transcribeAutoCutMediaWithConfiguredProvider logs automatic local STT model initialization to the browser console',
    );
    const speechSettingsAfterAutoInitialization = await services.getAutoCutSettings();
    assertEqual(
      speechSettingsAfterAutoInitialization.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'transcribeAutoCutMediaWithConfiguredProvider persists the auto-initialized local STT model path',
    );
    assertEqual(
      speechSettingsAfterAutoInitialization.speechTranscription.lastProbeReady,
      true,
      'transcribeAutoCutMediaWithConfiguredProvider stores the successful execution preflight after automatic local STT initialization',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechSettingsAfterAutoInitialization.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const bundledSidecarLocalProviderCommands = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: false,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      downloadSpeechTranscriptionModel: async (request) => {
        bundledSidecarLocalProviderCommands.push({ kind: 'download-model', request });
        return {
          providerId: request.providerId,
          presetId: request.presetId,
          fileName: request.fileName,
          modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
          byteSize: 573571072,
          downloaded: true,
          sourceUrl: request.url,
          sha256: request.sha256,
        };
      },
      probeSpeechTranscription: async (request) => {
        bundledSidecarLocalProviderCommands.push({ kind: 'probe', request });
        const executablePath = request.executablePath ?? 'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe';
        return {
          ready: Boolean(executablePath && request.modelPath),
          executablePath,
          modelPath: request.modelPath ?? '',
          sourceKind: request.executablePath ? 'execution-preflight' : 'bundled-sidecar',
          diagnostics: request.modelPath ? [] : ['AutoCut local speech transcription modelPath is not configured.'],
          versionLine: request.modelPath ? 'bundled whisper sidecar' : undefined,
        };
      },
      transcribeMedia: async (request) => {
        bundledSidecarLocalProviderCommands.push({ kind: 'transcribe', request });
        return createStandardNativeVideoSliceTranscriptResult(request, 'bundled-sidecar-local-transcript-task');
      },
    });
    const bundledSidecarLocalProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-bundled-sidecar-auto-model-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertEqual(
      bundledSidecarLocalProviderCommands[0]?.kind,
      'probe',
      'transcribeAutoCutMediaWithConfiguredProvider probes packaged local STT sidecar discovery before model initialization',
    );
    assertEqual(
      bundledSidecarLocalProviderCommands[0]?.request?.executablePath,
      undefined,
      'transcribeAutoCutMediaWithConfiguredProvider lets native host resolve the packaged Whisper sidecar when executablePath is not manually configured',
    );
    assertEqual(
      bundledSidecarLocalProviderCommands[1]?.kind,
      'download-model',
      'transcribeAutoCutMediaWithConfiguredProvider downloads the recommended local STT model after packaged sidecar discovery',
    );
    assertEqual(
      bundledSidecarLocalProviderCommands[2]?.kind,
      'probe',
      'transcribeAutoCutMediaWithConfiguredProvider probes packaged sidecar plus downloaded model before dispatch',
    );
    assertEqual(
      bundledSidecarLocalProviderCommands[2]?.request?.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'transcribeAutoCutMediaWithConfiguredProvider persists the packaged Whisper sidecar executablePath before execution preflight',
    );
    assertEqual(
      bundledSidecarLocalProviderCommands[3]?.kind,
      'transcribe',
      'transcribeAutoCutMediaWithConfiguredProvider dispatches transcription after packaged sidecar and model initialization both verify',
    );
    assertEqual(
      bundledSidecarLocalProviderTranscription.segments.length,
      2,
      'transcribeAutoCutMediaWithConfiguredProvider returns transcript segments after packaged sidecar local STT initialization',
    );
    const speechSettingsAfterBundledSidecarInitialization = await services.getAutoCutSettings();
    assertEqual(
      speechSettingsAfterBundledSidecarInitialization.speechTranscription.executablePath,
      'D:/Program Files/SDKWork Video Cut/resources/binaries/windows-x86_64/whisper-cli.exe',
      'transcribeAutoCutMediaWithConfiguredProvider persists the packaged Whisper sidecar executablePath from native probe evidence',
    );
    assertEqual(
      speechSettingsAfterBundledSidecarInitialization.speechTranscription.modelPath,
      'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
      'transcribeAutoCutMediaWithConfiguredProvider persists the recommended model path after packaged sidecar initialization',
    );
    assertEqual(
      speechSettingsAfterBundledSidecarInitialization.speechTranscription.lastProbeReady,
      true,
      'transcribeAutoCutMediaWithConfiguredProvider stores successful readiness after packaged sidecar initialization',
    );
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechSettingsAfterBundledSidecarInitialization.speechTranscription,
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const missingExecutableLocalProviderCommands = [];
    const missingExecutableSpeechDiagnostics = await captureConsoleDiagnosticsAsync(async () => {
      configureAutoCutNativeHostClient(services, {
        getCapabilities: async () => ({
          mediaImportCommandReady: true,
          speechTranscriptionCommandReady: true,
          speechTranscriptionToolchainReady: false,
          speechTranscriptionProbeCommandReady: true,
          speechTranscriptionModelDownloadCommandReady: true,
        }),
        downloadSpeechTranscriptionModel: async (request) => {
          missingExecutableLocalProviderCommands.push({ kind: 'download-model', request });
          return {
            providerId: request.providerId,
            presetId: request.presetId,
            fileName: request.fileName,
            modelPath: 'D:/autocut/media/models/speech/ggml-large-v3-turbo-q5_0.bin',
            byteSize: 573571072,
            downloaded: true,
            sourceUrl: request.url,
            sha256: request.sha256,
          };
        },
        probeSpeechTranscription: async (request) => {
          missingExecutableLocalProviderCommands.push({ kind: 'probe', request });
          return {
            ready: false,
            executablePath: '',
            modelPath: request.modelPath ?? '',
            sourceKind: request.sourceKind ?? 'execution-preflight',
            diagnostics: ['AutoCut local speech transcription executablePath is not configured; select or bundle whisper-cli before running Smart Slice.'],
          };
        },
        transcribeMedia: async (request) => {
          missingExecutableLocalProviderCommands.push({ kind: 'transcribe', request });
          return createStandardNativeVideoSliceTranscriptResult(request, 'missing-executable-local-transcript-task');
        },
      });
      await assertRejects(
        () => services.transcribeAutoCutMediaWithConfiguredProvider({
          assetUuid: 'provider-local-missing-executable-asset',
          language: 'en',
          outputRootDir: 'D:/autocut/media',
        }),
        'executablePath',
        'transcribeAutoCutMediaWithConfiguredProvider blocks local STT initialization when the Whisper executable is not configured or bundled',
      );
    });
    assertEqual(
      missingExecutableLocalProviderCommands[0]?.kind,
      'probe',
      'transcribeAutoCutMediaWithConfiguredProvider probes local STT setup before deciding whether executable initialization is possible',
    );
    assertRule(
      !missingExecutableLocalProviderCommands.some((entry) => entry.kind === 'download-model' || entry.kind === 'transcribe'),
      'transcribeAutoCutMediaWithConfiguredProvider does not download models or dispatch transcription before a local STT executable is configured',
    );
    assertRule(
      missingExecutableSpeechDiagnostics.some((call) =>
        call.level === 'error' &&
        String(call.args?.[0] ?? '').includes('[AutoCut:speech-transcription] Local speech-to-text executable is missing')
      ),
      'transcribeAutoCutMediaWithConfiguredProvider logs missing local STT executable diagnostics to the browser console',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const incompleteLocalProviderTranscriptionRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      probeSpeechTranscription: async (request) => ({
        ready: false,
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [
          'AutoCut local speech-to-text model is missing or incomplete; download the recommended offline Whisper model again.',
        ],
      }),
      transcribeMedia: async (request) => {
        incompleteLocalProviderTranscriptionRequests.push(request);
        return createStandardNativeVideoSliceTranscriptResult(request, 'incomplete-local-model-task');
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'provider-local-incomplete-model-asset',
        language: 'en',
        outputRootDir: 'D:/autocut/media',
      }),
      'Use and download the recommended offline Whisper model',
      'transcribeAutoCutMediaWithConfiguredProvider blocks execution when the fresh local STT model probe reports an incomplete model',
    );
    assertEqual(
      incompleteLocalProviderTranscriptionRequests.length,
      0,
      'transcribeAutoCutMediaWithConfiguredProvider does not dispatch native transcription when the local STT model is incomplete',
    );
    const speechSettingsAfterIncompletePreflight = await services.getAutoCutSettings();
    assertEqual(
      speechSettingsAfterIncompletePreflight.speechTranscription.lastProbeReady,
      false,
      'transcribeAutoCutMediaWithConfiguredProvider stores failed execution preflight readiness on settings',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const missingProbeCommandTranscriptionRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: false,
        speechTranscriptionModelDownloadCommandReady: true,
      }),
      transcribeMedia: async (request) => {
        missingProbeCommandTranscriptionRequests.push(request);
        return createStandardNativeVideoSliceTranscriptResult(request, 'missing-probe-command-task');
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'provider-local-missing-probe-command-asset',
        language: 'en',
        outputRootDir: 'D:/autocut/media',
      }),
      'desktop host speech-to-text validation',
      'transcribeAutoCutMediaWithConfiguredProvider blocks local STT execution when the desktop host cannot validate model integrity',
    );
    assertEqual(
      missingProbeCommandTranscriptionRequests.length,
      0,
      'transcribeAutoCutMediaWithConfiguredProvider does not dispatch native transcription without execution-time local STT validation',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const localProviderTranscriptionRequests = [];
    const localProviderExecutionProbeRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      transcribeMedia: async (request) => {
        localProviderTranscriptionRequests.push(request);
        return createStandardNativeVideoSliceTranscriptResult(request, 'provider-local-transcript-task');
      },
      probeSpeechTranscription: async (request) => ({
        ...(localProviderExecutionProbeRequests.push(request), {
          ready: Boolean(request.executablePath && request.modelPath),
          executablePath: request.executablePath,
          modelPath: request.modelPath,
          sourceKind: request.sourceKind ?? 'settings',
          diagnostics: [],
          versionLine: 'provider contract',
        }),
      }),
    });
    const localProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertEqual(
      localProviderExecutionProbeRequests[0]?.sourceKind,
      'execution-preflight',
      'transcribeAutoCutMediaWithConfiguredProvider performs a fresh execution preflight probe before local STT dispatch',
    );
    assertEqual(localProviderTranscription.segments.length, 2, 'transcribeAutoCutMediaWithConfiguredProvider returns normalized local provider segments');
    assertSegmentsOrderedAndNonOverlapping(
      localProviderTranscription.segments,
      'transcribeAutoCutMediaWithConfiguredProvider returns local STT segments as an ordered non-overlapping timeline',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const fastPreviewLocalProviderRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      probeSpeechTranscription: async (request) => ({
        ready: Boolean(request.executablePath && request.modelPath),
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'execution-preflight',
        diagnostics: [],
        versionLine: 'fast preview STT workflow preset contract',
      }),
      transcribeMedia: async (request) => {
        fastPreviewLocalProviderRequests.push(request);
        return createStandardNativeVideoSliceTranscriptResult(request, 'provider-local-fast-preview-transcript-task');
      },
    });
    const fastPreviewLocalProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-fast-preview-asset',
      language: 'auto',
      outputRootDir: 'D:/autocut/media',
      workflowPurpose: 'smart-slice-transcript-evidence',
      sttPresetId: 'smart-slice-fast-preview',
    });
    assertEqual(
      fastPreviewLocalProviderTranscription.sttPresetId,
      'smart-slice-fast-preview',
      'transcribeAutoCutMediaWithConfiguredProvider returns the resolved STT workflow preset id for Smart Slice evidence',
    );
    assertEqual(
      fastPreviewLocalProviderTranscription.executionProfile,
      'fast-preview',
      'transcribeAutoCutMediaWithConfiguredProvider returns the resolved STT execution profile for Smart Slice evidence',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.sttPresetId,
      'smart-slice-fast-preview',
      'transcribeAutoCutMediaWithConfiguredProvider forwards the selected STT workflow preset id to native transcription',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.sttExecutionProfile,
      'fast-preview',
      'transcribeAutoCutMediaWithConfiguredProvider forwards the selected STT execution profile to native transcription',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperChunkParallelism,
      4,
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview chunk parallelism to native long-video STT',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperChunkThreadCount,
      2,
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview per-chunk thread count to native long-video STT',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperChunkSourceStrategy,
      'source-direct',
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview source-direct chunking to native long-video STT',
    );
    assertNotOwnProperty(
      fastPreviewLocalProviderRequests[0],
      'whisperAudioContext',
      'transcribeAutoCutMediaWithConfiguredProvider does not forward hallucination-prone audio context truncation by default',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperBeamSize,
      1,
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview local Whisper beam size to native STT',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperBestOf,
      1,
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview local Whisper best-of count to native STT',
    );
    assertEqual(
      fastPreviewLocalProviderRequests[0]?.whisperNoFallback,
      true,
      'transcribeAutoCutMediaWithConfiguredProvider forwards fast-preview local Whisper no-fallback mode to native STT',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const repeatedSpeechPhrase = decodeEscapedText('\\u540c\\u65f6\\u7ed9\\u81ea\\u5df1\\u7559\\u8db3');
    const repeatedSpeechTail = decodeEscapedText('\\u957f\\u671f\\u7684\\u53d1\\u5c55\\u56f0\\u4ef6');
    const repeatedSpeechText = `${repeatedSpeechPhrase},${repeatedSpeechPhrase},${repeatedSpeechTail}.`;
    const repeatedSpeechRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      transcribeMedia: async (request) => {
        repeatedSpeechRequests.push(request);
        return createNativeVideoSliceTranscriptResult(
          request,
          `provider-local-repeat-dedupe-${repeatedSpeechRequests.length}`,
          'D:/autocut/media',
          [
            {
              startMs: 1000,
              endMs: 7200,
              text: repeatedSpeechText,
              speaker: 'Speaker 1',
            },
            {
              startMs: 7600,
              endMs: 12200,
              text: 'Keep the rest of the transcript intact.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: Boolean(request.executablePath && request.modelPath),
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'settings',
        diagnostics: [],
        versionLine: 'provider repeated speech dedupe contract',
      }),
    });
    const dedupedRepeatedSpeechTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-repeat-dedupe-asset',
      language: 'auto',
      outputRootDir: 'D:/autocut/media',
      dedupeRepeatedSpeech: true,
    });
    const preservedRepeatedSpeechTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-repeat-preserve-asset',
      language: 'auto',
      outputRootDir: 'D:/autocut/media',
      dedupeRepeatedSpeech: false,
    });
    assertEqual(
      repeatedSpeechRequests[0]?.dedupeRepeatedSpeech,
      true,
      'transcribeAutoCutMediaWithConfiguredProvider forwards enabled STT repeated-speech dedupe to the provider request',
    );
    assertEqual(
      repeatedSpeechRequests[1]?.dedupeRepeatedSpeech,
      false,
      'transcribeAutoCutMediaWithConfiguredProvider forwards disabled STT repeated-speech dedupe to the provider request',
    );
    assertEqual(
      dedupedRepeatedSpeechTranscription.segments[0]?.text,
      `${repeatedSpeechPhrase},${repeatedSpeechTail}.`,
      'transcribeAutoCutMediaWithConfiguredProvider removes adjacent repeated speech phrases during STT normalization when enabled',
    );
    assertRule(
      !dedupedRepeatedSpeechTranscription.text.includes(`${repeatedSpeechPhrase},${repeatedSpeechPhrase}`),
      'transcribeAutoCutMediaWithConfiguredProvider returns aggregate transcript text without adjacent repeated speech when STT dedupe is enabled',
    );
    assertEqual(
      preservedRepeatedSpeechTranscription.segments[0]?.text,
      repeatedSpeechText,
      'transcribeAutoCutMediaWithConfiguredProvider preserves repeated speech phrases when STT dedupe is disabled',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    const secondsScaleLocalProviderRequests = [];
    configureAutoCutNativeHostClient(services, {
      getCapabilities: async () => ({
        mediaImportCommandReady: true,
        speechTranscriptionCommandReady: true,
        speechTranscriptionToolchainReady: true,
        speechTranscriptionProbeCommandReady: true,
      }),
      transcribeMedia: async (request) => {
        secondsScaleLocalProviderRequests.push(request);
        return createNativeVideoSliceTranscriptResult(
          request,
          'provider-local-seconds-scale-transcript-task',
          'D:/autocut/media',
          [
            {
              startMs: 0,
              endMs: 8.5,
              text:
                'Why provider timestamp normalization matters is simple. Because tiny speech ranges break planning, the provider result must be repaired before storage.',
              speaker: 'Speaker 1',
            },
            {
              startMs: 8.52,
              endMs: 19,
              text:
                'So the transcription service converts seconds-scale millisecond fields into a standard millisecond timeline.',
              speaker: 'Speaker 1',
            },
          ],
        );
      },
      probeSpeechTranscription: async (request) => ({
        ready: Boolean(request.executablePath && request.modelPath),
        executablePath: request.executablePath,
        modelPath: request.modelPath,
        sourceKind: request.sourceKind ?? 'settings',
        diagnostics: [],
        versionLine: 'provider seconds timestamp contract',
      }),
    });
    const secondsScaleLocalProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'provider-local-seconds-scale-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertEqual(
      secondsScaleLocalProviderTranscription.segments.at(-1)?.endMs,
      19_000,
      'transcribeAutoCutMediaWithConfiguredProvider normalizes seconds-scale startMs/endMs provider timestamps to milliseconds',
    );
    assertSegmentsOrderedAndNonOverlapping(
      secondsScaleLocalProviderTranscription.segments,
      'transcribeAutoCutMediaWithConfiguredProvider keeps seconds-normalized provider timestamps ordered and non-overlapping',
    );
    assertEqual(
      secondsScaleLocalProviderRequests.length,
      1,
      'transcribeAutoCutMediaWithConfiguredProvider still dispatches seconds-scale provider transcription exactly once',
    );
    await saveVerifiedLocalSpeechTranscriptionSettings(services, {
      language: speechTranscriptionSettingsAfterProbe.speechTranscription.language,
    });
    assertEqual(
      localProviderTranscriptionRequests[0]?.providerId,
      'local-whisper-cli',
      'transcribeAutoCutMediaWithConfiguredProvider sends the selected local providerId to native transcription',
    );
    assertEqual(
      localProviderTranscriptionRequests[0]?.executablePath,
      'D:/tools/whisper-cli.exe',
      'transcribeAutoCutMediaWithConfiguredProvider sends the configured executablePath only from the local provider adapter',
    );
    const apiProviderBridgeCalls = [];
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe(request, runtime) {
        apiProviderBridgeCalls.push({ request, runtime });
        return {
          artifactUuid: 'api-provider-transcript-artifact',
          taskUuid: 'api-provider-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: '',
          taskOutputDir: request.outputRootDir ?? '',
          language: request.language ?? runtime.language,
          text: 'API provider transcript.',
          segments: [
            {
              startMs: 1000,
              endMs: 4200,
              text: 'API provider transcript.',
              speaker: 'Speaker 1',
            },
          ],
          providerId: runtime.providerId,
        };
      },
      async test(runtime) {
        return {
          ready: true,
          providerId: runtime.providerId,
          sourceKind: 'api-bridge',
          diagnostics: [],
        };
      },
    });
    await services.saveAutoCutSpeechTranscriptionSettings({
      ...speechTranscriptionSettings.speechTranscription,
      providerId: 'openai-transcription',
      modelVendor: 'openai',
      model: 'gpt-4o-transcribe',
      executablePath: '',
      modelPath: '',
      language: 'auto',
    });
    const apiProviderRuntimeConfig = await services.resolveAutoCutSpeechTranscriptionRuntimeConfig();
    assertEqual(apiProviderRuntimeConfig.providerId, 'openai-transcription', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the selected API STT provider id');
    assertEqual(apiProviderRuntimeConfig.modelVendor, 'openai', 'resolveAutoCutSpeechTranscriptionRuntimeConfig returns the selected API STT ModelVendor');
    assertEqual(apiProviderRuntimeConfig.configured, false, 'resolveAutoCutSpeechTranscriptionRuntimeConfig fails closed for API providers until the matching API key is configured');
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-asset-unconfigured',
        language: 'en',
      }),
      'API key is required',
      'transcribeAutoCutMediaWithConfiguredProvider fails closed before dispatching an unconfigured API provider',
    );
    assertEqual(apiProviderBridgeCalls.length, 0, 'unconfigured API STT provider does not dispatch to the API bridge');
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'openai',
      model: 'gpt-5.2',
      apiKey: 'sk-stt-openai-provider',
    });
    services.configureAutoCutSpeechTranscriptionProviderBridge(null);
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-missing-bridge-asset',
        language: 'en',
      }),
      'configured speech transcription provider bridge',
      'transcribeAutoCutMediaWithConfiguredProvider blocks configured API STT providers when the provider bridge is unavailable',
    );
    assertEqual(apiProviderBridgeCalls.length, 0, 'API STT provider does not dispatch when its bridge is unavailable');
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe(request, runtime) {
        apiProviderBridgeCalls.push({ request, runtime });
        return {
          artifactUuid: 'api-provider-transcript-artifact',
          taskUuid: 'api-provider-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: '',
          taskOutputDir: request.outputRootDir ?? '',
          language: request.language ?? runtime.language,
          text: 'API provider transcript.',
          segments: [
            {
              startMs: 1000,
              endMs: 4200,
              text: 'API provider transcript.',
              speaker: 'Speaker 1',
            },
          ],
          providerId: runtime.providerId,
        };
      },
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-stt-gemini-provider',
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-mismatched-vendor-asset',
        language: 'en',
      }),
      'matching ModelVendor openai',
      'transcribeAutoCutMediaWithConfiguredProvider blocks API STT providers when the configured ModelVendor does not match the selected provider',
    );
    assertEqual(apiProviderBridgeCalls.length, 0, 'API STT provider does not dispatch when its ModelVendor runtime is mismatched');
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'openai',
      model: 'gpt-5.2',
      apiKey: 'sk-stt-openai-provider',
    });
    const configuredApiProviderTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'api-provider-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertEqual(configuredApiProviderTranscription.providerId, 'openai-transcription', 'API STT provider transcription result records the selected provider id');
    assertEqual(
      configuredApiProviderTranscription.standardTranscript?.schema,
      'smart-slice.transcript.v1',
      'API STT provider transcription returns the canonical Smart Slice transcript schema',
    );
    assertEqual(
      configuredApiProviderTranscription.standardTranscript?.segments?.[0]?.speakerId,
      'speaker-1',
      'API STT provider transcription normalizes speaker labels into stable speaker ids',
    );
    assertEqual(
      configuredApiProviderTranscription.standardTranscript?.speakers?.[0]?.label,
      'Speaker 1',
      'API STT provider transcription records speaker labels for diarization-aware planning',
    );
    assertEqual(configuredApiProviderTranscription.segments[0]?.text, 'API provider transcript.', 'API STT provider bridge returns normalized transcript segments');
    assertEqual(apiProviderBridgeCalls.length, 1, 'configured API STT provider dispatches exactly once through the standard provider bridge');
    assertEqual(apiProviderBridgeCalls[0]?.runtime.providerId, 'openai-transcription', 'API STT provider bridge receives the selected provider runtime');
    assertEqual(apiProviderBridgeCalls[0]?.runtime.sessionApiKey, 'sk-stt-openai-provider', 'API STT provider bridge receives the API key only inside runtime bridge context');
    const smartSliceCloudPreset = types.AUTOCUT_SPEECH_TRANSCRIPTION_WORKFLOW_PRESETS.find((preset) => preset.id === 'smart-slice-cloud-stt');
    assertEqual(
      smartSliceCloudPreset?.available,
      true,
      'Smart Slice cloud STT workflow preset is available as the commercial default provider route',
    );
    assertEqual(
      smartSliceCloudPreset?.recommended,
      true,
      'Smart Slice cloud STT workflow preset is marked as the commercial recommended mode',
    );
    assertEqual(
      types.AUTOCUT_DEFAULT_SPEECH_TRANSCRIPTION_WORKFLOW_PRESET_ID,
      'smart-slice-cloud-stt',
      'Smart Slice default STT workflow preset uses cloud STT for commercial large-video throughput',
    );
    assertEqual(
      types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((provider) => provider.id === 'openai-transcription')
        ?.capabilities.supportsSpeakerDiarization,
      true,
      'OpenAI STT provider declares speaker diarization support for multi-speaker Smart Slice modes',
    );
    const standardCloudSttProviderIds = [
      'iflytek-long-form',
      'volcengine-asr',
      'qwen-paraformer',
      'tencent-cloud-asr',
      'baidu-cloud-asr',
    ];
    assertDeepEqual(
      types.AUTOCUT_SMART_SLICE_CLOUD_STT_PROVIDER_IDS,
      standardCloudSttProviderIds,
      'AutoCut Smart Slice standard cloud STT provider set covers iFlytek, Volcengine, Qwen Paraformer, Tencent Cloud, and Baidu Cloud',
    );
    for (const providerId of standardCloudSttProviderIds) {
      const provider = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((entry) => entry.id === providerId);
      assertRule(Boolean(provider), `AutoCut STT registry defines ${providerId}`);
      assertEqual(provider?.kind, 'api', `${providerId} is routed through the API provider bridge`);
      assertEqual(provider?.capabilities.preferredForLongForm, true, `${providerId} is marked as long-form preferred`);
      assertEqual(provider?.capabilities.supportsTimestamps, true, `${providerId} declares timestamp support`);
      assertEqual(provider?.capabilities.supportsSegments, true, `${providerId} declares segment support`);
      assertEqual(provider?.capabilities.requiresLongAudio, true, `${providerId} requires long-audio recognition`);
      assertEqual(provider?.capabilities.requiresSegmentTimestamps, true, `${providerId} requires segment timestamps`);
      assertRule(
        typeof provider?.officialDocsUrl === 'string' && provider.officialDocsUrl.startsWith('https://'),
        `${providerId} records an official HTTPS documentation URL`,
      );
      assertRule(
        Array.isArray(provider?.optionSchema) && provider.optionSchema.length > 0,
        `${providerId} exposes provider option schema for Settings Center`,
      );
      assertRule(
        provider?.optionSchema?.some((option) =>
          option.requiredForSmartSlice === true &&
            option.defaultValue === true &&
            option.locked === true
        ),
        `${providerId} locks at least one Smart Slice timestamp option on`,
      );
    }
    const qwenParaformerProvider = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((provider) => provider.id === 'qwen-paraformer');
    assertRule(
      qwenParaformerProvider?.optionSchema?.some((option) => option.key === 'enableWordTimestamps' && option.defaultValue === true),
      'Qwen Paraformer STT defaults word timestamps on for subtitle and semantic clip construction',
    );
    assertRule(
      qwenParaformerProvider?.optionSchema?.some((option) => option.key === 'enableSpeakerDiarization' && option.defaultValue === true),
      'Qwen Paraformer STT defaults speaker diarization on when the provider bridge supports it',
    );
    const volcengineProvider = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((provider) => provider.id === 'volcengine-asr');
    assertRule(
      volcengineProvider?.optionSchema?.some((option) => option.key === 'showUtterances' && option.defaultValue === true),
      'Volcengine ASR locks utterance output on so provider results can be normalized into transcript segments',
    );
    const tencentProvider = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((provider) => provider.id === 'tencent-cloud-asr');
    assertRule(
      tencentProvider?.optionSchema?.some((option) => option.key === 'sentenceMaxLength' && option.kind === 'number'),
      'Tencent Cloud ASR exposes sentence segmentation length as a typed Settings Center parameter',
    );
    const baiduProvider = types.AUTOCUT_SPEECH_TRANSCRIPTION_PROVIDER_DEFINITIONS.find((provider) => provider.id === 'baidu-cloud-asr');
    assertRule(
      baiduProvider?.optionSchema?.some((option) => option.key === 'enableTimestamps' && option.locked === true),
      'Baidu Cloud ASR locks timestamp output on for Smart Slice',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe(request, runtime) {
        return {
          artifactUuid: 'api-provider-unsorted-overlap-artifact',
          taskUuid: 'api-provider-unsorted-overlap-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: '',
          taskOutputDir: request.outputRootDir ?? '',
          language: request.language ?? runtime.language,
          segments: [
            {
              startMs: 4_000,
              endMs: 7_000,
              text: 'Second API sentence.',
            },
            {
              startMs: 1_000,
              endMs: 4_050,
              text: 'First API sentence.',
            },
          ],
          providerId: runtime.providerId,
        };
      },
    });
    const normalizedApiProviderTimeline = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'api-provider-unsorted-overlap-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertDeepEqual(
      normalizedApiProviderTimeline.segments.map((segment) => ({
        startMs: segment.startMs,
        endMs: segment.endMs,
        text: segment.text,
      })),
      [
        {
          startMs: 1_000,
          endMs: 4_050,
          text: 'First API sentence.',
        },
        {
          startMs: 4_050,
          endMs: 7_000,
          text: 'Second API sentence.',
        },
      ],
      'transcribeAutoCutMediaWithConfiguredProvider sorts and repairs lightly overlapping provider STT segments',
    );
    assertSegmentsOrderedAndNonOverlapping(
      normalizedApiProviderTimeline.segments,
      'transcribeAutoCutMediaWithConfiguredProvider normalizes provider STT segments into a stable planning timeline',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-tail-filler-overlap-artifact',
          taskUuid: 'api-provider-tail-filler-overlap-task',
          segments: [
            {
              startMs: 43_200,
              endMs: 60_900,
              text: 'Because the tail example shows the real payoff, so the solution clamps only the final segment and preserves the complete story.',
            },
            {
              startMs: 59_980,
              endMs: 61_100,
              text: 'um',
            },
          ],
        };
      },
    });
    const normalizedApiProviderTailFillerTimeline = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'api-provider-tail-filler-overlap-asset',
      language: 'en',
      outputRootDir: 'D:/autocut/media',
    });
    assertDeepEqual(
      normalizedApiProviderTailFillerTimeline.segments.map((segment) => ({
        startMs: segment.startMs,
        endMs: segment.endMs,
        text: segment.text,
      })),
      [
        {
          startMs: 43_200,
          endMs: 60_900,
          text: 'Because the tail example shows the real payoff, so the solution clamps only the final segment and preserves the complete story.',
        },
      ],
      'transcribeAutoCutMediaWithConfiguredProvider drops severely overlapping low-information filler instead of rejecting the STT timeline',
    );
    assertSegmentsOrderedAndNonOverlapping(
      normalizedApiProviderTailFillerTimeline.segments,
      'transcribeAutoCutMediaWithConfiguredProvider keeps tail filler overlap repair output ordered and non-overlapping',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-severe-overlap-artifact',
          taskUuid: 'api-provider-severe-overlap-task',
          segments: [
            {
              startMs: 1_000,
              endMs: 4_500,
              text: 'First severe overlap sentence.',
            },
            {
              startMs: 4_000,
              endMs: 7_000,
              text: 'Second severe overlap sentence.',
            },
          ],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-severe-overlap-asset',
        language: 'en',
      }),
      'overlapping speech segments',
      'transcribeAutoCutMediaWithConfiguredProvider rejects provider STT segments whose overlap exceeds the repair tolerance',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-empty-segments-artifact',
          taskUuid: 'api-provider-empty-segments-task',
          segments: [],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-empty-segments-asset',
        language: 'en',
      }),
      'valid timestamped speech segments',
      'transcribeAutoCutMediaWithConfiguredProvider rejects API provider results with no structured speech segments',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-blank-segment-artifact',
          taskUuid: 'api-provider-blank-segment-task',
          segments: [
            {
              startMs: 1000,
              endMs: 2200,
              text: '   ',
              speaker: 'Speaker 1',
            },
          ],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-blank-segment-asset',
        language: 'en',
      }),
      'segment 1 to contain recognized speech text',
      'transcribeAutoCutMediaWithConfiguredProvider rejects API provider segments with blank transcript text',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-zero-duration-segment-artifact',
          taskUuid: 'api-provider-zero-duration-segment-task',
          segments: [
            {
              startMs: 2400,
              endMs: 2400,
              text: 'Zero duration speech.',
              speaker: 'Speaker 1',
            },
          ],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-zero-duration-segment-asset',
        language: 'en',
      }),
      'segment 1 endMs to be after startMs',
      'transcribeAutoCutMediaWithConfiguredProvider rejects API provider segments with zero speech duration',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-nonfinite-timestamp-artifact',
          taskUuid: 'api-provider-nonfinite-timestamp-task',
          segments: [
            {
              startMs: Number.NaN,
              endMs: 3200,
              text: 'Invalid timestamp speech.',
              speaker: 'Speaker 1',
            },
          ],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-nonfinite-timestamp-asset',
        language: 'en',
      }),
      'segment 1 startMs to be a finite non-negative timestamp',
      'transcribeAutoCutMediaWithConfiguredProvider rejects API provider segments with non-finite speech timestamps',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async transcribe() {
        return {
          artifactUuid: 'api-provider-negative-timestamp-artifact',
          taskUuid: 'api-provider-negative-timestamp-task',
          segments: [
            {
              startMs: -1200,
              endMs: 3200,
              text: 'Negative timestamp speech.',
              speaker: 'Speaker 1',
            },
          ],
        };
      },
    });
    await assertRejects(
      () => services.transcribeAutoCutMediaWithConfiguredProvider({
        assetUuid: 'api-provider-negative-timestamp-asset',
        language: 'en',
      }),
      'segment 1 startMs to be a finite non-negative timestamp',
      'transcribeAutoCutMediaWithConfiguredProvider rejects API provider segments with negative speech timestamps',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge({
      async test(runtime) {
        return {
          ready: true,
          providerId: runtime.providerId,
          sourceKind: 'provider-bridge',
          diagnostics: [`${runtime.providerId} provider bridge ready`],
        };
      },
      async transcribe(request, runtime) {
        apiProviderBridgeCalls.push({ request, runtime });
        return {
          artifactUuid: 'qwen-paraformer-transcript-artifact',
          taskUuid: 'qwen-paraformer-transcript-task',
          sourceAssetUuid: request.assetUuid,
          transcriptPath: '',
          taskOutputDir: request.outputRootDir ?? '',
          language: request.language ?? runtime.language,
          text: 'Qwen Paraformer long audio transcript.',
          segments: [
            {
              startMs: 1200,
              endMs: 2600,
              text: 'Qwen Paraformer long audio transcript.',
              speaker: 'Speaker 1',
              words: [
                { startMs: 1200, endMs: 1680, text: 'Qwen' },
                { startMs: 1680, endMs: 2300, text: 'Paraformer' },
              ],
            },
          ],
          providerId: runtime.providerId,
        };
      },
    });
    const qwenParaformerSettings = await services.saveAutoCutSpeechTranscriptionSettings({
      ...(await services.getAutoCutSettings()).speechTranscription,
      providerId: 'qwen-paraformer',
      language: 'zh',
      providerOptions: {
        enableSegmentTimestamps: false,
        enableWordTimestamps: true,
        enableSpeakerDiarization: true,
        audioFormat: 'auto',
        channelId: 0,
      },
    });
    assertEqual(qwenParaformerSettings.speechTranscription.providerId, 'qwen-paraformer', 'saveAutoCutSpeechTranscriptionSettings persists Qwen Paraformer STT provider id');
    assertEqual(
      qwenParaformerSettings.speechTranscription.providerOptions?.enableSegmentTimestamps,
      true,
      'saveAutoCutSpeechTranscriptionSettings repairs locked Smart Slice segment timestamp options to true',
    );
    assertEqual(
      qwenParaformerSettings.speechTranscription.providerOptions?.enableWordTimestamps,
      true,
      'saveAutoCutSpeechTranscriptionSettings preserves enabled cloud STT word timestamp options',
    );
    assertEqual(
      qwenParaformerSettings.speechTranscription.providerOptions?.audioFormat,
      'auto',
      'saveAutoCutSpeechTranscriptionSettings persists selected cloud STT enum options',
    );
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'qwen',
      model: 'qwen3.6-plus',
      apiKey: 'sk-stt-qwen-paraformer',
    });
    const qwenParaformerTranscription = await services.transcribeAutoCutMediaWithConfiguredProvider({
      assetUuid: 'qwen-paraformer-long-audio-asset',
      language: 'zh',
      outputRootDir: 'D:/autocut/media',
    });
    const qwenParaformerBridgeCall = apiProviderBridgeCalls.at(-1);
    assertEqual(qwenParaformerTranscription.providerId, 'qwen-paraformer', 'Qwen Paraformer STT transcription records the selected provider id');
    assertEqual(qwenParaformerBridgeCall?.runtime.providerId, 'qwen-paraformer', 'STT provider bridge receives Qwen Paraformer runtime provider id');
    assertEqual(qwenParaformerBridgeCall?.runtime.providerOptions?.enableSegmentTimestamps, true, 'STT provider bridge receives locked segment timestamp option');
    assertEqual(qwenParaformerBridgeCall?.runtime.providerOptions?.enableWordTimestamps, true, 'STT provider bridge receives word timestamp option');
    assertEqual(qwenParaformerBridgeCall?.runtime.providerOptions?.enableSpeakerDiarization, true, 'STT provider bridge receives speaker diarization option');
    assertEqual(qwenParaformerBridgeCall?.runtime.sessionApiKey, 'sk-stt-qwen-paraformer', 'STT provider bridge receives provider API key only in runtime context');
    assertEqual(
      qwenParaformerTranscription.standardTranscript?.segments?.[0]?.startMs,
      1200,
      'Cloud STT standard transcript preserves provider segment start time for semantic clip construction',
    );
    assertEqual(
      qwenParaformerTranscription.standardTranscript?.segments?.[0]?.words?.[0]?.startMs,
      1200,
      'Cloud STT standard transcript preserves provider word timestamps for WYSIWYG timeline preview',
    );
    const qwenParaformerProbe = await services.testAutoCutSpeechTranscriptionProvider();
    assertEqual(qwenParaformerProbe.ready, true, 'testAutoCutSpeechTranscriptionProvider accepts configured cloud STT provider bridge probes');
    const qwenParaformerSettingsAfterProbe = await services.getAutoCutSettings();
    const qwenParaformerChangedOptions = await services.saveAutoCutSpeechTranscriptionSettings({
      ...qwenParaformerSettingsAfterProbe.speechTranscription,
      providerOptions: {
        ...qwenParaformerSettingsAfterProbe.speechTranscription.providerOptions,
        enableSpeakerDiarization: false,
      },
    });
    assertEqual(
      qwenParaformerChangedOptions.speechTranscription.lastProbeReady,
      undefined,
      'saveAutoCutSpeechTranscriptionSettings clears cloud STT probe state when provider options change',
    );
    services.configureAutoCutSpeechTranscriptionProviderBridge(null);
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      modelVendor: 'gemini',
    });
    await services.saveAutoCutLlmSettings({
      ...(await services.getAutoCutSettings()).llm,
      model: 'gemini-3-flash-preview',
      apiKey: 'sk-native-stored-secret',
    });
    const llmBridgeCalls = [];
    services.configureAutoCutApprovedAiSdkBridge({
      async createChatCompletion(request, runtime) {
        llmBridgeCalls.push({ request, runtime });
        return {
          id: 'llm-test-contract',
          model: request.model,
          content: 'pong',
          runtime,
        };
      },
    });
    const llmConnectionTestResult = await services.testAutoCutLlmConnection();
    services.configureAutoCutApprovedAiSdkBridge(null);
    assertEqual(llmConnectionTestResult.success, true, 'testAutoCutLlmConnection reports success when the approved AI SDK bridge responds');
    assertEqual(llmConnectionTestResult.modelVendor, 'gemini', 'testAutoCutLlmConnection reports the active ModelVendor');
    assertEqual(llmConnectionTestResult.model, 'gemini-3-flash-preview', 'testAutoCutLlmConnection reports the active model');
    assertEqual(llmConnectionTestResult.content, 'pong', 'testAutoCutLlmConnection returns the bridge response content');
    assertEqual(llmBridgeCalls.length, 1, 'testAutoCutLlmConnection performs exactly one bridge chat completion call');
    assertEqual(llmBridgeCalls[0]?.request.model, 'gemini-3-flash-preview', 'testAutoCutLlmConnection sends the active runtime model to the bridge');
    assertEqual(llmBridgeCalls[0]?.request.maxTokens, 16, 'testAutoCutLlmConnection uses a small output budget for the test request');
    assertEqual(llmBridgeCalls[0]?.runtime.baseUrl, 'https://generativelanguage.googleapis.com/v1beta/openai', 'testAutoCutLlmConnection passes normalized runtime config to the bridge');
    assertEqual(llmBridgeCalls[0]?.runtime.sessionApiKey, 'sk-native-stored-secret', 'testAutoCutLlmConnection passes the restored native-secret API key only inside runtime bridge context');
    const vercelAiSdkBridgeInputs = [];
    services.configureAutoCutVercelAiSdkBridge(async (input) => {
      vercelAiSdkBridgeInputs.push(input);
      return {
        id: 'vercel-ai-sdk-test',
        model: input.request.model,
        content: 'pong',
        runtime: input.runtime,
      };
    });
    const vercelBridgeConnectionTestResult = await services.testAutoCutLlmConnection();
    services.configureAutoCutApprovedAiSdkBridge(null);
    assertEqual(vercelBridgeConnectionTestResult.success, true, 'configureAutoCutVercelAiSdkBridge wires a working Vercel AI SDK connection test');
    assertEqual(vercelBridgeConnectionTestResult.content, 'pong', 'configureAutoCutVercelAiSdkBridge normalizes Vercel AI SDK text content');
    assertEqual(vercelAiSdkBridgeInputs[0]?.provider.baseURL, 'https://generativelanguage.googleapis.com/v1beta/openai', 'Vercel AI SDK bridge receives the active OpenAI-compatible base URL');
    assertEqual(vercelAiSdkBridgeInputs[0]?.provider.apiKey, 'sk-native-stored-secret', 'Vercel AI SDK bridge receives the restored native-secret API key without persisting it');
    assertEqual(vercelAiSdkBridgeInputs[0]?.provider.name, 'gemini', 'Vercel AI SDK bridge names the provider from the active ModelVendor');
    assertEqual(vercelAiSdkBridgeInputs[0]?.request.model, 'gemini-3-flash-preview', 'Vercel AI SDK bridge uses the active model');
    assertEqual(vercelAiSdkBridgeInputs[0]?.request.maxTokens, 16, 'Vercel AI SDK bridge receives the normalized maxTokens budget');
    assertEqual(vercelAiSdkBridgeInputs[0]?.request.temperature, 0, 'Vercel AI SDK bridge receives the normalized temperature');
    assertEqual(vercelAiSdkBridgeInputs[0]?.request.messages[0]?.content, 'Reply with exactly: pong', 'Vercel AI SDK bridge receives the connection-test prompt');
    assertRule(typeof vercelAiSdkBridgeInputs[0]?.provider.fetch === 'function', 'Vercel AI SDK bridge configures desktop-native fetch for OpenAI-compatible calls');
    const llmSecretDeletedSettings = await services.clearAutoCutLlmApiKey();
    assertEqual(llmSecretDeletedSettings.llm.apiKeyConfigured, false, 'clearAutoCutLlmApiKey clears configured LLM API key state');
    assertEqual(llmSecretDeletedSettings.llm.maskedApiKey, undefined, 'clearAutoCutLlmApiKey clears masked LLM API key display state');
    assertRule(
      settingsNativeSecretCommands.some((entry) => entry.command === 'autocut_delete_llm_secret'),
      'clearAutoCutLlmApiKey deletes LLM API keys from the native secret store',
    );
    resetStorage();
    await assertRejects(
      () => services.testAutoCutLlmConnection(),
      'API key',
      'testAutoCutLlmConnection fails closed when the LLM API key is not configured',
    );
    await services.saveAutoCutLlmSettings(llmGeminiFlashRuntimeSettings.llm);
    await assertRejects(
      () => services.createAutoCutOpenAiCompatibleChatCompletion({
        messages: [{ role: 'user', content: 'Generate a short AutoCut title.' }],
      }),
      'approved AI SDK bridge',
      'createAutoCutOpenAiCompatibleChatCompletion blocks raw HTTP fallback',
    );
    await assertRejects(
      () => services.createAutoCutOpenAiCompatibleChatCompletion({
        model: 'gpt-5.2',
        messages: [{ role: 'user', content: 'Generate a short AutoCut title.' }],
      }),
      'configured ModelVendor',
      'createAutoCutOpenAiCompatibleChatCompletion rejects a cross-provider model override',
    );
    assertEqual(apiKeySettings.apiKeys[0]?.name, 'Contract Key', 'createAutoCutApiKey prepends the new API key');
    assertRule(Boolean(revokedSettings.apiKeys[0]?.revokedAt), 'revokeAutoCutApiKey marks the key as revoked');
    assertEqual(cacheClearedSettings.storage.cacheGb, 0, 'clearAutoCutStorageCache resets cache size');
    assertRule(invoiceSettings.billing.invoicesLoaded > cacheClearedSettings.billing.invoicesLoaded, 'loadMoreAutoCutInvoices increments invoice count');
    assertRule(Boolean(avatarSettings.account.avatarChangeRequestedAt), 'requestAutoCutAvatarChange records an avatar request timestamp');
    assertRule(Boolean(subscriptionManagementSettings.billing.subscriptionManagementOpenedAt), 'openAutoCutSubscriptionManagement records a management open timestamp');
    assertRule(Boolean(passwordSettings.security.passwordChangeRequestedAt), 'requestAutoCutPasswordChange records a password change request timestamp');
    assertEqual(twoFactorSettings.security.twoFactorEnabled, true, 'setAutoCutTwoFactorEnabled persists 2FA status');
    assertRule(Boolean(sessionsRevokedSettings.security.sessionsRevokedAt), 'revokeAutoCutSessions stores a revocation timestamp');
    assertEqual(subscriptionSettings.billing.subscriptionActive, false, 'cancelAutoCutSubscription deactivates the subscription');
    assertRule(Boolean(deletedAccountSettings.security.accountDeletedAt), 'deleteAutoCutAccount stores the deletion timestamp');
    assertEqual(storedSettings.security.accountDeletedAt, deletedAccountSettings.security.accountDeletedAt, 'settings workflow writes the final state to storage');
    assertRule(settingsEvents.details.length >= 17, 'settings workflows dispatch settingsUpdated events');

    resetStorage();
    const isolatedRuntimeLlmSecretValues = new Map();
    const isolatedRuntimeSecretCommands = [];
    services.configureAutoCutRuntimeEnvironment('dev');
    configureAutoCutNativeHostClient(services, services.createAutoCutNativeHostClient(async (command, args) => {
      isolatedRuntimeSecretCommands.push({ command, args });
      if (command === 'autocut_host_capabilities') {
        return {
          contractVersion: 'settings-runtime-isolation-test',
          hostKind: 'native-host',
          databaseContractReady: true,
          sqliteMigrationReady: true,
          databaseHealthCommandReady: true,
          ffmpegProbeCommandReady: true,
          mediaImportCommandReady: true,
          mediaFileDescribeCommandReady: true,
          localVideoFileSelectCommandReady: true,
          audioExtractionCommandReady: true,
          audioExtractionFromAssetReady: true,
          videoGifCommandReady: true,
          videoCompressCommandReady: true,
          videoConvertCommandReady: true,
          videoEnhanceCommandReady: true,
          llmHttpCommandReady: true,
          llmSecretStoreReady: true,
          nativeTaskQueryCommandReady: true,
          nativeTaskCancelCommandReady: true,
          nativeTaskRecoveryCommandReady: true,
          nativeTaskRetryCommandReady: true,
          nativeTaskProgressEventsReady: true,
          nativeWorkerLeaseReady: true,
          ffmpegToolchainManifestReady: true,
          ffmpegToolchainResolverReady: true,
          ffmpegBundledReady: false,
          ffmpegExecutionReady: false,
          supportedCommands: [
            'autocut_save_llm_secret',
            'autocut_get_llm_secret',
            'autocut_delete_llm_secret',
          ],
        };
      }
      if (command === 'autocut_save_llm_secret') {
        isolatedRuntimeLlmSecretValues.set(args.request.secretName, args.request.secretValue);
        return {
          secretName: args.request.secretName,
          saved: true,
        };
      }
      if (command === 'autocut_get_llm_secret') {
        const secretValue = isolatedRuntimeLlmSecretValues.get(args.request.secretName);
        return {
          secretName: args.request.secretName,
          configured: Boolean(secretValue),
          ...(secretValue ? { secretValue } : {}),
        };
      }
      if (command === 'autocut_delete_llm_secret') {
        const deleted = isolatedRuntimeLlmSecretValues.delete(args.request.secretName);
        return {
          secretName: args.request.secretName,
          deleted,
        };
      }

      throw new Error(`Unexpected isolated runtime native host command: ${command}`);
    }));

    const devLlmSettings = await withImmediateTimers(async () => services.saveAutoCutLlmSettings({
      ...defaultSettings.llm,
      modelVendor: 'openai',
      baseUrl: 'https://api.openai.com/v1',
      model: 'gpt-5.2',
      apiKey: 'sk-dev-secret',
      maxTokens: 9000,
    }));
    const configuredDevOutputDirectory = 'D:/autocut-dev-output-root';
    const devWorkspaceSettings = await withImmediateTimers(async () => services.saveAutoCutWorkspaceSettings({
      ...(await services.getAutoCutSettings()).workspace,
      outputDirectory: configuredDevOutputDirectory,
    }));
    const devSpeechTranscriptionSettings = await withImmediateTimers(async () => services.saveAutoCutSpeechTranscriptionSettings({
      ...(await services.getAutoCutSettings()).speechTranscription,
      executablePath: 'D:/dev-tools/whisper-cli.exe',
      modelPath: 'D:/dev-models/ggml-large-v3-turbo.bin',
      language: 'zh',
    }));
    services.clearTransientAutoCutLlmApiKeyForTest();
    const devRuntimeAfterRestart = await withImmediateTimers(() => services.resolveAutoCutLlmRuntimeConfig());
    const devOutputDirectoryAfterRestart = await withImmediateTimers(() => services.resolveAutoCutOutputRootDir());
    const devSpeechTranscriptionAfterRestart = await withImmediateTimers(() => services.resolveAutoCutSpeechTranscriptionRuntimeConfig());
    services.configureAutoCutRuntimeEnvironment('release');
    const releaseSettingsAfterSwitch = await withImmediateTimers(() => services.getAutoCutSettings());
    const releaseRuntimeAfterSwitch = await withImmediateTimers(() => services.resolveAutoCutLlmRuntimeConfig());
    const releaseOutputDirectoryAfterSwitch = await withImmediateTimers(() => services.resolveAutoCutOutputRootDir());
    const releaseSpeechTranscriptionAfterSwitch = await withImmediateTimers(() => services.resolveAutoCutSpeechTranscriptionRuntimeConfig());

    assertEqual(devLlmSettings.llm.modelVendor, 'openai', 'dev runtime stores its selected LLM ModelVendor');
    assertEqual(devWorkspaceSettings.workspace.outputDirectory, configuredDevOutputDirectory, 'dev runtime stores its selected output directory');
    assertEqual(devSpeechTranscriptionSettings.speechTranscription.modelPath, 'D:/dev-models/ggml-large-v3-turbo.bin', 'dev runtime stores its selected local speech-to-text model path');
    assertEqual(devRuntimeAfterRestart.modelVendor, 'openai', 'dev runtime reloads the selected LLM ModelVendor after restart');
    assertEqual(devRuntimeAfterRestart.model, 'gpt-5.2', 'dev runtime reloads the selected LLM model after restart');
    assertEqual(devRuntimeAfterRestart.baseUrl, 'https://api.openai.com/v1', 'dev runtime reloads the selected LLM base URL after restart');
    assertEqual(devRuntimeAfterRestart.maxTokens, 9000, 'dev runtime reloads the selected LLM maxTokens after restart');
    assertEqual(devRuntimeAfterRestart.sessionApiKey, 'sk-dev-secret', 'dev runtime restores its API key from the dev native secret after restart');
    assertEqual(devOutputDirectoryAfterRestart, configuredDevOutputDirectory, 'dev runtime reloads the selected output directory after restart');
    assertEqual(devSpeechTranscriptionAfterRestart.executablePath, 'D:/dev-tools/whisper-cli.exe', 'dev runtime reloads the selected local speech-to-text executable after restart');
    assertEqual(devSpeechTranscriptionAfterRestart.modelPath, 'D:/dev-models/ggml-large-v3-turbo.bin', 'dev runtime reloads the selected local speech-to-text model after restart');
    assertEqual(releaseSettingsAfterSwitch.llm.modelVendor, 'deepseek', 'release runtime does not read dev LLM settings');
    assertEqual(releaseSettingsAfterSwitch.workspace.outputDirectory, '', 'release runtime does not read dev output directory and keeps the native app-data fallback unconfigured');
    assertEqual(releaseSettingsAfterSwitch.speechTranscription.configured, false, 'release runtime does not read dev local speech-to-text settings');
    assertEqual(releaseRuntimeAfterSwitch.sessionApiKey, undefined, 'release runtime does not reuse the dev transient or native LLM API key');
    assertEqual(releaseOutputDirectoryAfterSwitch, undefined, 'release runtime leaves outputRootDir unset so the desktop host resolves a per-user app-data media root');
    assertEqual(releaseSpeechTranscriptionAfterSwitch.configured, false, 'release runtime keeps local speech-to-text unconfigured after switching from dev');
    assertRule(Boolean(localStorage.getItem('autocut_dev_settings')), 'dev LLM settings persist under the dev storage namespace');
    assertEqual(localStorage.getItem('autocut_release_settings'), null, 'dev LLM settings do not write the release storage namespace');
    assertRule(
      isolatedRuntimeSecretCommands.some((entry) => entry.command === 'autocut_save_llm_secret' && entry.args?.request?.secretName === 'dev-default'),
      'dev LLM API key saves to the dev native secret namespace',
    );
    assertRule(
      isolatedRuntimeSecretCommands.some((entry) => entry.command === 'autocut_get_llm_secret' && entry.args?.request?.secretName === 'dev-default'),
      'dev LLM API key reloads from the dev native secret namespace',
    );
    assertRule(
      isolatedRuntimeSecretCommands.some((entry) => entry.command === 'autocut_get_llm_secret' && entry.args?.request?.secretName === 'release-default'),
      'release LLM runtime uses a separate native secret namespace after environment switch',
    );

    const workflowVideoFile = (name) => new File(['video'], name, { type: 'video/mp4' });
    const rejectedProcessingWorkflows = [
      {
        name: 'slicer with unsupported browser fallback',
        process: processVideoSlice,
        params: {
          fileId: 'asset-source-slicer-browser-file',
          file: workflowVideoFile('browser-source.mp4'),
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'slicer with external URL instead of desktop local source',
        process: processVideoSlice,
        params: {
          url: 'https://video.example.com/source.mp4',
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
        expectFailedTask: false,
        expectedMessagePart: 'external URL sources yet',
      },
      {
        name: 'extractor text with unsupported browser fallback',
        process: processExtractorText,
        params: {
          fileId: 'asset-source-extractor-text',
          file: workflowVideoFile('extractor-text.mp4'),
          language: 'zh',
          format: 'raw',
          separateSpeakers: true,
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'extractor audio with unsupported browser fallback',
        process: processAudioExtraction,
        params: {
          fileId: 'asset-source-extractor-audio',
          file: workflowVideoFile('extractor-audio.mp4'),
          format: 'mp3',
          quality: '320',
          channel: 'stereo',
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'video gif with unsupported browser fallback',
        process: processVideoGif,
        params: {
          fileId: 'asset-source-video-gif',
          file: workflowVideoFile('video-gif.mp4'),
          fps: '12',
          resolution: '720p',
          dither: true,
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'video compress with unsupported browser fallback',
        process: processVideoCompress,
        params: {
          fileId: 'asset-source-video-compress',
          file: workflowVideoFile('video-compress.mp4'),
          compressionMode: 'balanced',
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'video convert with unsupported browser fallback',
        process: processVideoConvert,
        params: {
          fileId: 'asset-source-video-convert',
          file: workflowVideoFile('video-convert.mp4'),
          targetFormat: 'mp4',
          videoCodec: 'h264',
          audioCodec: 'aac',
          resolution: '1080p',
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'video enhance with unsupported browser fallback',
        process: processVideoEnhance,
        params: {
          fileId: 'asset-source-video-enhance',
          file: workflowVideoFile('video-enhance.mp4'),
          targetResolution: '4k',
          enhanceMode: 'balanced',
          frameRate: '60',
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'subtitle translate with unsupported browser fallback',
        process: processSubtitleTranslate,
        params: {
          fileId: 'asset-source-subtitle-translate',
          file: workflowVideoFile('subtitle-translate.mp4'),
          sourceLang: 'zh',
          targetLang: 'en',
          keepOriginal: true,
          hardcode: true,
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'voice translate with unsupported browser fallback',
        process: processVoiceTranslate,
        params: {
          fileId: 'asset-source-voice-translate',
          file: workflowVideoFile('voice-translate.mp4'),
          sourceLang: 'zh',
          targetLang: 'en',
        },
        expectFailedTask: true,
        expectedMessagePart: 'trusted local desktop media file',
      },
      {
        name: 'slicer without media source',
        process: processVideoSlice,
        params: {
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
      },
      {
        name: 'slicer with blank external source URL',
        process: processVideoSlice,
        params: {
          url: '   ',
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
      },
      {
        name: 'slicer with unsafe external source URL',
        process: processVideoSlice,
        params: {
          url: 'javascript:alert(1)',
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
      },
      {
        name: 'slicer with local file URL',
        process: processVideoSlice,
        params: {
          url: 'file:///tmp/source.mp4',
          mode: 'contract-mode',
          llmModel: 'gpt-4o',
          minDuration: 15,
          maxDuration: 60,
          baseAlgorithm: 'scene',
          highlightEngine: 'emotion',
          enableNoiseReduction: true,
          enableCoughFilter: true,
          enableRepeatFilter: true,
          enableSubtitles: true,
        },
      },
      {
        name: 'extractor text without media source',
        process: processExtractorText,
        params: {
          language: 'zh',
          format: 'raw',
          separateSpeakers: true,
        },
      },
      {
        name: 'extractor audio without media source',
        process: processAudioExtraction,
        params: {
          format: 'mp3',
          quality: '320',
          channel: 'stereo',
        },
      },
      {
        name: 'video gif without media source',
        process: processVideoGif,
        params: {
          fps: '12',
          resolution: '720p',
          dither: true,
        },
      },
      {
        name: 'video compress without media source',
        process: processVideoCompress,
        params: {
          compressionMode: 'balanced',
        },
      },
      {
        name: 'video convert without media source',
        process: processVideoConvert,
        params: {
          targetFormat: 'mp4',
          videoCodec: 'h264',
          audioCodec: 'aac',
          resolution: '1080p',
        },
      },
      {
        name: 'video enhance without media source',
        process: processVideoEnhance,
        params: {
          targetResolution: '4k',
          enhanceMode: 'balanced',
          frameRate: '60',
        },
      },
      {
        name: 'subtitle translate without media source',
        process: processSubtitleTranslate,
        params: {
          sourceLang: 'zh',
          targetLang: 'en',
          keepOriginal: true,
          hardcode: true,
        },
      },
      {
        name: 'voice translate without media source',
        process: processVoiceTranslate,
        params: {
          sourceLang: 'zh',
          targetLang: 'en',
        },
      },
    ];

    for (const workflow of rejectedProcessingWorkflows) {
      await assertRejectedProcessingWorkflow({ services, workflow });
    }
  } finally {
    rmSync(moduleLoaderOutDir, { recursive: true, force: true });
  }
}

await run();

if (failures.length > 0) {
  console.error('AutoCut service behavior check failed:');
  for (const failure of failures) {
    console.error(`- ${failure}`);
  }
  console.error(`\n${pass.length} checks passed, ${failures.length} checks failed.`);
  process.exit(1);
}

console.log(`AutoCut service behavior check passed (${pass.length} checks).`);
